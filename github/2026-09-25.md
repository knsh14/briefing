# GitHub ダイジェスト — 2026-09-25

## 本日のハイライト

1. ![Issue](icons/issue-opened.svg) **[chestnut: losing the USB device while driving kills modeld, no small model fallback](https://github.com/commaai/openpilot/issues/39002)** (commaai/openpilot) — 走行中に chestnut の USB デバイスを失うと small model へのフォールバックも失敗し、modeld が停止したままになるという実車報告である。関連する 3 件の Issue に対し、realtime スケジューリングやフォールバック判定の修正 PR が同日にまとめてマージされ、残りは tinygrad#18389 待ちと整理された。
2. ![Issue](icons/issue-opened.svg) **[Direct URL requirements with a `sig` query parameter are reinstalled on every run: `direct_url.json` stores the redacted URL (regression in 0.12.8)](https://github.com/astral-sh/uv/issues/21969)** (astral-sh/uv) — Azure Storage の SAS URL などが毎回再インストールされる 0.12.8 以降の回帰である。#21971 で修正され、0.12.19 へのバージョン更新もマージされた。
3. ![PR](icons/git-pull-request.svg) **[perf(lua): share compatible file watchers](https://github.com/neovim/neovim/pull/41778)** (neovim/neovim) — LSP の file watcher を共有し、大規模ワークスペースでの setup と teardown の負荷を下げる性能改善で、justinmk の承認を経てマージされた。
4. ![Issue](icons/issue-opened.svg) **[1.139.1 Endgame](https://github.com/microsoft/vscode/issues/337801)** (microsoft/vscode) — リカバリーリリース 1.139.1 の endgame 計画である。cherry-pick と Insiders ビルドまで完了し、version bump の PR も並行して進んでいる。
5. ![PR](icons/git-pull-request.svg) **[sessions: add remote session delegation tools](https://github.com/microsoft/vscode/pull/336690)** (microsoft/vscode) — Agents ウィンドウからリモートの agent host にセッションを委譲するツール群を追加する、約 6500 行の Draft PR である。Copilot レビューが AI 無効ゲートへの未対応と未ローカライズのエラーを指摘している。

---

## sunnypilot/sunnypilot

> upstream の commaai/openpilot master を取り込む同期 PR が引き続き更新されている。HKG 向けの angle steering と ccNC 移植の Draft PR 2 件も更新されたが、新しい議論は出ていない。

### ![PR](icons/git-pull-request.svg) [Sync: commaai/openpilot:master -> sunnypilot/sunnypilot:master](https://github.com/sunnypilot/sunnypilot/pull/2036) `#2036`
**ステータス:** Open | **ラベル:** なし

**概要:** commaai/openpilot の master（コミット 6080cc6）を sunnypilot の master に同期する PR である。opendbc、panda、tinygrad の各サブモジュールについても sunnypilot 側で対応する PR が作成されている。

**直近のアクティビティ:** 直近 24 時間のコメントなし。既存のコメントは github-actions bot の process replay レポート（66 件 pass、差分 0、エラー 0）のみである。

**変更ファイル:** 304 ファイル、+16867/-10815。モデルビルド用 workflow（`sunnypilot-build-model.yaml`、`build-default-models.yaml` など）の改修、warp カーネルをコンパイルする `compile-warp-kernels.yaml` の新規追加、`Jenkinsfile`、`launch_chffrplus.sh`、`log.capnp`、`services.py` の更新、opendbc サブモジュールの更新が含まれる。

**次のアクション:** 変更規模が大きいため、サブモジュール PR 3 件と合わせてレビューし、マージ順序を調整する必要がある。

---

### ![PR](icons/git-pull-request.svg) [Hkg angle steering 2025](https://github.com/sunnypilot/sunnypilot/pull/807) `#807`
**ステータス:** Open (Draft) | **ラベル:** なし

**概要:** Hyundai 系車両の angle steering 対応を進める PR で、#801 と #641 を置き換える。対象として Ioniq 5 PE、GV80、GV80 coupe がチェックリストに並ぶが、いずれも未完了である。Sourcery の要約によれば、joystick 制御を `inputs` から pygame ベースに置き換えて PlayStation と Xbox のコントローラに対応し、横方向制御向けに平滑化、トルク、オーバーライドを調整できる angle tuning 設定を追加する。

**直近のアクティビティ:** 直近 24 時間のコメントなし。8 月の process replay レポートでは HYUNDAI2 のセグメントで `carOutput.actuatorsOutput.torque` に 1093 件の差分が出ていた。

**変更ファイル:** 12 ファイル、+2485/-2。大半は車両移植用ノートブック `hkg_canfd_angle_steering.ipynb`（+2196）と `hyundai_angle_steering.ipynb`（+228）で、ほかに IDE の実行設定ファイル、`version.py`、opendbc サブモジュールの更新である。

**次のアクション:** Draft のままであり、チェックリストの車種検証と opendbc 側 PR（sunnypilot/opendbc#119）の進捗を待つ。

---

### ![PR](icons/git-pull-request.svg) [HKG: ccNC port (non-HDA2)](https://github.com/sunnypilot/sunnypilot/pull/1306) `#1306`
**ステータス:** Open (Draft) | **ラベル:** car

**概要:** HKG の ccNC（non-HDA2）車両への移植 PR である。本体の変更は sunnypilot/opendbc#94 にあり、この PR はそれを参照するためのサブモジュール更新のみである。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**変更ファイル:** 1 ファイル、+1/-1。`opendbc_repo` サブモジュールの参照更新のみである。

**次のアクション:** 参照先の opendbc PR のレビューとマージを待つ。

---

## commaai/openpilot

> chestnut（外付け GPU）の信頼性改善が活動の中心である。実車で見つかった 3 件のバグ Issue に対し、realtime スケジューリングの変更、フォールバック判定の修正、デバイス列挙待ち、タイムアウト設定の修正がマージされた。残る問題は tinygrad 側の修正（tinygrad#18389）待ちと整理され、外部コントリビュータによる modeld のフォールバック強化 PR 2 件はクローズされた。

### ![PR](icons/git-pull-request.svg) [hardwared: ignore invalid thermal readings](https://github.com/commaai/openpilot/pull/39014) `#39014`
**ステータス:** Open | **ラベル:** なし

**概要:** #36690 を修正する PR である。thermal zone の読み取りエラー時に NaN を返してログ上で失敗が見えるようにし、存在しない thermal zone については従来どおり 0 を返す。thermal status やファンの入力を算出する際には非有限値を無視し、センサー読み取りの失敗が下流のフィルタを汚染しないようにする。

**直近のアクティビティ:** github-actions bot の process replay レポートのみで、66 件 pass、差分 0 である。

**変更ファイル:** 3 ファイル、+47/-6。`common/hardware/base.py` に `max_valid_temperature` を追加し、`hardwared.py` をそれを使う形に改修した。テスト `test_hardware_thermal.py`（3 件）を新規追加している。

**次のアクション:** メンテナによるレビューを待つ。

---

### ![PR](icons/git-pull-request.svg) [mici: block parent scroll during bookmark swipe](https://github.com/commaai/openpilot/pull/39013) `#39013`
**ステータス:** Open | **ラベル:** ui

**概要:** #38934 を修正する PR である。bookmark のスワイプイベントを、親の横スクローラが入力を消費する前に処理し、スワイプが左に動いた時点で親スクローラを無効化する。左スワイプ、右スワイプ、指を離した後の後処理を検証するテストを追加している。

**直近のアクティビティ:** github-actions bot の process replay レポートのみで、66 件 pass、差分 0 である。

**変更ファイル:** 3 ファイル、+81/-1。ジェスチャー処理を担う `mici/onroad/bookmark_gesture.py` の新規追加、`augmented_road_view.py` への組み込み、テスト `test_bookmark_scroll_block.py` の追加である。

**次のアクション:** メンテナによるレビューを待つ。

---

### ![PR](icons/git-pull-request.svg) [modeld: fix 3s chestnut timeout](https://github.com/commaai/openpilot/pull/39012) `#39012`
**ステータス:** Merged | **ラベル:** なし

**概要:** 直近の tinygrad 更新で `HCQDEV_WAIT_TIMEOUT_MS` が削除されたため、chestnut 向けの 3 秒のタイムアウトを `AMDDevice` 側に設定するよう修正した。

**直近のアクティビティ:** github-actions bot の process replay レポートのみで、66 件 pass、差分 0 である。

**変更ファイル:** 1 ファイル、+2/-1。`modeld.py` のタイムアウト設定箇所の修正である。

**次のアクション:** マージ済みのため対応は不要である。

---

### ![PR](icons/git-pull-request.svg) [modeld: wait for chestnut to enumerate](https://github.com/commaai/openpilot/pull/39011) `#39011`
**ステータス:** Merged | **ラベル:** なし

**概要:** onroad になる直前に chestnut を接続した場合、ASM チップの列挙に最大 10 秒かかる。その間に big model のロードを失敗させるのではなく、列挙を待つようにした。

**直近のアクティビティ:** github-actions bot の process replay レポート（66 件 pass、差分 0）と、commaci-public による model replay の差分プロットのみである。

**変更ファイル:** 3 ファイル、+16/-5。`modeld/helpers.py` に待機処理を追加し、`modeld.py` と `ui_state.py` を改修した。

**次のアクション:** マージ済みのため対応は不要である。

---

### ![PR](icons/git-pull-request.svg) [hardwared: log usb-c connected](https://github.com/commaai/openpilot/pull/39010) `#39010`
**ステータス:** Merged | **ラベル:** なし

**概要:** AUX USB にケーブルが物理的に挿されたときにログへ記録するようにした。

**直近のアクティビティ:** github-actions bot の process replay レポートのみで、66 件 pass、差分 0 である。

**変更ファイル:** 3 ファイル、+8/-2。`log.capnp` へのフィールド追加、`common/hardware/usb.py` の検出処理追加、`ui_state.py` の修正である。

**次のアクション:** マージ済みのため対応は不要である。

---

### ![PR](icons/git-pull-request.svg) [modeld: warm resident Small before inference](https://github.com/commaai/openpilot/pull/38980) `#38980`
**ステータス:** Closed | **ラベル:** なし

**概要:** chestnut 利用時に Big 推論が例外を投げた場合に備えて保持している Small モデルを、live 推論の前に warmup しておく PR である。作者の計測では、例外発生から有効な出力が出るまでの時間が約 390 ms から約 93 ms に短縮され、Small の初回推論は約 258 ms から 27.5 ms になった。2 段構成の貢献の 1 段目という位置づけで、#38982 がこれに依存していた。

**直近のアクティビティ:** elkoled が、フォールバック時に約 0.2 秒短縮できる一方で eGPU 起動のたびに約 0.4 秒とプロセス負荷が増えると指摘した。フォールバック時は soft-disengage するため、起動コストに見合わないと判断してクローズした。

**変更ファイル:** 3 ファイル、+142/-0。`modeld.py` への 2 行の追加と、7 件の回帰テスト `test_modeld_fallback.py` の新規追加である。

**次のアクション:** クローズ済みのため対応は不要である。

---

### ![PR](icons/git-pull-request.svg) [modeld: supervise primary inference with warm standby failover](https://github.com/commaai/openpilot/pull/38982) `#38982`
**ステータス:** Closed | **ラベル:** なし

**概要:** #38980 に依存する 2 段目の PR である。例外を投げずに同期呼び出しがブロックした場合は既存の Small フォールバックに到達できないため、Big をワーカープロセスで実行して 150 ms の推論期限を設け、親プロセスが warmup 済みの Small と出力の publish を担う構成にする。フェイルオーバーは modeld の再起動まで一方向で、通常時の実行時間は upstream 比で約 3.8 % 増えると報告されている。

**直近のアクティビティ:** elkoled が、この問題は tinygrad 側（tinygrad#18389）で修正されるとコメントし、クローズした。

**変更ファイル:** 5 ファイル、+617/-21（#38980 の変更を含む表示）。ワーカー実装 `inference_process.py` の新規追加、`modeld.py` の改修、ワーカーとフォールバックのテスト追加である。

**次のアクション:** クローズ済みのため対応は不要である。根本対応は tinygrad#18389 の進捗に依存する。

---

### ![Issue](icons/issue-opened.svg) [chestnut: losing the USB device while driving kills modeld, no small model fallback](https://github.com/commaai/openpilot/issues/39002) `#39002`
**ステータス:** Open | **ラベル:** bug

**概要:** engage 中に chestnut の bulk IN 転送が I/O エラーになり、その後の small model へのフォールバックも失敗して modeld がクラッシュしたという報告である。原因として、tinygrad の host device の pending map に死んだ AMD デバイスが残って `NPY hang detected` が発生したこと、`put_bool("ChestnutActive", False)` が非同期書き込みのため次のループで古い値を読んだことが挙げられている。manager はクラッシュしたプロセスを再起動しないため、以降は「Process Not Running」のままだった。

**直近のアクティビティ:** elkoled が、#39008 で一部を修正済みで、残りは tinygrad#18389 で扱うとコメントした。

**次のアクション:** tinygrad#18389 の修正を取り込んだ後、フォールバックが機能するか確認してクローズする。

---

### ![PR](icons/git-pull-request.svg) [ui: force experimental mode while chestnut is attached](https://github.com/commaai/openpilot/pull/39009) `#39009`
**ステータス:** Open (Draft) | **ラベル:** ui

**概要:** 本文は空である。タイトルと変更ファイルから見ると、chestnut 接続中は experimental mode を強制する UI 変更である。

**直近のアクティビティ:** github-actions bot の process replay レポートのみで、66 件 pass、差分 0 である。

**変更ファイル:** 3 ファイル、+12/-1。`ui_state.py`、mici の `home.py` と設定画面 `toggles.py` の修正である。

**次のアクション:** Draft のため、作者による本文の記述と Ready for review への移行を待つ。

---

### ![PR](icons/git-pull-request.svg) [selfdrived: report comm issues only when engagement-relevant](https://github.com/commaai/openpilot/pull/38730) `#38730`
**ステータス:** Open | **ラベル:** stale

**概要:** #35918 で提案された案 1 に沿い、汎用的な通信ヘルスチェックを openpilot が有効な間か engage を試みている間だけ実行する PR である。無効状態で engage も試みていないときに `commIssue` を出さないようにし、個別の故障イベントは変更しない。Ford のルートリプレイは LFS のモデル依存が手元に無いため未実施である。

**直近のアクティビティ:** github-actions bot が、24 日間活動がないため 7 日後に自動クローズすると通知した。

**変更ファイル:** 3 ファイル、+18/-4。`selfdrived.py` と `state.py` の条件分岐の変更と、`test_state_machine.py` へのテスト追加である。

**次のアクション:** 自動クローズを避けるには、作者またはメンテナによる反応が必要である。

---

### ![Issue](icons/issue-opened.svg) [Add support for custom eSIM profile loading](https://github.com/commaai/openpilot/issues/34924) `#34924`
**ステータス:** Open | **ラベル:** enhancement

**概要:** comma 3X に搭載された eSIM にユーザー自身の SIM プロファイルを読み込めるようにするバウンティである。provisioning には lpac を使う必要があり、EG25 内蔵の LPA に依存する `system/hardware/tici/esim.py` は使わない。当初は connect で実装する想定だったが、デバイス上で実装する方針に変わり、greatgitsby が担当している。

**直近のアクティビティ:** valerito が eUICC を manual mode に切り替える方法を質問した。

**次のアクション:** 質問への回答と、担当者による実装の進捗報告を待つ。

---

### ![Issue](icons/issue-opened.svg) [chestnut: unplugging during big model load hangs modeld, UI stuck on Big Model Loading](https://github.com/commaai/openpilot/issues/39003) `#39003`
**ステータス:** Open | **ラベル:** bug

**概要:** big model のロード中に chestnut を抜くと、xHCI host controller が停止して libusb がエラーを返さず、modeld が CPU 100 % のまま残り続けたという報告である。modelV2 は publish されず、UI は「Big Model Loading / openpilot unavailable」のまま small model に切り替わらなかった。tinygrad の `bulk_wait` のループが原因候補とされ、60 秒の `loader.join(BIG_MODEL_TIMEOUT)` が効かなかった理由は未解明である。

**直近のアクティビティ:** elkoled が、直近の tinygrad 更新による regression であるとし、tinygrad#18389 を起票して tinygrad 側に伝えたとコメントした。

**次のアクション:** tinygrad#18389 の修正を待ち、取り込み後に再現テストを行う。

---

### ![PR](icons/git-pull-request.svg) [modeld: check model.chestnut in fallback](https://github.com/commaai/openpilot/pull/39008) `#39008`
**ステータス:** Merged | **ラベル:** なし

**概要:** フォールバック判定で `ChestnutActive` パラメータではなく `model.chestnut` の状態を参照するようにした。パラメータの値が古いまま読まれると競合状態が起きるためで、#39002 の一部を修正する。

**直近のアクティビティ:** github-actions bot の process replay レポート（66 件 pass、差分 0）と、commaci-public による model replay の差分プロットのみである。

**変更ファイル:** 1 ファイル、+1/-1。`modeld.py` の判定条件 1 行の変更である。

**次のアクション:** マージ済みのため対応は不要である。

---

### ![PR](icons/git-pull-request.svg) [modeld: precompiled context-9 action model at 5 Hz](https://github.com/commaai/openpilot/pull/38944) `#38944`
**ステータス:** Open (Draft) | **ラベル:** なし

**概要:** 本文は空である。変更内容から見ると、worldmodel 系のモデル（`worldmodel.py`、`worldmodeld.py`、`worldmodel_kernels.py`）とコンパイル用スクリプト `compile_worldmodel.py` を追加し、precompiled の context-9 action model を 5 Hz で動かす PR である。

**直近のアクティビティ:** 直近 24 時間のコメントなし。既存のコメントは commaci-public の model replay 差分プロットと、github-actions の process replay レポート（66 件 pass、差分 0）のみである。

**変更ファイル:** 26 ファイル、+4279/-28。ハイパーパラメータ `models/worldmodel/hparams.json`（+2821）、worldmodel の推論とカーネル実装、説明文書 `worldmodel.md`、テスト `test_worldmodel.py` を新規追加している。`log.capnp` と `services.py` へのメッセージ追加、`process_config.py` へのプロセス登録、`modeld.py` の改修、tinygrad サブモジュール更新も含む。

**次のアクション:** Draft のため、作者による本文の記述と Ready for review への移行を待つ。

---

### ![Issue](icons/issue-opened.svg) [chestnut: cold-cache big model load stalls controlsd startup, cruise fault on Tesla](https://github.com/commaai/openpilot/issues/39001) `#39001`
**ステータス:** Closed | **ラベル:** bug

**概要:** Model Y で、page cache が冷えた状態で 773 MB の big model を mmap して読み込むと約 12 秒のメモリ回収が発生し、controlsd の起動が 7.2 秒から 20.0 秒に遅れたという報告である。その結果 selfdrived が初期化タイムアウトに達し、panda が stock の `DAS_control` をブロックして `DI_cruiseState` が fault した。ファイルがキャッシュ済みの場合は問題が起きなかった。

**直近のアクティビティ:** elkoled が、メモリ使用量の削減は tinygrad のローダーに任せ、ロード中の約 835 MB は現状問題にならないとコメントした。修正として #39007 を示してクローズした。

**次のアクション:** クローズ済みのため対応は不要である。

---

### ![PR](icons/git-pull-request.svg) [modeld: set realtime scheduling after model load](https://github.com/commaai/openpilot/pull/39007) `#39007`
**ステータス:** Merged | **ラベル:** なし

**概要:** modeld の realtime スケジューリング設定をモデルロード後に移し、ロード中に他プロセスの起動が CPU を奪われないようにした。big model のロード時間は 18〜20 秒で変わらず、controlsd の最大起動時間は最大 20 秒から 5.5 秒に短縮された。ロード時間を約 1 秒節約するため、garbage collection の無効化は維持している。#39001 の一部を修正する。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**変更ファイル:** 1 ファイル、+4/-1。`modeld.py` の realtime 設定の呼び出し位置の変更である。

**次のアクション:** マージ済みのため対応は不要である。

---

## commaai/opendbc

> 長期間停滞していた車両移植 PR の多くが 9 月 24 日の同時刻帯に一斉に更新されたが、新たなコメントやレビューは伴っていない。実質的な動きは Honda radarless のブレーキ修正に関するやり取り、Kia EV6 の fingerprint 追加 PR の出し直し、Tesla のテストルート更新のマージである。

### ![PR](icons/git-pull-request.svg) [Honda: fix radarless braking](https://github.com/commaai/opendbc/pull/2050) `#2050`
**ステータス:** Open | **ラベル:** DBC signals, car, honda

**概要:** 2025/2026 Civic Hybrid と 2025 Acura ADX で、要求どおりの強さでブレーキがかからない問題の修正である。当初は radarless カメラ側の対策を試みたが、stock ACC のブレーキ中は idlestop メッセージが常にオンであることがわかり、ブレーキ時にこのビットを立てることで解消した。Hybrid Civic での検証と ICE Civic での回帰テストは完了している。

**直近のアクティビティ:** davkg が、PR の更新を忘れていて diff が古い変更のままではないかと指摘した。作者の mvl-boston は、最新の master をマージし直したが問題は見当たらず、何を更新すべきかわからないと返答した。

**変更ファイル:** 2 ファイル、+3/-2。`hondacan.py` での idlestop ビットの設定と、`honda_bosch_radarless.dbc` の 1 行修正である。

**次のアクション:** davkg の指摘の意図を確認し、diff が意図した変更になっているかを双方で合わせる必要がある。

---

### ![PR](icons/git-pull-request.svg) [HKG: Consolidate cluster lanelines platforms with int flag](https://github.com/commaai/opendbc/pull/2647) `#2647`
**ステータス:** Open | **ラベル:** car, hyundai

**概要:** 機能変更を伴わないリファクタリングで、`hyundaican.py` を簡潔にするため、クラスタに車線を表示する 28 プラットフォームを新しい int フラグでまとめる。

**直近のアクティビティ:** 直近 24 時間のコメントなし。2025 年 8 月に adeebshihadeh、jyoung8607、sshane とのレビューのやり取りがある。

**変更ファイル:** 2 ファイル、+31/-32。`values.py` へのフラグ追加と `hyundaican.py` の条件分岐の整理である。

**次のアクション:** メンテナによる再レビューを待つ。

---

### ![PR](icons/git-pull-request.svg) [Hyundai Safety: safety flags in dedicated namespaces](https://github.com/commaai/opendbc/pull/1964) `#1964`
**ステータス:** Open | **ラベル:** car, hyundai, car safety

**概要:** opendbc#1942 の一部として、`HyundaiSafetyFlags` を safety mode ごとの名前空間に分割し、CAN と CAN FD のフラグが同じビット空間を共有しないようにする。共通フラグ、CAN 専用の `HyundaiCanSafetyFlags`、CAN FD 専用の `HyundaiCanFDSafetyFlags` に分け、ビット値は変えない。safety 側でも `hyundai_common_init()` は共通フラグのみを扱い、各モードの init が専用フラグを解釈する。

**直近のアクティビティ:** 直近 24 時間のコメントなし。2025 年 3 月に sshane とのレビューのやり取りがある。

**変更ファイル:** 9 ファイル、+51/-44。`values.py` と `interface.py` のフラグ定義の分割、safety の `hyundai.h`、`hyundai_canfd.h`、`hyundai_common.h` の init 処理の整理、関連テストの更新である。

**次のアクション:** メンテナによる再レビューを待つ。

---

### ![PR](icons/git-pull-request.svg) [HKG: Car Port for Genesis GV80 2025-26 (HDA2, LFA2)](https://github.com/commaai/opendbc/pull/1646) `#1646`
**ステータス:** Open | **ラベル:** DBC signals, car, hyundai, car safety

**概要:** Genesis GV80 2025-26（HDA2、LFA2、CAN-FD）の車両移植 PR である。ハーネスは Hyundai Q と Hyundai R を使い、チェックリストはすべて完了している。

**直近のアクティビティ:** 直近 24 時間のコメントなし。2026 年 3 月に jyoung8607 が、トルク低減ゲインの制御器は概念としては許容するが、angle の平滑化関数は openpilot から渡されたアクチュエータ値を忠実に実行するという原則に反すると指摘し、CHANGES_REQUESTED を付けている。

**変更ファイル:** 13 ファイル、+731/-33。`carcontroller.py`（+150）の改修、`torque_reduction_gain.py` の新規追加、safety の `hyundai_canfd.h` の拡張とテスト `test_hyundai_canfd.py`（+361）の追加が中心である。

**次のアクション:** angle 平滑化についての CHANGES_REQUESTED への対応が必要である。

---

### ![PR](icons/git-pull-request.svg) [HKG: Car Port for Hyundai Palisade and Kia Telluride 2023-25 (non-HDA2)](https://github.com/commaai/opendbc/pull/1273) `#1273`
**ステータス:** Open | **ラベル:** DBC signals, car, hyundai, car safety

**概要:** Hyundai Palisade 2023 と Kia Telluride 2023-25 の non-HDA2（CAN-FD）車両への移植 PR である。stock と openpilot の両方のルートが添付され、panda、opendbc、openpilot の関連 PR に依存している。

**直近のアクティビティ:** 直近 24 時間のコメントなし。2024 年 9 月に jyoung8607 が、`CanBus` クラスを values に移して全車でバス選択に使うことや、CAN packer 関数に bus 引数を持たせることなど、事前のリファクタリングを別 PR で行うよう求めて CHANGES_REQUESTED を付けている。

**変更ファイル:** 14 ファイル、+235/-57。`hyundaican.py`（+78）、safety の `safety_hyundai.h` の改修、fingerprint とテストの追加が中心である。

**次のアクション:** 求められた事前リファクタリングの完了と CHANGES_REQUESTED の解消が必要である。

---

### ![PR](icons/git-pull-request.svg) [GWM Haval H6: Initial Platform Support](https://github.com/commaai/opendbc/pull/3263) `#3263`
**ステータス:** Open | **ラベル:** CI / testing, DBC signals, car, car safety

**概要:** GWM Haval H6 を新しいブランドとして初期サポートする PR である。複数ユーザーのルートと動画、ハーネスの回路図やカメラカバーの分解手順が添付されている。Haval Jolion は EPS の応答の送り方が異なるため別の移植が必要とされている。

**直近のアクティビティ:** 直近 24 時間のコメントなし。2026 年 5 月に adeebshihadeh が、かなり完成度の高い移植だと評価したうえで、codex などの LLM を使ってさらに整理するよう勧めている。

**変更ファイル:** 20 ファイル、+1019/-43。`opendbc/car/gwm/` 以下の carcontroller、carstate、interface などの新規追加、safety mode `gwm.h`（+211）とテスト `test_gwm.py` の追加、DBC `gwm_haval_h6_mk3.dbc` の更新である。

**次のアクション:** レビュー指摘を反映したうえでの再レビューが必要である。

---

### ![PR](icons/git-pull-request.svg) [Add Toyota Corolla Cross Hybrid 2026](https://github.com/commaai/opendbc/pull/3575) `#3575`
**ステータス:** Open | **ラベル:** car, toyota, fingerprint

**概要:** 台湾仕様の Toyota Corolla Cross GR Hybrid 2026（TSS 2.0）の fingerprint を追加する PR である。dongle ID とルートが添付されている。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**変更ファイル:** 1 ファイル、+3/-0。`toyota/fingerprints.py` への追加のみである。

**次のアクション:** メンテナによるレビューを待つ。

---

### ![PR](icons/git-pull-request.svg) [Tesla: PartyCAN dbc sync](https://github.com/commaai/opendbc/pull/3301) `#3301`
**ステータス:** Open | **ラベル:** DBC signals

**概要:** Tesla の PartyCAN 用 DBC を整理する PR である。ソフトウェア 2026.8.3 に存在しないメッセージを削除し、Vehicle CAN から PartyCAN にミラーされるメッセージを同期し、加速度計やブレーキトルクなど既知のメッセージを追加する。openpilot が使うシグナルには触れていない。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**変更ファイル:** 1 ファイル、+89/-29。`tesla_model3_party.dbc` の更新である。

**次のアクション:** メンテナによるレビューを待つ。

---

### ![PR](icons/git-pull-request.svg) [VW MQB: work around standstill limit](https://github.com/commaai/opendbc/pull/2764) `#2764`
**ステータス:** Open (Draft) | **ラベル:** DBC signals, car, volkswagen

**概要:** EPB のない MQB 車は停止中に ESP がブレーキを長く保持すると fault するため、停止前に stopping から starting に遷移させてブレーキ要求を続けることで、EPB なしでも完全な stop-and-go を実現する。急な坂での後退を防ぐため、後退の危険がある速度を推定し、それを下回ったら停止を強制する。ACC type 1 ではドライバーがブレーキを踏んでいる間 `long_active` を抑制する。openpilot#37800 と opendbc#3322 に依存する。

**直近のアクティビティ:** 直近 24 時間のコメントなし。2025 年 10 月から 11 月に jyoung8607 と作者の間でレビューのやり取りがある。

**変更ファイル:** 7 ファイル、+610/-22。`mqbcan.py`（+235）の改修とテスト `test_volkswagen.py`（+333）の追加が中心で、`vw_mqb.dbc` にもシグナルを追加している。

**次のアクション:** 依存 PR の進捗と、古くなった検証ルートの更新が必要である。

---

### ![PR](icons/git-pull-request.svg) [safety: add longitudinal pre-enable state for brake at a standstill](https://github.com/commaai/opendbc/pull/2271) `#2271`
**ステータス:** Open | **ラベル:** ignore stale, car safety

**概要:** openpilot#25425 に対応するため、停止中にブレーキを踏んでいる状態向けの longitudinal pre-enable 状態を safety に追加する。panda#1123 の再挑戦である。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**変更ファイル:** 1 ファイル、+11/-3。`opendbc/safety/safety.h` の変更である。

**次のアクション:** メンテナによるレビューを待つ。

---

### ![PR](icons/git-pull-request.svg) [HKG: Car Port for Hyundai Palisade and Kia Telluride 2023-24 (HDA2)](https://github.com/commaai/opendbc/pull/1268) `#1268`
**ステータス:** Open | **ラベル:** DBC signals, car, hyundai, ignore stale

**概要:** Hyundai Palisade 2023 と Kia Telluride 2023-24 の HDA2（CAN-FD）車両への移植 PR である。Palisade は CAN バスのペアを手作業で入れ替えた Hyundai A（Modified）ハーネス、Telluride は Hyundai P ハーネスで検証している。opendbc#1269 と panda#1243 に依存する。

**直近のアクティビティ:** 直近 24 時間のコメントなし。2024 年 12 月に adeebshihadeh のレビューコメントがある。

**変更ファイル:** 15 ファイル、+3531/-33。大半は生成済みを含む DBC ファイル（`hyundai_palisade_2023.dbc`、`hyundai_canfd.dbc` など）の追加で、carstate、interface、fingerprint の改修も含む。

**次のアクション:** 依存 PR の解消とメンテナによるレビューを待つ。

---

### ![PR](icons/git-pull-request.svg) [2024 Kia EV9](https://github.com/commaai/opendbc/pull/1225) `#1225`
**ステータス:** Open (Draft) | **ラベル:** DBC signals, car, hyundai, fingerprint

**概要:** 旧リポジトリの Kia EV9 対応 PR を新しい構成に移植する WIP である。横方向制御は初期対応済みだが縦方向制御は未対応で、作者は未テストであると明記している。テストルートも未取得である。

**直近のアクティビティ:** 直近 24 時間のコメントなし。bot による PR 作成時の定型案内のみである。

**変更ファイル:** 8 ファイル、+134/-18。`carcontroller.py`（+59）と `hyundaicanfd.py` の改修、fingerprint と DBC の追加である。

**次のアクション:** 実車でのテストとルートの取得が必要である。

---

### ![PR](icons/git-pull-request.svg) [GM: Car Port for Buick Baby Enclave (CN Only)](https://github.com/commaai/opendbc/pull/1212) `#1212`
**ステータス:** Open (Draft) | **ラベル:** car, gm, fingerprint

**概要:** 中国専用の Buick Baby Enclave 2020-23 の移植 PR である。comma が販売するハーネスは無く、ルートは未取得である。

**直近のアクティビティ:** 直近 24 時間のコメントなし。2026 年 2 月に adeebshihadeh のレビューコメントがある。

**変更ファイル:** 5 ファイル、+15/-1。GM の fingerprint、interface、values への追加とテストルートの登録である。

**次のアクション:** openpilot と stock でのルート取得が必要である。

---

### ![PR](icons/git-pull-request.svg) [Hyundai: hide FCA Errors ](https://github.com/commaai/opendbc/pull/1206) `#1206`
**ステータス:** Open (Draft) | **ラベル:** car, hyundai

**概要:** `CF_Lkas_FusionState` を転送すると一部の車種、特に Kia Niro MY19 で FCA fault が表示される問題への対処である。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**変更ファイル:** 1 ファイル、+3/-0。`hyundaican.py` への追加のみである。

**次のアクション:** Draft のため、作者による検証結果の追記を待つ。

---

### ![PR](icons/git-pull-request.svg) [Hyundai: Car Port for Creta (Non-US only) 2024](https://github.com/commaai/opendbc/pull/1139) `#1139`
**ステータス:** Open | **ラベル:** car, hyundai

**概要:** 米国外仕様の Hyundai Creta 2024 の移植 PR で、openpilot#32933 を置き換える。stock のルートとハーネス A での検証はあるが、openpilot でのルートとテストルートの登録は未完了である。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**変更ファイル:** 5 ファイル、+17/-2。fingerprint、values、`hyundaican.py`、`CARS.md` の更新である。

**次のアクション:** openpilot でのルート取得とテストルートの登録が必要である。

---

### ![PR](icons/git-pull-request.svg) [HKG: Car Port for Genesis GV80 2021-22 (Middle East only)](https://github.com/commaai/opendbc/pull/1136) `#1136`
**ステータス:** Open | **ラベル:** CI / testing, DBC signals, car, body, chrysler, ford, gm, honda, hyundai, mazda, nissan, subaru, tesla, toyota, volkswagen, rivian, car safety

**概要:** 中東仕様の Genesis GV80 2021-22（CAN-FD）の移植 PR で、openpilot#30145 を置き換える。CAN バスのペアを入れ替えた Hyundai M（Modified）ハーネスで、stock と openpilot のルートが添付されている。opendbc#1297 に依存する。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**変更ファイル:** 234 ファイル、+12220/-509。Chrysler、Ford、GM など他メーカーのファイルや workflow、`LICENSE.md` まで含んでおり、GV80 移植と無関係な変更が diff に大量に混ざっている。

**次のアクション:** diff を移植に必要な変更だけに整理する必要がある。

---

### ![PR](icons/git-pull-request.svg) [HKG: Car Port for Genesis G70 2024](https://github.com/commaai/opendbc/pull/1134) `#1134`
**ステータス:** Open (Draft) | **ラベル:** car, hyundai

**概要:** Genesis G70 2024 の移植 PR で、openpilot#31625 を置き換える。ハーネス F で stock と openpilot の両ルートが揃っており、高速道路で openpilot の操舵と stock の縦方向制御が同時に動作している間は「grasp not detected」を抑制する。panda#2045 に依存する。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**変更ファイル:** 4 ファイル、+16/-1。fingerprint、values、`hyundaican.py`、torque データの更新である。

**次のアクション:** 依存する panda#2045 の解消と Ready for review への移行を待つ。

---

### ![PR](icons/git-pull-request.svg) [Add new fingerprint for 210630 kia ev6](https://github.com/commaai/opendbc/pull/3810) `#3810`
**ステータス:** Open | **ラベル:** car, hyundai, fingerprint

**概要:** Kia EV6 の fingerprint を 1 件追加する PR で、dongle ID とルートを添えて #3809 を出し直したものである。

**直近のアクティビティ:** github-actions bot によるコントリビューション案内と Car behavior report（Hyundai 系 260 セグメントのテスト）のみである。

**変更ファイル:** 1 ファイル、+1/-0。`hyundai/fingerprints.py` への 1 行追加である。

**次のアクション:** メンテナによるレビューを待つ。

---

### ![PR](icons/git-pull-request.svg) [Add new fingerprint for KIA EV6](https://github.com/commaai/opendbc/pull/3809) `#3809`
**ステータス:** Closed | **ラベル:** car, hyundai, fingerprint

**概要:** 2021 Kia EV6 の fingerprint を追加する PR だったが、検証用の dongle ID とルートが空欄のままだった。

**直近のアクティビティ:** github-actions bot によるコントリビューション案内と Car behavior report のみである。約 30 分後にクローズされ、同じ作者が #3810 を出し直している。

**変更ファイル:** 1 ファイル、+1/-0。`hyundai/fingerprints.py` への 1 行追加である。

**次のアクション:** クローズ済みのため対応は不要である。

---

### ![PR](icons/git-pull-request.svg) [2025 Subaru Crosstrek Lateral Support + Angle Based LKAS Support for Subarus](https://github.com/commaai/opendbc/pull/2864) `#2864`
**ステータス:** Open | **ラベル:** car, subaru, car safety

**概要:** angle ベースの LKAS を持つ Subaru の対応を修正し、2025 Crosstrek の横方向制御をサポートする PR である。作者によれば、この変更により 2024 年以降の Crosstrek、2023 年以降の Ascent、2022 年以降の Outback、2025 Impreza は fingerprint と values の追加だけで移植できる。

**直近のアクティビティ:** 直近 24 時間のコメントなし。2026 年 1 月から 2 月に sshane と作者の間でレビューのやり取りがある。Car behavior report では Subaru の 79 セグメント中 10 件で `carParams.safetyConfigs` などの差分が出ている。

**変更ファイル:** 11 ファイル、+397/-39。`carcontroller.py` の angle 制御対応、safety の `subaru.h`（+72）とテスト `test_subaru.py`（+148）の追加が中心である。

**次のアクション:** この作業をもとにした #3795 がメンテナ側で進んでおり、どちらを採用するか整理が必要である。

---

### ![PR](icons/git-pull-request.svg) [2025 Subaru Crosstrek Lateral Support](https://github.com/commaai/opendbc/pull/3795) `#3795`
**ステータス:** Open (Draft) | **ラベル:** car, subaru, car safety

**概要:** #2864 の Jacob Waller の作業をもとに、angle ベースの Subaru に angle steering を追加する PR である。対象は 2024 年以降の Crosstrek、2023 年以降の Ascent、2022 年以降の Outback、2025 Impreza である。停止後に `ES_Brake` が high のまま残るため cruise engage の判定に `ES_Status` を使い（Hybrid は `ES_Brake` のまま）、4 m/s 未満では操舵の振動を抑えるデッドバンドを設ける。

**直近のアクティビティ:** 直近 24 時間のコメントなし。既存のコメントは Car behavior report（79 セグメント中 10 件に差分）のみである。

**変更ファイル:** 12 ファイル、+438/-39。`carcontroller.py` の改修、safety の `subaru.h` と `safety.h` の変更、テスト `test_subaru.py`（+118、+148）の追加が中心である。

**次のアクション:** TODO として、LKAS 失敗事例の調査、injection test、lateral maneuver report、デッドバンド値の調整が残っている。

---

### ![PR](icons/git-pull-request.svg) [Tesla: Fix Autopark](https://github.com/commaai/opendbc/pull/2902) `#2902`
**ステータス:** Open (Draft) | **ラベル:** car, tesla, car safety

**概要:** Autopark は `DAS_steeringControl` で操舵するが、カメラからは `LANE_KEEP_ASSIST` しか透過されていなかったため失敗していた。stock LKAS の透過条件を `NONE` 以外の任意の操舵制御タイプに一般化し、Emergency Lane Departure Avoidance も透過されるようにする。あわせて、Summon と Smart Summon が設定する `DI_autoparkState` 由来の状態名を `autopark` から `summon` に改名する（挙動は変わらない）。`DAS_status` による検出は送信周期が約 2.4 Hz と safety の RX チェック下限 10 Hz を下回るため見送った。

**直近のアクティビティ:** 作者の AmyJeanes がレビューコメントを投稿した（本文はデータに含まれない）。

**変更ファイル:** 4 ファイル、+92/-75。safety の `tesla.h` の透過条件の変更、`carstate.py` の改名、テスト `test_tesla.py` の更新である。

**次のアクション:** Autopark、stock LKAS、Summon などの検証ルートの追加が必要である。

---

### ![PR](icons/git-pull-request.svg) [Tesla: new Model 3 route](https://github.com/commaai/opendbc/pull/3808) `#3808`
**ステータス:** Merged | **ラベル:** car

**概要:** 本文は空である。Tesla Model 3 のテストルートを新しいものに差し替えた。

**直近のアクティビティ:** github-actions bot の Car behavior report のみで、変更は検出されなかった。

**変更ファイル:** 1 ファイル、+1/-1。`car/tests/routes.py` の 1 行変更である。

**次のアクション:** マージ済みのため対応は不要である。

---

## commaai/panda

> 直近24時間の更新はありません。

## neovim/neovim

> 10 月の Nvim 0.13 リリースを控え、justinmk による multicursor の不具合修正が次々とマージされ、dot-repeat と CmdAtom の設計（`setrepeat()`、namespace、per-cursor state）をめぐる Issue も相次いで起票された。LSP の file watcher を共有する性能改善のマージ、runtime コードの nil-safety 診断の有効化、libuv v1.53.0 への更新で Windows と macOS のテストが壊れる問題も動いている。

### ![Issue](icons/issue-opened.svg) [nvim_buf_set_text in insert mode should update dot repeat](https://github.com/neovim/neovim/issues/19806) `#19806`
**ステータス:** Open | **ラベル:** api, needs:discussion, insert-mode

**概要:** Insert mode 中に `nvim_buf_set_text` で行った変更が dot-repeat に記録されず、`.` を押すと `ciw` だけが繰り返されて挿入内容が失われるという報告である。挿入（`start == end`）や削除（`replacement == [""]`）のように連続した領域への変更は、`nvim_input` と同様に dot-repeat へ蓄積すべきだと提案している。vscode-neovim では現在、scratch buffer で変更を打ち直して再生する回避策が必要になっており、この改善で回避策を削除できるとしている。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**次のアクション:** `needs:discussion` のため、API として dot-repeat への蓄積をどう扱うかの方針決定が必要である。

---

### ![Issue](icons/issue-opened.svg) [CTRL-Y/mouse wheel gets stuck or scrolls down next to conceal_lines + virt_lines](https://github.com/neovim/neovim/issues/42077) `#42077`
**ステータス:** Open | **ラベル:** input, mouse, marks

**概要:** `conceal_lines` で隠した行の隣に `virt_lines` がある場合、`CTRL-Y` やマウスホイールの上スクロールが止まる、または逆に下へスクロールするという報告である。markdown レンダラーがブロックを仮想行で置き換える構成で発生し、カーソルがウィンドウ最下行から引き上げられる場合に限って起きる。報告者は `scrolldown()` を読み、concealed 行の調整ループで `w_topline` が移動した際に filler 行が `done` に数えられず、カーソル補正が不足して `update_topline()` が下へ戻すことが原因と分析している。

**直近のアクティビティ:** justinmk が nightly での再現確認を依頼した。報告者は `v0.13.0-dev-1708` の nightly でも 0.12.5 と全く同じ結果になり、ウィンドウの高さによって停止位置が変わることも含めて再現すると回答した。

**次のアクション:** master でも再現することが確認されたため、報告者の分析をもとに `scrolldown()` の修正を検討する段階である。

---

### ![PR](icons/git-pull-request.svg) [feat(redo)!: dot-repeat input payload of 'opfunc'/:omap](https://github.com/neovim/neovim/pull/42040) `#42040`
**ステータス:** Open (Draft) | **ラベル:** multicursor, breaking-change, editor-state

**概要:** 'operatorfunc' や `:omap` のモーション（vim-sneak の `dz{target}` など）に渡されたユーザー入力（`getchar`/`input`）が、`CmdAtom.keys` には届くのに redo（dot-repeat）には含まれない問題を解決する破壊的変更である。'operatorfunc' では `restore_redobuff()` の後に、`:omap` ではオペレーター取得時に入力を追記する。プラグイン向けの指針は「キャッシュせず再読み込みする」ことで、入力をキャッシュして dot-repeat に対応してきたプラグインには適応を求める。

**直近のアクティビティ:** justinmk が、multicursor の状態を返す専用 API `nvim_mcursor_get()` の提案に対して、今は API より基本 UX を固めることを優先すると回答した。`lhs` と `keys` の違いに関する反論は、議論を Issue #41657 に集約するためそちらで返信したと述べている。

**変更ファイル:** 12 ファイル、+405/-103。`input_cmdatom.c` と `input.c`、`ops.c` での入力追記、`repeat.txt` や `lua-plugin.txt` へのプラグイン向け指針の追記、`cmdatom_spec.lua` と `mcursor_spec.lua` のテスト拡充である。

**次のアクション:** Draft のため、#41657 での議論の決着とプラグイン作者との合意を経て Ready for review に移行するのを待つ。

---

### ![Issue](icons/issue-opened.svg) [LSP: high cpu usage with huge number of files with `workspace/didChangeWatchedFiles` enabled](https://github.com/neovim/neovim/issues/23291) `#23291`
**ステータス:** Open | **ラベル:** performance, revisit-at-release, lsp

**概要:** 大量のファイル（6 万個）を含むディレクトリがあるリポジトリで rust-analyzer を使うと、`workspace/didChangeWatchedFiles` の動的登録によって CPU 使用率が高くなり動作が引っかかるという報告である。サーバーが送るパターンに該当しないファイルを除外すれば改善する可能性があるとしている。

**直近のアクティビティ:** justinmk が、#41778 のマージで改善が見られたらコメントしてほしい、そうすればクローズできると呼びかけた。

**次のアクション:** #41778 を含むビルドで報告者が改善を確認し、結果に応じてクローズする。

---

### ![Issue](icons/issue-opened.svg) [<S-Del> inside a terminal does not seem to get through](https://github.com/neovim/neovim/issues/42073) `#42073`
**ステータス:** Open | **ラベル:** encoding, tui, terminal, input, has:workaround

**概要:** Neovim の `:terminal` 内で `<S-Del>` を押しても、端末内のプロセスに届かないという報告である。wezterm、kitty、xterm、alacritty など複数の端末や tmux の有無を変えても同じで、Vim では正常に届くという。

**直近のアクティビティ:** 報告者が、当面の回避策として `tmap <S-Del> <Del>` を使っていると追記した。本来の動作ではないが許容範囲だとしている。

**次のアクション:** terminal モードで `<S-Del>` をどのシーケンスとして子プロセスに送るかを調査する必要がある。

---

### ![Issue](icons/issue-opened.svg) [multicursor: `cc` does not live-preview until next char](https://github.com/neovim/neovim/issues/41968) `#41968`
**ステータス:** Open | **ラベル:** multicursor

**概要:** multicursor で `cc` を実行した直後、追加カーソル側の行が次の文字を入力するまでクリアされないという報告である。

**直近のアクティビティ:** 直近 24 時間のコメントなし。以前に justinmk が、見た目だけの問題で Insert mode の操作は ESC 後にカスケードされると説明し、`cc` は行のインデントを変えるため `:h mcursor-limitations` に記載された制約に該当すると追記している。

**次のアクション:** 既知の制約に該当するため、ドキュメント上の制約として扱うかクローズするかの判断が必要である。

---

### ![PR](icons/git-pull-request.svg) [fix(multicursor): insert-mode <c-r>x per-cursor registers](https://github.com/neovim/neovim/pull/42076) `#42076`
**ステータス:** Merged | **ラベル:** multicursor

**概要:** Insert mode の `<c-r>x` がレジスタの文字列そのものを挿入するため、multicursor のカスケードで全カーソルにプライマリと同じ文字列が入る問題の修正である。`insert_reg()` が literal text ではなく `<c-r>x` として redo を準備するよう変更し、`atom_key_class` で CTRL-R が insert-span を区切るようにした。#41933 を修正する。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**変更ファイル:** 4 ファイル、+48/-18。`register.c`、`mcursor.c`、`input_cmdatom.c` の修正と `mcursor_spec.lua` へのテスト追加である。

**次のアクション:** マージ済みのため対応は不要である。

---

### ![Issue](icons/issue-opened.svg) [multicursor: wrong preview on insert-mode `<c-r>x`](https://github.com/neovim/neovim/issues/41933) `#41933`
**ステータス:** Closed | **ラベル:** display, multicursor

**概要:** multicursor を置いた状態で `cw<c-r>-` を実行すると、画面表示が正しくならないという報告である。動画で症状が示されている。

**直近のアクティビティ:** 直近 24 時間のコメントなし。#42076 のマージによりクローズされた。

**次のアクション:** クローズ済みのため対応は不要である。

---

### ![PR](icons/git-pull-request.svg) [feat: conceal-aware 'wrap'](https://github.com/neovim/neovim/pull/40897) `#40897`
**ステータス:** Open | **ラベル:** marks, conceal, AI assisted 🤖

**概要:** `'wrap'` 有効時に、conceal で隠したセルがあっても行が隠す前の列数で折り返される長年の問題（#14409、Vim 由来）を解決する PR である。`plines` で画面レイアウト幅と仮想幅を分け、`screenpos()`、マウス位置、`wincol()`、`gj`/`gk` などが再配置後の画面列に従うようにする。tree-sitter の `@conceal` も reflow させるため新しい decoration provider コールバック `_on_conceal` を追加する。仮想列（`virtcol()` など）は変えず、`matchadd()` 系や legacy の `:syntax` conceal は対象外である。conceal なしの場合でも再描画が 7〜12 % 程度遅くなる計測結果が示されている。

**直近のアクティビティ:** 直近 24 時間のコメントなし。8 月に justinmk と作者の間でレビューのやり取りが計 8 件あった。

**変更ファイル:** 50 ファイル、+5419/-427。`drawline.c`、`decoration.c`、`decoration_provider.c`、`drawscreen.c`、treesitter の `highlighter.lua` などが中心で、`api.txt` と `news.txt` に挙動変更を記載している。

**次のアクション:** 変更規模が大きく性能面の影響もあるため、メンテナによる再レビューが必要である。

---

### ![PR](icons/git-pull-request.svg) [perf(lua): share compatible file watchers](https://github.com/neovim/neovim/pull/41778) `#41778`
**ステータス:** Merged | **ラベル:** lsp, AI assisted 🤖

**概要:** LSP サーバーが古い登録を削除する前に同一の file watcher を登録し直すと、そのたびに watcher tree が作られ、大規模なワークスペースで setup と teardown が重くなる問題の改善である。glob のコンパイルを `vim._watch` に移し、複数ルートのルールを受け付けるようにしたうえで、互換性のある backend を最後の購読が解除されるまで共有する。#41755 を置き換え、#23291 に関連する。

**直近のアクティビティ:** 作者の lewis6991 が、共有の仕組みをすべて `_watch.lua` 側に移し、`vim.glob` による LSP glob に対応させたと報告した。コード量は増えたが設計が良くなり `_watchfiles.lua` が簡潔になったとしている。justinmk が方向性は正しいと承認し、マージされた。

**変更ファイル:** 7 ファイル、+877/-445。`_watch.lua` の拡張、`lsp/_watchfiles.lua` の簡素化、`watch_spec.lua` と `lsp_spec.lua` のテスト追加である。

**次のアクション:** マージ済みである。#23291 の報告者による効果確認を待つ。

---

### ![PR](icons/git-pull-request.svg) [Pos range](https://github.com/neovim/neovim/pull/42075) `#42075`
**ステータス:** Open | **ラベル:** なし

**概要:** `vim.pos` と `vim.range` のドキュメントに整数キーの記述がない点を補う PR である。#39867 のコメントを受けたもので、名前付きのキーは利便性のために存在するだけだと明記する。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**変更ファイル:** 3 ファイル、+33/-25。`lua.txt`、`pos.lua`、`range.lua` のドキュメント修正である。

**次のアクション:** メンテナによるレビューを待つ。

---

### ![PR](icons/git-pull-request.svg) [feat(img): relative images (editor & buffer) using unicode placeholder](https://github.com/neovim/neovim/pull/39496) `#39496`
**ステータス:** Open | **ラベル:** ci:skip-news

**概要:** `vim.ui.img.Opts` に `relative`（`ui`、`editor`、`buffer`）と `buf` を追加し、unicode placeholder を使って画像をエディタや buffer に相対配置できるようにする PR である。`editor` は floating window、`buffer` は virtual lines と extmark で画像を埋め込む。セルのピクセルサイズを CSI 16t で取得する `query_csi()` と `cell_px()` も追加する。ghostty と kitty では動作するが、WezTerm では placeholder 方式が正しく表示されないという。

**直近のアクティビティ:** justinmk が、`vim.ui.img` へのフィードバックがまだ一件もないため、この PR は Nvim 0.13 のリリース（10 月）後まで待つべきだと述べた。これに対し wurli が、この機能こそが `vim.ui.img` を使い始められない唯一の理由であり、experimental API としてでもマージされればすぐ使うと返信した。

**変更ファイル:** 7 ファイル、+2289/-173。`img.lua` の拡張、`_placement.lua`、`_diacritic.lua`、`_util.lua` の新規追加と、`img_spec.lua` への大量のテスト追加である。

**次のアクション:** 0.13 リリース後にフィードバックを踏まえて再検討する。前提となる #39484 の完了とリベースも必要である。

---

### ![Issue](icons/issue-opened.svg) [nvim becomes unresponsive when another process deletes the parent directory of a file that nvim has an open buffer to (on Windows)](https://github.com/neovim/neovim/issues/41851) `#41851`
**ステータス:** Open | **ラベル:** platform:windows, filesystem

**概要:** Windows で、開いているファイルの親ディレクトリを別プロセスが削除すると、`vim/log.lua:107` の `attempt to index local 'info' (a nil value)` が繰り返し表示されて Neovim が応答しなくなるという報告である。ファイルだけを削除した場合は通常どおり `E211` が表示される。以前のビルドでは `_watch.lua` で `EPERM` を含むエラーが出ており、#41776 のマージでエラー箇所が変わったと推測している。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**次のアクション:** Windows 環境で `_watch.lua` と `log.lua` のエラー処理を調査する必要がある。

---

### ![PR](icons/git-pull-request.svg) [fix(ui): ui.open() modifies opts.cmd](https://github.com/neovim/neovim/pull/41885) `#41885`
**ステータス:** Merged | **ラベル:** ui

**概要:** `vim.ui.open()` がパスを呼び出し元のコマンドリストに追記してしまうため、同じ options を再利用すると以前のパスも再び開かれる問題の修正である。追記の前にコマンドリストをコピーするようにした。

**直近のアクティビティ:** 直近 24 時間のコメントなし。9 月 14 日に barrettruth が承認していた。

**変更ファイル:** 2 ファイル、+12/-1。`ui.lua` の 1 行修正と、同じ opener options を 2 回使うテストの追加である。

**次のアクション:** マージ済みのため対応は不要である。

---

### ![PR](icons/git-pull-request.svg) [fix(folds): avoid computing disabled folds on file open](https://github.com/neovim/neovim/pull/41914) `#41914`
**ステータス:** Closed | **ラベル:** folds

**概要:** `nofoldenable` でファイルを開いても、`hasFoldingWin()` が folding の有効判定より前に fold を更新するため、buffer 全体の fold が計算されてしまう問題（#9084）の修正案である。先に `hasAnyFolding()` を確認するよう変更する。

**直近のアクティビティ:** justinmk が、低労力の AI による貢献に見え（「Validation」節は CONTRIBUTING.md を理解していない兆候だとしている）、Issue も緊急ではないため見送ると述べてクローズした。

**変更ファイル:** 2 ファイル、+44/-2。`fold.c` の条件順序の変更と `fold_spec.lua` のテスト追加である。

**次のアクション:** クローズ済みのため対応は不要である。#9084 は未解決のまま残る。

---

### ![PR](icons/git-pull-request.svg) [build(vim-patch): misc n/a patches for runtime/](https://github.com/neovim/neovim/pull/42044) `#42044`
**ステータス:** Merged | **ラベル:** なし

**概要:** #42045 と組み合わせて、vX.Y.0000 系のパッチの大半を N/A として検出できるようにする PR である。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**変更ファイル:** 2 ファイル、+2/-0。`vim-patch.sh` と `vim_na_hunks_help.txt` への追記である。

**次のアクション:** マージ済みのため対応は不要である。

---

### ![PR](icons/git-pull-request.svg) [feat(pos,range): overload `.extmark()` to take `{buf}`, `{extmark}` args](https://github.com/neovim/neovim/pull/41234) `#41234`
**ステータス:** Open | **ラベル:** なし

**概要:** `vim.pos.extmark()` と `vim.range.extmark()` を、`{buf}` と `{extmark}` を引数に取る形でも呼べるようにオーバーロードする PR である。#41187 を解決するもので、テストも追加された。

**直近のアクティビティ:** barrettruth が承認した後、justinmk がレビューコメントを 6 件付け、PR の説明とコミットメッセージに明確で簡潔な problem/solution が書かれていないと指摘した。

**変更ファイル:** 7 ファイル、+188/-41。`pos.lua`、`range.lua`、新規の `pos/_util.lua`、`lua.txt` と `news.txt`、`pos_spec.lua` と `range_spec.lua` のテストである。

**次のアクション:** 作者が PR 説明とコミットメッセージに problem/solution を書き、justinmk のレビューコメントに対応する必要がある。

---

### ![PR](icons/git-pull-request.svg) [feat(multicursor): gQ restores primary-cursor position](https://github.com/neovim/neovim/pull/42071) `#42071`
**ステータス:** Merged | **ラベル:** multicursor, ci:skip-news

**概要:** `gv` と異なり `gQ` がプライマリカーソルの位置を復元しないため、カーソルを一時的に消して移動した後に multicursor セッションへ戻りにくい問題への対応である。`'nvim.multicursor.last'` namespace の extmark-id 1 にプライマリカーソルの位置を保存する。復元を望まないユーザーは `gQ` の直前にこの extmark を削除すればよいが、インターフェースが確定していないため意図的に文書化していない。#42004 と #41939 を修正する。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**変更ファイル:** 4 ファイル、+31/-13。`mcursor.c` と `_core/mcursor.lua` の修正、`repeat.txt` の更新、テスト追加である。

**次のアクション:** マージ済みのため対応は不要である。

---

### ![Issue](icons/issue-opened.svg) [multicursor: `gQ` does not restore all cursor positions](https://github.com/neovim/neovim/issues/41939) `#41939`
**ステータス:** Closed | **ラベル:** multicursor

**概要:** `gQ` が直前の N 個のカーソルのうち N-1 個しか復元せず、現在のカーソルの位置は元に戻らないという報告である。現在のカーソルも multicursor の 1 つとして位置を復元すべきだとしている。

**直近のアクティビティ:** 直近 24 時間のコメントなし。#42071 のマージによりクローズされた。

**次のアクション:** クローズ済みのため対応は不要である。

---

### ![Issue](icons/issue-opened.svg) [multicursor: `[C`/`]C` from a mapping, merges cursors in follow-mode](https://github.com/neovim/neovim/issues/41995) `#41995`
**ステータス:** Closed | **ラベル:** multicursor

**概要:** `[C`/`]C` をキーマップ経由で実行すると、follow mode が有効な場合にカーソルが統合（削除）されてしまうという報告である。マップを使わずに直接実行した場合は問題が起きない。

**直近のアクティビティ:** justinmk が議論を重ね、プライマリカーソルとの重なりだけではモーションで重複除去しない一方、非プライマリ同士の重なりは常に除去するというモデルを #42060 で実装したと報告した。follow mode で `k` によりカーソルがバッファ端を越える場合はカーソルが失われるのが想定どおりであり、`[C` は絶対移動のジャンプなのでカーソルは追従しないと説明している。その後、この Issue を修正対象とする #42063 がマージされ、クローズされた。

**次のアクション:** クローズ済みのため対応は不要である。

---

### ![PR](icons/git-pull-request.svg) [fix(lua): enable nil-safety diagnostics in runtime code](https://github.com/neovim/neovim/pull/42067) `#42067`
**ステータス:** Open | **ラベル:** defaults, lua, lsp, multicursor, treesitter, diagnostic, filetype, snippet, plugin-editorconfig, ux-visibility

**概要:** runtime コードで無効になっていた EmmyLua の `need-check-nil` 診断を有効にし、出てきた警告を型の精緻化、明示的な不変条件、実際に nil になりうる箇所へのガード追加で解消する PR である。あわせて `vim.fn` のオーバーロード生成、`getline()` のシグネチャ修正、`getqflist()` の型付け、ドキュメントパーサーへの `@return_cast` 対応、同期的な `LanguageTree:parse()` の戻り値の non-nil 化なども行う。

**直近のアクティビティ:** gpanders が承認し、justinmk も複数のレビューコメントの後に承認した。コメントが改善された箇所があり、LSP 関連のバグ報告の改善にもつながると期待している。

**変更ファイル:** 67 ファイル、+615/-436。`.emmyrc.json` の設定変更、`vimfn.gen.lua` と `vimfn_types.lua` の型定義、diagnostic、ui2、treesitter など runtime 全体の Lua ファイルの修正である。

**次のアクション:** 2 名の承認が揃っているため、作者の最終調整後にマージされる見込みである。

---

### ![PR](icons/git-pull-request.svg) [build(deps): bump libuv to v1.53.0](https://github.com/neovim/neovim/pull/42072) `#42072`
**ステータス:** Open (Draft) | **ラベル:** build

**概要:** 依存ライブラリ libuv を v1.53.0 に更新する PR である。allyourcodebase/libuv 側の更新がまだだが、何が壊れるかを確認するために作成された Draft である。

**直近のアクティビティ:** 作者の clason が、Windows で `WaitOnAddress` と `WakeByAddressSingle` の未解決シンボルによるリンクエラーが出ると報告し、`target_link_libraries` に `synchronization` などを追加する必要があるかを dundargoc に尋ねた。さらに macOS でも `server_spec.lua` の `startup --listen validates` テストが flaky ではなく失敗すると報告した。

**変更ファイル:** 1 ファイル、+2/-2。`cmake.deps/deps.txt` のバージョンとハッシュの更新である。

**次のアクション:** Windows のリンク設定と macOS のテスト失敗への対処、および allyourcodebase/libuv の更新が必要である。

---

### ![Issue](icons/issue-opened.svg) [multicursor: per cursor state](https://github.com/neovim/neovim/issues/42070) `#42070`
**ステータス:** Closed | **ラベル:** multicursor, marks

**概要:** multicursor の再生時に、キー入力をまたいでモジュールレベルの状態を持つ Lua コードが前回の再生の副作用を受ける問題（#42000）への対策として、カーソルごとの状態を持てるようにする提案である。カーソル削除時に状態も片付くよう、インスタンス ID ではなく状態そのものを持たせることを求めている。

**直近のアクティビティ:** justinmk が extmark へのデータやタグの付与（#34683、#18572）で解決できないかと問い、multicursor 専用に設計する理由はなく、namespace や extmark のフックが必要だと述べた。起票者の altermo はカーソルが extmark の範囲外に移動しうる点や状態の片付け方を懸念したが、最終的に namespace の扱いとして一般化できることに気づいていなかったと認め、Issue はクローズされた。

**次のアクション:** クローズ済みである。extmark へのデータ付与の Issue（#34683、#18572）側で検討が続く。

---

### ![PR](icons/git-pull-request.svg) [fix(multicursor): restore primary cursor position with `gQ`](https://github.com/neovim/neovim/pull/42004) `#42004`
**ステータス:** Closed | **ラベル:** multicursor

**概要:** multicursor を消去した時点のプライマリカーソル位置を記録し、`gQ` で復元する外部コントリビューターの修正案である。

**直近のアクティビティ:** justinmk が、新たな namespace を増やさずに実現したいとして別途 #42071 を作成し、この PR はクローズされた。

**変更ファイル:** 3 ファイル、+28/-0。`_core/mcursor.lua`、`mcursor.c`、`mcursor_spec.lua` への追加である。

**次のアクション:** クローズ済みのため対応は不要である。

---

### ![PR](icons/git-pull-request.svg) [fix(multicursor): skip gQ during an active session](https://github.com/neovim/neovim/pull/42068) `#42068`
**ステータス:** Merged | **ラベル:** multicursor

**概要:** multicursor セッションが有効な間の `gQ` は意味をなさないため、何もせずメッセージを表示するようにした。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**変更ファイル:** 3 ファイル、+13/-5。`_core/mcursor.lua`、`repeat.txt`、`mcursor_spec.lua` の修正である。

**次のアクション:** マージ済みのため対応は不要である。

---

### ![PR](icons/git-pull-request.svg) [build(vim-patch): faster lookup for vim-patch: commits](https://github.com/neovim/neovim/pull/42065) `#42065`
**ステータス:** Merged | **ラベル:** build, vim-patch

**概要:** `vim-patch.sh` で git log のためだけのサブシェルをやめ、検索開始コミットを指定し、`$tokens` 配列のキーから `vim-patch:` 接頭辞を除いてメモリ使用量を減らす変更である。`vim-patch:8.1.0` のようなゼロ埋めのないトークン用のキーを追加する余地を作る目的がある。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**変更ファイル:** 1 ファイル、+11/-9。`scripts/vim-patch.sh` の修正である。

**次のアクション:** マージ済みのため対応は不要である。

---

### ![Issue](icons/issue-opened.svg) [Operator that asks for user input while supporting dot-repeat and multicursor](https://github.com/neovim/neovim/issues/41657) `#41657`
**ステータス:** Open | **ラベル:** multicursor

**概要:** ユーザー入力を求め、dot-repeat では再度尋ねず、multicursor でも動作するアクションをプラグインで実装する方法についての Issue である。一般的な「'operatorfunc' を設定して `g@l` を返し、入力をキャッシュする」方式では、multicursor や `:h edit-repeat` の再生時に `g@l` と入力が一緒に再生されて不整合が起きる。expression mapping 評価時に入力を確定させる回避策は、textlock などの制約に当たるとしている。

**直近のアクティビティ:** justinmk が #42040 での反論に返信し、解決済みの RHS（`keys`）は失敗しても処理が少なくなるだけで `u` 1 回で戻せる一方、LHS の再生は副作用が際限なく広がりうると説明した。multicursor のカスケードは編集とモーションにしか意味がなく、LHS はプライマリでは妥当でもカスケード先では不適切な Ex コマンドや Lua に展開されうるとしている。

**次のアクション:** #42040 の方針（入力を redo に含め、プラグインはキャッシュせず再読み込みする）について、プラグイン作者との合意形成が続く。

---

### ![Issue](icons/issue-opened.svg) [CmdAtom: `setrepeat()` API (plugin can override dot-repeat register)](https://github.com/neovim/neovim/issues/42069) `#42069`
**ステータス:** Open | **ラベル:** api, needs:design, multicursor, editor-state

**概要:** #41415 から切り出された Issue で、プラグインが dot-repeat の適用方法をカスタマイズできる `setrepeat()` API を提供する提案である。現状プラグインは `<expr>` マッピングの動的生成や値のキャッシュで対応しており、multicursor のカスケードと相性が悪い。`:FzfLua files` のように terminal mode とやり取りするマッピングは、Nvim がアクションの終わりを判断できず再生できないため、プラグイン側が終わりと繰り返し方を宣言できるようにしたいとしている。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**次のアクション:** `needs:design` のため、API 設計の検討が必要である。

---

### ![Issue](icons/issue-opened.svg) [CmdAtom: include `namespace` field](https://github.com/neovim/neovim/issues/41415) `#41415`
**ステータス:** Open | **ラベル:** multicursor, mappings, namespaces-scopes

**概要:** `vim.keymap.set()` や `nvim_create_user_command` に namespace を付けられるようにし、`CmdAtom` イベント発生時に元になったマッピングやコマンドの namespace を `CmdAtom.namespace` フィールドとして含める提案である。今回、プラグイン定義のアクションの用途として #42069 への参照が追記された。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**次のアクション:** #42069 と合わせて設計を検討する段階である。

---

### ![PR](icons/git-pull-request.svg) [fix(multicursor): mapped jump merges cursors in follow-mode](https://github.com/neovim/neovim/pull/42063) `#42063`
**ステータス:** Merged | **ラベル:** multicursor

**概要:** ジャンプは意図的にカスケードされず atom を積まないが、follow mode ではカーソルを動かしたのに atom を積まないマッピングが LHS でカスケードされるため、`nnoremap <Down> ]C` が各カーソルで `]C` を再生してカーソルが重なり統合されていた。composite にジャンプが含まれる場合は `composite.queued` を設定して LHS の再生を無効にする。#41995 を修正する。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**変更ファイル:** 2 ファイル、+19/-3。`input_cmdatom.c` の修正と `mcursor_spec.lua` へのテスト追加である。

**次のアクション:** マージ済みのため対応は不要である。

---

### ![PR](icons/git-pull-request.svg) [feat(lua): in script mode -l, use raw output for print()](https://github.com/neovim/neovim/pull/42064) `#42064`
**ステータス:** Open (Draft) | **ラベル:** lua

**概要:** `nvim -l` でスクリプトを実行したときの `print()` の挙動を、素の Lua に近づける PR である。現状は末尾の改行が 1 つ無視される、空の `print ''` で空行が出ない、エスケープシーケンスがエスケープされて色が付かない、メッセージの末尾でカーソルが止まるといった問題がある。作者の bfredl は、これらは対話的エディタ向けのメッセージ処理を通ることによる意図しない挙動だとしている。

**直近のアクティビティ:** justinmk が、長年の不満点なので賛成だとコメントした。

**変更ファイル:** 1 ファイル、+11/-5。`src/nvim/lua/executor.c` の修正である。

**次のアクション:** Draft のため、作者による仕上げと Ready for review への移行を待つ。

---

### ![PR](icons/git-pull-request.svg) [fix(tui): terminal response split by a pause is typed as keys](https://github.com/neovim/neovim/pull/42062) `#42062`
**ステータス:** Open (Draft) | **ラベル:** tui

**概要:** 端末からの応答が 'ttimeoutlen' より長い間隔で分割されて届くと、キー入力として扱われてしまう問題の修正である。例として、SSH 越しに OSC 52 のクリップボードで `"+p` を行うと、端末が許可を確認している間に通信が止まり、base64 がバッファに入ってしまう。数字で始まり制御文字を含まない DCS、OSC、APC 文字列については残りを最低 1 秒待つ。Vim の 'osctimeoutlen' と同じ問題への対処である。#42061 に依存する。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**変更ファイル:** 7 ファイル、+106/-14。`tui/input.c`、`termkey/driver-csi.c` の修正、`options.lua` 関連のドキュメント更新、`tui_spec.lua` と `termkey_spec.lua` のテスト追加である。

**次のアクション:** AI エージェントが作成した Draft で、作者自身がレビューとテストを終えるまで Draft のままとされている。

---

### ![PR](icons/git-pull-request.svg) [fix(tui): terminal response is typed as keys with 'nottimeout'](https://github.com/neovim/neovim/pull/42061) `#42061`
**ステータス:** Open (Draft) | **ラベル:** tui

**概要:** 'nottimeout' や負の 'ttimeoutlen' の設定で、`ESC \` で終わる端末応答が一度の read で届いてもキー入力として扱われる問題（#29047）の修正である。起動時の OSC 11 応答でカーソル下の文字が "g" に置き換わる症状がある。`tinput_read_cb()` と同様にタイムアウト 0 を使うようにする。副作用として、同じ read 内の `ESC ESC [ A` が `<M-Up>` と解釈されるようになる。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**変更ファイル:** 2 ファイル、+20/-12。`tui/input.c` の修正と `tui_spec.lua` へのテスト追加である。

**次のアクション:** #42062 と同じく、作者自身のレビューとテスト完了を待つ Draft である。

---

### ![PR](icons/git-pull-request.svg) [fix(multicursor): don't dedupe primary-overlapping cursor](https://github.com/neovim/neovim/pull/42060) `#42060`
**ステータス:** Merged | **ラベル:** multicursor

**概要:** プライマリと重なるカーソルは編集前に重複除去されるが、Visual や Insert のアクションが先にプライマリを動かすとスキップされない問題の修正である。重なるカーソルは除去せずに再生だけをスキップし、位置をプライマリに同期させる。重なり判定（`CmdOrigin.mcursor`）はアクション開始時にサンプリングするよう変更した。#42025 を修正し、#41995 を部分的に修正する。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**変更ファイル:** 5 ファイル、+85/-47。`mcursor.c`、`input_cmdatom.c` とヘッダの修正、`mcursor_spec.lua` のテスト追加である。

**次のアクション:** マージ済みのため対応は不要である。

---

### ![Issue](icons/issue-opened.svg) [multicursor: immediate edit after `{Visual}Q` is applied twice at main cursor](https://github.com/neovim/neovim/issues/42025) `#42025`
**ステータス:** Closed | **ラベル:** multicursor

**概要:** `{Visual}Q` の直後に編集すると、メインカーソル位置で編集が 2 回適用されるという報告である。メインカーソルと追加カーソルが同じセルにあり、まだ重複除去されていないことが原因と推測している。

**直近のアクティビティ:** 直近 24 時間のコメントなし。#42060 のマージによりクローズされた。

**次のアクション:** クローズ済みのため対応は不要である。

---

### ![PR](icons/git-pull-request.svg) [feat(complete): per-item "startcol" and "filter_text"](https://github.com/neovim/neovim/pull/41938) `#41938`
**ステータス:** Open | **ラベル:** なし

**概要:** complete-items に `startcol` と `filter_text` を追加し、各項目が自分の列から置換され、自分のテキストで絞り込まれるようにする PR である。現状の `vim.lsp.completion` は全項目を最小の開始列に揃えて余白を埋め、挿入テキストと絞り込みテキストを 1 つの文字列で兼ねるため、選択を誤ると入力中に項目が消えたり、範囲のない項目が二重に挿入されたりする。LSP 仕様の記述に沿った変更で、"preinsert" を extmark による Lua 実装へ移す道も開くとしている。

**直近のアクティビティ:** justinmk が補完プラグインの作者からのフィードバックを求めた。nvim-dap の補完に取り組む igorlfs が、カーソルより前を置換する `startcol` を指定すると項目がメニューに出ないと質問し、glepnir は置換されるテキストが `filter_text`（なければ `word`）の先頭と一致しないと除外されると回答した。igorlfs はこれを受け、開始列が混在するケースへの懸念を動画付きで示した。

**変更ファイル:** 9 ファイル、+448/-103。`insexpand.c` の大幅な改修、`insert.txt` などのドキュメント更新、`completion_spec.lua` のテスト追加である。

**次のアクション:** 補完プラグイン作者からのフィードバックを集め、開始列が混在するケースの扱いを詰める必要がある。

---

### ![PR](icons/git-pull-request.svg) [perf(mbyte): single decoder loop for both cluster byte length and cell width](https://github.com/neovim/neovim/pull/42023) `#42023`
**ステータス:** Open | **ラベル:** performance

**概要:** ホットループ内で文字クラスタを何度もデコードしていた処理を、バイト長とセル幅の両方を 1 回のデコードで求めるよう統合する性能改善である。

**直近のアクティビティ:** 直近 24 時間のコメントなし。以前に justinmk が、コミット種別を `perf(mbyte)` にし news に記載する価値があるかと指摘しており、`news.txt` への追記が含まれている。

**変更ファイル:** 17 ファイル、+220/-159。`mbyte.c` と `mbyte.h` の中心的な変更と、`charset.c`、`plines.c`、`indent.c`、`ops.c` など呼び出し側の修正である。

**次のアクション:** メンテナによるレビューを待つ。

---

### ![PR](icons/git-pull-request.svg) [fix(health): close floating window on BufLeave](https://github.com/neovim/neovim/pull/42056) `#42056`
**ステータス:** Closed | **ラベル:** ux-visibility

**概要:** `:checkhealth` の floating window が BufLeave で閉じるようにする修正案で、#39307 を対象としている。

**直近のアクティビティ:** justinmk が、これも場当たり的な対処にすぎないと却下した。'previewpopup' があるので healthcheck は `:pedit` を使い、popup にするかどうかは 'previewpopup'（おそらく既定で有効にすべき）で制御する方針を提案している。PR はクローズされた。

**変更ファイル:** 1 ファイル、+12/-0。`runtime/lua/vim/health.lua` への追加である。

**次のアクション:** クローズ済みのため対応は不要である。#39307 は `:pedit` 方式で改めて対応する必要がある。

---

### ![PR](icons/git-pull-request.svg) [fix(terminal): clear extmarks on damage](https://github.com/neovim/neovim/pull/42055) `#42055`
**ステータス:** Closed | **ラベル:** terminal

**概要:** terminal の damage 時に extmark を消去する修正案で、#42053 を対象としている。本文には Issue 番号以外の説明はない。

**直近のアクティビティ:** 作者自身がアプローチが誤っているとしてクローズした。

**変更ファイル:** 1 ファイル、+7/-1。`src/nvim/terminal.c` の修正である。

**次のアクション:** クローズ済みである。#42053 は別の方法での修正が必要である。

---

### ![Issue](icons/issue-opened.svg) [`checkhealth` floating window not closed when the focused buffer changes](https://github.com/neovim/neovim/issues/39307) `#39307`
**ステータス:** Open | **ラベル:** plugin, runtime, complexity:low, ux-visibility

**概要:** `vim.g.health = { style = 'float' }` で開いた `:checkhealth` の floating window が、`:spl` や `:tabe` で focus が別のバッファに移っても閉じないという報告である。floating window の場合は BufLeave で閉じるべきだとしている。

**直近のアクティビティ:** justinmk が #40536 の過去コメントを参照したうえで、'previewpopup' を使い healthcheck を `:pedit` で表示する方針を提案した。

**次のアクション:** `:pedit` と 'previewpopup' を使う方式での実装が必要である。

---

### ![PR](icons/git-pull-request.svg) [fix(options): reject restricted assignments before parsing callbacks](https://github.com/neovim/neovim/pull/42038) `#42038`
**ステータス:** Merged | **ラベル:** options

**概要:** 制限されたオプション代入が、拒否される前にコールバックを解析してしまう問題の修正である。modeline から発火した Syntax ハンドラが、変更できないはずのオプションの bound arguments を評価できてしまっていた。726d1a92d2 でコールバック構築が権限チェックより前に移ったことが原因で、権限チェックをコールバック解析より前に行うよう戻した。あわせて option callback の引数を型付きポインタに整理するリファクタリングも含む。

**直近のアクティビティ:** 直近 24 時間のコメントなし。9 月 22 日に justinmk が承認していた。

**変更ファイル:** 6 ファイル、+107/-77。`option.c` と `optionstr.c` の修正、`option_defs.h` の型変更、`options_spec.lua` へのテスト追加である。

**次のアクション:** マージ済みのため対応は不要である。modeline 経由の評価に関わる修正のため、リリースノートでの扱いを確認するとよい。

---

### ![Issue](icons/issue-opened.svg) [build: src/gen/keyset.lua is not a tracked codegen dependency, so generated keysets go stale and the build breaks](https://github.com/neovim/neovim/issues/42059) `#42059`
**ステータス:** Closed | **ラベル:** build

**概要:** `src/gen/keyset.lua` が 2 つのコード生成器から `require` されているのに `src/nvim/CMakeLists.txt` の依存関係に含まれていないため、このファイルだけが変わると生成物が古いまま残りビルドが壊れるという報告である。a4773f1980 で `FieldHashfn` の型が `const` 付きに変わった際、既存のビルドツリーでは GCC 14 以降で `-Wincompatible-pointer-types` エラーになる。CI は毎回新しいビルドディレクトリを使い、Clang は警告のみのため気づかれなかった。

**直近のアクティビティ:** justinmk が、`file(GLOB ... CONFIGURE_DEPENDS)` で `src/gen/*.lua` 全体を依存に含める案は試す価値があるとコメントした。Issue はクローズされた。

**次のアクション:** クローズ済みである。依存関係の追加が反映されたかを確認するとよい。

---

### ![Issue](icons/issue-opened.svg) [LSP: Potential issue with setting root_spec when dealing with symlink sources](https://github.com/neovim/neovim/issues/33004) `#33004`
**ステータス:** Open | **ラベル:** lsp, filesystem

**概要:** `~/dotfiles/.config/nvim` を `~/.config/nvim` へ symlink している環境で、`~/dotfiles` から Lua ファイルを開くと LSP の root がリンク先の `~/.config/nvim` に変わり、それに依存するツール（live grep など）の動作範囲が変わるという報告である。BashLS では起きず、Terraform LSP では symlink なしでも同様の挙動があるため、サーバー固有の問題の可能性もあるとしている。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**次のアクション:** 最小再現手順が示されていないため、再現条件の切り分けが必要である。

---

### ![PR](icons/git-pull-request.svg) [fix(search): show search count of ";" repeated search](https://github.com/neovim/neovim/pull/42057) `#42057`
**ステータス:** Open | **ラベル:** なし

**概要:** `/foo/;/bar` のような `;` で連結した検索で、`do_search()` が `;` と次の検索方向の 2 文字を読み飛ばすのに `patlen` を 1 しか減らさないため、パターンの NUL 終端がメッセージに入り検索件数の表示が消える問題の修正である。ui2 ではメッセージから件数を抽出できず `attempt to concatenate a nil value` エラーになる。`patlen` から 2 を引くよう修正する。#41929 を解決する。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**変更ファイル:** 2 ファイル、+13/-1。`search.c` の 1 行修正と `messages2_spec.lua` へのテスト追加である。

**次のアクション:** メンテナによるレビューを待つ。

---

### ![PR](icons/git-pull-request.svg) [vim-patch:9.1.0000,9.2.0000](https://github.com/neovim/neovim/pull/42045) `#42045`
**ステータス:** Merged | **ラベル:** test, vim-patch

**概要:** Vim の 9.1.0000 と 9.2.0000 のパッチを取り込む PR である。関連するメジャーバージョンでパッチ `0` を追加するよう `vimpatch.lua` の対応を必要とし、テストを通すため #42019 の変更を引き継いでいる。

**直近のアクティビティ:** zeertzjq が承認した。作者の janlazo は、タイトルの `vim-patch:9.2.000` で `0` が 1 つ足りず、パッチ名は完全一致が必要だと補足した。

**変更ファイル:** 3 ファイル、+32/-40。`version.c` のパッチ一覧更新と、`has_spec.lua`、`test_expr.vim` のテスト修正である。

**次のアクション:** マージ済みのため対応は不要である。

---

### ![PR](icons/git-pull-request.svg) [build(clang-tidy): disable checks renamed from cert-*](https://github.com/neovim/neovim/pull/42049) `#42049`
**ステータス:** Merged | **ラベル:** build

**概要:** clang-tidy 22 で `cert-err34-c` が `bugprone-unchecked-string-to-number-conversion` に、`cert-env33-c` が `bugprone-command-processor` に改名され、旧名は alias として残ったため、`insexpand.c` の `atoi()` が報告されるようになった問題への対応である。改名後のチェックを無効にする。

**直近のアクティビティ:** 直近 24 時間のコメントなし。9 月 23 日に justinmk、zeertzjq、作者の間でレビューのやり取りがあった。

**変更ファイル:** 1 ファイル、+3/-0。`.clang-tidy` への追記である。

**次のアクション:** マージ済みのため対応は不要である。

---

### ![PR](icons/git-pull-request.svg) [fix(ui2): preserve message highlights from window-local override](https://github.com/neovim/neovim/pull/36717) `#36717`
**ステータス:** Closed | **ラベル:** needs:response, ui2

**概要:** メッセージ出力のハイライトがウィンドウローカルの _extui 定義の影響を受け、`hi IncSearch` などが正しく表示されない問題（#36691）の修正案である。上書きごとに退避用のハイライト定義を作り、メッセージの extmark に使う。

**直近のアクティビティ:** github-actions bot が、情報提供の依頼に 30 日間応答がなかったとして自動でクローズした。

**変更ファイル:** 2 ファイル、+23/-8。`_extui/messages.lua` と `_extui/shared.lua` の修正である。

**次のアクション:** 作者が求められた情報を提供すれば再オープンできる。

---

### ![PR](icons/git-pull-request.svg) [fix(filetype): default .h files to C](https://github.com/neovim/neovim/pull/42028) `#42028`
**ステータス:** Merged | **ラベル:** filetype

**概要:** `.h` ファイルの既定の filetype を C++ から C に変える PR である。Vim の現在の検出ロジックを移植し、先頭 100 行に C++ や Objective-C の構文があればそちらと判定する。既存の `g:c_syntax_for_h` と `g:ch_syntax_for_h` は引き続きサポートし、明示的な上書き用に `g:filetype_h` を追加する。#41990 を解決する。

**直近のアクティビティ:** zeertzjq が lint の失敗を指摘し、作者が修正した後に zeertzjq が承認してマージされた。

**変更ファイル:** 3 ファイル、+36/-13。`filetype/detect.lua` の検出ロジックと `filetype.txt`、`syntax.txt` のドキュメント更新である。

**次のアクション:** マージ済みである。`.h` の既定が変わるため、C++ ヘッダを `.h` で書いているユーザーは `g:filetype_h` などでの設定が必要になる。

---

### ![Issue](icons/issue-opened.svg) [Use C syntax for .h files](https://github.com/neovim/neovim/issues/41990) `#41990`
**ステータス:** Closed | **ラベル:** has:vim-patch, filetype

**概要:** `.h` ファイルを開くと C++ として扱われ、C にするには `vim.g.c_syntax_for_h` の設定が必要という報告である。2025 年 4 月の Vim のコミット 3041cf6 以降の upstream と挙動がずれているとしている。

**直近のアクティビティ:** 直近 24 時間のコメントなし。#42028 のマージによりクローズされた。

**次のアクション:** クローズ済みのため対応は不要である。

---

## nvim-telescope/telescope.nvim

> 直近24時間の更新はありません。

## ghostty-org/ghostty

> v1.4 リリースに向けた文字列凍結の中、main ビルドのバージョン文字列に余分な `-` が入る不具合の報告と修正 PR が同日に出された。kitty graphics の参照カウント化による renderer の性能改善、GTK の fractional scaling 対応、`CSI 8 t` によるウィンドウリサイズ、代替画面モードの報告修正など機能や互換性の PR が並び、vouch 制度による未承認コントリビューターの PR 自動クローズも見られた。

### ![PR](icons/git-pull-request.svg) [build: trim branch name before sanitizing it for the version](https://github.com/ghostty-org/ghostty/pull/14386) `#14386`
**ステータス:** Open | **ラベル:** なし

**概要:** `git rev-parse` の出力末尾の改行がハイフンに置換され、`1.3.2-main-+7c40388b2` のようなバージョンになっていた問題の修正である。ブランチ名をサニタイズする前に trim する。#14385 を修正する。

**直近のアクティビティ:** vancluever が承認した。

**変更ファイル:** 1 ファイル、+7/-4。`src/build/GitVersion.zig` の修正である。

**次のアクション:** 承認済みのため、マージを待つ。

---

### ![PR](icons/git-pull-request.svg) [kitty,renderer: Add ref-counted images and eliminate unnecessary copies in the renderer](https://github.com/ghostty-org/ghostty/pull/14379) `#14379`
**ステータス:** Open | **ラベル:** なし

**概要:** #13285 を土台に、画像データと形式やサイズを持つ `CpuImage` と、参照カウントで共有する `ArcCpuImage` を追加し、`kitty/graphics_storage.zig` と `renderer/image.zig` の画像型を統一する PR である。renderer は参照カウントを増やすだけで GPU アップロード待ちの画像を保持でき、GraphicsAPI 側がネイティブ対応する形式を判断して余分なコピーや swizzle を省く。CPU 側の形式変換もアップロード時まで遅延する。kitty storage の eviction は共有参照を手放すだけになり、アニメーション編集は参照カウントが 1 なら排他所有に切り替えて変更する。`mpv --vo=kitty` のプロファイルで swizzle 処理（全体の 3.80 %）が消えたとしている。

**直近のアクティビティ:** 作者が rockorager にレビューを依頼した。`mpv --vo=kitty` ではまだ kitty 本体よりフレーム落ちが多い（10 秒で約 62 対約 52）ものの、1.3.1 以降の改善と合わせてほぼ滑らかに動画を再生できると補足した。

**変更ファイル:** 14 ファイル、+1132/-364。新規の `src/terminal/image.zig`、`renderer/image.zig`、kitty graphics の `graphics_storage.zig`、`graphics_image.zig`、`graphics_exec.zig`、OpenGL と Metal の Texture 実装の修正である。

**次のアクション:** rockorager などメンテナによるレビューが必要である。eviction の意味の変更（共有参照によるメモリの過大計上）について合意を得る必要がある。

---

### ![PR](icons/git-pull-request.svg) [Update VOUCHED list](https://github.com/ghostty-org/ghostty/pull/14387) `#14387`
**ステータス:** Merged | **ラベル:** なし

**概要:** jcollie による discussion コメントを受け、ghostty-vouch bot が @pltrz を VOUCHED リストに追加した自動 PR である。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**変更ファイル:** 1 ファイル、+1/-0。`.github/VOUCHED.td` への追加である。

**次のアクション:** マージ済みのため対応は不要である。

---

### ![Issue](icons/issue-opened.svg) [Extra `-` in Ghostty version](https://github.com/ghostty-org/ghostty/issues/14385) `#14385`
**ステータス:** Open | **ラベル:** なし

**概要:** main ブランチからビルドした Ghostty のバージョンが `1.3.2-main+7c40388b2` ではなく `1.3.2-main-+7c40388b2` になるという報告である。ad9225a4c 以降の退行で、`git rev-parse --abbrev-ref HEAD` の出力末尾の `\n` がハイフンに置換されることが原因とされ、サニタイズ前に trim すれば直ることを報告者が確認している。discussion #14381 から Issue 化された。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**次のアクション:** 修正 PR #14386 のマージでクローズされる見込みである。

---

### ![PR](icons/git-pull-request.svg) [libghostty-vt: let the png decode hook report an allocation cap](https://github.com/ghostty-org/ghostty/pull/14133) `#14133`
**ステータス:** Open | **ラベル:** なし

**概要:** `GhosttySysImage` が `data_len` しか持たず、ライブラリがその長さでピクセルバッファを解放するため、デコードコールバックはピクセルデータちょうどの大きさで確保するしかなかった問題（#12163）の修正である。確保したバイト数を表す `data_cap` を追加し、解放には `data_cap`、ピクセルの読み取りには `data_len` を使う。0 なら従来どおり `data_len` と同じ扱いで、`data_len` 未満の値は不正として画像を拒否する。

**直近のアクティビティ:** 直近 24 時間のコメントなし。9 月 3 日に jcollie と作者の間でレビューのやり取りがあった。

**変更ファイル:** 5 ファイル、+175/-5。`include/ghostty/vt/sys.h` の API 追加、`src/terminal/c/sys.zig` の実装とテスト、`sys.zig` の `capacity` フィールドと `deinit` 追加、C のサンプル更新である。

**次のアクション:** メンテナによる再レビューを待つ。

---

### ![PR](icons/git-pull-request.svg) [terminal: fix reporting and restoring of modes 47/1047/1048/1049](https://github.com/ghostty-org/ghostty/pull/14362) `#14362`
**ステータス:** Open (Draft) | **ラベル:** vt

**概要:** モード 47、1047、1048、1049 が端末の実際の状態ではなくモードビットから報告されている問題の修正である。47、1047、1049 は代替画面が有効かどうかから、1048 はカーソルが実際に保存されているかどうかから報告し保存するようにする。XTRESTORE 1049 で画面が消去される問題も直し、代替画面は離れた時のカーソル位置で戻る。xterm の実装を参照して挙動の違いを説明しており、mode 1046 対応（#4502）の下準備でもあるとしている。#14199 を解決する見込みである。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**変更ファイル:** 3 ファイル、+398/-40。`Terminal.zig` への状態保存と報告関数の追加、`stream_terminal.zig` と `termio/stream_handler.zig` の修正である。

**次のアクション:** Draft のため、`Terminal.modes.values` に残る不要なビットの扱いを詰めたうえで Ready for review に移行するのを待つ。

---

### ![Issue](icons/issue-opened.svg) [DECRQM reports alt screen modes 47/1047/1049 independently of the active screen](https://github.com/ghostty-org/ghostty/issues/14199) `#14199`
**ステータス:** Open | **ラベル:** needs-confirmation

**概要:** モード 47、1047、1049 はいずれも代替画面が有効かどうかを表すが、Ghostty はモードごとに独立したビットを持ち、`setMode` が画面切り替えの前にビットを書くため、DECRQM が実際の画面と食い違う値を返すという報告である。`?47h` で代替画面に入り `?1049l` で抜けると、主画面なのにモード 47 が set と報告される。xterm は単一のバッファフラグ `screen->whichBuf` から 3 つとも答えている。

**直近のアクティビティ:** korikhin が修正 PR #14362 を作成したと報告し、jcollie に知らせた。

**次のアクション:** #14362 のレビューとマージを待つ。

---

### ![PR](icons/git-pull-request.svg) [macos,gtk: add broadcast support to sync across panes](https://github.com/ghostty-org/ghostty/pull/13745) `#13745`
**ステータス:** Open | **ラベル:** なし

**概要:** 複数のペインをグループ（最大 16）にまとめ、グループ内の 1 ペインへの入力を全ペインに送る同期入力機能を追加する PR である。#3227、#11797、#12832 で要望されていたもので、グループはスプリット、タブ、ウィンドウをまたげる。修飾キー付きクリックや `toggle_broadcast_group:N` キーバインドでペインを追加し、`clear_broadcast_groups` で全解除する。グループごとに色付きの枠が表示され、色は `broadcast-group-colors` で変更できる。コア側で制御し、macOS と GTK の両ランタイムと libghostty の binding-action API から利用できる。

**直近のアクティビティ:** 直近 24 時間のコメントなし。8 月に pluiedev と bo2themax が承認した。jcollie は番号を 0 始まりに統一する修正などを加えたうえで、1.4 リリース後にマージ可能と承認した。trag1c は 1.4 の文字列凍結中であるため、新しい文字列を理由に CHANGES_REQUESTED を付けている。

**変更ファイル:** 52 ファイル、+2827/-1643。`include/ghostty.h`、macOS の Swift コード、GTK ランタイムの修正と、新しい文字列による各言語の `.po` ファイルの更新である。

**次のアクション:** 1.4 リリースで文字列凍結が解除された後、trag1c のブロックを解いてマージする流れである。

---

### ![PR](icons/git-pull-request.svg) [Support resizing the window with CSI 8 t](https://github.com/ghostty-org/ghostty/pull/14375) `#14375`
**ステータス:** Open | **ラベル:** なし

**概要:** xterm のウィンドウ操作シーケンス `CSI 8 ; rows ; columns t`（XTWINOPS）を実装し、プログラムがセル単位で端末サイズを要求できるようにする PR である。#1115 に関連する。SSH 越しの悪用に備え、新設の `vt-window-resize-allowed` で明示的に有効にしない限り無効で、40 列 × 10 行未満の要求は引き上げ、画面より大きい要求は画面サイズに制限する。スプリット、複数タブ、quick terminal、最大化やタイル配置などウィンドウマネージャーがサイズを管理する場合は無視する。GNOME では `gtk_window_set_default_size()` を使い、macOS と Linux で動作確認済みとしている。

**直近のアクティビティ:** 作者がリサイズの動作を示すスクリーンショットを投稿した。

**変更ファイル:** 12 ファイル、+297/-0。`terminal/stream.zig` のパーサー、`Surface.zig`、`apprt/action.zig`、GTK の `window.zig` と `application.zig`、macOS の `TerminalController.swift` と `Ghostty.App.swift`、`Config.zig` への設定追加である。

**次のアクション:** メンテナによるレビューが必要である。新しい設定項目の追加と、セキュリティ面の制約が十分かの確認が論点になる。

---

### ![PR](icons/git-pull-request.svg) [macos: don't report deep press when Force Click is disabled](https://github.com/ghostty-org/ghostty/pull/14376) `#14376`
**ステータス:** Closed | **ラベル:** os/macos

**概要:** macOS 27 以降、Force Click を無効にしていても通常のトラックパッドのクリックで AppKit が pressure stage 2（deep press）を報告するため、Ghostty がクリックとドラッグのたびに単語選択してしまう問題の修正案である。`com.apple.trackpad.forceClick` がオフの場合は通常のクリックとして扱う。

**直近のアクティビティ:** ghostty-vouch bot が、作者が VOUCHED リストに含まれていないとして自動でクローズした。

**変更ファイル:** 1 ファイル、+21/-8。`SurfaceView_AppKit.swift` の修正である。

**次のアクション:** クローズ済みである。macOS 27 での不具合自体は未解決のため、discussion #14286 経由で vouch を得たコントリビューターかメンテナによる対応が必要である。

---

### ![PR](icons/git-pull-request.svg) [gtk: support fractional surface scaling](https://github.com/ghostty-org/ghostty/pull/14269) `#14269`
**ステータス:** Open | **ラベル:** なし

**概要:** GTK ランタイムで `gtk_widget_get_scale_factor()` の代わりに `gdk_surface_get_scale()` を使い、デバイスサイズと DPI を切り上げた整数ではなく実際の fractional scale に合わせる PR である。テクスチャをデバイスピクセルと 1:1 に保ち、`GdkSurface` の `notify::scale` を監視して resize と再描画する。fractional scaling でフォントや罫線文字がぼやける問題（#1938 の退行）への対応で、Niri Wayland の 1.3 倍、1.5 倍、1.666 倍で確認済み、X11 は未確認としている。

**直近のアクティビティ:** 作者が pluiedev に、ログのレベルを下げたと報告し、細かな改善は今後の PR で議論したいと述べた。jcollie と pluiedev はすでに承認しており、pluiedev はリサイズ時の伸びやティアリングの問題も解消されたと評価している。

**変更ファイル:** 6 ファイル、+328/-48。新規の `apprt/gtk/scale.zig`、`render_surface.zig` と `surface.zig` の修正、`Surface.zig` と `SharedDeps.zig` の調整である。

**次のアクション:** 2 名の承認が揃っており、jcollie は pluiedev の了承を得てマージするとしているため、マージを待つ段階である。

---

### ![PR](icons/git-pull-request.svg) [Update VOUCHED list](https://github.com/ghostty-org/ghostty/pull/14373) `#14373`
**ステータス:** Merged | **ラベル:** なし

**概要:** jcollie による discussion コメントを受け、ghostty-vouch bot が @shreeve（#14375 の作者）を VOUCHED リストに追加した自動 PR である。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**変更ファイル:** 1 ファイル、+1/-0。`.github/VOUCHED.td` への追加である。

**次のアクション:** マージ済みのため対応は不要である。

---

### ![PR](icons/git-pull-request.svg) [terminal/c: expose the application-requested mouse shape](https://github.com/ghostty-org/ghostty/pull/14371) `#14371`
**ステータス:** Open | **ラベル:** なし

**概要:** `ghostty_terminal_get` に `GHOSTTY_TERMINAL_DATA_MOUSE_SHAPE` を追加し、公開 enum `GhosttyMouseShape` を返すことで、C のフロントエンドが OSC 22 で要求されたポインタ形状を自前で OSC を解析せずに取得できるようにする PR である。文字列をそのまま渡す低レベルな方式も Discord で議論されたが、Zig 側と同じく整理された enum を使う方針を選んだとしている。AI を多用して作成したと明記されている。

**直近のアクティビティ:** 作者が、この方式について考えを持っていた pluiedev に言及してレビューを求めた。

**変更ファイル:** 5 ファイル、+116/-0。新規の `include/ghostty/vt/mouse/shape.h`、`terminal.h` と `mouse.h` の更新、`src/terminal/c/terminal.zig` と `types.zig` の実装である。

**次のアクション:** pluiedev などメンテナによる API 方針のレビューが必要である。

---

## astral-sh/uv

> 0.12.19 へのバージョン更新がマージされ、0.12.8 以降の `direct_url.json` の `sig` 秘匿化による再インストール回帰が修正された。charliermarsh による URL 永続化ポリシーの整理、lockfile の入力正規化や未使用設定の省略、konstin による packaged lock からのツールインストールなど、preview 機能の積み上げ PR が多数進行している。

### ![PR](icons/git-pull-request.svg) [Report signal-terminated child processes](https://github.com/astral-sh/uv/pull/21758) `#21758`
**ステータス:** Open | **ラベル:** なし

**概要:** 子プロセスが Unix シグナルで終了した場合に、既存の `128 + signal` の終了コード変換は維持しつつ、シグナル名を stderr に出力する PR である。#11886 を修正し、シグナル終了と同じ数値で正常終了したケースを区別できるようにする。診断は uv の printer 経由で出し、`-q` では表示、`-qq` では抑止され、core dump の有無も表示する。

**直近のアクティビティ:** Abhirup0 が CHANGES_REQUESTED で、bash や zsh と同様に `SIGINT`（Ctrl+C）と `SIGPIPE`（`| head` など）では診断を出さないよう求めていた。作者は SIGINT と SIGPIPE を対象外にし、診断の書き込み失敗を無視して終了ステータスがエラーに変わらないようにしたと報告した。

**変更ファイル:** 6 ファイル、+66/-17。`crates/uv/src/child.rs` の終了ステータス処理の改修が中心で、`run`、`format`、`ty` チェック、`tool run` の呼び出し側を合わせて修正し、SIGSEGV と通常の `exit(139)` を区別する回帰テストを追加している。

**次のアクション:** 指摘への対応が済んだため、レビュアーによる再レビューが必要である。

---

### ![Issue](icons/issue-opened.svg) [Adding AWS SSO support for S3 Endpoints](https://github.com/astral-sh/uv/issues/21976) `#21976`
**ステータス:** Open | **ラベル:** enhancement

**概要:** preview の S3 機能で `AWS_PROFILE`、`AWS_DEFAULT_PROFILE` と SSO ベースのトークン更新に対応してほしいという要望である。現状は `aws sso login` と `aws configure export-credentials` で認証情報を環境変数に書き出して回避している。提案として `[[tool.uv.index]]` に `signing = "s3"` を指定すると、uv がプロファイルを読み、必要に応じて SSO のトークンを更新する API を示している。報告者は実装も申し出ており、シェル変数に書き出すより安全だと述べている。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**次のアクション:** メンテナによる API 設計の判断と、報告者に実装を任せるかどうかの回答が必要である。

---

### ![PR](icons/git-pull-request.svg) [Make URL persistence policies explicit](https://github.com/astral-sh/uv/pull/21973) `#21973`
**ステータス:** Open (Draft) | **ラベル:** なし

**概要:** #21971 の後続で、URL をどう永続化するかを型で明示する PR である。`DisplaySafeUrl` は表示の秘匿化のみを担い、汎用の Serde シリアライズを実装しなくなる。呼び出し側は、認証情報を保持する型、userinfo を除去する型、userinfo と既知のクエリ認証情報を除去する型のいずれかを選ぶ。成果物の場所や内部データはロスレスに、`direct_url.json` はクエリを保持し、index や設定の参照では既知の認証情報を除去する。`without_credentials` と `remove_credentials` は `without_userinfo` に改名する。

**直近のアクティビティ:** bot のみである。astral-sh-bot のテスト一覧（`uv-redacted` で 2 件追加、1 件削除）と、codspeed の性能レポート（0.16 % 低下、実行環境の違いに関する警告付き）である。

**変更ファイル:** 36 ファイル、+502/-160。新規の `crates/uv-redacted/src/persistence.rs`（+231）と `uv-redacted/src/lib.rs` の改修が中心で、`pylock_toml.rs`、`uv-lock`、`direct_url.rs`、`requirement.rs`、`index_url.rs` など URL を保存する箇所を広く置き換えている。

**次のアクション:** Draft のため、作者による Ready for review への移行を待つ。#21974 がこの PR の上に積まれている。

---

### ![PR](icons/git-pull-request.svg) [Bump version to 0.12.19](https://github.com/astral-sh/uv/pull/21975) `#21975`
**ステータス:** Merged | **ラベル:** なし

**概要:** 自動化 bot による 0.12.19 へのバージョン更新 PR である。本文は空で、CHANGELOG と各クレートのバージョンを更新している。

**直近のアクティビティ:** bot が crates.io 公開ポリシーのチェック失敗を通知し、新たに公開対象となるクレートを astral-sh/crates-policies に登録するよう求めた。charliermarsh は `uv-once-map` は存在するので失敗はおかしいとコメントした。

**変更ファイル:** 156 ファイル、+408/-374。`CHANGELOG.md`（+33）、`Cargo.toml`、`Cargo.lock` と各クレートの `Cargo.toml` と `README.md` のバージョン表記の更新である。

**次のアクション:** マージ済みである。公開ポリシーチェックの失敗原因を確認し、リリース公開に支障がないか確かめる必要がある。

---

### ![PR](icons/git-pull-request.svg) [Sync latest Python releases](https://github.com/astral-sh/uv/pull/21970) `#21970`
**ステータス:** Merged | **ラベル:** なし

**概要:** 最新の Python リリース情報を取り込む自動更新 PR である。

**直近のアクティビティ:** jjhelmus が承認した。

**変更ファイル:** 3 ファイル、+1441/-999。`crates/uv-python/download-metadata.json` の更新が大半で、sysconfig のマッピング生成コードも更新している。

**次のアクション:** マージ済みのため対応は不要である。

---

### ![Issue](icons/issue-opened.svg) [`uv lock` scales exponentially with the size of a single `[tool.uv] conflicts` group](https://github.com/astral-sh/uv/issues/21954) `#21954`
**ステータス:** Closed | **ラベル:** bug

**概要:** `[tool.uv] conflicts` で相互排他にした extras の数に対して `uv lock` の時間が指数的に増えるという報告である。`transformers` のようにリリース履歴が多い依存をフォーク間で共有している場合に起き、N=50 で 23.4 秒、N=65 で 109.9 秒、N=70 以上では 120 秒以内に終わらない。多数の小さなグループに分けても同じで、効いているのは `conflicts` 全体の extras の総数だとしている。再現スクリプトも添付されている。

**直近のアクティビティ:** charliermarsh が #18317 とほぼ同じ問題ではないかと指摘し、報告者が重複としてクローズしてよいと応じた。

**次のアクション:** 重複としてクローズ済みで、追跡は #18317 で行う。

---

### ![PR](icons/git-pull-request.svg) [Align URL validation across CLI and configuration](https://github.com/astral-sh/uv/pull/21974) `#21974`
**ステータス:** Open (Draft) | **ラベル:** なし

**概要:** #21973 の上に積まれ、#21729 の診断出力の秘匿化修正を前提として含む PR である。人が入力する URL のデシリアライズで `DisplaySafeUrl::parse` を適用し、`tool.uv.sources` などの設定でも CLI と同じく曖昧な認証情報を拒否する。プロキシ URL もスキーム省略時を含めて同様に検証する。永続化された URL 型はこの曖昧さを判定せず、`package:version@revision` のようなパスを保持し、Bazel 認証ヘルパーは明示的にこの挙動を選ぶ。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**変更ファイル:** 6 ファイル、+217/-21。`crates/uv-redacted/src/lib.rs`（+89）と `persistence.rs`、`proxy_url.rs` の検証処理、`auth/helper.rs` の Bazel 対応、`sync.rs` のテスト追加、`uv.schema.json` の更新である。

**次のアクション:** Draft のため、#21973 のレビューが進んだ後に Ready for review となるのを待つ。

---

### ![PR](icons/git-pull-request.svg) [Preserve query parameters in direct URL metadata](https://github.com/astral-sh/uv/pull/21971) `#21971`
**ステータス:** Merged | **ラベル:** なし

**概要:** #21360 で `sig` を `DisplaySafeUrl` の秘匿対象に加えた結果、`direct_url.json` にも `sig=****` が書かれ、他のクエリ値も再エンコードされていた問題の修正である。保存 URL が要求 URL と一致せず、毎回アンインストールと再インストールが起きていた。userinfo の認証情報だけを除いた元の URL をシリアライズし、表示上の秘匿は維持する。#21969 をクローズする。

**直近のアクティビティ:** astral-sh-bot のテスト一覧（`direct_url_json_query` を 1 件追加）のみである。

**変更ファイル:** 2 ファイル、+55/-1。`crates/uv-pypi-types/src/parsed_url.rs` のシリアライズ修正と、`pip_install.rs` の回帰テスト追加である。

**次のアクション:** マージ済みのため対応は不要である。

---

### ![Issue](icons/issue-opened.svg) [Direct URL requirements with a `sig` query parameter are reinstalled on every run: `direct_url.json` stores the redacted URL (regression in 0.12.8)](https://github.com/astral-sh/uv/issues/21969) `#21969`
**ステータス:** Closed | **ラベル:** bug

**概要:** 0.12.8 以降、クエリに `sig=` を含む direct URL 要求（Azure Storage の SAS URL など）が `uv pip install` のたびに再インストールされるという報告である。`direct_url.json` に `sig=****` が保存され、`st` などのクエリも再エンコードされるため、次回の比較で別 URL と判定される。二分探索で 0.12.7 は正常、0.12.8 が最初の不具合版、0.12.18 でも再現すると特定し、#21360 が原因と推測している。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**次のアクション:** #21971 のマージでクローズ済みのため対応は不要である。

---

### ![Issue](icons/issue-opened.svg) [uv sync accesses unavailable files even when told not to](https://github.com/astral-sh/uv/issues/21972) `#21972`
**ステータス:** Open | **ラベル:** bug

**概要:** パス指定の依存を dependency group に置き、ロック時にはそのパスが存在した場合、別のマシンで `uv sync --locked --no-group lab` を実行してもそのファイルにアクセスして失敗するという報告である。`Distribution not found at` のエラーになり、`--no-sources-package` を付けると解決不能エラーになる。`[[tool.uv.dependency-metadata]]` を使うとハッシュが必要だと言われる。OS 間やアクセス権の違うマシン間で起きるとし、バグか想定挙動かを尋ねている。Windows、uv 0.12.18 での報告である。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**次のアクション:** メンテナによる再現確認と、想定挙動かどうかの判断が必要である。

---

### ![Issue](icons/issue-opened.svg) [Dependency Dashboard](https://github.com/astral-sh/uv/issues/2658) `#2658`
**ステータス:** Open | **ラベル:** internal

**概要:** Renovate による依存更新の一覧 Issue である。`actions/attest-build-provenance` に置き換え候補があること、長期間更新のない依存が 32 件あることなどを示している。

**直近のアクティビティ:** 直近 24 時間のコメントなし（bot による本文の更新のみ）

**次のアクション:** 定常的な管理用 Issue のため、個別の更新 PR の対応を進める。

---

### ![PR](icons/git-pull-request.svg) [Add `PackageServer` to simplify tests needing to test a compromised index](https://github.com/astral-sh/uv/pull/21952) `#21952`
**ステータス:** Open | **ラベル:** internal

**概要:** パッケージファイルとその index エントリを配信し、内容や公開ハッシュを途中で変更できる共有テストサーバーを追加する PR である。packse に組み込むのはきれいでないと判断して別に用意した。既存と今後のテストを簡素化する。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**変更ファイル:** 10 ファイル、+278/-450。新規の `crates/uv-test/src/package_server.rs` と、`lock.rs`、`pip_install.rs`、`build/cache.rs`、`tool_upgrade.rs` などのテストの置き換えである。

**次のアクション:** #21944 の上に積まれたスタックのため、下位 PR とあわせてレビューが必要である。

---

### ![PR](icons/git-pull-request.svg) [Use `generate_wheel_with_files` for `locked_build_dependency_wheel`](https://github.com/astral-sh/uv/pull/21944) `#21944`
**ステータス:** Open | **ラベル:** internal

**概要:** スタックの準備中に見つけた小さな整理で、`locked_build_dependency_wheel` テストで `generate_wheel_with_files` を使うようにする。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**変更ファイル:** 1 ファイル、+19/-36。`crates/uv/tests/lock/lock.rs` のみである。

**次のアクション:** #21942 の上に積まれており、レビュー待ちである。

---

### ![PR](icons/git-pull-request.svg) [Unify generation of sdists with "malicious" build backends in tests](https://github.com/astral-sh/uv/pull/21942) `#21942`
**ステータス:** Open | **ラベル:** internal

**概要:** 副作用を持つビルドバックエンド付き sdist を作り、それが決して import されないことを確かめるテストが増えてきたため、生成処理を 1 つの関数にまとめる PR である。関数はマーカーを置くパスを受け取る。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**変更ファイル:** 5 ファイル、+176/-267。`crates/uv-test/src/archive.rs` への関数追加と、`lock.rs`、`pip_compile.rs`、`build/cache.rs`、`direct_url_hashes.rs` のテストの書き換えである。

**次のアクション:** レビュー待ちである。

---

### ![PR](icons/git-pull-request.svg) [Filter distribution hashes in tests](https://github.com/astral-sh/uv/pull/21941) `#21941`
**ステータス:** Merged | **ラベル:** internal

**概要:** テストのスナップショットに含まれる配布物のハッシュをフィルタする PR である。不透明なハッシュがあるとスナップショットの差分が回帰か無関係な変動か判別しにくく、後続 PR でハッシュが大きく変わる予定でもあるためである。

**直近のアクティビティ:** charliermarsh が承認した。

**変更ファイル:** 2 ファイル、+83/-39。`lock.rs` と `pip_compile.rs` のテストである。

**次のアクション:** マージ済みのため対応は不要である。

---

### ![PR](icons/git-pull-request.svg) [bench: attempt to stabilize digest benchmarks](https://github.com/astral-sh/uv/pull/21966) `#21966`
**ステータス:** Open | **ラベル:** internal, area:benchmarks

**概要:** digest のマイクロベンチマークが 1 回の関数呼び出しを計測していてノイズが大きかったため、500 回ずつまとめて計測するよう変える PR である。機能変更はなく、ベンチマークのみの変更である。

**直近のアクティビティ:** 作者の woodruffw が調査メモを残した。#21963 で digest ベンチマークが改善して見えたのは、バイナリ配置の変化で `str::from_utf8` が命令キャッシュラインの先頭に来て触るライン数が 5 から 4 に減ったことと、CodSpeed のシミュレーションモードが計測ごとにキャッシュを消すため cold cache のコストが毎回乗っていたことの組み合わせだという。代替として walltime 計測への切り替えもあるが、#20165 のとおりノイズが増え変化を検出しにくくなると述べ、ベンチマークを誤ってシミュレーション専用や walltime 専用に分類しにくくする仕組みも必要だとしている。

**変更ファイル:** 1 ファイル、+28/-14。`crates/uv-bench/benches/uv_pypi_types.rs` である。

**次のアクション:** メンテナによるレビューと、計測モードの方針判断が必要である。

---

### ![PR](icons/git-pull-request.svg) [Omit unused scoped overrides and exclusions from lockfiles](https://github.com/astral-sh/uv/pull/21895) `#21895`
**ステータス:** Closed | **ラベル:** enhancement, preview

**概要:** 解決結果に影響しないスコープ付き overrides と exclusions を lockfile に記録しないようにし、それらの変更で `--locked` が無効化されないようにする PR であった。`--preview-features missing-scoped-settings-lock` で有効化する設計だった。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**変更ファイル:** 8 ファイル、+463/-4。`uv-lock/src/lock/mod.rs` の検証処理と `lock.rs` のテスト（+361）が中心である。

**次のアクション:** #21913 に置き換えられてクローズされたため対応は不要である。

---

### ![PR](icons/git-pull-request.svg) [Omit unused dependency-metadata entries from lockfiles](https://github.com/astral-sh/uv/pull/21894) `#21894`
**ステータス:** Closed | **ラベル:** preview

**概要:** 実行時の解決で使われない `dependency-metadata` エントリを lockfile から省き、その変更で `--locked` が無効化されないようにする PR であった。`--preview-features missing-dependency-metadata-lock` で有効化し、バージョン指定のエントリをバージョンなしの fallback より優先する規則を解決器と共有する設計だった。

**直近のアクティビティ:** 直近 24 時間のコメントなし（9 月 22 日に konstin が `None` バージョンの扱いに質問を付けて承認していた）

**変更ファイル:** 7 ファイル、+636/-25。`dependency_metadata.rs`、`uv-lock/src/lock/mod.rs` の改修と `lock.rs` のテスト（+558）である。

**次のアクション:** #21913 に置き換えられてクローズされたため対応は不要である。

---

### ![PR](icons/git-pull-request.svg) [Support package-scoped dependency constraints](https://github.com/astral-sh/uv/pull/21899) `#21899`
**ステータス:** Closed | **ラベル:** enhancement

**概要:** `constraint-dependencies` で特定パッケージ（任意でバージョンも指定）が宣言する依存に対して制約を掛けられるようにする PR である。選ばれた依存は宣言上の要件と、一致するすべてのグローバル制約とスコープ付き制約を満たす必要がある。セレクタは lockfile とツールのレシートに記録し、変更時はロックを無効化する。

**直近のアクティビティ:** 直近 24 時間のコメントなし（bot のテスト一覧は 9 月 21 日のもの）

**変更ファイル:** 33 ファイル、+1025/-249。`crates/uv-configuration/src/constraints.rs`（+257/-36）が中心で、resolver、`uv-lock`、pip と project の各コマンド、ツールのレシートにまたがる。

**次のアクション:** マージされずにクローズされたが、理由はデータから読み取れない。この PR を土台とする #21900 の扱いを確認する必要がある。

---

### ![PR](icons/git-pull-request.svg) [Omit unused runtime settings from lockfiles](https://github.com/astral-sh/uv/pull/21913) `#21913`
**ステータス:** Merged | **ラベル:** enhancement, preview

**概要:** `resolution-inputs` preview 機能を有効にすると、使われない実行時設定を `uv.lock` から省く PR である。グローバル制約、グローバルとスコープ付きの overrides と exclusions、dependency metadata、`exclude-newer-package` のうち解決中に参照されたものを記録し、ロックされたパッケージとバックトラック中に参照された設定を保持する。無関係な設定の変更はロックを無効化せず、次の再解決時に反映される。#21894 と #21895 を置き換える。

**直近のアクティビティ:** EliteTK が preview 機能として承認したが、大量の clone は性能影響はなさそうでも好ましくないと述べ、`check_respects_exclude_newer_package_for_ty_selection` のスナップショット更新が必要だと指摘した。bot のテスト一覧では `uv::lock` に 11 件追加されている。

**変更ファイル:** 29 ファイル、+1947/-87。新規の `uv-lock/src/lock/inputs.rs`（+173）と `resolution_recorder.rs`（+94）、resolver の候補選択や yanked 判定、`distribution_database.rs` への記録処理の追加が中心である。

**次のアクション:** マージ済みのため対応は不要である。clone の多さについては後続で検討の余地がある。

---

### ![Issue](icons/issue-opened.svg) [uv no longer exports `FlatDistributions`](https://github.com/astral-sh/uv/issues/21964) `#21964`
**ステータス:** Closed | **ラベル:** なし

**概要:** #21540 で `FlatDistributions` が非公開になり、pixi で `VersionMap` を構築できなくなったため、再公開を求める報告である。

**直近のアクティビティ:** 報告者が礼を述べ、zanieb が応じた。

**次のアクション:** #21965 で修正済みのため対応は不要である。

---

### ![Issue](icons/issue-opened.svg) [uv sync leaves an environment with missing NCCL library unchanged](https://github.com/astral-sh/uv/issues/21968) `#21968`
**ステータス:** Open | **ラベル:** なし

**概要:** Linux の HPC 環境で `libnccl.so.2` が見つからず torch の import に失敗した報告である。`nvidia_nccl_cu13` の dist-info と `RECORD` は存在したが `nvidia/nccl/` ディレクトリが丸ごと欠けており、`uv sync --frozen` を繰り返しても復旧も警告もされなかった。`--reinstall-package` で復旧した。ホームのファイルシステムが満杯だったことを誘因と疑っているが原因は特定できていない。再インストールせずに欠損ファイルを検出する方法があるかを尋ねている。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**次のアクション:** メンテナによる回答（検出手段の有無、または機能要望としての扱い）が必要である。

---

### ![PR](icons/git-pull-request.svg) [Add support for requiring hashes for build dependencies in `uv pip`](https://github.com/astral-sh/uv/pull/21314) `#21314`
**ステータス:** Open | **ラベル:** enhancement

**概要:** `uv pip install` と `uv pip sync` に opt-in の `--require-build-hashes`（環境変数 `UV_PIP_REQUIRE_BUILD_HASHES`）を追加する PR である。ビルド用の隔離環境に入る依存にハッシュを必須とし、対象環境に入る実行時要件のポリシーとは分けて扱う。

**直近のアクティビティ:** 直近 24 時間のコメントなし（レビューは 8 月から 9 月 18 日にかけての本文なしのコメントのみ）

**変更ファイル:** 13 ファイル、+385/-24。`crates/uv-types/src/hash.rs` のハッシュポリシー、CLI と設定、`pip_sync.rs` と `pip_install.rs` のテストである。

**次のアクション:** メンテナによるレビューの継続が必要である。

---

### ![Issue](icons/issue-opened.svg) [Allow per-package overrides](https://github.com/astral-sh/uv/issues/2686) `#2686`
**ステータス:** Closed | **ラベル:** enhancement

**概要:** グローバルな override ではなく、特定パッケージ内の依存要件だけを置き換える override を求める 2024 年の要望である。グローバル版よりリスクが低く、エラーメッセージもわかりやすくなるとしている。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**次のアクション:** クローズ済みである。クローズ理由はデータから読み取れない。

---

### ![PR](icons/git-pull-request.svg) [Add a preview feature for lazy build backend imports](https://github.com/astral-sh/uv/pull/21967) `#21967`
**ステータス:** Merged | **ラベル:** performance, preview

**概要:** `build-lazy-imports` preview 機能を追加し、CPython 3.15 以降でビルドフックを `-X lazy_imports=all` 付きで実行する PR である。短命のプロセスで使わないバックエンドコードの import を避けて起動を速くする。サードパーティのバックエンドの import 時副作用に影響しうるため opt-in としている。CPython `3.15.0rc2` での warm ビルド計測では、setuptools、Flit Core、Hatchling、Poetry Core、pdm-backend の各ケースで約 10〜17.5 % の時間短縮を示した。

**直近のアクティビティ:** charliermarsh が承認した。

**変更ファイル:** 9 ファイル、+168/-3。`crates/uv-build-frontend/src/lib.rs` のフラグ付与、preview 定義、`.python-versions` への 3.15 追加、`build.rs` のテスト（+146）である。

**次のアクション:** マージ済みのため対応は不要である。

---

### ![PR](icons/git-pull-request.svg) [Install tools from packaged locks](https://github.com/astral-sh/uv/pull/21962) `#21962`
**ステータス:** Open (Draft) | **ラベル:** なし

**概要:** `locked-tools` preview 機能のもとで `uv tool install`、`uv tool run`/`uvx`、`uv tool upgrade` に `--locked` を追加する PR である。依存は選ばれた wheel の `.dist-info/pylock.toml` から取り、成果物ハッシュを必須として検証する。依存は PyPI が該当名とバージョンで公開する URL に限り、ローカルパスや VCS は拒否する。`--with`、overrides、exclusions、依存への constraints も拒否する。#21961 の上に積まれている。

**直近のアクティビティ:** zanieb が 4 件の質問を出した。`--with` はレイヤー環境に入るのになぜ拒否するのか、exclusions は使えるべきではないか、Python やプラットフォームの非互換リリースを飛ばす処理は小さな事前ソルバーを新設したのか、ソースツリーの場合を検討したか、である。

**変更ファイル:** 20 ファイル、+1209/-57。新規の `crates/uv/src/commands/tool/locked.rs`（+372）とテスト `tool_locked.rs`（+493）、`install.rs`、`run.rs`、`upgrade.rs` の改修、`docs/concepts/tools.md` の追記である。

**次のアクション:** 作者が zanieb の設計上の質問に回答する必要がある。

---

### ![PR](icons/git-pull-request.svg) [Support package-scoped build constraints](https://github.com/astral-sh/uv/pull/21900) `#21900`
**ステータス:** Open (Draft) | **ラベル:** なし

**概要:** `build-constraint-dependencies` にパッケージとバージョンのスコープを追加する PR である。スコープはビルド対象のパッケージを選び、推移的依存やバックエンドフックが返す要件を含むビルド環境全体を制約する。グローバル制約と一致するすべてのスコープを組み合わせ、スコープ付き制約とハッシュは lockfile とツールのレシートに記録される。#21899 の上に積まれている。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**変更ファイル:** 49 ファイル、+1715/-409。新規の `uv-configuration/src/build_constraints.rs`、`constraints.rs`、`uv-build-frontend`、`uv-dispatch`、`uv-build-backend/src/metadata.rs` の改修などである。

**次のアクション:** 土台の #21899 がクローズされたため、ベースの付け替えなど今後の扱いを作者が判断する必要がある。

---

### ![PR](icons/git-pull-request.svg) [Disable incremental compilation when publishing docs](https://github.com/astral-sh/uv/pull/21955) `#21955`
**ステータス:** Merged | **ラベル:** なし

**概要:** ドキュメント公開のワークフローで `CARGO_INCREMENTAL=0` を設定し、`uv-dev` のビルドで不要な incremental compilation の状態を書かないようにする PR である。公開ワークフローは docs チェック側と違い Rust キャッシュアクションを使っていなかった。

**直近のアクティビティ:** zanieb が他のリリースビルドでも同じ設定をすべきか、release profile の既定ではないかと尋ね、自ら既定であると補足した。charliermarsh は他の箇所ではすでに有効になっているようだと答えた。

**変更ファイル:** 1 ファイル、+1/-0。`.github/workflows/publish-docs.yml` である。

**次のアクション:** マージ済みのため対応は不要である。

---

### ![PR](icons/git-pull-request.svg) [Export packaged locks with uv_build](https://github.com/astral-sh/uv/pull/21961) `#21961`
**ステータス:** Open (Draft) | **ラベル:** なし

**概要:** `locked-tools` preview 機能のもとで、`uv_build` が `uv.lock` の実行時依存を wheel の `.dist-info` に `pylock.toml` として書き出す PR である。sdist には `uv.lock` を含め、sdist から再ビルドしても同じロックになる。ロックがありすべての依存が PyPI 由来なら自動で書き出し、`tool.uv.build-backend.export-lock` と `UV_BUILD_BACKEND_EXPORT_LOCK` で有効化や無効化ができる。ビルド時に依存の解決や更新は行わない。#21963 の上に積まれている。

**直近のアクティビティ:** zanieb が自動書き出しと設定による上書きの設計、特にワークスペースとの長期的な関係について説明を求めた。codspeed は 8 件のベンチマークで 6.7 % の性能低下を報告している。

**変更ファイル:** 19 ファイル、+538/-30。新規の `crates/uv-build-backend/src/lock.rs`、`uv-build-backend/src/lib.rs`（+155）、`build_backend.rs` のテスト（+145）、`docs/concepts/build-backend.md` の追記である。

**次のアクション:** 作者による設計説明と、性能低下の報告の確認が必要である。

---

### ![PR](icons/git-pull-request.svg) [Restore `FlatDistributions` for downstream resolvers](https://github.com/astral-sh/uv/pull/21965) `#21965`
**ステータス:** Merged | **ラベル:** area:rustlib

**概要:** #21540 で非公開になった `FlatDistributions` の公開 re-export と `BTreeMap` からの変換を復活させる PR である。pixi が `VersionMap` の構築に使っているためで、この下流 API を `hawk.toml` に記録する。#21964 を修正する。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**変更ファイル:** 3 ファイル、+16/-2。`crates/uv-resolver/src/flat_index.rs`、`lib.rs`、`hawk.toml` である。

**次のアクション:** マージ済みのため対応は不要である。

---

### ![PR](icons/git-pull-request.svg) [Use Astra for PR security reviews](https://github.com/astral-sh/uv/pull/21959) `#21959`
**ステータス:** Merged | **ラベル:** なし

**概要:** PR のセキュリティレビューに使うモデルを `gpt-5.6-sol` から `gpt-6-astra` に切り替える PR である。

**直近のアクティビティ:** ewdurbin が承認した。

**変更ファイル:** 1 ファイル、+1/-1。`.github/workflows/pull-request-security-review.yml` である。

**次のアクション:** マージ済みのため対応は不要である。

---

### ![PR](icons/git-pull-request.svg) [Separate lock handling from network clients](https://github.com/astral-sh/uv/pull/21963) `#21963`
**ステータス:** Open (Draft) | **ラベル:** なし

**概要:** 既存 lockfile の読み込みと書き出しが `uv-lock` に HTTP クライアントとインタプリタ探索を引き込んでおり、スタンドアロンのビルドバックエンドから exporter を再利用できない問題に対応する PR である。認証の値を新クレート `uv-auth-types` に分け、配布物のメタデータを `uv-distribution-types` に移し、resolver のグラフ型とハッシュポリシーをネットワーククライアントから独立させる。

**直近のアクティビティ:** 作者が、新しい `uv-auth-types` はリリース前に crates-policies での公開ポリシー設定が必要だと記した。codspeed は 8 件のベンチマークで 7.38 % の改善を報告した（実行環境の違いの警告付き）。

**変更ファイル:** 68 ファイル、+1770/-1646。新クレート `crates/uv-auth-types`（`credentials.rs` +352 など）と、それに伴う `uv-auth`、`uv-client`、`uv-configuration`、`uv-distribution-types` の移動や整理である。

**次のアクション:** Draft のため Ready for review を待つ。公開ポリシーへの新クレート登録も必要である。

---

### ![PR](icons/git-pull-request.svg) [Warm the interpreter cache when syncing workspace metadata](https://github.com/astral-sh/uv/pull/21304) `#21304`
**ステータス:** Open | **ラベル:** performance

**概要:** `uv workspace metadata --sync` が作る環境のインタプリタ情報をキャッシュせず、次回も Python に問い合わせていた問題に対応する PR である。初回に親インタプリタの情報から推測（インタプリタの場所や `site.getsitepackages()`）してキャッシュし、2 回目以降を速くする。

**直近のアクティビティ:** 直近 24 時間のコメントなし（レビューは 9 月 18 日までの本文なしのコメントのみ）

**変更ファイル:** 15 ファイル、+357/-42。`crates/uv-python/src/interpreter.rs`（+93/-26）と `environment.rs` の改修、`workspace_metadata.rs`、`venv.rs`、`sync.rs` のテスト追加である。

**次のアクション:** レビューの継続が必要である。#21960 はこの PR の上に積まれていた。

---

### ![PR](icons/git-pull-request.svg) [Retain base site-packages when creating virtual environments](https://github.com/astral-sh/uv/pull/21960) `#21960`
**ステータス:** Closed | **ラベル:** なし

**概要:** system site packages を含む仮想環境では、推測したインタプリタ情報にベースのインストールの site-packages を含める必要があるとして、それを保持する PR であった。インタプリタキャッシュのバージョンも上げていた。#21304 の上に積まれていた。

**直近のアクティビティ:** 作者の konstin が、先にキャッシュ化を行わないと意味がないとしてクローズした。codspeed は 1 件のベンチマークで 5.29 % の低下を報告していた。

**変更ファイル:** 8 ファイル、+90/-5。`get_interpreter_info.py`、`interpreter.rs`、`virtualenv.rs`、`venv.rs` のテストなどである。

**次のアクション:** クローズ済みである。#21304 の進捗を待って再提出される可能性がある。

---

### ![Issue](icons/issue-opened.svg) [Interpreter cache pollution by system-site-packages venvs](https://github.com/astral-sh/uv/issues/18510) `#18510`
**ステータス:** Open | **ラベル:** bug, great writeup

**概要:** 以前 `--system-site-packages` 付きだった `.venv` を削除して作り直しても、インタプリタキャッシュ（`interpreter-v4`）に残った情報のせいで `uv run --with` の一時環境に `/usr/lib/python3/dist-packages` などが入り込むという報告である。コンテナでの再現手順とログが添付され、キャッシュを消すと一時的に直る。

**直近のアクティビティ:** konstin が、`UV_CACHE_DIR` を一時ディレクトリにした短い別の再現手順を投稿した。

**次のアクション:** 修正 PR の作成が必要である。関連する #21960 は #21304 待ちでクローズされている。

---

### ![PR](icons/git-pull-request.svg) [Update Rust crate toml to v1.1.4](https://github.com/astral-sh/uv/pull/21028) `#21028`
**ステータス:** Open | **ラベル:** internal, build:skip-release, build:skip-docker

**概要:** Renovate による `toml` の 1.1.3 から 1.1.4 へのパッチ更新である。1.1.6 も保留中である。

**直近のアクティビティ:** 直近 24 時間のコメントなし（codspeed の 8 月の性能レポートのみ）

**変更ファイル:** 1 ファイル、+4/-4。`Cargo.lock` である。

**次のアクション:** メンテナによるマージ判断が必要である。

---

### ![PR](icons/git-pull-request.svg) [Update Rust crate hyper to v1.11.0](https://github.com/astral-sh/uv/pull/20903) `#20903`
**ステータス:** Open | **ラベル:** internal, build:skip-release, build:skip-docker

**概要:** Renovate による `hyper` の 1.10.1 から 1.11.0 へのマイナー更新である。リリースノートには http1 の content-length と transfer-encoding の扱い、trailer 処理、`max_buf_size` の厳格化などのバグ修正が含まれる。

**直近のアクティビティ:** 直近 24 時間のコメントなし（codspeed の 8 月の性能レポートのみ）

**変更ファイル:** 1 ファイル、+20/-20。`Cargo.lock` である。

**次のアクション:** メンテナによるマージ判断が必要である。

---

### ![PR](icons/git-pull-request.svg) [Update Rust crate toml_parser to v1.1.3](https://github.com/astral-sh/uv/pull/21029) `#21029`
**ステータス:** Open | **ラベル:** internal, build:skip-release, build:skip-docker

**概要:** Renovate による `toml_parser` の 1.1.2 から 1.1.3 へのパッチ更新である。

**直近のアクティビティ:** 直近 24 時間のコメントなし（codspeed の 8 月の性能レポートのみ）

**変更ファイル:** 1 ファイル、+2/-2。`Cargo.lock` である。

**次のアクション:** メンテナによるマージ判断が必要である。

---

### ![PR](icons/git-pull-request.svg) [Update Rust crate async-compression to v0.4.43](https://github.com/astral-sh/uv/pull/20888) `#20888`
**ステータス:** Open | **ラベル:** internal, build:skip-release, build:skip-docker

**概要:** Renovate による `async-compression` の 0.4.19 から 0.4.43 へのパッチ更新である。破損した後続 zstd フレームのデコードでハングする問題の修正などを含む。

**直近のアクティビティ:** 直近 24 時間のコメントなし（codspeed の 8 月の性能レポートのみ）

**変更ファイル:** 1 ファイル、+47/-31。`Cargo.lock` である。

**次のアクション:** メンテナによるマージ判断が必要である。

---

### ![PR](icons/git-pull-request.svg) [Update Rust crate test-log to v0.2.21](https://github.com/astral-sh/uv/pull/19721) `#19721`
**ステータス:** Open | **ラベル:** internal, build:skip-release, build:skip-docker

**概要:** Renovate による `test-log` の 0.2.20 から 0.2.21 へのパッチ更新で、生成コードの span 修正により `rust-analyzer` との連携が改善される。

**直近のアクティビティ:** 直近 24 時間のコメントなし（codspeed の 6 月の性能レポートのみ）

**変更ファイル:** 1 ファイル、+24/-24。`Cargo.lock` である。

**次のアクション:** メンテナによるマージ判断が必要である。

---

### ![PR](icons/git-pull-request.svg) [Update Rust crate base64 to 0.23.0](https://github.com/astral-sh/uv/pull/20901) `#20901`
**ステータス:** Open | **ラベル:** internal, build:skip-release, build:skip-docker

**概要:** Renovate による `base64` の 0.22.1 から 0.23.0 へのマイナー更新である。0.23.0 では既定で有効な `simd-unsafe` feature のもとで SIMD 高速化エンジンが追加され、MSRV が 1.71.0 に上がっている。

**直近のアクティビティ:** 直近 24 時間のコメントなし（codspeed の 8 月の性能レポートのみ）

**変更ファイル:** 2 ファイル、+5/-5。`Cargo.lock` と `Cargo.toml` である。

**次のアクション:** メンテナによるマージ判断が必要である。

---

### ![PR](icons/git-pull-request.svg) [feat(python): add --all-variants flag to python list](https://github.com/astral-sh/uv/pull/18881) `#18881`
**ステータス:** Open | **ラベル:** なし

**概要:** `uv python list` に `--all-variants` フラグを追加し、既定では隠れている debug ビルドを表示できるようにする PR である。`uv python list 3.13+debug` のように明示的に要求した場合も debug ビルドを表示する。既存の `--all-platforms` などと同じ方式に従う。#15983 を修正する。

**直近のアクティビティ:** 作者が既定表示、`--all-variants`、`+debug` 指定を確かめるテスト `python_list_all_variants` を追加し、main を取り込んで `allows_debug` を公開にしたと報告した。bot のテスト一覧でも 1 件追加が確認できる。

**変更ファイル:** 6 ファイル、+107/-3。`crates/uv-cli/src/lib.rs` のフラグ定義、`commands/python/list.rs` の絞り込み処理、`python_list.rs` のテスト（+89）である。

**次のアクション:** メンテナによるレビューが必要である。

---

### ![PR](icons/git-pull-request.svg) [Update dependency buildx to v0.36.1](https://github.com/astral-sh/uv/pull/21667) `#21667`
**ステータス:** Open | **ラベル:** internal, build:skip-release, build:skip-docker

**概要:** Renovate による Docker イメージ公開ワークフローの buildx を v0.33.0 から v0.36.1 に更新する PR である。v0.37.1 も保留中である。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**変更ファイル:** 1 ファイル、+1/-1。`.github/workflows/publish-docker-image.yml` である。

**次のアクション:** メンテナによるマージ判断が必要である。

---

### ![PR](icons/git-pull-request.svg) [Update Codex automation dependencies](https://github.com/astral-sh/uv/pull/21876) `#21876`
**ステータス:** Open | **ラベル:** internal, build:skip-release, build:skip-docker

**概要:** Renovate による `@openai/codex` の 0.147.0 から 0.149.1 と、`openai/codex-action` の v1.11 から v1.12 への更新である。v1.12 では Linux ランナーの権限分離と Responses API プロキシの認証情報の扱いが強化されている。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**変更ファイル:** 9 ファイル、+19/-19。issue triage、bug 修正、セキュリティレビュー、リリース準備などの Codex 自動化ワークフローである。

**次のアクション:** メンテナによるマージ判断が必要である。

---

### ![PR](icons/git-pull-request.svg) [Update Rust crate webpki to v0.103.15](https://github.com/astral-sh/uv/pull/21875) `#21875`
**ステータス:** Open | **ラベル:** internal, build:skip-release, build:skip-docker

**概要:** Renovate による `webpki` の 0.103.14 から 0.103.15 へのパッチ更新である。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**変更ファイル:** 1 ファイル、+2/-2。`Cargo.lock` である。

**次のアクション:** メンテナによるマージ判断が必要である。

---

### ![PR](icons/git-pull-request.svg) [Update Rust crate h2 to v0.4.19](https://github.com/astral-sh/uv/pull/21873) `#21873`
**ステータス:** Open | **ラベル:** internal, build:skip-release, build:skip-docker

**概要:** Renovate による `h2` の 0.4.15 から 0.4.19 へのパッチ更新である。小さな DATA フレームの過剰送信の制限、HPACK エンコードテーブルの上限、接続クローズ時のビジーループなどの修正を含む。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**変更ファイル:** 1 ファイル、+20/-20。`Cargo.lock` である。

**次のアクション:** メンテナによるマージ判断が必要である。

---

### ![PR](icons/git-pull-request.svg) [Update Rust crate cargo-util to v0.2.31](https://github.com/astral-sh/uv/pull/21872) `#21872`
**ステータス:** Open | **ラベル:** internal, build:skip-release, build:skip-docker

**概要:** Renovate による `cargo-util` の 0.2.30 から 0.2.31 へのパッチ更新である。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**変更ファイル:** 1 ファイル、+20/-20。`Cargo.lock` である。

**次のアクション:** メンテナによるマージ判断が必要である。

---

### ![PR](icons/git-pull-request.svg) [Update digital-blueprint/gitlab-pipeline-trigger-action action to v1.4.3](https://github.com/astral-sh/uv/pull/20886) `#20886`
**ステータス:** Open | **ラベル:** internal, build:skip-release, build:skip-docker

**概要:** Renovate による CI の `digital-blueprint/gitlab-pipeline-trigger-action` の v1.4.0 から v1.4.3 への更新である。主に依存更新で、v1.4.1 では SBOM ファイルが同梱されるようになった。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**変更ファイル:** 1 ファイル、+1/-1。`.github/workflows/ci.yml` である。

**次のアクション:** メンテナによるマージ判断が必要である。

---

### ![Issue](icons/issue-opened.svg) [Export extras from uv export](https://github.com/astral-sh/uv/issues/12072) `#12072`
**ステータス:** Open | **ラベル:** question

**概要:** pants と組み合わせるため `uv export --format requirements-txt` で extras も出力してほしいという 2025 年の要望である。extras が出ないと後段の要件推論で問題が起きるという。

**直近のアクティビティ:** jasonqsong が、以前のメンテナの「意味上の違いはない」という回答に対し、pip のフラットな site-packages では違いはないが、bazel や pants のように各要件を個別のクロージャとして扱うモノレポのビルドシステムでは違いがあると反論した。例として `fastmcp-slim==4.0.5` と `fastmcp-slim[client,server]==4.0.5` で作られる site-packages が異なることを挙げている。

**次のアクション:** メンテナによる再検討と回答が必要である。

---

### ![PR](icons/git-pull-request.svg) [docs(pip): document `--universal` flag for cross-platform requirements locking](https://github.com/astral-sh/uv/pull/21958) `#21958`
**ステータス:** Open | **ラベル:** なし

**概要:** `docs/pip/compile.md` に `uv pip compile --universal` の説明を追加するドキュメント PR である。既定では現在の環境向けに解決するが、`--universal` では環境マーカーを保ったまま OS、アーキテクチャ、Python バージョンをまたいで使える解決結果になることを記す。#11305 をクローズする。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**変更ファイル:** 1 ファイル、+13/-0。`docs/pip/compile.md` である。

**次のアクション:** メンテナによるレビューが必要である。

---

### ![PR](icons/git-pull-request.svg) [Normalize requirement declarations in lockfiles](https://github.com/astral-sh/uv/pull/21951) `#21951`
**ステータス:** Open | **ラベル:** enhancement, preview

**概要:** #21930 の正規化を lockfile に広げる PR である。lock が入力を満たすかの判定で依存の集合を正規化し、`foo>=1`、`foo>=2`、`foo<3` と `foo>=2,<3` のような等価な宣言なら再解決せずに既存の lock を使う。この意味比較は古い lock を含め既定で有効で、正規化した内容の書き出しだけが `--preview-features lockfile-normalization` を必要とする。

**直近のアクティビティ:** 直近 24 時間のコメントなし（bot による本文なしのレビューのみ）

**変更ファイル:** 30 ファイル、+1140/-489。新規の `uv-lock/src/lock/requirements.rs`（+296）、`uv-lock/src/lock/mod.rs`（+146/-404）、`uv-configuration/src/normalized_requirements.rs` の改修とスナップショット更新である。

**次のアクション:** #21930 の上に積まれており、下位 PR とあわせてレビューが必要である。

---

### ![PR](icons/git-pull-request.svg) [Intersect hashes from repeated constraints](https://github.com/astral-sh/uv/pull/21956) `#21956`
**ステータス:** Open | **ラベル:** bug

**概要:** 同じレジストリのピンを異なるハッシュリストで繰り返す constraints で、最後のリストだけが残っていた問題の修正である。並べ替え（lockfile 書き出し時のソートを含む）で受け入れるハッシュが変わってしまうため、リストの共通部分を取るようにする。`{A, B}` と `{B, C}` なら `B` のみ許可し、共通部分がなければ競合として報告する。通常の constraints と build constraints の両方に適用する。

**直近のアクティビティ:** astral-sh-bot のテスト一覧（`uv::sync` に 2 件追加）のみである。

**変更ファイル:** 4 ファイル、+229/-53。`crates/uv-types/src/hash.rs` の修正と、`sync.rs`、`build.rs` のテストである。

**次のアクション:** メンテナによるレビューが必要である。#21930 がこの PR に依存している。

---

### ![PR](icons/git-pull-request.svg) [Normalize dependency inputs in tool receipts](https://github.com/astral-sh/uv/pull/21930) `#21930`
**ステータス:** Open | **ラベル:** enhancement

**概要:** `--with` を繰り返すとツールのレシートに要件が重複して記録される問題に対応し、requirements、constraints、overrides、exclusions、build constraints を正規化する PR である。等価な入力なら既存環境を再利用し、`--with 'httpie<1' --with 'httpie<2'` は `httpie<1` 1 件として記録される。#14023 をクローズする。

**直近のアクティビティ:** 直近 24 時間のコメントなし（9 月 23 日に konstin が承認済み）

**変更ファイル:** 13 ファイル、+2053/-376。新規の `crates/uv-tool/src/requirements.rs`（+821）と、`tool_install.rs`（+913/-257）、`tool_upgrade.rs` のテストが中心である。

**次のアクション:** 承認済みのため、依存する #21956 のマージ後にマージできる。

---

### ![Issue](icons/issue-opened.svg) [Unable to install nvidia-cudnn-cu12==9.7.0.66](https://github.com/astral-sh/uv/issues/11156) `#11156`
**ステータス:** Closed | **ラベル:** bug

**概要:** pytorch の依存として `nvidia-cudnn-cu12` をインストールする際、ダウンロードが途中から再開しなおしたように見えた後に停止するという 2025 年の報告である。

**直近のアクティビティ:** AaronRohrbacher が、EC2 インスタンス固有の問題ではないかと推測し、ほぼ同じ容量で止まる経験があると書き込んだ。

**次のアクション:** クローズ済みである。新しい書き込みは推測にとどまり、再現情報が出れば別 Issue での報告を促すのが妥当である。

---

## microsoft/vscode

> Issue では、1.139.0 の Copilot Chat と Agents window まわりの不具合報告（C# Dev Kit の `track-skill-invocation.ps1` が外部アプリで開く問題、GHE の認証ループ、大きなセッションによる OOM など）と、enterprise managed settings や OTel、Customizations 画面の改善に関する起票とクローズが集中している。 PR では Agent Host（Copilot、Codex、Claude の各 provider）と Agents ウィンドウの Sessions 機能の改修が大半を占め、customization marketplace（Discover、MCP Gallery、Plugin marketplaces、Copilot Connectors）を段階的に積み上げるスタック PR 群と、1.139.1 リカバリーリリースに向けた version bump が並行して進んでいる。

### ![Issue](icons/issue-opened.svg) [Numpad Delete key not working in version 1.107.0 (regression from 1.106.3)](https://github.com/microsoft/vscode/issues/283460) `#283460`
**ステータス:** Open | **ラベル:** bug, upstream, electron, regression, chromium, electron-39-update

**概要:** 1.107.0 以降、テンキー側の Delete キーを押してもカーソル位置の文字が削除されないという報告である。通常の Delete キーは動作し、1.106.3 では発生せず、拡張機能を無効にしても NumLock の状態によらず再現する。環境は Kubuntu 24.04 である。

**直近のアクティビティ:** x80486 が、Chromium 152.x でようやく修正されたと報告し、そのバージョンへの更新を期待するとコメントした。

**次のアクション:** upstream の修正を含む Electron（Chromium 152 系）への更新を待ち、取り込まれた時点で再現しないことを確認する。

---

### ![Issue](icons/issue-opened.svg) [Tab name is not readable](https://github.com/microsoft/vscode/issues/337717) `#337717`
**ステータス:** Open | **ラベル:** workbench-tabs

**概要:** 1.139.0 に更新した後、エディタのタブの見た目が変わり、タブ名が読みにくくなったという報告である。作者はタブ幅を広げずに以前の表示へ戻す方法を求めている。

**直近のアクティビティ:** monolithed が macOS でも同じ症状であるとスクリーンショット付きで報告し、`workbench.colorCustomizations` で指定したタブの色設定（`tab.activeForeground` など）も影響を受けていると述べた。

**次のアクション:** 1.139.0 のタブ表示変更との関係を切り分け、色のカスタマイズが効かなくなった点を含めて回帰かどうかを判断する必要がある。

---

### ![Issue](icons/issue-opened.svg) [Sorry, no response was returned.](https://github.com/microsoft/vscode/issues/253880) `#253880`
**ステータス:** Open | **ラベル:** bug, confirmation-pending, capi

**概要:** Copilot Chat でどのプロンプトを送っても出力が返らず、「Sorry, no response was returned.」になるという 2025 年 7 月の報告である。環境は macOS、拡張機能 0.28.5、VS Code 1.101.2 である。

**直近のアクティビティ:** rlint が #301448 に投稿した分析コメントへのリンクを 2 回貼り、原因は異なるかもしれないが同じ挙動の可能性があると述べた。

**次のアクション:** #301448 で示された `finish_reason: "length"` による打ち切りと同一の原因かを確認し、重複として整理するか判断する。

---

### ![Issue](icons/issue-opened.svg) [Meta: Sorry, no response was returned](https://github.com/microsoft/vscode/issues/253126) `#253126`
**ステータス:** Open | **ラベル:** bug, capi, claude

**概要:** Agent Mode で Claude 3.5 に複数の作業を依頼すると、一部は完了するが、しばらくすると「Sorry, no response was returned.」で止まるという報告をまとめた Meta Issue である。拡張機能を更新しても改善しなかった。

**直近のアクティビティ:** rlint が #253880 と同じく、#301448 の分析コメントへのリンクを共有した。

**次のアクション:** #301448 の分析内容が本 Issue の症状にも当てはまるかを確認する。

---

### ![Issue](icons/issue-opened.svg) [Sorry, no response was returned.](https://github.com/microsoft/vscode/issues/301448) `#301448`
**ステータス:** Open | **ラベル:** なし

**概要:** プロジェクト全体の分析ファイルを生成させようとすると、「Sorry, no response was returned.」や、`HTTP/2: "stream timeout after 300000"` を伴うネットワークエラーが繰り返し発生するという報告である。環境は Windows 上の WSL2 と Dev Container で、10 回以上のリクエストを消費したと述べている。

**直近のアクティビティ:** rlint が Copilot の助けを借りた分析を投稿した。モデルが max_tokens に達して API が `finish_reason: "length"` と部分的な応答を返した場合でも、Copilot Chat は部分応答を捨てて汎用のエラーメッセージを表示しており、トークン上限による打ち切りという対処可能な問題を全体の失敗のように見せていると指摘し、出力ログを根拠として示した。

**次のアクション:** 打ち切り時に部分応答を表示するか、トークン上限に達したことを明示するエラーに変えるかをメンテナが検討する必要がある。

---

### ![Issue](icons/issue-opened.svg) [Authentication Loop with GitHub Copilot Enterprise (GHE)](https://github.com/microsoft/vscode/issues/337822) `#337822`
**ステータス:** Open | **ラベル:** recent-regression, new release

**概要:** 1.139 で GHE のインスタンス（`https://mycompany.ghe.com`）向けに Copilot を設定すると、ブラウザで認可して VS Code に戻った後も再びサインインを求められ、ループするという報告である。Chat ペインと Agents window の両方で起き、Windows 11 と Ubuntu 24.04 のいずれも 1.138 に戻すと解消する。#331464 に似ているとしている。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**次のアクション:** 1.138 から 1.139 の間の GHE 認証まわりの変更を調べ、回帰の原因を特定する必要がある。

---

### ![Issue](icons/issue-opened.svg) [VSCode should follow .lnk](https://github.com/microsoft/vscode/issues/89743) `#89743`
**ステータス:** Closed | **ラベル:** feature-request, file-explorer, *out-of-scope

**概要:** エクスプローラーで Windows の `.lnk` ファイルをクリックしたとき、リンク先のファイルを開けるようにしてほしいという 2020 年の機能要望である。out-of-scope としてクローズされている。

**直近のアクティビティ:** mogggen が、`.lnk` の代わりにリンク先の元ファイルを開く拡張機能を作ったと紹介した。Windows の単一ファイルでのみ試しており、追加の検証が必要だと述べている。

**次のアクション:** クローズ済みのため対応は不要である。

---

### ![Issue](icons/issue-opened.svg) [Expose large shell output artifact paths through the Copilot SDK](https://github.com/microsoft/vscode/issues/329724) `#329724`
**ステータス:** Open | **ラベル:** feature-request, terminal, agent-host

**概要:** Copilot runtime が大きなシェル出力をファイルに退避した場合、そのパスはツール完了時の表示テキストにしか含まれず、構造化された `shell_exit` には `outputPreview` と `outputTruncated` しかない。そのため VS Code は出力全体を型安全に扱えない。runtime の wire contract と公開 SDK 型に退避先のパスまたは URI を任意フィールドとして追加し、寿命やアクセスの意味を定め、Agent Host がそれを使うように改めることを受け入れ条件としている。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**次のアクション:** SDK と runtime 側での契約追加を待ち、その後 Agent Host 側で対応する。

---

### ![Issue](icons/issue-opened.svg) [Ensure enterprise Copilot OTel settings override environment variables](https://github.com/microsoft/vscode/issues/336839) `#336839`
**ステータス:** Open | **ラベル:** なし

**概要:** enterprise で管理される Copilot の OTel 設定が、VS Code 拡張機能では環境変数で上書きできてしまうという問題である。たとえば `COPILOT_OTEL_ENABLED=false` で管理設定のテレメトリを無効化でき、`OTEL_EXPORTER_OTLP_ENDPOINT` で送信先を変えられる。Rust の runtime 側は管理値を優先しているが、拡張機能のリゾルバが policy 由来の値を通常の `setting*` 入力として渡しているのが原因であり、`policy*` 入力で `resolveOTelConfig` に渡すよう修正し、回帰テストを追加することを提案している。

**直近のアクティビティ:** amunger が、起票元は確認できないが関連 Issue の rwoll によるものと想定するとコメントした。

**次のアクション:** 担当者を決め、提案どおり policy の出自を保持する修正と回帰テストを実装する。

---

### ![Issue](icons/issue-opened.svg) [Add Auto Tier Managed Setting](https://github.com/microsoft/vscode/issues/336829) `#336829`
**ステータス:** Closed | **ラベル:** on-testplan, insiders-released

**概要:** github/agent-control-plane で議論されているモデル tier の managed settings 設計を VS Code で扱えるようにする追跡 Issue である。設定名とスキーマは upstream で調整中とされている。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**次のアクション:** クローズ済みで、テストプラン #337818 の対象になっている。

---

### ![Issue](icons/issue-opened.svg) [Explain enterprise minVersion restrictions in Chat, the Agents window, and a window-level banner](https://github.com/microsoft/vscode/issues/337086) `#337086`
**ステータス:** Closed | **ラベル:** feature-request, verification-needed, on-testplan, insiders-released

**概要:** enterprise の managed settings の `minVersion` で古い VS Code の AI 機能がブロックされたとき、その理由をユーザーに示す機能である。Chat には更新の案内を、エディタには Workspace Trust 風のバナーを出し、Agents window はウィンドウ全体を覆うオーバーレイだけを表示する。組み込みの更新が policy で無効な場合は管理者に問い合わせるよう案内する。実装は #337151 である。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**次のアクション:** テストプラン #337818 の section 4 で検証される。

---

### ![Issue](icons/issue-opened.svg) [[Opt-in only] open telemetry should include some PII data in GitHub Copilot in VS Code](https://github.com/microsoft/vscode/issues/302930) `#302930`
**ステータス:** Closed | **ラベル:** feature-request, verification-needed, on-testplan, insiders-released

**概要:** ユーザーごとの利用状況を分析できるよう、Copilot の OpenTelemetry にユーザー情報を opt-in で含めてほしいという要望である。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**次のアクション:** テストプラン #337818 の section 1（Local OTel の identity capture）で検証される。

---

### ![Issue](icons/issue-opened.svg) [Test: Enterprise managed settings and Local OTel](https://github.com/microsoft/vscode/issues/337818) `#337818`
**ステータス:** Open | **ラベル:** testplan-item

**概要:** 1.140 Insiders に入った enterprise managed settings と Local OTel 関連の PR 6 件を対象とするテストプランで、所要時間は 30〜45 分程度とされる。`npm run mock-policy-server` でポリシーを模擬し、ローカルの collector でテレメトリを受ける手順が示されている。検証項目は `capture.identity` による `user.name` などの identity 情報の出し分けなどである。

**直近のアクティビティ:** rwoll が、このテストプランで #302930（section 1）、#336829（section 2）、#337086（section 4）を扱うと整理した。1.139.1 へのバックポートは #337804 で別途追跡しており、section 3 は簡単な確認にとどまると補足している。

**次のアクション:** 担当の jruales によるテスト実施を待つ。

---

### ![Issue](icons/issue-opened.svg) [Model picker jumps around when the selection changes](https://github.com/microsoft/vscode/issues/337824) `#337824`
**ステータス:** Open | **ラベル:** bug

**概要:** 1.140.0-insider の新しいチャットのモデルピッカーで、選択を変えると項目の位置がずれ、カーソルの下で表示が安定しないという報告である。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**次のアクション:** 選択変更時にリストが再配置される原因を調べる。

---

### ![Issue](icons/issue-opened.svg) [Agent-created worktree sessions default to Local and disappear](https://github.com/microsoft/vscode/issues/335825) `#335825`
**ステータス:** Closed | **ラベル:** bug, unreleased, voice-mode

**概要:** voice mode で Copilot に worktree の作成と作業を依頼すると、新しいセッションが元のワークスペースではなく **Local** に割り当てられ、ワークスペースを認識せず、作成後にセッション一覧から消えることがあるという報告である。選択中の harness、または既定の **Copilot** を使うべきとしている。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**次のアクション:** クローズ済みのため対応は不要である。

---

### ![Issue](icons/issue-opened.svg) [Managed settings are dropped after transient refresh failures](https://github.com/microsoft/vscode/issues/337804) `#337804`
**ステータス:** Open | **ラベル:** bug, candidate, new release

**概要:** #330896 で入った回帰で、キャッシュが 1 時間より古くなった状態で managed settings の再取得に失敗すると（無応答、一時的な 401/403、429、5xx など）、適用済みの enterprise ポリシーが破棄され既定値に戻る。OTel endpoint の場合は「Copilot OTel endpoint will change to http://localhost:4318 after reload.」という通知が繰り返し表示され、他の制限も一時的に外れる。失敗時は古くても直前の設定を保持すべきとしている。main の修正は #337583、`release/1.139` へのバックポートは #337739 で、いずれもマージ済みである。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**次のアクション:** 1.139.1 の candidate として、リカバリーリリースでの検証を待つ。

---

### ![Issue](icons/issue-opened.svg) [Copilot Agent Host not able to switch account](https://github.com/microsoft/vscode/issues/330477) `#330477`
**ステータス:** Open | **ラベル:** bug, agent-host

**概要:** Agent Host で Copilot harness を使っていると、アカウントを切り替えても前のアカウントが使われ続け、クレジット上限のエラーが新しいセッションでも出るという報告である。VS Code を再起動しても解消しない。

**直近のアクティビティ:** dmitrivMS が、重複先の Issue に従い Agent Host を再起動する回避策を示し、重複先が引き続き追跡していると述べた。kieronlanning は重複ではなく再起動でも解決しないと反論し、何日も何週間も、複数のマシンで起きていると補足した。dmitrivMS は再起動したのが Agent Host か VS Code のウィンドウかを確認している。

**次のアクション:** Agent Host の再起動でも解消しないのかを確認し、重複扱いの妥当性を判断する必要がある。

---

### ![Issue](icons/issue-opened.svg) [Claude Opus 5.5 unsupported in GitHub Copilot Chat.](https://github.com/microsoft/vscode/issues/337642) `#337642`
**ステータス:** Open | **ラベル:** なし

**概要:** 1.139.0 の Copilot Chat の Agent Mode で Claude Agent SDK を使い Claude Opus 5.5 を選ぶと、「Claude Code 2.1.258 does not support this model; version 2.1.280 or newer is required.」という 400 エラーで即座に失敗するという報告である。作者は Copilot がローカルの Claude Code のバージョンに依存する理由と、Opus 5.5 の対応状況の説明を求めている。

**直近のアクティビティ:** mmontagnana が、1.137 以降は同梱の Agent SDK が 0.3.258 に固定されており Opus 5.5 に対応していないと指摘した。Insiders でも `"@anthropic-ai/claude-agent-sdk": "0.3.258"` のままであり、VS Code 更新時の SDK インストールの問題（#334077）とも関係するかもしれないと述べている。

**次のアクション:** 同梱の Claude Agent SDK を Opus 5.5 対応版に更新する必要がある。

---

### ![Issue](icons/issue-opened.svg) [Magnifiying glass icon showing over text](https://github.com/microsoft/vscode/issues/337773) `#337773`
**ステータス:** Open | **ラベル:** なし

**概要:** 1.140.0-insider のチャットで、エージェントがツールを検索している最中に表示される虫眼鏡アイコンがテキストの上に重なって表示されるという報告である。ログには `tool_search` の呼び出しが記録されている。

**直近のアクティビティ:** sean-mcmanus が最新の Insiders の Explore Copilot アイコンでも同じ現象を確認した。作者は、新しい緑色のアニメーションアイコンの上に虫眼鏡が重なり、アニメーション終了後も 2 つのアイコンが重なったまま残る別の例を追加した。

**次のアクション:** 進捗アイコンの切り替え時に古いアイコンが残る表示不具合として調査する。

---

### ![Issue](icons/issue-opened.svg) [Agent host crash-loops with V8 OOM when a large Claude session (GB-sized subagent transcripts) is restored, taking down all sessions](https://github.com/microsoft/vscode/issues/337688) `#337688`
**ステータス:** Open | **ラベル:** freeze-slow-crash-leak, new release

**概要:** 1.139.0 と 1.138.0 で、`~/.claude/projects` から見つかった大きな Claude Code セッション（本体 268 MB、サブエージェントの transcript 1,365 件で計 9.4 GB）を復元すると、Agent Host が約 3.2 GB のヒープで V8 OOM になる。5 回再起動した後に諦め、Agents window の他のセッションもすべて開けなくなる。履歴のストリーミングやページング、サブエージェント読み込みの上限を設け、大きすぎるセッションはそのセッションだけを失敗させるべきとしている。

**直近のアクティビティ:** 作者が追加検証を投稿した。`subagents/` フォルダを隠しても 268 MB の本体だけでクラッシュループは続き、起動時に復元されるのは終了時に開いていたセッションだけなので、小さいセッションを開いた状態で終了すればループを避けられると報告した。

**次のアクション:** 起動時のセッション復元でメモリを抑える仕組みと、1 セッションの失敗を Agent Host 全体に波及させない分離が必要である。

---

### ![Issue](icons/issue-opened.svg) [Terminal memory leak (oom-kill)](https://github.com/microsoft/vscode/issues/334979) `#334979`
**ステータス:** Open | **ラベル:** freeze-slow-crash-leak, terminal

**概要:** Arch Linux の 1.136.1 で、ターミナルで terraform や systemd journal などを実行していると、ときどきターミナルが点滅して oom-kill でプロセスが殺されるという報告である。journal には約 10 GB を使った `rg` プロセスの kill が記録され、terminal.log には pty host の終了が残っている。

**直近のアクティビティ:** dmitrivMS が、ターミナルの OOM に対する修正が 1.139 に入ったので、そのバージョンで再現するか試してほしいと依頼した。

**次のアクション:** 作者が 1.139 で再現確認を行うのを待つ。

---

### ![Issue](icons/issue-opened.svg) [Hosted Agent: Codex Agent: agent unable to read it's own path, e.g. `~/.codex`, in `Auto-Review` mode](https://github.com/microsoft/vscode/issues/336394) `#336394`
**ステータス:** Closed | **ラベル:** unreleased

**概要:** 1.138.0 の Hosted Agents window で Codex エージェントを `Auto-Review` モードで動かすと、`.codex/plugins/cache/` や `.codex/skills`、ワークスペース内のファイルに対して `Operation not permitted` が多発するという報告である。`Full Access` モードでは問題なく、`chat.agent.sandbox.enabled` などは無効にしていた。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**次のアクション:** クローズ済みのため対応は不要である。

---

### ![Issue](icons/issue-opened.svg) [Copilot Chat OOM crash (20+ GB memory leak) on large structured responses — reproducible across models](https://github.com/microsoft/vscode/issues/303387) `#303387`
**ステータス:** Open | **ラベル:** info-needed, freeze-slow-crash-leak

**概要:** 約 1,716 ファイルの写真アーカイブについて RAW と JPEG のフォルダ構成を比較させると、大きな構造化応答の生成中にウィンドウのレンダラープロセスが約 350 MB から 20 GB 超まで膨らみ、OOM で落ちるという報告である。Gemini 3 Flash と GPT 5.4 の両方で再現し、拡張ホストではなくレンダラー側の問題としている。

**直近のアクティビティ:** dmitrivMS が現在の Insiders と 1.112 でシミュレーションしたが再現しなかったと述べた。最新版での再確認、再現する場合のヒープスナップショットの取得、`--disable-gpu` と単一ウィンドウでの挙動確認を依頼した。

**次のアクション:** 作者からの追加情報を待つ。

---

### ![Issue](icons/issue-opened.svg) [selecting a remote workspace in picker does not actually make a selection](https://github.com/microsoft/vscode/issues/337321) `#337321`
**ステータス:** Closed | **ラベル:** なし

**概要:** ピッカーでリモートワークスペースを選んでも実際には選択されないという報告で、本文は動画のみである。

**直近のアクティビティ:** meganrogge がサブメニューから選択する必要があると説明した。作者は項目をクリックするとドロップダウンが閉じるだけで何も起きないのは仕様なのかと問い返したが、その後ドロップダウン自体が変わりこの問題は当てはまらなくなったとしてクローズした。

**次のアクション:** クローズ済みのため対応は不要である。

---

### ![Issue](icons/issue-opened.svg) [Web Agents omits remote SDK sessions as unsupported or missing client name](https://github.com/microsoft/vscode/issues/332185) `#332185`
**ステータス:** Closed | **ラベル:** bug, verified, agent-host

**概要:** Dev box のトンネル経由で insider.vscode.dev などから Agents に接続すると、リモートの SDK セッションが外部セッションとして除外され、直近 2 件しか表示されないという報告である。`copilotAgent.ts` が `isRemote` のセッションの `clientName` を一律に捨てているため、対応済みの client name でも除外されると分析している。

**直近のアクティビティ:** dmitrivMS が #331187 で修正済みかを尋ね、作者は最近は問題が起きていないと答えた。

**次のアクション:** クローズ済みのため対応は不要である。

---

### ![Issue](icons/issue-opened.svg) [Main process crashes with SQLITE_BUSY on new APPLICATION_SHARED storage (.vscode-shared\sharedStorage\state.vscdb) when two instances run concurrently](https://github.com/microsoft/vscode/issues/324612) `#324612`
**ステータス:** Open | **ラベル:** bug, freeze-slow-crash-leak, windows, under-discussion

**概要:** Windows で VS Code を 2 インスタンス同時に起動すると、数分以内に `SQLITE_BUSY: database is locked` がメインプロセスの未捕捉例外となりクラッシュするという報告である。原因は、VS Code と Agents アプリの間でプロセスをまたいで共有するために追加された `APPLICATION_SHARED` ストレージ（`.vscode-shared\sharedStorage\state.vscdb`）への同時書き込みであり、`SQLiteStorageDatabase` の扱いに問題があると分析している。

**直近のアクティビティ:** MaxTranced が macOS で、VS Code を 2 つ開かなくても同じエラーでクラッシュしたと報告した。バックアップ用の rsync が通常の `globalStorage/state.vscdb` を長時間読んでいる間に発生しており、共有ストレージに限らない問題として範囲を広げるべきだと述べている。

**次のアクション:** `SQLITE_BUSY` をメインプロセスの致命的な例外にしない対策（再試行やエラー処理）を検討する必要がある。

---

### ![Issue](icons/issue-opened.svg) [Automations View: Should show pickers like in new session view](https://github.com/microsoft/vscode/issues/336999) `#336999`
**ステータス:** Closed | **ラベル:** unreleased

**概要:** Automations のダイアログではピッカーの位置が新規セッション画面と異なり操作しにくいため、同じ位置に揃え、`prompt` や `session configuration` の見出しを削除してほしいという要望である。

**直近のアクティビティ:** vs-code-engineering bot が、マージ済みの #337590 の内容を検証用に共有した。Agent と Model のピッカーをプロンプト入力欄の中に移し、「Session configuration」見出しを削除し、Target 行は変更しない実装である。

**次のアクション:** クローズ済みで、次のリリースでの検証を待つ。

---

### ![Issue](icons/issue-opened.svg) [Copilot Chat stuck loading](https://github.com/microsoft/vscode/issues/337809) `#337809`
**ステータス:** Open | **ラベル:** なし

**概要:** 1.139.0（Windows）で Copilot Chat のウィンドウが 15 分以上読み込み中のまま止まるという報告である。ここ数日で起きるようになったため、最近の更新が原因と推測している。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**次のアクション:** 添付された issue-data と動画からトリアージを行う。

---

### ![Issue](icons/issue-opened.svg) [Terminal OutputMonitor may report "needs input" for long-running commands that continuously produce output](https://github.com/microsoft/vscode/issues/330790) `#330790`
**ステータス:** Open | **ラベル:** confirmation-pending, chat-terminal

**概要:** `tqdm` のプログレスバーのように出力を更新し続ける長時間のコマンドを実行すると、入力を必要としていないのにエージェントが `bash ... may need input` と通知するという報告である。出力のないコマンドでは起きない。AI の支援による分析として、`outputMonitor.ts` の状態機械が idle に到達できず、延長ポーリングのタイムアウト経路で入力待ちと判定している可能性を挙げている。

**直近のアクティビティ:** dmitrivMS が #312148 と #312180 で修正済みと思われるとコメントした。

**次のアクション:** 作者による最新版での確認を経てクローズされる見込みである。

---

### ![Issue](icons/issue-opened.svg) [Markdown Editor: expose a command to toggle Editing / Locked mode](https://github.com/microsoft/vscode/issues/331423) `#331423`
**ステータス:** Closed | **ラベル:** feature-request, markdown-editor

**概要:** Markdown Editor（`vscode.markdown.editor`）の Editing と Locked の切り替えを、キーバインドを割り当てられるコマンドとして公開してほしいという要望である。Agents window では Locked モードで範囲指定の Agent Feedback が使えるため、キーボードで素早く切り替えたいとしている。

**直近のアクティビティ:** dmitrivMS が #329414 で修正済みとしてクローズした。

**次のアクション:** クローズ済みのため対応は不要である。

---

### ![Issue](icons/issue-opened.svg) [1.139.1 Endgame](https://github.com/microsoft/vscode/issues/337801) `#337801`
**ステータス:** Open | **ラベル:** endgame-plan

**概要:** リカバリーリリース 1.139.1 の endgame 計画である。マイルストーン作成、バージョン更新、candidate Issue の割り当てとレビュー、修正の main へのマージと release ブランチへの cherry-pick、Insiders ビルドの開始までは完了している。Insiders でのテスト、stable ビルド、検証、リリースノート更新、公開が残っている。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**次のアクション:** Insiders で candidate の修正を検証し、24 時間の公開期間を経て stable をビルドして公開する。

---

### ![Issue](icons/issue-opened.svg) [Latest copilot keeps annoyingly opening Notepad!](https://github.com/microsoft/vscode/issues/337522) `#337522`
**ステータス:** Closed | **ラベル:** new release

**概要:** 1.139.0（Windows）でチャットにプロンプトを送るたびに、`track-skill-invocation.ps` というスクリプトがメモ帳で前面に開かれるという報告である。スクリプトのコメントによれば、C# Dev Kit が有効なときだけ読み込まれる、アンケート表示のためのスキル起動追跡用の PostToolUse hook である。

**直近のアクティビティ:** pwang347 が microsoft/vscode-dotnettools#3568 の重複としてクローズした。

**次のアクション:** クローズ済みのため対応は不要である。修正は vscode-dotnettools 側で追跡される。

---

### ![Issue](icons/issue-opened.svg) [VS Code agent repeatedly requests to open track-skill-invocation.ps1 in an external application](https://github.com/microsoft/vscode/issues/337671) `#337671`
**ステータス:** Closed | **ラベル:** new release

**概要:** エージェントを使うと `track-skill-invocation.ps1` を外部アプリケーションで開こうとする要求が繰り返し出るという報告である。追記として、C# のクラス 15 ファイルを record に変換するだけで約 1 時間かかるほどエージェントが遅いとも述べている。

**直近のアクティビティ:** pwang347 が #337522 と同じく microsoft/vscode-dotnettools#3568 の重複としてクローズした。

**次のアクション:** クローズ済みのため対応は不要である。

---

### ![Issue](icons/issue-opened.svg) [track-skill-invocation.ps1 opens in notepad for every skill use.](https://github.com/microsoft/vscode/issues/337557) `#337557`
**ステータス:** Closed | **ラベル:** なし

**概要:** スキルを使うたびに `track-skill-invocation.ps1` がメモ帳で開くという報告である。本文にはエージェント自身による調査ログが貼られており、C# Dev Kit のプラグインの `hooks.json` は PowerShell 用のコマンドを持つのに、Windows 上で汎用または Unix 向けの分岐が選ばれている可能性を指摘している。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**次のアクション:** クローズ済みのため対応は不要である。同種の報告は microsoft/vscode-dotnettools#3568 に集約されている。

---

### ![Issue](icons/issue-opened.svg) [Show a migration warning indicator on the Customizations side panel entry](https://github.com/microsoft/vscode/issues/337806) `#337806`
**ステータス:** Open | **ラベル:** なし

**概要:** 移行が必要なカスタマイズがある場合に、サイドパネルの **Customizations** の項目に黄色の警告ドットを表示し、移行が解消されたら消すという機能要望である。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**次のアクション:** 同時に起票された #337805、#337807、#337808 と合わせて実装を進める。

---

### ![Issue](icons/issue-opened.svg) [Show the enabled customization count in the Customizations side panel entry](https://github.com/microsoft/vscode/issues/337805) `#337805`
**ステータス:** Open | **ラベル:** なし

**概要:** サイドパネルの **Customizations** の項目に、現在のワークスペースで有効なカスタマイズの数を表示するという機能要望である。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**次のアクション:** 関連する Customizations 改善の Issue と合わせて実装を進める。

---

### ![Issue](icons/issue-opened.svg) [Add a customization breakdown and migration explanation tooltip](https://github.com/microsoft/vscode/issues/337807) `#337807`
**ステータス:** Open | **ラベル:** なし

**概要:** **Customizations** の項目にツールチップを追加し、有効なカスタマイズの内訳と、移行警告の理由を表示するという機能要望である。ビューを開かなくてもカウンターと警告の意味がわかるようにする。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**次のアクション:** #337805 と #337806 の実装に合わせて対応する。

---

### ![Issue](icons/issue-opened.svg) [Agent sessions: coordinate independently scoped child sessions](https://github.com/microsoft/vscode/issues/331521) `#331521`
**ステータス:** Open | **ラベル:** feature-request

**概要:** 複数のリポジトリ、ワークスペース、worktree、プロバイダーにまたがる作業で、エージェントが独立したスコープの子セッションを作成して調整できるようにする提案である。同じワークスペースを共有する並列作業はセッション内のチャットで扱い、独立して再開可能な作業は子セッションに委任するという区別を示している。子セッションごとに明示的なタスク、ワークスペース、状態を持たせ、作成元が完了、入力待ち、失敗の通知を受け取れることなどを受け入れ条件としている。

**直近のアクティビティ:** dmitrivMS が #331518 で修正済みかを尋ねた。

**次のアクション:** 作者が #331518 で受け入れ条件を満たしたかを確認し、クローズを判断する。

---

### ![Issue](icons/issue-opened.svg) [Server-managed private plugin marketplace cannot use VS Code session credentials](https://github.com/microsoft/vscode/issues/337199) `#337199`
**ステータス:** Closed | **ラベル:** bug, unreleased, authentication-github, chat

**概要:** サーバー配信の Copilot managed settings の `extraKnownMarketplaces` で指定したプライベートな GitHub プラグインマーケットプレイスが、設定は届いているのに VS Code から使えないという報告である。同じマーケットプレイスを手動で追加すると Git 認証が求められて成功する。VS Code や Agent Host の認証情報で取得するか、明確なサインイン操作を提示すべきとしている。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**次のアクション:** クローズ済みで、次のリリースでの検証を待つ。

---

### ![Issue](icons/issue-opened.svg) [Customization hard to find if I don't know where it is](https://github.com/microsoft/vscode/issues/333757) `#333757`
**ステータス:** Closed | **ラベル:** ai-customizations

**概要:** #333535 のテスト中に起票されたもので、全体検索がなく、カード間で検索語も引き継がれないため、どのセクションにあるかわからないカスタマイズを探しにくいという指摘である。

**直近のアクティビティ:** houghj16 が #337110 でクローズしたとコメントした。

**次のアクション:** クローズ済みのため対応は不要である。

---

### ![Issue](icons/issue-opened.svg) [Support searching across all customization types](https://github.com/microsoft/vscode/issues/336202) `#336202`
**ステータス:** Closed | **ラベル:** ai-customizations

**概要:** Customizations エディタでスキル、MCP サーバー、プラグイン、ツールなどをタブごとに検索しなければならない問題に対し、すべての種類を横断する検索を追加する要望である。結果に種類を表示し、そこから有効化や無効化ができることを求めている。

**直近のアクティビティ:** houghj16 が #337110 でクローズしたとコメントした。

**次のアクション:** クローズ済みのため対応は不要である。

---

### ![Issue](icons/issue-opened.svg) [Cannot click on "Chat" in the New Session workspace picker](https://github.com/microsoft/vscode/issues/337641) `#337641`
**ステータス:** Open | **ラベル:** bug, agents-window

**概要:** 1.140.0-insider（macOS）の Agents window で、新規セッションのワークスペースピッカーにある「Chat」をクリックできないという報告である。本文は動画とバージョン情報のみである。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**次のアクション:** 動画をもとに再現を確認し、修正する。

---

### ![Issue](icons/issue-opened.svg) [The workspace picker can sometimes be too wide](https://github.com/microsoft/vscode/issues/337643) `#337643`
**ステータス:** Open | **ラベル:** bug, agents-window

**概要:** 1.140.0-insider（macOS）の Agents window で、ワークスペースピッカーの幅が広くなりすぎることがあるという報告である。本文は動画とバージョン情報のみである。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**次のアクション:** ピッカー幅の上限の扱いを確認して修正する。

---

### ![Issue](icons/issue-opened.svg) [VS Code Interface Problem with Borders & Modern UI 😢](https://github.com/microsoft/vscode/issues/330645) `#330645`
**ステータス:** Open | **ラベル:** なし

**概要:** 1.132 から 1.133 に更新した後、エディタ、ターミナル、アクティビティバーなど多くの UI 要素の周りに枠線が追加され、小さい画面では特に邪魔だという報告である。無効にする設定が見つからないため、元に戻すか切り替え設定を用意してほしいと求めている。

**直近のアクティビティ:** rokobuljan が、このデザインは gap、radius、borderWidth などのパラメータとしてテーマから調整でき、既定では無効であるべきだと述べた。

**次のアクション:** 新しい枠線のデザインを無効化する設定やテーマトークンを提供するかを判断する必要がある。

---

### ![Issue](icons/issue-opened.svg) [Use the Extensions icon for the Customizations side panel entry](https://github.com/microsoft/vscode/issues/337808) `#337808`
**ステータス:** Open | **ラベル:** なし

**概要:** サイドパネルの **Customizations** の項目のアイコンを Extensions のアイコンに変え、他のカスタマイズ関連ツールと揃えるという機能要望である。担当は Hawk Ticehurst と Martin Aeschlimann とされている。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**次のアクション:** 担当者によるアイコン変更を待つ。

---

### ![Issue](icons/issue-opened.svg) [Copilot ](https://github.com/microsoft/vscode/issues/337799) `#337799`
**ステータス:** Open | **ラベル:** new release

**概要:** 1.139.0（macOS）で過去の会話を再開しようとすると、フォークした新しい会話でも「An image attached earlier in this conversation is no longer accessible」というエラーになり、`vision_attachment_not_accessible` の 400 が返るという報告である。

**直近のアクティビティ:** 作者が、1.139.0 に更新してから起きるようになったと補足した。

**次のアクション:** 会話の再開時に過去の画像添付がアクセス不能になる原因を調べる。

---

### ![Issue](icons/issue-opened.svg) [GitHub Copilot context window size difference of over 300K when switching models.](https://github.com/microsoft/vscode/issues/326489) `#326489`
**ステータス:** Closed | **ラベル:** なし

**概要:** 会話の要約を無効にして Claude Opus 4.8（1M context）でコンテキストの約半分まで使った後、GPT-5.6 Luna に切り替えると使用量が約 140K と表示され、Claude に戻すと 500K 超に戻るという疑問である。

**直近のアクティビティ:** 作者が、何度バージョンやモデルが変わっても同様に起き、Anthropic から OpenAI への切り替えで大きく減ると述べた。トークナイザの違いによるものと推測し、具体的な不具合を示せないとして自らクローズした。

**次のアクション:** クローズ済みのため対応は不要である。

---

### ![Issue](icons/issue-opened.svg) [Support trusted “Try This” experiences in release notes and documentation](https://github.com/microsoft/vscode/issues/337794) `#337794`
**ステータス:** Closed | **ラベル:** feature-request

**概要:** リリースノートやドキュメントから、VS Code 内の対応する機能を直接試せる信頼済みの **Try This** リンクを提供する提案である。Markdown には安定した識別子だけを書き、タイトル、コマンド、サンプル、手順などはインストール済みの VS Code 側が持つ。ワークスペースの信頼、設定の有効化、拡張機能のインストール、Chat の送信などを自動では行わず、外部ドキュメントからの起動には確認を求める。参考実装は #337713 である。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**次のアクション:** クローズ済みで、実装は #337713 で扱われる。

---

### ![Issue](icons/issue-opened.svg) [Renderer OOM in JSON.stringify / serializeRequestArguments when invoking agent with large chat history](https://github.com/microsoft/vscode/issues/336200) `#336200`
**ステータス:** Open | **ラベル:** freeze-slow-crash-leak

**概要:** 1.137.0（Windows）で、多数のツール呼び出しと数 MB のテキスト結果を含む長い会話を復元して使い続けると、チャットエージェント呼び出しの RPC 引数を `serializeRequestArguments` で `JSON.stringify` する際にレンダラーが OOM で終了するという報告である。JS ヒープは 4,096 MiB の上限に対して約 1,571 MiB で、連続した空き領域が 477 MiB しかなかった。大きな履歴データは上限を設けるか外部化し、回復可能なエラーにすべきとしている。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**次のアクション:** 作者はダンプを提供できるとしているため、ダンプをもとに大きな履歴のシリアライズ経路を調査する。

---

### ![Issue](icons/issue-opened.svg) [Customizations view does not identify signed-out MCP server](https://github.com/microsoft/vscode/issues/337792) `#337792`
**ステータス:** Open | **ラベル:** なし

**概要:** MCP サーバー（例として Slack）がサインアウト状態のとき、Customizations ビューに大きな **Sign In** ボタンが出るだけで、どのサーバーの認証が必要なのかが示されないという報告である。「Sign in to Slack」のようにサーバー名を示すべきとしている。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**次のアクション:** 認証プロンプトに対象の MCP サーバー名を表示するよう修正する。

---


### ![PR](icons/git-pull-request.svg) [agentHost: handle relay publish acknowledgement failures](https://github.com/microsoft/vscode/pull/337819) `#337819`
**ステータス:** Open | **ラベル:** なし

**概要:** relay への publish で acknowledgement を要求しながら結果を無視していたため、拒否または未応答の publish があっても無関係な通信で接続が健全に見え、リクエストが保留のまま残る問題への対応である。チャンクごとに ack ID を追跡し、成功と `Duplicate` は受理、拒否時は既存の handshake または transport-close の経路で失敗させる。各 publish に 30 秒の ack 期限を設け、チャンク順序は維持し、自動再送はしない。#337778 の follow-up である。

**直近のアクティビティ:** Copilot の自動レビューは指摘なしだが、live relay と E2E の検証が未実施である点を要確認とした。ほかは github-actions のスクリーンショット差分レポートのみである。

**変更ファイル:** 2 ファイル、+416/-20。`webPubSubRelayTransport.ts` の改修（+78/-18）と、拒否、欠落、順序逆転、タイムアウト境界などを網羅するテスト（+338/-2）である。

**次のアクション:** live relay と E2E の手動で検証し、人間のレビューを受ける必要がある。

---

### ![PR](icons/git-pull-request.svg) [test: cover managed OTel export in Agent Host smoke tests](https://github.com/microsoft/vscode/pull/337795) `#337795`
**ステータス:** Open (Draft) | **ラベル:** なし

**概要:** Agent Host の managed OTel export を実際に検証するデスクトップ smoke テストを、既存の `VSCODE_SMOKE_TEST_POLICY=1` オプトインの下に追加する。bare collector URL と明示的な `/v1/traces` エンドポイントの両方を対象とし、loopback の OTLP/HTTP JSON receiver で成功した native Copilot turn span と managed header を受信することを要求する。6 つの GitHub/Azure デスクトップ CI レーンに managed ファイルの準備と後始末を追加し、既存ポリシーがあれば上書きせず拒否する。#337413 関連だが、修正そのものは含まない。

**直近のアクティビティ:** 作者 rwoll が、マージ前に対象の製品修正が入ること、CI で各テストを 10 回実行して不安定さや遅さがないことを確認する必要があるとコメントした。Copilot レビューは指摘なしだが、クロスプラットフォーム実行が未了で依存修正も未解決な点を要確認とした。

**変更ファイル:** 12 ファイル、+375/-15。GitHub Actions workflow 3 本と Azure Pipelines のテストステップ 3 本の改修、`agentHostManagedOTel.test.ts`、`managedSettings.ts`、`otlpCollector.ts`、README の新規追加である。

**次のアクション:** 依存する修正（エンドポイント routing は #337783 でマージ済み、managed telemetry 初期化は未解決）を待ち、フル smoke 実行と Windows runner で検証する。

---

### ![PR](icons/git-pull-request.svg) [test: cover managed sandbox enforcement in Agent Host smoke tests](https://github.com/microsoft/vscode/pull/337796) `#337796`
**ステータス:** Open (Draft) | **ラベル:** なし

**概要:** managed sandbox の強制を検証する macOS、Linux、Windows 向けの Agent Host 回帰 smoke テストである。デバイスの managed-settings ファイルで `sandbox.enabled: true` と `sandbox.allowBypass: false` を強制し、ローカル sandbox の off と on の両方で、SDK shell がポリシー衝突なく完了し、sandbox 外への書き込みが拒否されることを要求する。Windows は `chat.agent.sandbox.enabledWindows` と PowerShell、macOS/Linux は Bash を使う。#337795 の上に積まれている。

**直近のアクティビティ:** rwoll が #337795 と同じく、製品修正のマージと CI での 10 回実行による安定性確認が必要とコメントした。

**変更ファイル:** 3 ファイル、+105/-1。`agentHostManagedSandbox.test.ts` の新規追加、README と `main.ts` への登録である。

**次のアクション:** sandbox 起動修正のマージと #337795 の先行マージを待ち、各プラットフォームで 10 回のパスを確認する。

---

### ![PR](icons/git-pull-request.svg) [chat: Gate Discover separately and default the GitHub Feed on](https://github.com/microsoft/vscode/pull/337810) `#337810`
**ステータス:** Open | **ラベル:** なし

**概要:** MCP Gallery、Plugin marketplace、Copilot Connectors の各 PR が共有する土台である。既定 off の `chat.customizations.marketplace.enabled` で Agent Customizations に Discover と従来の Overview のどちらを出すかを制御し、`chat.customizations.marketplace.sources.publicFeed.enabled` を既定 on にする。GitHub Feed は両方の設定が on のときだけクエリやインストールを開始でき、どちらかの切り替えで進行中の処理をキャンセルする。Marketplace エントリにはアダプタが割り当てる内部優先度を導入し、browse では優先度順に round-robin で並べる。

**直近のアクティビティ:** Copilot レビューが変更推奨 1 件を挙げた。GitHub Feed 設定の説明文が、フィードの動作が Discover の表示に依存するかのように読める点である。ほかは github-actions のスクリーンショット差分（5 件変更）である。

**変更ファイル:** 17 ファイル、+305/-40。`customizationMarketplaceService.ts`、`customizationMarketplaceSources.ts`、`customizationMarketplaceConfiguration.ts` の改修と、ゲートやランキングのテスト追加である。

**次のアクション:** 設定説明文を修正し、上に積まれた #337574、#337577、#337649 のためにレビューとマージを進める。

---

### ![PR](icons/git-pull-request.svg) [Chat: onboarding spotlight tour for the chat input](https://github.com/microsoft/vscode/pull/337814) `#337814`
**ステータス:** Open (Draft) | **ラベル:** なし

**概要:** Agents ウィンドウのツアーと同じオンボーディングエンジンを使い、Chat ビューに spotlight tour を追加する。`chat.onboarding.experience` が `spotlight` のとき、エディタで一度もチャットを送信していないユーザーに mode picker と model picker を説明する 2 ステップのツアーを 1 回だけ表示する。判定に使う `chat.editorUsage.messages` は #336792（9 月 18 日頃）以降しか記録されていないため、それ以前の利用者も新規に見えるという注意点が書かれている。ExP 連携は未完了である。

**直近のアクティビティ:** Copilot レビューが変更推奨 1 件を挙げた。ウィンドウ間やツアーの待機中に対象判定が古くなり、対象外のユーザーに表示され得る点である。

**変更ファイル:** 6 ファイル、+442/-1。`chatInputTour.contribution.ts`（+240）の新規追加、`ChatInputPart` への picker 要素 getter 追加、`editorChatUsage.ts` の改修、テスト 6 件である。

**次のアクション:** 対象判定の再評価に関する指摘に対応し、ExP 展開前に「未チャット」判定のシグナルを見直す。

---

### ![PR](icons/git-pull-request.svg) [sessions: keep new-session controls opaque over dark chat backgrounds](https://github.com/microsoft/vscode/pull/337832) `#337832`
**ステータス:** Open (Draft) | **ラベル:** なし

**概要:** #337721 の修正である。chat 背景を設定すると、新規セッション composer 下段のコントロール（Mode + Permissions picker を含む）がダークテーマで透明になっていた。原因は `chatView.css` が `chatWidget.css` のルールを高い specificity で重複定義しており、新規セッション画面では `--vscode-chat-list-background` が未設定のため、ダークテーマで透明な `button.secondaryBackground` にフォールバックしていたことである。重複セレクタを削除し、不透明な `--session-view-background` にフォールバックする既存ルールを適用させる。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**変更ファイル:** 2 ファイル、+13/-7。`chatView.css` の重複ルール削除と、実際の DOM 構造を再現するよう直した `chatView.test.ts`（main では `rgba(0, 0, 0, 0)` で失敗し、修正後にパス）である。

**次のアクション:** Draft 解除とレビューを待つ。

---

### ![PR](icons/git-pull-request.svg) [Agents - Implement cached-file handling for Recomputing state in UI](https://github.com/microsoft/vscode/pull/337815) `#337815`
**ステータス:** Open | **ラベル:** なし

**概要:** agent host セッションの changeset adapter と chat input pills で `Recomputing` 状態を扱い、キャッシュ済みのファイル一覧を使う。`agentHostSessionInputPills.ts` は `Recomputing` 時にキャッシュを使い、`agentHostSessionChangesets.ts` は `Recomputing` スナップショット受信時に古い streaming データを解放する。パフォーマンストレースでセッション切り替え時の UI 遅延が大きく減ったとしている。

**直近のアクティビティ:** Copilot レビューは承認推奨で、非ブロッキングの JSDoc 修正 1 件のみである。pwang347 のレビューは dismissed となり、作者 lszomoru が本文なしのレビューコメントを付けた。

**変更ファイル:** 4 ファイル、+75/-26。上記 2 ファイルの改修と回帰テストの追加である。

**次のアクション:** JSDoc の修正と人間の承認が必要である。

---

### ![PR](icons/git-pull-request.svg) [Track customization marketplace installation records](https://github.com/microsoft/vscode/pull/337816) `#337816`
**ステータス:** Open (Draft) | **ラベル:** なし

**概要:** #337110 の follow-up で、marketplace のインストール来歴をローカル項目の探索から分離する。インストール成功時にマシンローカルのプロファイル記録（source、identifier、version、検証済み descriptor、インストール先 URI または MCP server ID）を保存し、実体と照合して `checking`、`installed`、`missing` を導出する。同名のローカル項目をインストール済みとみなさなくなり、欠損時は欠けたファイルだけを復元する **Repair** を追加する。拡張機能のインストールモデルに倣った設計である。

**直近のアクティビティ:** Copilot レビューが変更推奨 2 件を挙げた。記録の永続化について、ウィンドウ間のデータ消失、ファイルシステムエラー処理、性能、破棄済み UI のライフサイクルの問題である。

**変更ファイル:** 12 ファイル、+1171/-301。中心は `customizationMarketplaceInstallService.ts`（+655/-109）と `aiCustomizationDiscoveryPage.ts`（+175/-102）で、テストも拡充している。

**次のアクション:** 永続化まわりの指摘に対応する必要がある。

---

### ![PR](icons/git-pull-request.svg) [build: harden Linux product signing resilience](https://github.com/microsoft/vscode/pull/337361) `#337361`
**ステータス:** Open (Draft) | **ラベル:** なし

**概要:** Linux の product ジョブのタイムアウトは 90 分だが ESRP 署名は最大 120 分かかり得るため、署名後のスキャンやアップロード前にキャンセルされる恐れがある。タイムアウトを 180 分に延ばし、Debian/RPM 署名を Snap パッケージングより前に開始し、各署名プロセスの stdout、stderr、終了結果、経過時間を保持して失敗を即時に観測する（Windows/macOS の呼び出し元にも効く）。Linux では 20 分の遅延警告を出し、Copilot の source-map アップロードを publish の意図に結び付ける。署名ポリシーとリリースチェックは変えない。

**直近のアクティビティ:** Copilot レビューが 9 月 24 日に 3 回実行された。並列の Windows 署名失敗時に出力消費前に Node が終了し得るという指摘は解消されたが、他の非 publish 検証パイプラインに source-map アップロードの既定値が残る点と、全プラットフォームに影響する共通実装が現 head で実署名の検証を受けていない点を要確認とした。

**変更ファイル:** 10 ファイル、+708/-37。`build/azure-pipelines/common/codesign.ts`、Linux の pipeline YAML、`codesign.test.ts`（+260）、`productPublish.test.ts`（+185）などである。

**次のアクション:** 現 head での実署名 Product 検証と、collaborator による承認が必要である。

---

### ![PR](icons/git-pull-request.svg) [agentHost: attribute model responses to client window](https://github.com/microsoft/vscode/pull/337828) `#337828`
**ステータス:** Open (Draft) | **ラベル:** なし

**概要:** Copilot SDK の `copilotSdk/response.success` と `copilotSdk/response.error` テレメトリに `initiatorClientType`（`agents_window`、`editor_window`、`unknown`）を付与し、ターン経由の結合なしにウィンドウ別の分析をできるようにする。結合イベント `agentHost.modelCallTurnCorrelated` にも同じフィールドを載せる。起点のクライアント種別を root とサブエージェントのターンから取得して相関情報とともに運び、ターン完了後の相関に備えて有界のマッピングを保持する。

**直近のアクティビティ:** Copilot レビューが変更推奨とし、古いキャッシュメンバー参照のため型チェックが通らない点と、side-effects 伝播の境界テストが不足している点を挙げた。作者 amunger が本文なしのレビューコメントを 2 件付けた。

**変更ファイル:** 9 ファイル、+138/-87。`modelCallTurnCorrelation.ts`、`copilotAgent.ts`、`copilotGitHubTelemetryForwarder.ts` とそれぞれのテストである。

**次のアクション:** 型エラーの修正と境界テストの追加が必要である。

---

### ![PR](icons/git-pull-request.svg) [Fix routing and subscription issues for chat and changeset addresses](https://github.com/microsoft/vscode/pull/337823) `#337823`
**ステータス:** Open | **ラベル:** なし

**概要:** agent host における chat と changeset のアドレスルーティングの回帰を修正する。アドレスを自前で組み立てず、ホストが通知するものを使い、再接続後に変更の購読を更新し、セッション全体の変更とレスポンス単位の変更を分けて扱う。Changes ビューと pill ナビゲーションの信頼性向上が目的である。

**直近のアクティビティ:** Copilot レビューが変更推奨 1 件を挙げた。動的な default-chat ルーティングが、削除された opaque chat のキャッシュを adapter の生存期間中保持し続け得る点である。

**変更ファイル:** 14 ファイル、+481/-78。`baseAgentHostSessionsProvider.ts`、`agentHostSessionChangesets.ts`、`changesetUri.ts` の改修と、remote provider のテスト（+163/-11）などの回帰テストである。

**次のアクション:** キャッシュ保持の指摘に対応し、本文にあるとおりライブ環境での人手で検証する。

---

### ![PR](icons/git-pull-request.svg) [terminal: dispose auto responders when a persistent process is reattached](https://github.com/microsoft/vscode/pull/337827) `#337827`
**ステータス:** Open (Draft) | **ラベル:** freeze-slow-crash-leak, terminal

**概要:** ウィンドウのリロードなどで永続ターミナルプロセスに再アタッチするたびに `AutoRepliesPtyServiceContribution.handleProcessReady` が再実行され、古い `TerminalAutoResponder` を破棄せずに置き換えていた。そのため N 回リロードすると responder が N+1 個残り、一致したプロンプトに N+1 回応答していた。既存の responder を in-place で dispose してから新しいものを登録する。

**直近のアクティビティ:** Copilot レビューは承認推奨で、テストの実時間待機を避けられるという非ブロッキング指摘のみである。

**変更ファイル:** 2 ファイル、+49/-0。`autoRepliesContribController.ts` の 7 行追加と、`handleProcessReady` を 2 回呼んで応答が 1 回であることを確かめるテストである。

**次のアクション:** 同じテストファイルを追加する #336022 と、後からマージされる側でテストスイートを統合する必要がある。

---

### ![PR](icons/git-pull-request.svg) [sessions: add remote session delegation tools](https://github.com/microsoft/vscode/pull/336690) `#336690`
**ステータス:** Open (Draft) | **ラベル:** agent-host

**概要:** #336682 の修正で、Agents ウィンドウに picker を使わないリモート委譲ツールを追加する。`list_agent_hosts` と `create_remote_session` はリソースとモデルで絞り込み最も負荷の低いホストに配置し、`send_remote_message` は特定チャットへの追撃やバックグラウンド承認、`get_remote_session` は読み取り専用の状態確認を担う。既定 off の `chat.remoteSessions.tools.enabled` で制御し、experiment による既定値も持つ。

**直近のアクティビティ:** 直近では github-actions のスクリーンショット差分のみである。9 月 23 日の Copilot レビューは、ツールの表示が AI 無効ゲートに従わない点と、一部のユーザー向けエラーが未ローカライズな点を要確認とした。roblourens と pwang347 のレビューは dismissed である。

**変更ファイル:** 60 ファイル、+6513/-152。`agentHostResources.ts`、`agentRemoteSessionMeta.ts`、`remoteSessionOriginContribution.ts`、`agentService.ts`、`SESSIONS.md` などである。

**次のアクション:** AI 無効ゲートとローカライズに対応し、Draft を解除する。

---

### ![PR](icons/git-pull-request.svg) [Add prioritized Sessions Inbox notifications view](https://github.com/microsoft/vscode/pull/337081) `#337081`
**ステータス:** Open (Draft) | **ラベル:** なし

**概要:** Sessions に `inboxOne` contribution と、優先度付きの通知を並べる **Inbox** ビューを追加する。`IInboxNotificationsService` がセッション状態（`NeedsInput`、未読の `Completed`）と、セッションに紐付く PR のシグナル（必須 CI の失敗と成功、未解決の Copilot レビューコメント、マージ済み PR）を集約する。ワンクリックで `/fix-ci`、`/act-on-feedback`、`/merge` をセッションのチャットにバックグラウンド送信でき、マージ済み PR にはセッションのアーカイブや削除のアクションを付ける。

**直近のアクティビティ:** 直近 24 時間のコメントなし。9 月 21 日の Copilot レビューでは、未登録の CSS トークンによる hygiene 失敗と、通知分類、ライフサイクル、復元、アクセシビリティの問題など 8 件が指摘されている。

**変更ファイル:** 23 ファイル、+7613/-7。`inboxNotificationsService.ts`（+1942）、`inboxNotificationsView.ts`（+2262）、CSS（+767）、テスト（+1605）などの新規追加が中心である。

**次のアクション:** Copilot の指摘に対応して Draft を解除する必要がある。

---

### ![PR](icons/git-pull-request.svg) [Fix GitHub Enterprise authentication in Agent Host](https://github.com/microsoft/vscode/pull/337722) `#337722`
**ステータス:** Open | **ラベル:** なし

**概要:** Copilot（コーディングエージェント）による PR である。GitHub Enterprise ホストでは Copilot SDK クライアントを直接認証して `COPILOT_GH_HOST` を尊重させ、モデル探索、セッション作成、再開をそのクライアントで実行する。資格情報が実際に更新されたときは、アクティブなターンを止めない形で enterprise クライアントを再起動する。期限付き GHE 資格情報は未リリースのサインインフローにしかないため対象外である。#337485、#337562、#337661 を修正する。

**直近のアクティビティ:** Copilot レビューが変更推奨 1 件を挙げた。初期の 1 時間有効な資格情報や再起動の遅延で、空のカタログや古いトークンによるリクエストが起こり得る点である。roblourens が本文なしのレビューコメントを付け、vritant24、Yoyokrazy、dmitrivMS のレビューは dismissed となった。

**変更ファイル:** 4 ファイル、+190/-25。`copilotAgent.ts` と `copilotGitHubCredentials.ts` の改修と、テスト（+137）である。

**次のアクション:** 資格情報の指摘に対応し、承認を得る必要がある。

---

### ![PR](icons/git-pull-request.svg) [Bump version to 1.139.1 (CI attempt 2)](https://github.com/microsoft/vscode/pull/337831) `#337831`
**ステータス:** Open (Draft) | **ラベル:** なし

**概要:** #337813 と同一の 1.139.1 バージョン bump パッチを、CI の並列試行として出したものである。#337801 で追跡されるリカバリーリリース向けで、重複 PR のうち 1 件だけをマージする前提である。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**変更ファイル:** 4 ファイル、+5/-5。root の `package.json` と lockfile、Copilot 拡張の `engines.vscode` を `^1.139.1` に更新する。

**次のアクション:** #337813 系列のいずれか 1 件をマージし、残りをクローズする。

---

### ![PR](icons/git-pull-request.svg) [Bump version to 1.139.1 (CI attempt 3)](https://github.com/microsoft/vscode/pull/337830) `#337830`
**ステータス:** Open (Draft) | **ラベル:** なし

**概要:** #337831 と同じく、#337813 と同一の 1.139.1 バージョン bump を CI の 3 回目の並列試行として出したものである。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**変更ファイル:** 4 ファイル、+5/-5。変更内容は #337813 と同一である。

**次のアクション:** 重複 PR のため、1 件のマージ後にクローズする。

---

### ![PR](icons/git-pull-request.svg) [Respect preview-feature policy for HydraFusion](https://github.com/microsoft/vscode/pull/337829) `#337829`
**ステータス:** Open (Draft) | **ラベル:** なし

**概要:** `chat.copilot.hydraFusion.enabled` を VS Code 1.140 からの企業ポリシーとして登録し、組織が `chat_preview_features_enabled` でエディタのプレビュー機能を無効にしている場合は HydraFusion を強制的に off にする。プレビュー機能が許可または未指定なら設定値と experiment 値を維持する。更新したポリシーカタログもエクスポートする。

**直近のアクティビティ:** Copilot レビューは承認推奨で、指摘はない。

**変更ファイル:** 4 ファイル、+57/-1。`policyData.jsonc`、`chat.shared.contribution.ts`、`agentHostConfigPolicy.ts` とそのテストである。

**次のアクション:** Draft 解除と人間のレビューを待つ。

---

### ![PR](icons/git-pull-request.svg) [Add change scope review guidance](https://github.com/microsoft/vscode/pull/337802) `#337802`
**ステータス:** Open | **ラベル:** なし

**概要:** `.github/copilot-instructions.md` に、変更のスコープに関するリポジトリ全体の指針を加える。実装やレビューの前に意図したスコープを確かめ、変更されたファイルとサブシステムをすべて説明できるようにし、症状を消すだけで原因に触れない変更を指摘し、無関係な整理や推測的な改善は revert か分割を勧める。microsoft/vscode-engineering#3969 の指針の移植である。

**直近のアクティビティ:** Copilot レビューは承認推奨で、dmitrivMS と benvillalobos が承認した。

**変更ファイル:** 1 ファイル、+8/-0。

**次のアクション:** 承認済みのためマージを待つ。

---

### ![PR](icons/git-pull-request.svg) [agentHost: keep Codex permission tests out of /tmp](https://github.com/microsoft/vscode/pull/337826) `#337826`
**ステータス:** Open (Draft) | **ラベル:** なし

**概要:** Azure build 476995 の macOS worker で `os.tmpdir()` が `/tmp` を返し、Codex が macOS の system scratch root へのアクセスを意図的に許すため、Codex の permission 境界テスト 6 件が失敗した。最初に `.build` 配下へ移した修正は、GitHub の macOS ジョブで Codex がリポジトリの `AGENTS.md` を検出して失敗した。そこで Azure では `AGENT_TEMPDIRECTORY`、GitHub では `RUNNER_TEMP` を優先し、system scratch root とソースツリーの両方の外に fixture を作る。Agent Host の E2E や実 provider 統合テストの新規追加時に Azure DevOps での検証を必須とする記述も加える。

**直近のアクティビティ:** Copilot レビューは承認推奨で、指摘はない。

**変更ファイル:** 2 ファイル、+6/-3。`codexPermissions.integrationTest.ts` と `.github/skills/agent-host-e2e-tests/SKILL.md` である。

**次のアクション:** Draft 解除とレビューを待つ。

---

### ![PR](icons/git-pull-request.svg) [mcp: unify destinations and validate workspace install targets](https://github.com/microsoft/vscode/pull/337781) `#337781`
**ステータス:** Open | **ラベル:** なし

**概要:** **MCP: Add Server** で、agent-host セッション、Global、Remote、workspace の宛先を 1 つの picker にまとめる。Agents ウィンドウで **Workspace** がユーザーのプロジェクトではなくウィンドウ内部の設定ワークスペースを指していた問題も直し、プロジェクトフォルダを **Workspace (folder-name)** と表示する。Add Server、Install in Workspace、インストール検証で同じ宛先ルールを共有し、内部ワークスペース設定への書き込みは書き込み前に拒否する。

**直近のアクティビティ:** Copilot レビューは承認推奨で、Yoyokrazy のレビューは dismissed となった。

**変更ファイル:** 13 ファイル、+480/-82。`mcpCommandsAddConfiguration.ts` の改修、`mcpWorkspaceInstallTargetService.ts` の新規追加、`mcpConfigurationEntryPoints.test.ts`（+241/-13）などのテストである。

**次のアクション:** 人間の承認を得てマージする。

---

### ![PR](icons/git-pull-request.svg) [Hide GitHub Sandbox entries from Editor harness picker](https://github.com/microsoft/vscode/pull/337825) `#337825`
**ステータス:** Open (Draft) | **ラベル:** なし

**概要:** Editor の harness picker から「GitHub Sandboxes」の探索項目と個々のサンドボックスセッションを除外する。既存のサンドボックス会話は引き続き開け、保存済みの選択が新規チャットでサンドボックスを既定にすることもなくなる。代わりに Editor に「Run in GitHub Sandbox」チェックボックスを追加し、環境を harness として列挙せずにサンドボックスセッションを作れるようにする。

**直近のアクティビティ:** Copilot レビューが変更推奨 2 件を挙げた。リクエストのキャンセルで進行中のサンドボックス接続が止まらない点と、チェックボックスにアクセシブルな説明が必要な点である。

**変更ファイル:** 22 ファイル、+1604/-168。`editorCloudSandboxContribution.ts`（+213/-16）、新規の `cloudSandboxCheckbox.ts`、`chatServiceImpl.ts` の改修と、テスト（`editorCloudSandboxContribution.test.ts` +467/-12 など）である。

**次のアクション:** 2 件の指摘に対応する必要がある。

---

### ![PR](icons/git-pull-request.svg) [agentHost: support local Copilot runtime override](https://github.com/microsoft/vscode/pull/337817) `#337817`
**ステータス:** Open (Draft) | **ラベル:** なし

**概要:** ローカルでの dogfooding 用に、非表示でマシン単位の設定 `chat.agentHost.copilot.runtimePath` を追加する。同梱の SDK wrapper とネイティブモジュールはそのまま使い、指定パスが絶対パスで実行可能なファイルかを検証して、不正ならフォールバックせず明示的に失敗させる。パスは Copilot の起動設定に含めるため、変更時は既存の安全なクライアント再起動フローで反映される。

**直近のアクティビティ:** Copilot レビューが変更推奨 1 件を挙げた。リモートのユーザー設定から、リモートのファイルシステムパスがローカルの Agent Host に転送され得る点である。作者 roblourens が本文なしのレビューコメントを付けた。

**変更ファイル:** 6 ファイル、+200/-9。`copilotAgent.ts`（+41/-4）、`copilotCliConfig.ts`、設定登録と、テスト（+130）である。

**次のアクション:** リモートパス転送の指摘に対応する必要がある。

---

### ![PR](icons/git-pull-request.svg) [sessions: remove preview tag from mark as done confetti](https://github.com/microsoft/vscode/pull/337812) `#337812`
**ステータス:** Merged | **ラベル:** なし

**概要:** 既定で有効になっている Mark as Done の confetti 設定から preview タグを外す。既定値と experiment の構成は変えない。作業環境に Node.js がなくコンパイルは未実行と書かれている。

**直近のアクティビティ:** rzhao271 が承認してマージされた。Copilot レビューは、既存の schema テストで preview タグの削除を検証すべきと 1 件推奨していた。

**変更ファイル:** 1 ファイル、+0/-1。`sessions.contribution.ts` の 1 行削除である。

**次のアクション:** マージ済みのため対応は不要である。Copilot が推奨したテスト追加は任意の follow-up となる。

---

### ![PR](icons/git-pull-request.svg) [api: Preserve snippet whitespace in returned code actions](https://github.com/microsoft/vscode/pull/337740) `#337740`
**ステータス:** Open (Draft) | **ラベル:** なし

**概要:** #325990 の修正である。`vscode.executeCodeActionProvider` で取得したコードアクションの workspace edit を拡張 API 側に戻す変換で `SnippetTextEdit.keepWhitespace` が失われ、provider が整えた空白をエディタが再インデントしていた。変換時にフラグをコピーする。Windows の Code OSS で 2 スペースが 4 スペースに広がる現象を再現し、修正後に保たれることを確認している。

**直近のアクティビティ:** Copilot レビューは承認推奨である。ほかは github-actions のスクリーンショット差分のみである。

**変更ファイル:** 2 ファイル、+6/-2。`extHostTypeConverters.ts` の 1 行追加と、既存の round-trip テストへの空白のアサーション追加である。

**次のアクション:** Draft 解除とレビューを待つ。

---

### ![PR](icons/git-pull-request.svg) [Show credits for canceled local agent turns](https://github.com/microsoft/vscode/pull/317410) `#317410`
**ステータス:** Closed | **ラベル:** なし

**概要:** 本文はテンプレートのみである。5 月の Copilot レビューによれば、ユーザーが Stop でターンをキャンセルした場合でもモデルやクレジットの情報を表示するため、intent の `chatResult` とキャンセルを競合させ、最終結果の整形処理をリファクタする変更である。

**直近のアクティビティ:** 直近 24 時間のコメントなし。Draft のままマージされずにクローズされた。

**変更ファイル:** 1 ファイル、+26/-8。`chatParticipantRequestHandler.ts` である。

**次のアクション:** クローズ済みのため対応は不要である。

---

### ![PR](icons/git-pull-request.svg) [Update xAI model handling](https://github.com/microsoft/vscode/pull/314534) `#314534`
**ステータス:** Closed | **ラベル:** なし

**概要:** 本文はテンプレートのみである。Copilot レビューによれば、xAI（Grok）モデルの判定を `grok-code*` から `grok*` 全体に広げる `isXAIFamily()` を導入し、それに基づいて編集ツールの能力判定やプロンプトを調整し、xAI では Responses API の `reasoning.summary` と `reasoning.effort` を無効にする変更である。

**直近のアクティビティ:** 直近 24 時間のコメントなし。Draft のままマージされずにクローズされた。

**変更ファイル:** 5 ファイル、+30/-11。`chatModelCapabilities.ts`（+20/-2）、`responsesApi.ts`、`xAIPrompts.tsx` などである。

**次のアクション:** クローズ済みのため対応は不要である。

---

### ![PR](icons/git-pull-request.svg) [Add ability to disable individual customizations](https://github.com/microsoft/vscode/pull/311806) `#311806`
**ステータス:** Closed | **ラベル:** api

**概要:** #290290 の修正として、個々のカスタマイズを無効化できるようにする PR である。本文はテンプレートのみで、変更内容からは Claude と Copilot CLI のセッション設定サービス、無効化状態を保存する `extensionDisablementStore.ts`、customization provider、API proposal の追加が読み取れる。

**直近のアクティビティ:** 直近 24 時間のコメントなし。4 月に pwang347、joshspicer、mjbvz がレビューコメントを付けていたが、マージされずにクローズされた。

**変更ファイル:** 55 ファイル、+4682/-352。`baseSessionSettingsService.ts`、`claudeCustomizationProvider.ts`、`copilotCLICustomizationProvider.ts` とテスト群などである。

**次のアクション:** クローズ済みのため対応は不要である。

---

### ![PR](icons/git-pull-request.svg) [chore: bump @github/copilot-sdk to 1.0.15-preview.3](https://github.com/microsoft/vscode/pull/337821) `#337821`
**ステータス:** Open (Draft) | **ラベル:** copilot-sdk-auto-upgrade

**概要:** vs-code-engineering bot による署名付きの自動依存更新で、`@github/copilot-sdk` を `1.0.15-preview.2` から `1.0.15-preview.3` に上げる。検証が通るまで Draft とし、`sdk-vscode-bump-fix` エージェントが Linux の PR チェックで最大 10 サイクル、フルの product ビルドで最大 5 サイクル修正を試みる。公開パッケージのパス構成に変化はない。

**直近のアクティビティ:** Copilot レビューは承認推奨である。github-actions のスクリーンショット差分では 7 件の変更が報告された。

**変更ファイル:** 4 ファイル、+74/-74。root と `remote/` の `package.json` と lockfile である。

**次のアクション:** 検証の通過を待って Draft を解除する。

---

### ![PR](icons/git-pull-request.svg) [[TEST ONLY] validate automated replay expectation updates](https://github.com/microsoft/vscode/pull/334367) `#334367`
**ステータス:** Closed | **ラベル:** なし

**概要:** microsoft/vscode-engineering#3782 のための一時的な制御テストである。改修した `sdk-vscode-bump-fix` workflow が、過去に失敗した Electron ジョブから決定的な Copilot プロンプト期待値の変更だけを適用し、LLM fixture を記録しないことを示すのが目的である。`@github/copilot` CLI `1.0.83-4` への更新でベースのシステム指示が変わり、19 件の `.prompt.md` スナップショットが不一致になったため、それらを再生成している。

**直近のアクティビティ:** 直近 24 時間のコメントなし。テスト後のクローズという予定どおりクローズされた。

**変更ファイル:** 24 ファイル、+258/-246。package と lockfile、`mock-llm-server.ts`（+137/-74）、各モデルのプロンプトスナップショットである。

**次のアクション:** クローズ済みのため対応は不要である。

---

### ![PR](icons/git-pull-request.svg) [Persist full Codex command output across chat reloads](https://github.com/microsoft/vscode/pull/337390) `#337390`
**ステータス:** Open (Draft) | **ラベル:** なし

**概要:** #328596 の一部で、#335016 の follow-up である。Codex のコマンド出力は 80,000 文字まではインラインのまま残し、それを超える分はチャットのセッションデータベース（既存の 10 MiB 上限内）に保存して、先頭 400 文字と **Open Full Output (Read-Only)** を表示する。Codex の安定した item id を tool-call id として使い、`thread/revert` や `thread/rollback` でターンが消えたときは保存出力も削除する。コマンドは OS シェル非依存として扱う。

**直近のアクティビティ:** 作者 anthonykim1 が、プレビューを出力末尾ではなく先頭 400 文字をそのまま表示する方式に変えたと報告した。Codex adapter のみの変更で、8,145 件のテストがパスしている。Copilot レビューは、回復された completion が保存処理を経由しない点と、インクリメンタルな履歴更新で復元済みのターミナルリソースが失われ得る点の 2 件を指摘していた。

**変更ファイル:** 12 ファイル、+733/-50。`codexAgent.ts`（+130/-8）、`codexReplayMapper.ts`、新規の `codexTerminalOutput.ts` と、テスト（`codexPrewarmEviction.test.ts` +235 など）である。

**次のアクション:** 本文の検証項目には Copilot の指摘に対応するテストが含まれており、解消の確認とレビューを経て Draft を解除する。

---

### ![PR](icons/git-pull-request.svg) [Persist full Claude shell output in chat sessions](https://github.com/microsoft/vscode/pull/337389) `#337389`
**ステータス:** Open (Draft) | **ラベル:** なし

**概要:** #328596 の一部で、#337390 の Claude 版にあたる。Claude がトップレベルの `Bash` 結果をファイルに退避した場合、その完全な出力を完了通知の発行前にセッションデータベースへコピーする（10 MiB 上限、PowerShell も対象）。`buildNonPtyShellTerminalUri` で終了済みの非 PTY リソースを付け、ライブ表示と復元後の両方で **Open Full Output (Read-Only)** を使えるようにする。キャンセルが先に勝った場合は、保存した内容と DB 行を削除する。

**直近のアクティビティ:** 作者 anthonykim1 が macOS で検証し、262,199 文字の出力を Agent Host の再起動とウィンドウのリロード後も完全に開けたと報告した。Copilot レビューはキャンセル競合で未公開の出力が残る点を指摘していた。作者は Copilot 生成のセルフレビューも投稿し、P0 なし、P1 が 1 件、P2 が 5 件としている。

**変更ファイル:** 10 ファイル、+587/-17。新規の `claudeTerminalOutput.ts`（+182）、`claudeSdkMessageRouter.ts` の改修と、テスト（`claudeTerminalOutput.test.ts` +214 など）である。

**次のアクション:** セルフレビューの P1 に対応し、Draft を解除する。

---

### ![PR](icons/git-pull-request.svg) [agentHost: expand E2E workspace and lifecycle coverage](https://github.com/microsoft/vscode/pull/337627) `#337627`
**ステータス:** Open (Draft) | **ラベル:** なし

**概要:** 本番動作を変えずに、同梱 provider の E2E カバレッジを広げる。マルチルートや worktree の扱い、automation のキャンセルと重複排除、入力ドラフト、Git チェックアウトの安全性、MCP の side channel、チェックポイント比較、Copilot の会話圧縮、モデル API エラーの分類とリトライなどが対象である。2 ラウンドで 66 シナリオを宣言し 106 件のパスが増え、行カバレッジは 73.34 % から 74.05 % になったが、+10 ポイントの目標には届いていない。見つかった製品側の不足は `KNOWN_ISSUES.md` に記録している。

**直近のアクティビティ:** Copilot レビューは、ファイルシステムと再起動のシナリオについてクロスプラットフォームでの繰り返し安定性検証が進行中である点を要確認とした。

**変更ファイル:** 106 ファイル、+7896/-1107。82 件のモデルキャプチャ YAML の追加、`KNOWN_ISSUES.md`（+89）と README の更新、テストの追加である。

**次のアクション:** クロスプラットフォームでの安定性を確認する必要がある。

---

### ![PR](icons/git-pull-request.svg) [chat: Add Copilot connectors as an authenticated customization source](https://github.com/microsoft/vscode/pull/337649) `#337649`
**ステータス:** Open (Draft) | **ラベル:** なし

**概要:** 認証付きの Copilot Connectors ソースを customization marketplace と MCP 管理に追加する。#337810 の上に積まれ、クローズされた #337597 と #337645 を置き換える。コネクタの探索とサインインは匿名の GitHub Feed IPC から分け、デスクトップではスコープを限定したネイティブチャネルを使う。通常の GitHub サインインで汎用カタログを閲覧でき、**Check Connection** などの明示操作時にだけ追加権限を求める。既定 off の `chat.customizations.copilotConnectors.enabled` で制御し、Agent Host は GitHub OAuth トークンのリソースと同一の HTTPS オリジンにある MCP エンドポイントだけを供給する。

**直近のアクティビティ:** Copilot レビューが変更推奨とし、OAuth トークンの露出と、無関係な chat service の回帰を挙げた。github-actions のスクリーンショット差分では 37 件の変更が報告された。

**変更ファイル:** 61 ファイル、+7053/-371。新規の `agentHostMcpConnectorsService.ts`（+348）とテスト（+467）、`copilotConnectorsIpc.ts`、`copilotConnectorsRequestService.ts`、Claude、Codex、Copilot 各 agent の改修などである。

**次のアクション:** トークン露出の指摘に対応し、#337810 のマージを待つ。

---

### ![PR](icons/git-pull-request.svg) [Fix setting names in the npm extension README](https://github.com/microsoft/vscode/pull/337820) `#337820`
**ステータス:** Open | **ラベル:** なし

**概要:** npm 拡張の README に存在しない設定名が 2 つ書かれていた問題を直す。`npm.scriptCodeLens.enable` を実際に読まれる `debug.javascript.codelens.npmScripts` に、コマンド ID である `npm.runScriptFromFolder` を設定名 `npm.enableRunFromFolder` に置き換える。外部コントリビューターによる PR で、Claude Code の支援を受けて作成し、作者がソースと照合したと明記している。

**直近のアクティビティ:** Copilot レビューは承認推奨である。

**変更ファイル:** 1 ファイル、+2/-2。`extensions/npm/README.md` のみである。

**次のアクション:** メンテナの承認とマージを待つ。

---

### ![PR](icons/git-pull-request.svg) [[Experiment] Show a pip and popup when a Copilot sale is ongoing and the chat is closed](https://github.com/microsoft/vscode/pull/331588) `#331588`
**ステータス:** Merged | **ラベル:** なし

**概要:** Chat が閉じているとき、割引対象モデルの Copilot セールを知らせる pip とプロモーションポップアップを Copilot アイコンに表示する。Chat を開いている場合は既存の入力バナーのままで、pip のクリックやバナーの表示で pip は消える。ExP の実験として運用され、ホスト型モデル（OpenAI、MAI、Anthropic、Grok）がセール対象の場合も同じカードを出す。

**直近のアクティビティ:** lramos15 と dmitrivMS が承認してマージされた。lramos15 と Yoyokrazy の以前のレビューは dismissed である。vs-code-engineering bot の CODENOTIFY が statusbar の変更について benibenj に通知した。

**変更ファイル:** 19 ファイル、+884/-13。新規の `chatStatusPromo.ts`（+221）とテスト（+362）、`chatStatusEntry.ts` の改修、hover と statusbar の小改修である。

**次のアクション:** マージ済みのため、実験の結果を追う。

---

### ![PR](icons/git-pull-request.svg) [Add configured Plugin marketplaces to Discover](https://github.com/microsoft/vscode/pull/337577) `#337577`
**ステータス:** Open (Draft) | **ラベル:** なし

**概要:** 構成済みの Plugin marketplaces を、独立して有効化できるソースとして統合 Discover に追加する。インストール前には既存のプラグイン信頼と strict-marketplace ポリシーでエントリを再検証する。このソースが有効な間は重複する Plugins Available セクションを隠し、Marketplace 表示が on のときはカスタム Plugin marketplaces を先頭に出し、public GitHub Feed も on なら重複する Awesome Copilot のエントリを除く。#337810 の上に積まれている。

**直近のアクティビティ:** Copilot レビューが変更推奨とし、refresh 動作、キャンセル、部分失敗の報告に中程度の問題が 5 件残るとした。github-actions のスクリーンショット差分では 118 件の変更が報告された。

**変更ファイル:** 35 ファイル、+1316/-109。新規の `pluginCustomizationMarketplaceProvider.ts`（+135）、`pluginMarketplaceService.ts`、`pluginInstallService.ts`、Discover ページの改修とテストである。

**次のアクション:** 指摘に対応し、#337810 のマージを待つ。

---

### ![PR](icons/git-pull-request.svg) [Add MCP gallery as an independent customization feed](https://github.com/microsoft/vscode/pull/337574) `#337574`
**ステータス:** Open (Draft) | **ラベル:** なし

**概要:** 既定 on の MCP Gallery ソースを、public GitHub Feed とは独立に Discover へ追加する（表示は `chat.customizations.marketplace.enabled` の下）。インストール来歴は区別し、GitHub Feed はバージョン固定の feed レジストリ記録、MCP Gallery は構成済みの VS Code レジストリで正確な名前を解決し、別レジストリの同名サーバーでは代用しない。有効時は MCP の探索を Chat Customizations の管理ページから Discover に移し、インストール済みサーバーの管理は残す。358 件のテストがパスしている。

**直近のアクティビティ:** Copilot レビューが変更推奨とし、デスクトップのページネーション、レジストリの来歴、terminal サービスの登録、テスト構造にブロッキングな正確性の問題があるとした。

**変更ファイル:** 32 ファイル、+1104/-97。新規の `mcpGalleryMarketplaceProvider.ts`（+102）、electron-browser 側の `customizationMarketplaceService.ts`、`mcpGalleryService.ts`、`mcpListWidget.ts` の改修とテストである。

**次のアクション:** ブロッキング指摘に対応し、#337810 のマージを待つ。

---

### ![PR](icons/git-pull-request.svg) [chat: keep MCP sign-in buttons compact](https://github.com/microsoft/vscode/pull/337800) `#337800`
**ステータス:** Merged | **ラベル:** なし

**概要:** Customizations ビューで認証が必要な MCP サーバーの名前が消える問題を直す。アクションコンテナが `display: contents` のため、標準ボタンの幅 100 % が行全体を占めてサーバー情報を押しつぶしていた。インラインの Sign In ボタンを内容幅にし、伸縮しないようにする。#337399 のレイアウト変更に関連する。

**直近のアクティビティ:** rwoll が before/after のスクリーンショットを求め、作者 vritant24 が component fixture から撮影した画像を追加した。Copilot レビューは installed-home-row セレクタの回帰テスト不足を 1 件推奨した。dmitrivMS が承認してマージされた。

**変更ファイル:** 2 ファイル、+55/-0。CSS の 2 行追加と、350px と 600px 幅のレイアウトテストである。

**次のアクション:** マージ済みのため対応は不要である。

---

### ![PR](icons/git-pull-request.svg) [Preserve selected provider for voice-created worktree sessions](https://github.com/microsoft/vscode/pull/337094) `#337094`
**ステータス:** Merged | **ラベル:** なし

**概要:** Copilot（コーディングエージェント）による PR で、#335825 を修正する。音声で作成した worktree セッションが、選択中のセッションタイプがフォルダで提供されなくなると Local に切り替わっていた。`prepareNewVoiceSession` が使う `inheritableSessionTarget` が provider と type の組が使えないと両方を落とし、order が -1 の Local Agent Host が選ばれていたのが原因である。セッションタイプだけが使えない場合は provider を維持し、その provider の既定タイプを使わせる。

**直近のアクティビティ:** meganrogge、dmitrivMS、pwang347 が承認してマージされた。

**変更ファイル:** 2 ファイル、+68/-5。`voiceBridge.contribution.ts` の修正と `voiceBridge.test.ts` のテスト追加である。

**次のアクション:** マージ済みのため対応は不要である。

---

### ![PR](icons/git-pull-request.svg) [fix: memory leak in terminal auto replies](https://github.com/microsoft/vscode/pull/336022) `#336022`
**ステータス:** Open | **ラベル:** freeze-slow-crash-leak, terminal

**概要:** ターミナルの auto-reply を削除すると現在の responder は破棄されるが、pty host の設定 map に古いプロンプトが残り、次に開いたターミナルで再インストールされていた。すべての返信をアンインストールするときに保存済みの auto-reply もクリアする。追加と削除を 37 回繰り返すと 37 個の responder が残っていたが、修正後は増加しないとしている。

**直近のアクティビティ:** dmitrivMS が CHANGES_REQUESTED を付け、別コメントを参照しつつ回帰を持ち込むように見えると指摘した。

**変更ファイル:** 2 ファイル、+50/-0。`autoRepliesContribController.ts` の 1 行追加とテストである。

**次のアクション:** 回帰の指摘に対応する必要がある。同じテストファイルを追加する #337827 との統合も必要である。

---

### ![PR](icons/git-pull-request.svg) [sessions: list each pull request once in the multi-folder session hover](https://github.com/microsoft/vscode/pull/337797) `#337797`
**ステータス:** Merged | **ラベル:** なし

**概要:** マルチフォルダのセッションのホバーで、同じ PR が上部セクションと「Session summary」の両方に表示されていた。`getSessionSummaryHoverData` が上部に出した PR を PR URI で比較して Session summary から除き、残りがなければ PR リストを送らない。ヘルパーを `getPullRequestRefs` と `toHoverPullRequests` に分けて両セクションで共有する。

**直近のアクティビティ:** Copilot レビューは承認推奨で、roblourens が承認してマージされた。

**変更ファイル:** 2 ファイル、+110/-17。`sessionHoverContent.ts` の修正と `sessionsList.test.ts` への新しいテストスイートの追加である。

**次のアクション:** マージ済みのため対応は不要である。

---

### ![PR](icons/git-pull-request.svg) [Move new session composer layout above vs below chat input](https://github.com/microsoft/vscode/pull/337753) `#337753`
**ステータス:** Open | **ラベル:** なし

**概要:** `sessions.chat.experimental.newSessionComposerLayout` が新規セッションの composer だけを変えるよう修正する。Workspace、Worktree、Branch のコントロールを入力の直上の行に移し、Copilot/harness picker をその行の右端に固定し、model picker は入力内、mode/permissions は入力の下に置く。実行中セッションの composer は元のレイアウトのままである。

**直近のアクティビティ:** Copilot レビューは、Web での行サイズと狭い幅でのエディタレイアウトの回帰、新しい provider 契約の仕様カバレッジを挙げて変更推奨とした。meganrogge が `@copilot review` を 2 回依頼し、Copilot は重要な問題なしと回答して未使用の import を削除した。justschen の好意的なレビューは dismissed となった。github-actions のスクリーンショット差分では 95 件の変更が報告された。

**変更ファイル:** 13 ファイル、+104/-271。`agentHostSessionConfigPicker.ts`（+15/-48）、`newChatInput.ts`、`chatView.ts` の簡素化、CSS の削除、`blocks-ci-screenshots.md` の更新である。

**次のアクション:** 承認を得てマージする。

---

### ![PR](icons/git-pull-request.svg) [chat: allow skipping agent-host MCP startup](https://github.com/microsoft/vscode/pull/337779) `#337779`
**ステータス:** Merged | **ラベル:** なし

**概要:** MCP の起動が agent-host のターンをブロックしている間、インラインの **Skip** アクションで先に進めるようにする。対応する agent-host プロトコルのアクションと `blocking` 起動状態を導入し、Copilot SDK のセッション単位のバックグラウンド化を使う。受動的な一覧取得や有効化の照合では `mcp.list({ startServers: false })` を使い、ターン前に MCP サーバーを起動しない。公開済み SDK でもコンパイルできる互換キャストを入れたため SDK 更新はマージの前提ではなく、非起動の一覧取得は SDK と runtime の更新後に効く。

**直近のアクティビティ:** Copilot レビューは、MCP 有効化処理とキーボードフォーカスのアクセシビリティの不具合、SDK API が未公開である点を挙げて変更推奨とした。作者 connor4312 がレビューコメントで応じ、roblourens のレビューは dismissed、vritant24 が承認してマージされた。

**変更ファイル:** 27 ファイル、+1080/-56。プロトコルの actions、state、reducer、`copilotAgentSession.ts`（+43/-18）、`chatMcpServersStartingContentPart.ts`（+67/-10）と、テスト（`copilotAgentSession.test.ts` +461 など）である。

**次のアクション:** companion の github/copilot-agent-runtime#22835 と SDK 更新が入った後、非起動の一覧取得が機能するかを確認する。

---

### ![PR](icons/git-pull-request.svg) [sessions: feat: add Copilot canvas support](https://github.com/microsoft/vscode/pull/337780) `#337780`
**ステータス:** Open (Draft) | **ラベル:** なし

**概要:** Agents ウィンドウの保存済みチャットに、Copilot 限定の canvas 機能の最小の縦切りを加える。信頼済みの create/resume で Copilot SDK extensions と canvas レンダリングを有効にし、モデルが開いた canvas を最小限の AHP 契約で投影する。canvas のソースは該当するチャット、インスタンス、リビジョンに対してだけ解決し、Sessions 所有の復元しないエディタと、一覧に出ない一時的な BrowserView で表示する。プロトコル側の対応は microsoft/agent-host-protocol#472 である。

**直近のアクティビティ:** 作者 ulugbekna が Chess canvas の検証録画を複数投稿し、プロンプト送信後にクリックなしでモデルが `open_canvas` を呼び、canvas が自動表示されることを示した。4 コミットのレビュー順序も提示した。Copilot レビューは、外部ビューが通常どおり登録されている点と、ライフサイクル、秘匿、準備状態、アクセシビリティの上限設定の問題を挙げて変更推奨とした。

**変更ファイル:** 61 ファイル、+2624/-378。AHP プロトコル定義、`copilotAgentSession.ts`（+155/-2）、`copilotAgent.ts`（+82/-60）、新規の `copilotRuntimePaths.ts` などである。

**次のアクション:** Copilot の指摘に対応し、AHP 側の #472 と合わせてレビューを進める。

---

### ![PR](icons/git-pull-request.svg) [agentHost: allow native Codex skills and shared plugin resources](https://github.com/microsoft/vscode/pull/337724) `#337724`
**ステータス:** Merged | **ラベル:** なし

**概要:** #336394 の修正である。Auto-Review が native Codex skills を提示しながらそのファイルを読めず、同期したプラグインの skill が skill ディレクトリ外の共有リソースを参照できなかった。全ワークスペースルートで見つかった有効な native skill ディレクトリと、セッションで有効な同期プラグインパッケージに読み取り専用アクセスを与え、プラグイン無効化時は取り消す。パスを正規化し、ワークスペース権限で既にカバーされる場合は付与を省いて書き込み権限を格下げしないようにしている。

**直近のアクティビティ:** Copilot レビューは、セキュリティに関わるクロスプラットフォームのファイルシステム設定に Linux と Windows の直接的なユニットテストが必要と 1 件推奨した。dmitrivMS が承認してマージされた。

**変更ファイル:** 5 ファイル、+472/-17。`codexAgent.ts`（+49/-14）と、新規の統合テスト `codexSandbox.integrationTest.ts`（+382）などである。

**次のアクション:** ネイティブ sandbox の実行検証は macOS のみのため、Linux と Windows での確認が残る。

---

### ![PR](icons/git-pull-request.svg) [agentHost: resolve native OTLP trace endpoints](https://github.com/microsoft/vscode/pull/337783) `#337783`
**ステータス:** Merged | **ラベル:** なし

**概要:** #337413 のうち trace ルーティング部分に対応する。Agent Host は `http://localhost:4318` のような bare collector URL を native の trace 専用エンドポイントにそのままコピーしており、これはそのまま使われるため Copilot の native runtime が `/v1/traces` ではなく `/` に trace を送っていた。native HTTP trace 設定の構築で `resolveOtlpTracesEndpoint` を再利用する。修正前は `POST /` が 404、修正後は `POST /v1/traces` が 200 になることを 2 つの SDK で確認している。

**直近のアクティビティ:** Copilot レビューは承認推奨で、dmitrivMS が承認してマージされた。

**変更ファイル:** 4 ファイル、+73/-5。`agentHostOTelService.ts`、`OTEL.md` と統合テスト（+64/-1）である。

**次のアクション:** #337413 の managed telemetry 初期化と governed identity の部分は未解決のため、Issue は開いたままである。

---

### ![PR](icons/git-pull-request.svg) [fix(chat): preserve #file reference when editing text before it](https://github.com/microsoft/vscode/pull/333965) `#333965`
**ステータス:** Merged | **ラベル:** なし

**概要:** #333941 の修正である。インラインの `#file:` 参照の直前で Backspace を押したり単語を打ち直したりすると、参照そのものが削除されていた。`touchesReference` チェックで参照に重なる編集とその外側の編集を区別し、外側の編集では offset の差分から参照範囲を再計算する。参照に触れる編集では従来どおり削除する。

**直近のアクティビティ:** AshleyGrant が、数週間悩まされていたバグを直してくれたと作者に感謝するコメントを残した。justschen、dmitrivMS、chrmarti が 9 月中旬に承認済みである。

**変更ファイル:** 2 ファイル、+111/-2。`chatDynamicVariables.ts` の修正と `chatVariables.test.ts` への回帰テスト追加である。

**次のアクション:** マージ済みのため対応は不要である。

---

### ![PR](icons/git-pull-request.svg) [fix: memory leak in search editor](https://github.com/microsoft/vscode/pull/331014) `#331014`
**ステータス:** Open | **ラベル:** freeze-slow-crash-leak, search-editor

**概要:** Search editor の pane が各 input の設定リスナーを pane の生存期間中登録し、model factory が input 破棄後も解決済みのモデルを保持していたため、開閉を繰り返すと input、設定、結果のテキストモデルがリークしていた。設定リスナーをアクティブな input のスコープに限定し、`SearchEditorInput` の破棄時に model factory のエントリを削除する。

**直近のアクティビティ:** dmitrivMS がレビューコメントを付け、そのコメントを参照するよう CHANGES_REQUESTED とした。

**変更ファイル:** 4 ファイル、+55/-5。`searchEditor.ts`、`searchEditorInput.ts`、`searchEditorModel.ts` の修正とテストである。

**次のアクション:** dmitrivMS の指摘に対応する必要がある。

---

### ![PR](icons/git-pull-request.svg) [Bump version to 1.139.1](https://github.com/microsoft/vscode/pull/337813) `#337813`
**ステータス:** Open | **ラベル:** なし

**概要:** #337801 で追跡される 1.139.1 リカバリーリリースに向けたバージョン bump である。root の package と lockfile の 2 箇所を 1.139.1 にし、Copilot の `engines.vscode` を `^1.139.1` に更新する。#326222 と #334140 の前例に従っており、エンドゲーム計画全体ではなく bump だけを扱う。

**直近のアクティビティ:** Copilot レビューは承認推奨で、rwoll と rzhao271 が承認した。

**変更ファイル:** 4 ファイル、+5/-5。

**次のアクション:** マージし、CI 並列試行用の重複 PR #337830 と #337831 をクローズする。

---


## qmk/qmk_firmware

> 新規キーボード追加 PR 3 件に drashna がレビューや承認を付け、nk65_gen2 のキーマップ修正がマージされた。長期の XAP 実装 PR も更新され、core 変更 PR 2 件には stale 警告が出ている。

### ![PR](icons/git-pull-request.svg) [[Keyboard] Add support for JLW's Le Chiffre 36](https://github.com/qmk/qmk_firmware/pull/26447) `#26447`
**ステータス:** Open | **ラベル:** keyboard, keymap

**概要:** JLW のリポジトリにあった Le Chiffre 36 の定義を QMK 本体に取り込む PR である。`info.json` を `keyboard.json` に改名して `qmk migrate` を実行し、既定の `bootmap.matrix` を削除し、`features.encoder_map` と `auto_shift` を `default/rules.mk` に移した。

**直近のアクティビティ:** drashna がレビューコメントを投稿した（本文はデータに含まれない）。9 月 12 日と 14 日に drashna が CHANGES_REQUESTED を付け、作者は 19 日に対応のコメントを返している。

**変更ファイル:** 6 ファイル、+403/-0。`keyboard.json`、default キーマップ、`le_chiffre_36.c`、`readme.md` の新規追加である。

**次のアクション:** drashna の最新コメントへの対応と、CHANGES_REQUESTED の解消が必要である。

---

### ![PR](icons/git-pull-request.svg) [Add MyKeyClub MKC75](https://github.com/qmk/qmk_firmware/pull/26448) `#26448`
**ステータス:** Open | **ラベル:** keyboard, keymap

**概要:** MyKeyClub MKC75（75 % キーボード）のサポートを追加する PR である。ベンダーが提供していたコンパイル済みファームウェアと VIA 定義を、LLM の助けを借りて逆コンパイルと実機確認で解析し、QMK 上で再現した。物理的な RGB が無いため RGB ドライバは除き、ロータリーエンコーダ、複数の最下段配列、split left shift、split backspace、ISO enter、caps lock インジケータに対応する。

**直近のアクティビティ:** drashna が再度 APPROVED を付けた。9 月 14 日に zvecr が付けた CHANGES_REQUESTED は残っている。

**変更ファイル:** 4 ファイル、+454/-0。`keyboard.json`（+390）、default キーマップ、`readme.md` の新規追加である。

**次のアクション:** zvecr の CHANGES_REQUESTED の解消が必要である。VIA キーマップはマージ後に `the-via/qmk_userspace_via` へ別途提出される予定である。

---

### ![PR](icons/git-pull-request.svg) [Add MS Sculpt US keyboard by Chad Austin](https://github.com/qmk/qmk_firmware/pull/26476) `#26476`
**ステータス:** Open | **ラベル:** keyboard, keymap

**概要:** handwired の MS Sculpt US キーボードを追加する PR である。本文はテンプレートのままで、説明とチェックリストは記入されていない。

**直近のアクティビティ:** drashna が APPROVED を付けた。9 月 23 日に waffle87 が付けた CHANGES_REQUESTED は残っている。

**変更ファイル:** 4 ファイル、+224/-0。`keyboards/handwired/ms_sculpt_us/` 以下の `keyboard.json`、default キーマップ、`readme.md` の新規追加である。

**次のアクション:** waffle87 の CHANGES_REQUESTED の解消と、PR 本文の記入が必要である。

---

### ![PR](icons/git-pull-request.svg) [[WIP] XAP -- eXtensible Application Protocol](https://github.com/qmk/qmk_firmware/pull/13733) `#13733`
**ステータス:** Open (Draft) | **ラベル:** needs doc, in progress, core, keyboard, help wanted, keymap, needs testing, documentation, breaking_change, python, cli, feature, CI, no-ci, dd

**概要:** #11567 で提案された XAP（eXtensible Application Protocol）の実装を追跡する 2021 年からの WIP PR である。core、新機能、ドキュメントにまたがり、破壊的変更のラベルが付いている。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**変更ファイル:** 98 ファイル、+7830/-26。`builddefs/xap.mk`、XAP のスキーマ `xap.jsonschema`、各バージョンのプロトコル定義 `xap_0.x.x.hjson`、Python クライアントとドキュメントとファームウェア用コードの生成テンプレートの追加が中心である。

**次のアクション:** WIP のため、実装の継続とドキュメント整備、テスト追加を待つ。

---

### ![PR](icons/git-pull-request.svg) [Fix keymap on nk65_gen2](https://github.com/qmk/qmk_firmware/pull/26478) `#26478`
**ステータス:** Merged | **ラベル:** keymap

**概要:** NovelKeys NK65 gen2 の default キーマップを修正した。

**直近のアクティビティ:** drashna が APPROVED を付け、マージされた。前日に waffle87 も承認している。

**変更ファイル:** 1 ファイル、+4/-4。`nk65_gen2/keymaps/default/keymap.c` の修正である。

**次のアクション:** マージ済みのため対応は不要である。

---

### ![PR](icons/git-pull-request.svg) [Test upstream changes](https://github.com/qmk/qmk_firmware/pull/26479) `#26479`
**ステータス:** Closed | **ラベル:** keymap

**概要:** 本文はテンプレートのままである。GMMK Pro と Winry315 向けの個人キーマップと、コンパイル用シェルスクリプトを追加する内容で、作成から約 7 分でクローズされた。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**変更ファイル:** 14 ファイル、+699/-0。`paul.quine` という個人キーマップ 2 組と、リポジトリ直下のコンパイル用スクリプト 2 本である。

**次のアクション:** クローズ済みのため対応は不要である。

---

### ![PR](icons/git-pull-request.svg) [Handle ASCII in send_unicode_string with keycodes](https://github.com/qmk/qmk_firmware/pull/26272) `#26272`
**ステータス:** Open | **ラベル:** core, stale, breaking_changes_2026q4

**概要:** #25382 を修正する PR である。`send_unicode_string()` で、デコードした文字にキーコードの対応がある ASCII 文字は既存の send-string のキーコード経路で送出し、非 ASCII 文字は従来どおり設定された Unicode 入力モードを使う。

**直近のアクティビティ:** github-actions bot が、45 日間活動がないため stale とし、30 日後にクローズすると通知した。レビューは drashna が 2 回 APPROVED、tzarc が 6 月に CHANGES_REQUESTED を付けている。

**変更ファイル:** 4 ファイル、+39/-1。`send_string.c` と `send_string.h` へのヘルパー追加、`unicode.c` の分岐変更、`test_unicode.cpp` へのテスト追加である。

**次のアクション:** tzarc の CHANGES_REQUESTED への対応が必要である。breaking_changes_2026q4 の対象であり、stale でクローズされる前に進める必要がある。

---

### ![PR](icons/git-pull-request.svg) [Extend core API to enable mouse keymap community modules](https://github.com/qmk/qmk_firmware/pull/26232) `#26232`
**ステータス:** Open | **ラベル:** core

**概要:** `host_mouse_send()` に最後に渡されたボタンマスクを記録し、`host_last_mouse_buttons()` で参照できるようにする core の変更である。ボタン押下と移動が別経路で届くと移動レポートのボタンが 0 になり、ドラッグが途中で切れる問題を、Raw HID アダプタなどのコミュニティモジュールが回避できるようにする。

**直近のアクティビティ:** 直近 24 時間のコメントなし。8 月に drashna が APPROVED を付けている。

**変更ファイル:** 3 ファイル、+28/-0。`tmk_core/protocol/host.c` と `host.h` へのヘルパー追加と、`test_mousekeys.cpp` へのテスト追加である。

**次のアクション:** 追加のメンテナ承認とマージ判断を待つ。

---


# GitHub ダイジェスト — 2026-09-23

## 本日のハイライト

1. ![PR](icons/git-pull-request.svg) **[agent bakeoff](https://github.com/microsoft/vscode/pull/335498)** (microsoft/vscode) — 複数のエージェントとモデルを独立した worktree で並列実行し、Judge が diff を比較して推奨を示す機能である。レビューとコメントが 33 件、変更量は約 1.5 万行と直近 24 時間で最も大きく、P0 指摘への対応が続いている。
2. ![Issue](icons/issue-opened.svg) **[MCP HTTP redirects: allow/deny policy is not re-evaluated on the redirect destination](https://github.com/microsoft/vscode/issues/337194)** (microsoft/vscode) — MCP の HTTP transport がリダイレクト先で許可ポリシーを再評価せず、拒否対象サーバーとのセッション確立や独自ヘッダの別オリジン送信が起きるというセキュリティ報告である。
3. ![PR](icons/git-pull-request.svg) **[feat(redo)!: dot-repeat input payload of 'opfunc'/:omap](https://github.com/neovim/neovim/pull/42040)** (neovim/neovim) — dot-repeat の redo に `getchar`/`input` の入力を含める破壊的変更である。既存の dot-repeat 対応プラグインに適応を求める内容のため、プラグイン作者から強い反対が出ている。
4. ![PR](icons/git-pull-request.svg) **[Require GTK 4.18 and libadwaita 1.7](https://github.com/ghostty-org/ghostty/pull/14330)** (ghostty-org/ghostty) — v1.4 リリースを控え、GTK と libadwaita の最低要件を引き上げて古いバージョン向けコードを削除する約 1.2 万行の変更で、承認済みである。
5. ![PR](icons/git-pull-request.svg) **[Support package-scoped dependency constraints](https://github.com/astral-sh/uv/pull/21899)** (astral-sh/uv) — `constraint-dependencies` をパッケージやバージョン単位で指定できるようにする機能追加である。スコープ付き build constraints の #21900 などがこの PR の上に積まれている。

---

## sunnypilot/sunnypilot

> upstream の commaai/openpilot master を取り込む大規模同期 PR と、chestnut（外付け GPU）の電力上限を sunnylink から調整する機能追加 PR が進行中である。古いバグ Issue が 1 件クローズされた。

### ![PR](icons/git-pull-request.svg) [Sync: commaai/openpilot:master -> sunnypilot/sunnypilot:master](https://github.com/sunnypilot/sunnypilot/pull/2036) `#2036`
**ステータス:** Open | **ラベル:** なし

**概要:** commaai/openpilot の master（コミット 6080cc6）を sunnypilot の master に同期する PR である。opendbc、panda、tinygrad の各サブモジュールについても sunnypilot 側で対応する PR が作成されている。

**直近のアクティビティ:** github-actions bot の process replay レポートのみで、結果は 66 件 pass、差分 0、エラー 0 である。

**変更ファイル:** 304 ファイル、+16870/-10808。`common/qrcode.py` の拡張とテスト追加、mici UI への eSIM 設定画面 `esim_ui.py` の新規追加、upstream 側 `selfdrive/modeld/compile_modeld.py` の削除、sunnypilot 独自の `modeld_v2`（`compile_modeld.py`、`model_adapters.py`、`stock_dependencies.py`）の改修、モデルビルド用 workflow の更新が中心である。

**次のアクション:** 変更規模が大きいため、`modeld_v2` 周辺の upstream 追従部分とサブモジュール PR 3 件を合わせてレビューし、マージ順序を調整する必要がある。

---

### ![PR](icons/git-pull-request.svg) [modeld: chestnut power limit, adjustable via sunnylink](https://github.com/sunnypilot/sunnypilot/pull/2023) `#2023`
**ステータス:** Open | **ラベル:** なし

**概要:** 12V アクセサリ電源から給電する chestnut で、GPU の瞬間的な大電流により電圧が落ち込み USB リンクのリセットや `Device hang detected` が起きる問題への対策である。新しいパラメータ `ChestnutPowerLimit`（W 単位、既定値 0 = stock）を追加し、big model 転送の直前に SMU へ `PPSMC_MSG_SetPptLimit` を送る。SMU 呼び出しが失敗した場合は big model のロードを失敗扱いにして small model にフォールバックする。設定は sunnylink の Device ページからのみ行え、選択肢は Stock と 40〜100 W である。作者は 60 W や 80 W の上限でハングが解消し、推論速度も落ちないという実車での検証結果を示している。

**直近のアクティビティ:** 9 月 12 日に Discountchubbs が lint 修正などを求めて CHANGES_REQUESTED を付けていた。9 月 22 日に作者が再レビューを依頼し、指摘はすべて反映済みで CI も通っていると報告した。60 W 上限での 10 日分の追加走行データ（16 ドライブ、big model 稼働率 100 %、ハングなし）と、別ユーザーが 80 W 上限で同じ問題を独立に解決した事例も共有した。既定値を Stock のままにするか 100 W にするかの判断をメンテナに求めている。

**変更ファイル:** 6 ファイル、+203/-0。SMU を操作する `modeld_v2/chestnut_power_limit.py` の新規追加とそのユニットテスト、`modeld.py` の `load_big()` への 1 行のフック追加、sunnylink 設定 UI（YAML とコンパイル済み JSON）、`params_keys.h` へのパラメータ宣言である。

**次のアクション:** メンテナによる再レビューと、既定値（Stock か 100 W か）の決定が必要である。upstream の commaai/openpilot では #38992 で既定の GPU 電力上限を 100 W にする変更がマージされており、その扱いとの整合も確認すべきである。

---

### ![Issue](icons/issue-opened.svg) [Max Speed 5MPH Over Set Point](https://github.com/sunnypilot/sunnypilot/issues/1777) `#1777`
**ステータス:** Closed | **ラベル:** bug

**概要:** Rivian R1S で cruise から MADS に切り替え、再び cruise に戻すと、設定速度より 5 MPH 高い速度で走行するという報告である。バージョンは 2026.03.18-4342 で、再現ルートも添付されている。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**次のアクション:** クローズ済みのため対応は不要である。ただしクローズ理由はデータから読み取れず、同じ症状が再発した場合は再オープンを検討する。

---

## commaai/openpilot

> chestnut（外付け GPU）まわりの整備が活動の中心で、precompiled warp の同梱や GPU 電力上限 100 W 化がマージされ、実車で見つかったロード失敗やフォールバック不全のバグ Issue が 3 件起票された。process replay の参照ルート更新、opendbc と panda のサブモジュール更新、警告音の強化も並行して進んでいる。

### ![PR](icons/git-pull-request.svg) [modeld: precompiled context-9 action model at 5 Hz](https://github.com/commaai/openpilot/pull/38944) `#38944`
**ステータス:** Open (Draft) | **ラベル:** なし

**概要:** 本文は空である。変更内容から見ると、worldmodel 系のモデル（`worldmodel.py`、`worldmodeld.py`、`worldmodel_kernels.py`）と、そのコンパイル用スクリプト `compile_worldmodel.py` を追加し、タイトルのとおり precompiled の context-9 action model を 5 Hz で動かす PR である。

**直近のアクティビティ:** commaci-public による model replay の差分プロットと、github-actions による process replay レポート（66 件 pass、差分 0）のみである。

**変更ファイル:** 24 ファイル、+4164/-27。モデルのハイパーパラメータ `models/worldmodel/hparams.json`（+2821）、worldmodel の推論とカーネル実装、説明文書 `worldmodel.md`、テスト `test_worldmodel.py` を新規追加している。`log.capnp` と `services.py` へのメッセージ追加、`process_config.py` へのプロセス登録、`modeld.py` の改修、tinygrad サブモジュール更新も含む。

**次のアクション:** Draft のため、作者による本文の記述と Ready for review への移行を待つ。

---

### ![PR](icons/git-pull-request.svg) [add max critical alert sound](https://github.com/commaai/openpilot/pull/39006) `#39006`
**ステータス:** Merged | **ラベル:** ui

**概要:** 本文は空である。最も重大な警告用の音声ファイル `dm_critical_max.wav` を追加し、`soundd.py` でそれを再生できるようにした。クローズされた #38937 と同じ目的を、より小さな変更で実現したものである。

**直近のアクティビティ:** github-actions の process replay レポート（66 件 pass、差分 0）のみである。

**変更ファイル:** 2 ファイル、+16/-6。`soundd.py` の改修と新規音声ファイルの追加である。

**次のアクション:** マージ済みのため対応は不要である。

---

### ![PR](icons/git-pull-request.svg) [Red alert max sound](https://github.com/commaai/openpilot/pull/38937) `#38937`
**ステータス:** Closed | **ラベル:** なし

**概要:** comma four のスピーカーを物理的な上限まで鳴らす新しい音を追加し、最も深刻な警告で使う提案である。マージされずにクローズされた。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**変更ファイル:** 9 ファイル、+630/-23。音のエスカレーションのテスト `test_soundd_escalation.py`、音声プレビュー用ツール `tools/sound_preview/`、`critical_alert.py` の新規追加と `soundd.py` の改修を含む。

**次のアクション:** 同じ目的の #39006 がマージされたため対応は不要である。

---

### ![PR](icons/git-pull-request.svg) [fix AGNOS updater keyboard crash](https://github.com/commaai/openpilot/pull/39005) `#39005`
**ステータス:** Open (Draft) | **ラベル:** なし

**概要:** Issue #38993 で報告された、AGNOS updater で Wi-Fi パスワード入力時にクラッシュする問題の修正である。

**直近のアクティビティ:** github-actions の process replay レポート（66 件 pass、差分 0）のみである。

**変更ファイル:** 2 ファイル、+8/-4。`mici_keyboard.py` の修正と `common/hardware/comma/updater` の更新である。

**次のアクション:** Draft 解除後にレビューしてマージし、Issue #38993 にあるとおり新しい LFS updater バンドルを push する必要がある。

---

### ![PR](icons/git-pull-request.svg) [Split model compilation into ONNX and warp compilers](https://github.com/commaai/openpilot/pull/38863) `#38863`
**ステータス:** Closed | **ラベル:** なし

**概要:** 本文は空である。`compile_modeld.py` を削除し、モデルのコンパイルを `compile_onnx.py` と `compile_warp.py` の 2 つに分割する内容であった。マージされずにクローズされた。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**変更ファイル:** 11 ファイル、+500/-607。`compile_modeld.py`、`compile_dm_warp.py`、`get_model_metadata.py` を削除し、新しいコンパイラ 2 つを追加、`SConscript`、`helpers.py`、`dmonitoringmodeld.py` を改修している。

**次のアクション:** クローズ済みのため対応は不要である。

---

### ![PR](icons/git-pull-request.svg) [msgq: let SCons own the native build](https://github.com/commaai/openpilot/pull/38919) `#38919`
**ステータス:** Closed | **ラベル:** なし

**概要:** `scons` でビルドした後に `uv sync --reinstall-package msgq-ipc` を実行すると、`visionipc_pyx` の import で `undefined symbol: _Z10cloudlog_eiPKciS0_S0_z` の `ImportError` が起きるという壊れやすいビルドの再現手順を示し、msgq のネイティブビルドを SCons に任せる変更であった。Draft のままマージされずにクローズされた。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**変更ファイル:** 2 ファイル、+6/-19。`pyproject.toml` と `uv.lock` の変更である。

**次のアクション:** クローズ済みのため対応は不要である。

---

### ![PR](icons/git-pull-request.svg) [fix eliding](https://github.com/commaai/openpilot/pull/38682) `#38682`
**ステータス:** Closed | **ラベル:** ui, stale

**概要:** 本文は空である。UI のラベルウィジェットで長いテキストを省略表示する処理を改善する Draft であった。

**直近のアクティビティ:** github-actions bot が非アクティブを理由に自動クローズした。

**変更ファイル:** 1 ファイル、+45/-67。`system/ui/widgets/label.py` の改修である。

**次のアクション:** 作業を再開する場合は再オープンする。

---

### ![PR](icons/git-pull-request.svg) [process replay: new Y route](https://github.com/commaai/openpilot/pull/39000) `#39000`
**ステータス:** Merged | **ラベル:** CI / testing

**概要:** 本文は空である。process replay で使う Tesla Model Y のテストルートを新しいものに差し替える。#38998 で同じ変更をマージした後に #38999 で revert しており、その再投入にあたる。

**直近のアクティビティ:** github-actions の process replay レポートは 63 件 pass、3 件エラーであった。エラーは新しい TESLA ルートの参照データを読み込めなかったことによる。

**変更ファイル:** 1 ファイル、+2/-2。`test_processes.py` のルート指定の変更である。

**次のアクション:** 新ルートの参照データは #39004 で自動 push されるようになったため、以後の CI で TESLA のエラーが解消しているか確認する。

---

### ![PR](icons/git-pull-request.svg) [process replay: push new refs](https://github.com/commaai/openpilot/pull/39004) `#39004`
**ステータス:** Merged | **ラベル:** CI / testing

**概要:** process replay の参照データ（refs）が新たに追加されたとき、自動で push するようにした。

**直近のアクティビティ:** github-actions の process replay レポート（66 件 pass、差分 0）のみである。

**変更ファイル:** 1 ファイル、+9/-1。`test_processes.py` の変更である。

**次のアクション:** マージ済みのため対応は不要である。

---

### ![Issue](icons/issue-opened.svg) [chestnut: unplugging during big model load hangs modeld, UI stuck on Big Model Loading](https://github.com/commaai/openpilot/issues/39003) `#39003`
**ステータス:** Open | **ラベル:** bug

**概要:** big model のロード中に chestnut を抜くと、USB ホストコントローラが停止し（"HC died"）、modeld が CPU 100 % のまま生き続け、`modelV2` を publish せず `ChestnutLoading` もクリアしない。UI は small model にフォールバックせず "Big Model Loading" のまま止まる。libusb がエラーを返さないため、tinygrad の `bulk_wait` のループで止まっている可能性がある。60 秒の `loader.join(BIG_MODEL_TIMEOUT)` が効かなかった理由は未解明で、次回はスタックの取得が必要とされている。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**次のアクション:** 再現時にスタックを取得し、`bulk_wait` にタイムアウトを設けるか、タイムアウト後に確実に small model へ切り替わるよう修正する必要がある。

---

### ![Issue](icons/issue-opened.svg) [chestnut: cold-cache big model load stalls controlsd startup, cruise fault on Tesla](https://github.com/commaai/openpilot/issues/39001) `#39001`
**ステータス:** Open | **ラベル:** bug

**概要:** big model のローダは 773 MB の pickle を mmap して GPU にコピーするため、ページキャッシュが冷えた状態では約 12 秒のメモリ回収が発生する。その間に controlsd の起動が 7.2 秒から 20.0 秒に遅れ、selfdrived の 6 秒の初期化タイムアウトを超過した。その結果 panda が純正の DAS_control をブロックし、Tesla Model Y で `DI_cruiseState` がフォルトした。キャッシュ済みの状態では問題は起きない。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**次のアクション:** 本文には、ファイル全体を mmap せずに固定バッファへ分割して読み込む修正案が Claude の提案として記載されている（任意扱い）。対応方針の決定が必要である。

---

### ![Issue](icons/issue-opened.svg) [chestnut: losing the USB device while driving kills modeld, no small model fallback](https://github.com/commaai/openpilot/issues/39002) `#39002`
**ステータス:** Open | **ラベル:** bug

**概要:** 走行中（engaged）に chestnut への bulk IN が I/O エラーを返し、small model へのフォールバックも失敗して modeld がクラッシュした。原因は 3 つある。tinygrad のホストデバイスが停止した AMD デバイスを pending map に保持していた。`put_bool("ChestnutActive", False)` が非同期書き込みのため、次のループで古い値が読まれた。manager がクラッシュしたプロセスを再起動しない。このため、警告では small model が利用可能と表示されているのに、ドライブの残りは "Process Not Running" になった。車載時にはポートのリンクエラーが毎秒約 80 回発生しており、この取り付け環境のリンク品質が引き金とされている。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**次のアクション:** 本文の修正案（Claude の提案、任意扱い）は、フォールバック時に停止デバイスを tinygrad の pending map から除き、再 raise をローカルフラグで制御することである。リンクエラー率の警告の追加も提案されている。

---

### ![PR](icons/git-pull-request.svg) [Revert "process replay: new Y route"](https://github.com/commaai/openpilot/pull/38999) `#38999`
**ステータス:** Merged | **ラベル:** CI / testing

**概要:** #38998 で行った Tesla Model Y のテストルート差し替えを revert する。

**直近のアクティビティ:** github-actions の process replay レポート（66 件 pass、差分 0）のみである。

**変更ファイル:** 1 ファイル、+2/-2。`test_processes.py` の変更である。

**次のアクション:** 対応は不要である。同じ変更は #39000 で再投入された。

---

### ![PR](icons/git-pull-request.svg) [process replay: new Y route](https://github.com/commaai/openpilot/pull/38998) `#38998`
**ステータス:** Merged | **ラベル:** CI / testing

**概要:** 本文は空である。process replay の Tesla Model Y のテストルートを差し替える最初の PR である。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**変更ファイル:** 1 ファイル、+2/-2。`test_processes.py` の変更である。

**次のアクション:** #38999 で revert され、#39000 で再投入済みのため対応は不要である。

---

### ![PR](icons/git-pull-request.svg) [Tesla: auto-detect 3-bit steering control type](https://github.com/commaai/openpilot/pull/38997) `#38997`
**ステータス:** Merged | **ラベル:** car

**概要:** 本文は空である。opendbc サブモジュールを更新し、Tesla の 3-bit ステアリング制御方式を自動判別できるようにした。

**直近のアクティビティ:** github-actions の process replay レポートは 2 件の差分を示した。RIVIAN では `carParams.alphaLongitudinalAvailable`、TESLA では `safetyParam` や `steeringAngleDeg` などの出力に差分が出ている。

**変更ファイル:** 1 ファイル、+1/-1。`opendbc_repo` サブモジュールの更新である。

**次のアクション:** マージ済みのため対応は不要である。Tesla の実車でステアリング挙動に問題がないかを見守る。

---

### ![PR](icons/git-pull-request.svg) [Bump opendbc and panda for Rivian alpha longitudinal](https://github.com/commaai/openpilot/pull/38995) `#38995`
**ステータス:** Merged | **ラベル:** car

**概要:** 本文は空である。opendbc を更新し、Rivian で alpha longitudinal（実験的な縦制御）を利用可能にした。

**直近のアクティビティ:** github-actions の process replay レポートは RIVIAN の `carParams.alphaLongitudinalAvailable` に 1 件の差分を示した。

**変更ファイル:** 1 ファイル、+1/-1。`opendbc_repo` サブモジュールの更新である。

**次のアクション:** マージ済みのため対応は不要である。

---

### ![PR](icons/git-pull-request.svg) [Speed up LFS downloads with git-xet when available](https://github.com/commaai/openpilot/pull/38994) `#38994`
**ステータス:** Open (Draft) | **ラベル:** なし

**概要:** 本文は空である。タイトルと変更内容によれば、git-xet が利用可能な環境では LFS のダウンロードに git-xet を使って高速化する。

**直近のアクティビティ:** github-actions の process replay レポート（66 件 pass、差分 0）のみである。

**変更ファイル:** 4 ファイル、+53/-1。セットアップスクリプト `tools/release/setup_git_xet.sh` の新規追加と、`tests.yaml`、`release.yaml`、`tools/op.sh` での呼び出し追加である。

**次のアクション:** Draft のため、作者による Ready for review への移行を待つ。

---

### ![PR](icons/git-pull-request.svg) [bump opendbc and panda](https://github.com/commaai/openpilot/pull/38990) `#38990`
**ステータス:** Closed | **ラベル:** car

**概要:** panda#2458 と opendbc#3759 の後、opendbc と panda のバージョンがずれていたため、両者を揃えて更新する PR であった。

**直近のアクティビティ:** sshane が、破壊的なリファクタリングと safety tick の修正をマージしたとコメントした。process replay レポートでは HYUNDAI、HONDA、TESLA など 12 件に `carParams.flags` やトルク関連パラメータの差分が出た。

**変更ファイル:** 2 ファイル、+2/-2。`opendbc_repo` と `panda` のサブモジュール更新である。

**次のアクション:** #38996 でサブモジュールが更新されたため対応は不要である。

---

### ![PR](icons/git-pull-request.svg) [Bump submodules](https://github.com/commaai/openpilot/pull/38996) `#38996`
**ステータス:** Merged | **ラベル:** なし

**概要:** 本文は空である。opendbc と panda のサブモジュールを更新した。

**直近のアクティビティ:** sshane が、flags の変化は opendbc#3788、Honda のトルク変化は opendbc#3789 によるものと説明した。process replay レポートでは HYUNDAI、HONDA、GM、FORD など 11 件に差分が出た。

**変更ファイル:** 2 ファイル、+2/-2。`opendbc_repo` と `panda` のサブモジュール更新である。

**次のアクション:** マージ済みのため対応は不要である。

---

### ![PR](icons/git-pull-request.svg) [locationd: buffer message validity before calling the inputs bad](https://github.com/commaai/openpilot/pull/38929) `#38929`
**ステータス:** Open | **ラベル:** なし

**概要:** locationd の `inputs_valid` のうち、メッセージの valid フラグ側には閾値も減衰もないため、20 Hz のストリームで 1 メッセージが invalid になるだけで `inputsOK` が落ち、"TAKE CONTROL IMMEDIATELY" の警告が 2 秒間出てしまう。この PR では、不正なサイクルで 1 加算し、正常なサイクルで 0.5 減らし、3 に達したら入力不正とみなすカウンタ方式に変更する。実走行ログの再生で、起動時の正当な不正判定は維持され、誤警告だけが消えることを確認している。代償として、持続的な故障の検出が 100 ms 遅れる。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**変更ファイル:** 1 ファイル、+5/-1。`selfdrive/locationd/locationd.py` の変更である。

**次のアクション:** メンテナによるレビューが必要である。同じ条件のカウンタ側を扱う #38923 との関係も確認すべきである。

---

### ![Issue](icons/issue-opened.svg) [AGNOS updater crash when typing wifi password](https://github.com/commaai/openpilot/issues/38993) `#38993`
**ステータス:** Open | **ラベル:** bug

**概要:** AGNOS updater で Wi-Fi パスワードを入力すると `TypeError: DrawCircleGradient expected 4 arguments, got 5` でクラッシュする。修正に加えて、新しい LFS updater バンドルの push が必要とされている。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**次のアクション:** 修正 PR #39005（Draft）のマージと updater バンドルの更新を待つ。

---

### ![PR](icons/git-pull-request.svg) [Ship precompiled Chestnut camera warps](https://github.com/commaai/openpilot/pull/38991) `#38991`
**ステータス:** Merged | **ラベル:** なし

**概要:** 本文は空である。chestnut 用のカメラ warp（1344x760 と 1928x1208）をビルド時に生成するのをやめ、precompiled の pkl として LFS で同梱するようにした。

**直近のアクティビティ:** commaci-public の model replay プロットと、github-actions の process replay レポート（66 件 pass、差分 0）のみである。

**変更ファイル:** 9 ファイル、+22/-40。`modeld/SConscript` の生成処理の削除、warp の pkl 2 件の追加、`release_files.py`、`build_release.sh`、`setup_lfs.sh`、`.gitattributes` の調整である。

**次のアクション:** マージ済みのため対応は不要である。

---

### ![PR](icons/git-pull-request.svg) [Chestnut: default GPU power limit to 100W](https://github.com/commaai/openpilot/pull/38992) `#38992`
**ステータス:** Merged | **ラベル:** なし

**概要:** 本文は空である。タイトルのとおり、chestnut の GPU 電力上限の既定値を 100 W に設定した。

**直近のアクティビティ:** commaci-public の model replay プロットと、github-actions の process replay レポート（66 件 pass、差分 0）のみである。

**変更ファイル:** 1 ファイル、+1/-0。`selfdrive/modeld/modeld.py` への 1 行追加である。

**次のアクション:** マージ済みのため対応は不要である。

---

### ![PR](icons/git-pull-request.svg) [pandad: log NMI and hardfault resets](https://github.com/commaai/openpilot/pull/38737) `#38737`
**ステータス:** Open | **ラベル:** なし

**概要:** 本文は空である。panda の NMI や hardfault によるリセットを pandad でログに記録する。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**変更ファイル:** 3 ファイル、+6/-1。`log.capnp` へのフィールド追加、`pandad.cc` の記録処理、`panda` サブモジュールの更新である。

**次のアクション:** メンテナによるレビューが必要である。

---

### ![Issue](icons/issue-opened.svg) [LKAS dash faults and no bootkick: STM brown-out?](https://github.com/commaai/openpilot/issues/38488) `#38488`
**ステータス:** Closed | **ラベル:** needs testing, hardware

**概要:** 数日放置した RAV4 でカメラ関連のダッシュエラーが出て、comma four が bootkick しなかったという報告である。電源不良で STM が brown-out 状態になり、リレーを駆動して CAN バスを開いた可能性が疑われていた。プログラマブル電源で電流と電圧を掃引して再現を試みる案が挙がっていた。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**次のアクション:** クローズ済みのため対応は不要である。

---

## commaai/opendbc

> Tesla のステアリング制御検出をめぐる変更の差し戻しと再設計が進み、Rivian のアルファ縦制御有効化や Subaru の横制御対応など車種サポートの拡張も続いている。一方で古いコミュニティ車種移植 PR が stale bot により 2 件クローズされた。

### ![PR](icons/git-pull-request.svg) [HKG: Car Port for Genesis GV80 2021-22 (Middle East only)](https://github.com/commaai/opendbc/pull/1136) `#1136`
**ステータス:** Closed | **ラベル:** CI / testing, DBC signals, car, body, chrysler, ford, gm, honda, hyundai, mazda, nissan, subaru, tesla, toyota, volkswagen, stale, rivian, car safety

**概要:** 中東向け Genesis GV80 2021-22 (CAN-FD) の車種移植 PR である。ハーネスは Hyundai M を改造し CAN バスのペアを手作業で入れ替えて使用しており、stock と openpilot 双方のルートが提示されている。opendbc Issue #1297 に依存し、openpilot#30145 を置き換えるものであった。

**直近のアクティビティ:** github-actions bot が非アクティブを理由に自動クローズした。

**変更ファイル:** 234 ファイル、+12220/-509。ブランチが master から大きく乖離しており、Hyundai 以外に Honda、Chrysler、Tesla、Toyota、GM、Rivian など多数の車種ディレクトリや CI 設定、`opendbc/dbc/generator` の差分が含まれている。

**次のアクション:** 対応不要。移植を続ける場合は作者が最新 master にリベースして再オープンする必要がある。

---

### ![PR](icons/git-pull-request.svg) [Hyundai: Car Port for Creta (Non-US only) 2024](https://github.com/commaai/opendbc/pull/1139) `#1139`
**ステータス:** Closed | **ラベル:** car, hyundai, stale

**概要:** 米国外向け 2024 Hyundai Creta の車種移植 PR である。stock システムのルートと Harness A の使用は記載されているが、テストルートの追加と openpilot でのルートはチェックリスト上未完了であった。openpilot#32933 を置き換えるものである。

**直近のアクティビティ:** github-actions bot が非アクティブを理由に自動クローズした。

**変更ファイル:** 5 ファイル、+17/-2。`opendbc/car/hyundai/fingerprints.py` と `values.py` への車種追加、`docs/CARS.md` の更新、`torque_data/override.toml` への設定追加が中心である。

**次のアクション:** 対応不要。再開する場合は openpilot でのテストルートを揃えて再オープンする必要がある。

---

### ![PR](icons/git-pull-request.svg) [2025 Subaru Crosstrek Lateral Support](https://github.com/commaai/opendbc/pull/3795) `#3795`
**ステータス:** Open (Draft) | **ラベル:** car, subaru, car safety

**概要:** 角度ベースのステアリングを採用する Subaru 車 (2024+ Crosstrek、2023+ Ascent、2022+ Outback、2025 Impreza) に角度ステアリング制御を追加する。#2864 の Jacob Waller 氏の作業が土台である。停車後も `ES_Brake` が高いままになるため、クルーズ係合判定に `ES_Status` を使い (ハイブリッドは `ES_Brake` を維持)、4 m/s 未満ではステアリング振動を抑えるデッドバンドを入れている。

**直近のアクティビティ:** Car behavior report bot が 79 セグメント中 10 件で差分を報告した。差分は SUBARU_FORESTER_2022 の `carParams.safetyConfigs` で `safetyParam` が 0 から 8 に変わるものである。

**変更ファイル:** 11 ファイル、+393/-39。`opendbc/car/subaru/carcontroller.py` の制御ロジックと `opendbc/safety/modes/subaru.h` の安全モード拡張が主で、`test_subaru.py` (car と safety の両方) にテストを追加している。

**次のアクション:** TODO として LKAS 失敗の調査、インジェクションテスト、横制御マニューバレポート、デッドバンド値の調整が残っている。Forester 2022 の `safetyParam` 変化が意図どおりかの確認も必要である。

---

### ![PR](icons/git-pull-request.svg) [Tesla: detect new 3-bit steering control enum](https://github.com/commaai/opendbc/pull/3796) `#3796`
**ステータス:** Open (Draft) | **ラベル:** DBC signals, car, tesla, car safety

**概要:** Tesla の新しい 3 ビットのステアリング制御 enum を検出する変更である。本文は空である。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**変更ファイル:** 8 ファイル、+421/-136。`opendbc/dbc/tesla_model3_party.dbc` に大量のシグナル定義 (+340) を追加し、`carstate.py`、`interface.py`、`teslacan.py`、`values.py` と安全モード `opendbc/safety/modes/tesla.h` を簡素化している。

**次のアクション:** 同種の変更 (#3802, #3798) が #3806 で差し戻されたため、本 Draft がその再設計として進むか注視する。

---

### ![PR](icons/git-pull-request.svg) [Tesla: new model Y route](https://github.com/commaai/opendbc/pull/3807) `#3807`
**ステータス:** Merged | **ラベル:** car

**概要:** Tesla Model Y のテストルートを新しいものに差し替える。本文は空である。

**直近のアクティビティ:** Car behavior report bot は差分なしと報告した。

**変更ファイル:** 1 ファイル、+5/-4。`opendbc/car/tests/routes.py` のルート定義を更新している。

**次のアクション:** 対応不要。

---

### ![PR](icons/git-pull-request.svg) [Revert Tesla steering detection and firmware updates (#3802, #3798)](https://github.com/commaai/opendbc/pull/3806) `#3806`
**ステータス:** Merged | **ラベル:** DBC signals, car, tesla, car safety

**概要:** Tesla のステアリング検出とファームウェア更新を扱った #3802 と #3798 を差し戻す。本文は空である。

**直近のアクティビティ:** Car behavior report bot が TESLA_MODEL_3 と TESLA_MODEL_Y の 20 セグメントすべてで差分を報告した。差分は `carParams.dashcamOnly` である。

**変更ファイル:** 9 ファイル、+131/-73。`opendbc/car/tesla/` 配下の `carstate.py`、`fingerprints.py`、`interface.py`、`values.py` などと、`tesla_model3_party.dbc`、安全モード `tesla.h` およびそのテストを元に戻している。

**次のアクション:** Tesla ユーザーは `dashcamOnly` の扱いが変わっていないかを確認する。#3796 など後続の再実装を追う。

---

### ![PR](icons/git-pull-request.svg) [Rivian Gen 1: enable alpha longitudinal](https://github.com/commaai/opendbc/pull/3805) `#3805`
**ステータス:** Merged | **ラベル:** car, rivian

**概要:** Rivian Gen 1 でアルファ版の縦制御 (longitudinal) を有効化する。本文は空である。

**直近のアクティビティ:** Car behavior report bot が RIVIAN_R1 の 10 セグメントすべてで `carParams.alphaLongitudinalAvailable` の差分を報告した。

**変更ファイル:** 1 ファイル、+3/-3。`opendbc/car/rivian/interface.py` のフラグを変更している。

**次のアクション:** 対応不要。Rivian R1 ユーザーはアルファ縦制御が選択可能になる。

---

## commaai/panda

> STM32H7 ファームウェアの堅牢化を目的とした小規模な修正が 2 件マージされた。マイク出力バッファの初期化と、NMI やハードフォルトの検知および報告である。

### ![PR](icons/git-pull-request.svg) [mic: clear output buffers before startup and after idle](https://github.com/commaai/panda/pull/2455) `#2455`
**ステータス:** Merged | **ラベル:** なし

**概要:** マイクの出力バッファは起動時にクリアされる BSS の外側 (`.sram4 (NOLOAD)`) に置かれているため、出力 DMA が古いメモリ内容を送出してしまう問題を修正する。出力 DMA 有効化の前と、マイクがアイドルになった時点の両方で出力バッファを `memset` でゼロクリアする。#2454 から起動時の不具合だけを切り出したもので、クロック整合やゲイン、フィルタ、繰り返しブロックによるクリック音 (#2456 で別対応) には手を入れていない。

**直近のアクティビティ:** robbederks が「lgtm」とコメントし、マージされた。

**変更ファイル:** 1 ファイル、+4/-0。`board/stm32h7/sound.h` に `memset` 呼び出し 2 行とコメントを追加している。

**次のアクション:** 対応不要。本ブランチ単体ではデバイスへの再書き込みによる検証が未実施と記載されているため、openpilot 側統合 (commaai/openpilot#38984) での実機確認を見守る。

---

### ![PR](icons/git-pull-request.svg) [Handle and report NMI/hardfaults](https://github.com/commaai/panda/pull/2424) `#2424`
**ステータス:** Merged | **ラベル:** なし

**概要:** NMI とハードフォルトをハンドリングし、その発生を報告できるようにする。openpilot の Issue #38488 への対応として作成され、#2425 を土台としている。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**変更ファイル:** 8 ファイル、+35/-0。`board/early_init.h` にハンドラ処理を追加し、`board/health.h` や `board/main.c`、`board/main_comms.h` などで報告経路を整備している。リンカスクリプト `stm32h7x5_flash.ld` と `python/__init__.py` にも小さな追加がある。

**次のアクション:** 対応不要。マージ済みのため、元の openpilot Issue #38488 が解消されるかを確認する。

---

## neovim/neovim

> multicursor 周りの不具合報告と挙動修正が集中しており、dot-repeat の入力再生を変える破壊的変更 PR に対してプラグイン作者から強い反発が出ている。あわせて modeline やオプション設定の trust 制御を巡るセキュリティ修正と、AI 生成 PR のブロックが目立つ。

### ![PR](icons/git-pull-request.svg) [fix(options): reject restricted assignments before parsing callbacks](https://github.com/neovim/neovim/pull/42038) `#42038`
**ステータス:** Open | **ラベル:** options

**概要:** 制限付きのオプション代入が、変更を拒否する前にコールバックを解析してしまう問題を修正する PR である。modeline から起動された Syntax ハンドラが、変更できないオプションであっても bound arguments を評価できていた。726d1a92d2 でコールバック構築が権限チェックより前に移動したのが原因で、権限チェックを先に行うよう戻す。併せて `optset_T` のバッファ、ウィンドウポインタを型付きにし、冗長な引数を削除するリファクタリングも含む。

**直近のアクティビティ:** justinmk が Approve した。コメントはない。

**変更ファイル:** 6 ファイル、+107/-77。`src/nvim/option.c` と `src/nvim/optionstr.c` の権限チェック順序の変更が中心で、`test/functional/options/options_spec.lua` に回帰テストを追加している。

**次のアクション:** Approve 済みのためマージ待ちである。modeline 経由のセキュリティ修正として追跡する価値がある。

---

### ![Issue](icons/issue-opened.svg) [treesitter: support range-aware direct-child iteration for TSNode](https://github.com/neovim/neovim/issues/42046) `#42046`
**ステータス:** Open | **ラベル:** treesitter

**概要:** `TSNode:iter_children()` が常に先頭の子から走査するため、表示範囲の子だけを処理したいプラグインでも範囲より前の子をすべて辿る必要がある、という要望である。Tree-sitter 本体には `ts_node_first_child_for_byte()` や `ts_tree_cursor_goto_first_child_for_point()` があるが Lua API に公開されていない。報告者の concealer プロトタイプでは、5000 行のフラットな文書で 21.42 ms が 0.40 ms に短縮したという。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**次のアクション:** メンテナによるトリアージと、公開する API の形（位置指定付き `iter_children()` 等）の検討が必要である。

---

### ![PR](icons/git-pull-request.svg) [vim-patch:9.1.0000,9.2.0000](https://github.com/neovim/neovim/pull/42045) `#42045`
**ステータス:** Open | **ラベル:** test, vim-patch

**概要:** Vim の 9.1.0000 と 9.2.0000 パッチを取り込む PR である。`vimpatch.lua` がメジャーバージョンごとにパッチ `0` を追加する必要があり、テストを通すため #42019 の変更を引き継いでいる。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**変更ファイル:** 2 ファイル、+31/-39。`src/nvim/version.c` のパッチ一覧更新と `test/old/testdir/test_expr.vim` の修正である。

**次のアクション:** レビュー待ちである。#42019 との関係整理が必要である。

---

### ![PR](icons/git-pull-request.svg) [build(vim-patch): misc n/a patches for runtime/](https://github.com/neovim/neovim/pull/42044) `#42044`
**ステータス:** Open (Draft) | **ラベル:** なし

**概要:** runtime/ 配下の Neovim に該当しない Vim パッチを n/a として扱うための設定追加である。本文はない。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**変更ファイル:** 2 ファイル、+2/-0。`scripts/vim-patch.sh` と `scripts/vim_na_hunks_help.txt` に 1 行ずつ追加している。

**次のアクション:** Draft 解除を待つ。

---

### ![PR](icons/git-pull-request.svg) [feat(editing): gF jumps to tag after file name](https://github.com/neovim/neovim/pull/42043) `#42043`
**ステータス:** Open | **ラベル:** treesitter

**概要:** `gF` が `{fname}:{lnum}` に加えて `{fname}#{tag}` を受け付け、開いたバッファ内のタグへジャンプするようにする PR である（#30010 を close）。位置解決は ctags、LSP document symbols、Treesitter の見出し（Markdown/vimdoc、GitHub slug 対応）、help タグ、単語検索の順に試す。C 側はファイル名抽出のみを行い、解析とジャンプは `vim._core.tag` の Lua 実装に委ねる。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**変更ファイル:** 12 ファイル、+536/-50。`runtime/lua/vim/_core/tag.lua` の追加実装（+237）と `runtime/lua/vim/treesitter/_headings.lua` の拡張が主で、`src/nvim/file_search.c` などの C 側改修と `test/old/testdir/test_gf.vim` のテスト追加を含む。

**次のアクション:** 機能追加としてメンテナのレビューが必要である。

---

### ![PR](icons/git-pull-request.svg) [docs: update version.c](https://github.com/neovim/neovim/pull/42019) `#42019`
**ステータス:** Open (Draft) | **ラベル:** vim-patch

**概要:** CI bot による `version.c` の Vim パッチ一覧の自動更新 PR である。本文はない。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**変更ファイル:** 1 ファイル、+23/-33。`src/nvim/version.c` のみである。

**次のアクション:** #42045 がこの変更を引き継いでいるため、そちらの進捗に合わせて扱いを決める。

---

### ![PR](icons/git-pull-request.svg) [build(vim-patch): n/a patches from vim_na_regexp.txt](https://github.com/neovim/neovim/pull/42017) `#42017`
**ステータス:** Merged | **ラベル:** build, vim-patch

**概要:** `vim_na_regexp.txt` 由来の Neovim に該当しない Vim パッチを n/a 扱いにする設定変更である。本文はない。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**変更ファイル:** 2 ファイル、+8/-7。`scripts/vim-patch.sh` と `scripts/vim_na_hunks_help.txt` の更新である。

**次のアクション:** マージ済みのため対応不要である。

---

### ![PR](icons/git-pull-request.svg) [fix(filetype): default .h files to C](https://github.com/neovim/neovim/pull/42028) `#42028`
**ステータス:** Open | **ラベル:** filetype

**概要:** `.h` ファイルが C++ と判定される問題を、Vim の現行の判定ロジックを移植して修正する PR である（#41990 を close）。先頭 100 行に C++ や Objective-C を示す構文がなければ C と判定する。既存の `g:c_syntax_for_h` と `g:ch_syntax_for_h` は維持し、明示的な上書き用に `g:filetype_h` を追加する。

**直近のアクティビティ:** zeertzjq が複数回レビューコメントを付け、作者が応答した。作者はコミットメッセージに `vim-patch` を入れて commit lint の失敗を解消したと報告している。

**変更ファイル:** 4 ファイル、+62/-13。`runtime/lua/vim/filetype/detect.lua` の判定ロジック変更が中心で、ドキュメントと `test/old/testdir/test_filetype.vim` のテストを更新している。

**次のアクション:** zeertzjq のレビュー指摘への対応を確認し、再レビューを受ける。

---

### ![PR](icons/git-pull-request.svg) [docs(lua): note utf-8 idempotency of vim.str_byteindex](https://github.com/neovim/neovim/pull/41397) `#41397`
**ステータス:** Closed | **ラベル:** needs:response, lua, typo, AI assisted 🤖

**概要:** `vim.str_byteindex(s, "utf-8", n)` がバイトインデックスをそのまま返すことを文書化し、コードポイントからの変換には `"utf-32"` を使うと明記する PR であった（#35076 を修正対象とする）。

**直近のアクティビティ:** 情報提供の依頼に 30 日間応答がなかったため、bot が自動で close した。

**変更ファイル:** 2 ファイル、+10/-0。`runtime/doc/lua.txt` と `runtime/lua/vim/_core/editor.lua` へのドキュメント追記である。

**次のアクション:** 作者が要求された情報を提供すれば再オープンできる。それ以外の対応は不要である。

---

### ![PR](icons/git-pull-request.svg) [docs: document EOF filler lines in diff_filler()](https://github.com/neovim/neovim/pull/42041) `#42041`
**ステータス:** Closed | **ラベル:** documentation

**概要:** `diff_filler()` のドキュメントに、ファイル末尾の filler 行の扱いを追記する PR であった（#41256 を close 対象とする）。

**直近のアクティビティ:** zeertzjq が、#41256 は close し忘れていただけで追加のドキュメントは不要と説明し、PR は close された。作者は謝罪のコメントを残している。

**変更ファイル:** 1 ファイル、+6/-0。`runtime/doc/vimfn.txt` への追記である。

**次のアクション:** 対応不要である。

---

### ![Issue](icons/issue-opened.svg) [diff_filler(line("$")+1) gives the number of EOF Filler lines, but is undocumented](https://github.com/neovim/neovim/issues/41256) `#41256`
**ステータス:** Closed | **ラベル:** bug-vim, diff

**概要:** `diff_filler(line("$")+1)` でファイル末尾の filler 行数を取得できるが、その挙動が文書化されていないという報告である。報告者はドキュメントに特例として追記する文案を提示していた。

**直近のアクティビティ:** zeertzjq が #41283 で修正済みとして close した。

**次のアクション:** 対応不要である。

---

### ![PR](icons/git-pull-request.svg) [fix(filetype): using incorrect set of word characters](https://github.com/neovim/neovim/pull/42042) `#42042`
**ステータス:** Open | **ラベル:** filetype

**概要:** Lua パターンの `%w` は Vim パターンの `\w` と異なりアンダースコアに一致しないため、filetype 検出で `[%w_]` を使うよう修正する PR である。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**変更ファイル:** 1 ファイル、+6/-6。`runtime/lua/vim/filetype/detect.lua` のパターン修正である。

**次のアクション:** 小さな修正のためレビュー後マージ可能である。

---

### ![PR](icons/git-pull-request.svg) [feat(redo)!: dot-repeat input payload of 'opfunc'/:omap](https://github.com/neovim/neovim/pull/42040) `#42040`
**ステータス:** Open (Draft) | **ラベル:** multicursor, breaking-change, editor-state

**概要:** `'operatorfunc'` や `:omap` のモーション（vim-sneak の `dz{target}` など）が `getchar`/`input` で受け取った入力を、dot-repeat の redo に含めるようにする破壊的変更である（#41657 を修正対象とする）。従来プラグインは vim-repeat や入力キャッシュで dot-repeat を実現してきたが、multicursor の cascade はプラグインが再生時に通常どおり入力を待つことを前提とする。プラグイン向けの方針を「キャッシュせず再読み込みする」とし、既存の工夫をしているプラグインには適応を求める。

**直近のアクティビティ:** echasnovski が、既に dot-repeat を提供しているプラグインを罰する悪いトレードオフだと強く反対し、`CmdAtom.keys` の代わりに `CmdAtom.lhs` を使う代替案を挙げた。mikejmcguirk も、ラベル付きジャンプでは前回入力が dot-repeat 時に無意味になるため、再プロンプトか一貫したフォールバックが必要だと指摘した。

**変更ファイル:** 11 ファイル、+329/-100。`src/nvim/input_cmdatom.c` での入力追記と、`test/functional/editor/cmdatom_spec.lua`、`mcursor_spec.lua` のテスト改修が中心で、`repeat.txt` や `lua-plugin.txt` などのドキュメントも更新している。

**次のアクション:** プラグイン互換性への懸念に対し、#41657 で挙げられた代替案との比較を含めた方針決定が必要である。

---

### ![PR](icons/git-pull-request.svg) [fix(lua): annotation for void `vim._with` function](https://github.com/neovim/neovim/pull/41962) `#41962`
**ステータス:** Open | **ラベル:** lua, typo

**概要:** 戻り値のない関数を `vim._with` に渡すと `lua_ls` が `missing-return` を報告する問題に対し、アノテーションを修正して void 関数を許容する PR である。

**直近のアクティビティ:** justinmk、lewis6991、作者の MariaSolOs がレビューコメントをやり取りしている（本文なし）。

**変更ファイル:** 1 ファイル、+1/-0。`runtime/lua/vim/_core/shared.lua` のアノテーション追加である。

**次のアクション:** レビュー指摘の解消を確認してマージを判断する。

---

### ![Issue](icons/issue-opened.svg) [Operator that asks for user input while supporting dot-repeat and multicursor](https://github.com/neovim/neovim/issues/41657) `#41657`
**ステータス:** Open | **ラベル:** multicursor

**概要:** ユーザー入力を求め、dot-repeat で再入力なしに繰り返せ、multicursor でも動く操作をどう実装するかという問題提起である。一般的な手法は `'operatorfunc'` を設定して `'g@l'` を返し入力をキャッシュするものだが、multicursor や edit-repeat では解決済みキー `g@lo\r` が再生され、キャッシュした入力と食い違う。報告者は `ev.data.lhs` による再生、`map-expression` の制限緩和、dot-repeat で入力を再生する案を挙げ、最後の案は破壊的な影響が大きいとしている。

**直近のアクティビティ:** justinmk がこの手法の出典を問い、3 番目の案を #42040 で実装したと回答した。echasnovski は少なくとも 5 年間問題なく機能してきた手法だと答え、#42040 の方針に落胆を示した。

**次のアクション:** #42040 の方針と代替案のどちらを採るか、メンテナとプラグイン作者の合意形成が必要である。

---

### ![Issue](icons/issue-opened.svg) [OSC 133 marks are not wiped on terminal clear](https://github.com/neovim/neovim/issues/41961) `#41961`
**ステータス:** Open | **ラベル:** terminal, marks

**概要:** ターミナルで `clear` した後も OSC 133 のプロンプトマークが残り、`[[` が存在しない位置へジャンプする不具合報告である。`:marks nvim.terminal.prompt` でも確認できる。

**直近のアクティビティ:** jiaoshijie が、clear 時に行が通常バッファのように削除されず、サインやハイライトなど extmark 全般が同様に残ること、折り返し時に extmark が更新されないことを報告した。報告者の igorlfs は issue を広げないよう求め、別件は個別 issue にすべきで、折り返しは既知の #30117 だと返した。

**次のアクション:** 修正 PR #42035 は AI 生成としてブロックされたため、別の修正が必要である。

---

### ![PR](icons/git-pull-request.svg) [fix(lsp): resolve markdown backslash escapes in hover](https://github.com/neovim/neovim/pull/42039) `#42039`
**ステータス:** Closed | **ラベル:** lsp

**概要:** LSP の hover 表示で GFM のバックスラッシュエスケープ（`\.` や `\_`）が解決されず表示される問題に対し、`_normalize_markdown()` にエスケープ解決の処理を追加する PR であった（#41910 を修正対象とする）。

**直近のアクティビティ:** justinmk が低労力の AI 生成コントリビューションと判断し、CONTRIBUTING.md の確認を求めて close した。証拠のない AI PR は警告なくブロックする方針も示した。

**変更ファイル:** 2 ファイル、+133/-0。`runtime/lua/vim/lsp/util.lua` への処理追加と `util_spec.lua` のテスト追加である。

**次のアクション:** 対応不要である。#41910 自体は未解決のまま残る。

---

### ![Issue](icons/issue-opened.svg) [modeline prompts to trust file or workspace](https://github.com/neovim/neovim/issues/39044) `#39044`
**ステータス:** Open | **ラベル:** security, lifecycle, options, editor-state, workspace-project

**概要:** modeline は安全でないため、新たなオプションやホワイトリストではなく `:trust` の仕組みで扱うべきという提案である。trust データベースにない modeline を検出したら、ファイルかワークスペース単位で信頼するかをユーザーに尋ね、ハッシュは modeline の内容のみに対して取る。

**直近のアクティビティ:** lewis6991 が、多数のプロジェクトや短命な worktree で作業する場合プロンプトが煩雑になると反対し、`vim: set shiftwidth=2` のような単純な modeline は許可リストで通し、それ以外だけ trust プロンプトを出す案を示した。justinmk は場当たり的なロジックや sandbox 内部を削除できるならその案に賛成し、ftplugin こそ明示的な trust が必要という点にも同意した。

**次のアクション:** 許可リスト方式か全面 trust 方式かの設計判断が必要である。#42036 や #42038 の個別修正を置き換える根本策として注目される。

---

### ![Issue](icons/issue-opened.svg) [multicursor: immediate edit after `{Visual}Q` is applied twice at main cursor](https://github.com/neovim/neovim/issues/42025) `#42025`
**ステータス:** Open | **ラベル:** multicursor

**概要:** `{Visual}Q` で multicursor を開始した直後の編集が、メインカーソル位置で 2 回適用される不具合報告である。メインカーソルと同じセルに追加カーソルが存在し、編集時点で重複排除されていないことが原因と推測されている。

**直近のアクティビティ:** echasnovski が、最初の再現手順は解消したが、`r` による置換や、メインカーソルが追加カーソルと重なる場合のオペレータ（`g~~`）の二重適用は未解決だと報告した。

**次のアクション:** 残る再現ケースへの追加修正が必要である。

---

### ![Issue](icons/issue-opened.svg) [multicursor: undo/redo as mappings don't properly work after edit with follow mode enabled](https://github.com/neovim/neovim/issues/42037) `#42037`
**ステータス:** Open | **ラベル:** multicursor

**概要:** `u` や `<C-R>` を同じ動作のマッピングとして定義すると、follow mode 有効時の multicursor で undo/redo が 2 回分まとめて戻ったり、追加カーソルの位置がずれたりする不具合報告である。`q=` で follow mode を無効にすると期待どおり動作する。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**次のアクション:** メンテナによる再現確認と原因調査が必要である。

---

### ![PR](icons/git-pull-request.svg) [[WIP] crazy experiments with Bigly chars](https://github.com/neovim/neovim/pull/42023) `#42023`
**ステータス:** Open (Draft) | **ラベル:** performance

**概要:** ホットループ内で書記素クラスタを複数回デコードしている処理を、1 回のデコードで済ませる実験的な性能改善 PR である。

**直近のアクティビティ:** justinmk が、コミット種別を `perf(mbyte)` にすることと news への記載を提案した。

**変更ファイル:** 16 ファイル、+215/-158。`src/nvim/mbyte.c`、`mbyte.h` の改修が中心で、`plines.c`、`drawline.c`、`insert.c` など描画や編集の呼び出し側を更新し、`charset.c`、`charset.h` から関連コードを削除している。

**次のアクション:** WIP のため、実験結果と Draft 解除を待つ。

---

### ![Issue](icons/issue-opened.svg) [multicursor: After enabling `ui2`, in `v` mode, when using `h/l` to move the cursor, some cursors move extra step.](https://github.com/neovim/neovim/issues/41866) `#41866`
**ステータス:** Closed | **ラベル:** multicursor, ui2

**概要:** `ui2` を有効にした状態で Visual モードの `h`/`l` 移動時に、一部のカーソルが 1 つ余分に動く不具合報告であった。

**直近のアクティビティ:** 報告者が最新の master で修正されていることを確認し、close された。

**次のアクション:** 対応不要である。

---

### ![Issue](icons/issue-opened.svg) [vim.net as a full fledged http client](https://github.com/neovim/neovim/issues/38946) `#38946`
**ステータス:** Open | **ラベル:** lua, network

**概要:** GET のみ対応の `vim.net.request` を拡張し、HTTP メソッド指定、リクエストボディ、ヘッダー、レスポンスのステータスとヘッダーを扱えるようにする提案である。Python の `requests` に倣い、`vim.net.get` や `vim.net.post` などの簡易関数も追加する。

**直近のアクティビティ:** 提案者が実装 PR #41986 を作成し、意見を求めた。

**次のアクション:** #41986 のレビューが必要である。

---

### ![PR](icons/git-pull-request.svg) [refactor(charset)!: delete 'isprint' and cleanup 'iskeyword' and friends](https://github.com/neovim/neovim/pull/41971) `#41971`
**ステータス:** Merged | **ラベル:** なし

**概要:** `enc=utf-8` 前提の現在では不要となった `'isprint'` オプションを削除する破壊的変更である。U+0080 から U+00FF の可読性は Unicode 定義に従い、不正な `\x80` から `\xFF` のバイトは常に不可視とする。`'iskeyword'`、`'isfname'`、`'isident'` に個別のビットセットを持たせ、それぞれ独立に更新しエラー時に安全に戻れるようにした。

**直近のアクティビティ:** justinmk が #41942 と競合する可能性に触れつつ Approve し、マージされた。

**変更ファイル:** 28 ファイル、+98/-283。`src/nvim/charset.c` の整理が中心で、`options.txt` などのドキュメントや `options.gen.lua` から `'isprint'` の記述を削除し、`deprecated.txt` に追記している。

**次のアクション:** `'isprint'` を設定しているユーザー設定やプラグインへの影響を確認する。

---

### ![PR](icons/git-pull-request.svg) [fix(api): preserve option trust during merges](https://github.com/neovim/neovim/pull/42036) `#42036`
**ステータス:** Merged | **ラベル:** api, terminal

**概要:** API 経由の append、prepend、remove 操作がオプション setter に全置換として渡されるため、式の untrusted フラグが消えてファイル書き込みが可能になる問題を修正した。置換かどうかを setter に渡し、マージ操作では既存値の trust 制限を維持する。

**直近のアクティビティ:** lewis6991 は、modeline で設定された `statusline` に `vim.opt_local.statusline:append()` すると trusted になる狭い攻撃経路であり、AI がリファクタリング中に見つけたバグだと説明した。justinmk は `'statusline'` が modeline で許可されていること自体に驚きを示し、#39044 が実現すればこうした部分的対策は不要になると述べて Approve した。

**変更ファイル:** 11 ファイル、+71/-31。`src/nvim/option.c` の setter 変更が中心で、呼び出し側の各ファイルを更新し、`test/functional/api/vim_spec.lua` にテストを追加している。

**次のアクション:** マージ済みのため対応不要である。

---

### ![PR](icons/git-pull-request.svg) [ci: bump the github-actions group across 1 directory with 3 updates](https://github.com/neovim/neovim/pull/41941) `#41941`
**ステータス:** Open | **ラベル:** ci, dependencies, github_actions

**概要:** dependabot による GitHub Actions の更新である。`github/codeql-action` を 4.37.9 から 4.38.0 へ上げるほか、`vmactions/freebsd-vm` と `zizmorcore/zizmor-action` を更新する。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**変更ファイル:** 3 ファイル、+4/-4。`codeql.yml`、`test_bsd.yml`、`zizmor.yml` のバージョン更新である。

**次のアクション:** CI 結果を確認してマージする。

---

### ![PR](icons/git-pull-request.svg) [fix(restart): preserve argument list on ":restart"](https://github.com/neovim/neovim/pull/42009) `#42009`
**ステータス:** Closed | **ラベル:** なし

**概要:** `:restart` で引数リストがクリアされる問題に対し、クリア処理を削除する PR であった（#42002 を close 対象とする）。引数のバッファが存在しない場合、バッファリストが変わる可能性がある。

**直近のアクティビティ:** echasnovski は、バッファリストを順序も含めて正確に保つことを優先すべきで、`%argdelete` を外すと新規バッファが加わり煩わしいと主張した。justinmk は、args を消したいユーザーは自分で削除できると応じた。最終的に PR はマージされずに close された。

**変更ファイル:** 2 ファイル、+11/-1。`runtime/lua/vim/_core/server.lua` の 1 行削除と `tui_spec.lua` のテスト追加である。

**次のアクション:** #42002 の扱いを別途確認する。

---

### ![PR](icons/git-pull-request.svg) [perf(lsp): share identical file watcher registrations](https://github.com/neovim/neovim/pull/41778) `#41778`
**ステータス:** Open | **ラベル:** lsp, AI assisted 🤖

**概要:** LSP サーバーが同一のファイルウォッチャーを重複登録すると、そのたびにウォッチャーツリーが作られ起動が遅くなる問題に対し、クライアント内で同一のウォッチャー集合を共有する PR である。macOS で Pyright と virtualenv を含むワークスペース（1,590 ディレクトリ）では、最大の起動遅延が 98 秒から約 232 ms に短縮した。#41776 に依存する。

**直近のアクティビティ:** justinmk が、`compile_watchers` の考え方は `vim._watch` にも適用できるのではと質問した。lewis6991 は LSP 固有の実装であり、汎用化には `_watch.lua` が LSP の glob を直接扱えるようにする内部 API 変更が必要だと回答した。Linux でウォッチャーをデフォルト有効に戻すための改善の一環だとも説明している。

**変更ファイル:** 4 ファイル、+299/-44。`runtime/lua/vim/lsp/_watchfiles.lua` の共有ロジック実装と `test/functional/plugin/lsp_spec.lua` のテスト追加が中心である。

**次のアクション:** #41776 のマージと、`vim._watch` への汎用化を別途行うかの判断が必要である。

---

### ![PR](icons/git-pull-request.svg) [fix(terminal): extmarks survive a full-screen erase (ED 2) #41961](https://github.com/neovim/neovim/pull/42035) `#42035`
**ステータス:** Closed | **ラベル:** terminal

**概要:** ターミナルで画面全体の消去（ED 2）が行われた際に、消去された行の extmark を削除して #41961 のゴーストマーク問題を修正する PR であった。vterm の screen callback として消去を通知し、alternate screen 以外で全画面消去の場合のみ処理する。

**直近のアクティビティ:** justinmk が AI slop としてブロックし、CONTRIBUTING.md を参照するよう示した。

**変更ファイル:** 4 ファイル、+222/-0。`src/nvim/terminal.c` と `src/nvim/vterm/screen.c` の変更と、`buffer_spec.lua` のテスト追加である。

**次のアクション:** 対応不要である。#41961 は未解決のまま残る。

---

### ![PR](icons/git-pull-request.svg) [vim-patch:9.2.1125: missing check for invalid "syntax iskeyword" value](https://github.com/neovim/neovim/pull/42032) `#42032`
**ステータス:** Merged | **ラベル:** vim-patch

**概要:** Vim 9.2.1125 の取り込みで、`syntax iskeyword` に不正な値が指定された場合にエラーを返すチェックを追加した。元の修正は bfredl によるものである。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**変更ファイル:** 2 ファイル、+11/-0。`src/nvim/syntax.c` のチェック追加と `test_syntax.vim` のテスト追加である。

**次のアクション:** マージ済みのため対応不要である。

---

### ![PR](icons/git-pull-request.svg) [refactor(api): reuse parse_cmdline in nvim_cmd](https://github.com/neovim/neovim/pull/38508) `#38508`
**ステータス:** Merged | **ラベル:** api, revisit-at-release

**概要:** `nvim_cmd()` が独自に Ex コマンドのアドレス種別や範囲を設定し `build_cmdline_str()` でコマンドライン全体を組み立てていた重複を解消した。範囲、コマンド名、引数だけを組み立て、`exarg_T` の構築は `parse_cmdline()` に任せる。新しい `kCmdlineParseStructured` モードで修飾子や count などの解析を省く。#34398 を close した。

**直近のアクティビティ:** glepnir は、文字列の範囲指定を解析する部分は別 PR に分けると説明した。justinmk はその機能の要望が実際にあるか問い、なければ待つよう述べ、glepnir は #38507 を挙げつつ待つことに同意した。TomJo2000 は変更の簡潔さを評価するレビューを残した。

**変更ファイル:** 4 ファイル、+97/-135。`src/nvim/api/command.c` の重複ロジック削除が中心で、`ex_docmd.c` の解析モード追加と `vim_spec.lua` のテスト追加を含む。

**次のアクション:** `revisit-at-release` ラベルのため、リリース時に再確認する。文字列範囲対応の後続 PR を待つ。

---

### ![PR](icons/git-pull-request.svg) [fix(multicursor): revert "]C"/"[C" cursor-plopping behavior](https://github.com/neovim/neovim/pull/42034) `#42034`
**ステータス:** Merged | **ラベル:** multicursor

**概要:** `]C`/`[C` でカーソルへジャンプする際に元の位置へカーソルを追加する挙動（#41681）を取り消した。ナビゲーションに副作用があり、`]CQ` でカーソルを削除できない、離れた位置から `[C` で戻るとカーソルが増えるなど、デフォルトとして予想外だったためである。回転的な挙動を望む場合のマッピング例をドキュメントに記載した。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**変更ファイル:** 3 ファイル、+41/-24。`runtime/lua/vim/_core/mcursor.lua` の 2 行削除、`repeat.txt` のドキュメント更新、`mcursor_spec.lua` のテスト修正である。

**次のアクション:** マージ済みのため対応不要である。

---

### ![Issue](icons/issue-opened.svg) [refactor: use nvim_parse_cmd in Ex cmd code](https://github.com/neovim/neovim/issues/34398) `#34398`
**ステータス:** Closed | **ラベル:** refactor

**概要:** `nvim_parse_cmd` が Ex コマンドの解析ロジックを大量に重複しているため、`do_one_cmd` などでも同じ解析処理を使い、ロジックを一か所にまとめるべきという提案であった。

**直近のアクティビティ:** 直近 24 時間のコメントなし（#38508 のマージにより close された）

**次のアクション:** 対応不要である。

---

### ![Issue](icons/issue-opened.svg) [`vim.cmd` does not accept the full set of Ex command-line range specifiers](https://github.com/neovim/neovim/issues/38507) `#38507`
**ステータス:** Open | **ラベル:** api, vimscript, editor

**概要:** `vim.cmd` のテーブル形式では `range` に数値しか指定できず、`.`、`$`、`%` などの範囲指定や単一の範囲指定が使えないという報告である。`vim.cmd.foldclose({ bang = true, range = {'.', '%'} })` のような書き方が現状は無効になる。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**次のアクション:** #38508 のマージを受け、文字列範囲を解析する後続 PR を待つ。

---

### ![Issue](icons/issue-opened.svg) [`[C` / `]C` should place a multicursor at the current position](https://github.com/neovim/neovim/issues/41664) `#41664`
**ステータス:** Closed | **ラベル:** multicursor

**概要:** `[C`/`]C` で移動するとメインカーソルの位置にあったカーソルが消えるため、移動元にカーソルを残すべきという要望であった。メインカーソルも multicursor の名前空間に含める案も添えられていた。

**直近のアクティビティ:** justinmk が #42034 で挙動を取り消したと報告した。ジャンプ時に `'` マーク以外の副作用を残すのは Nvim の他のジャンプとも multicursor 内の他のジャンプとも矛盾すると説明し、helix などがそう見えるのはメインカーソルを独立に動かす手段がないためだと述べた。

**次のアクション:** 対応不要である。

---

## nvim-telescope/telescope.nvim

> 直近24時間の更新はありません。

---

## ghostty-org/ghostty

> v1.4 リリースを約 2 週間後に控え、GTK まわりの PR (GTK 4.18 への最低要件引き上げ、フラクショナルスケーリング、kitty clipboard protocol) が相次いで承認された。VT 層の互換性修正や性能改善の PR も活発に出ている。

### ![PR](icons/git-pull-request.svg) [terminal: fix reporting and restoring of modes 47/1047/1048/1049](https://github.com/ghostty-org/ghostty/pull/14362) `#14362`
**ステータス:** Open (Draft) | **ラベル:** vt

**概要:** モード 47/1047/1048/1049 が端末の実際の状態ではなくモードビットから報告されている問題を修正し、#14199 の解決を狙う。あわせて XTRESTORE 1049 で画面が消去される問題も直し、代替スクリーンがカーソル位置を保ったまま復帰するようにする。レビュー補助として xterm ソースへのリンクを含む「REVIEW」コメントをコード中に残しており、後で削除する予定である。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**変更ファイル:** 3 ファイル、+398/-40。`src/terminal/Terminal.zig` に 9 件のユニットテストを含む大きな追加があり、`stream_terminal.zig` と `src/termio/stream_handler.zig` のモード処理を修正している。

**次のアクション:** Draft 解除後にメンテナのレビューが必要である。コード中の「REVIEW」コメントはマージ前に削除される予定である。

---

### ![PR](icons/git-pull-request.svg) [gtk: clear surface notifications on focus and close](https://github.com/ghostty-org/ghostty/pull/14108) `#14108`
**ステータス:** Open | **ラベル:** gtk

**概要:** GTK ではサーフェスをフォーカスまたはクローズした後も通知が残る問題があり、macOS と同様に通知を取り下げるようにする。通知をサーフェスごとに追跡し、フォーカス中に送られた通知は 3 秒後に消える。同じ文面の通知は置き換え、異なるサーフェスの通知は独立に扱う。

**直近のアクティビティ:** pluiedev が 2 度変更を要求した。手動のハッシュ計算は過剰設計であり、array hash map か `std.Deque` を使うべきこと、通知管理を `surface.zig` から別ファイルに分けること、マージコンフリクトの解消を求めている。作者の EriksRemess はレビューコメントに多数返信している。

**変更ファイル:** 3 ファイル、+435/-25。新規ファイル `src/apprt/gtk/desktop_notifications.zig` (+408) に通知管理を切り出し、`src/apprt/gtk/class/surface.zig` を修正している。

**次のアクション:** pluiedev による再レビューが必要である。

---

### ![PR](icons/git-pull-request.svg) [New Screen Storage](https://github.com/ghostty-org/ghostty/pull/10) `#10`
**ステータス:** Merged | **ラベル:** なし

**概要:**「screen」の実装を書き直す初期の PR である。`cat` による大きなファイル出力が約 25% 高速化し、スクロールバック満載時のメモリ使用量が 25% 減り、スクロールバックが動的確保になる。リサイズも高速化され、将来のリガチャや書記素クラスタ処理、行単位の dirty 追跡の土台となる。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**変更ファイル:** 11 ファイル、+1876/-1140。`src/terminal/Screen.zig` の全面書き換えと、新規のリングバッファ `src/terminal/circ_buf.zig` の追加が中心である。

**次のアクション:** 対応不要。過去にマージ済みの PR が更新検出されたものである。

---

### ![PR](icons/git-pull-request.svg) [Reflow on Resize](https://github.com/ghostty-org/ghostty/pull/2) `#2`
**ステータス:** Merged | **ラベル:** なし

**概要:** リサイズ時のテキストとカーソルのリフローを導入する初期の PR である。列数を縮める処理は非効率であると作者自身が認め、Screen のデータ構造を再考する必要があるとしたうえで、リフローのテストスイートを整備している。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**変更ファイル:** 4 ファイル、+1067/-84。`src/terminal/Screen.zig` にリフロー処理とテストを追加している。

**次のアクション:** 対応不要。過去にマージ済みの PR が更新検出されたものである。

---

### ![PR](icons/git-pull-request.svg) [ gtk: support fractional surface scaling](https://github.com/ghostty-org/ghostty/pull/14269) `#14269`
**ステータス:** Open | **ラベル:** なし

**概要:** `gtk_widget_get_scale_factor()` の代わりに `gdk_surface_get_scale()` を使い、切り上げた整数ではなく実際のフラクショナルスケールでデバイスサイズと DPI を扱う。テクスチャをデバイスピクセルと 1:1 に保ち、`GdkSurface` の `notify::scale` を監視して再描画する。#1938 で閉じられたが再発していたぼやけの問題 (Discussion #13911) への対応であり、X11 は未検証である。

**直近のアクティビティ:** jcollie は GTK バージョンの comptime チェックを削るよう求めた後、承認に切り替えた。pluiedev はリサイズ時の引き伸ばしやティアリングも直る利点を挙げ、細かい整理は残るものの現状でマージすべきとして承認した。

**変更ファイル:** 6 ファイル、+328/-48。新規の `src/apprt/gtk/scale.zig` にスケール処理を置き、`render_surface.zig` と `surface.zig` を修正している。

**次のアクション:** 2 名の承認が揃っており、マージ待ちである。

---

### ![PR](icons/git-pull-request.svg) [Require GTK 4.18 and libadwaita 1.7](https://github.com/ghostty-org/ghostty/pull/14330) `#14330`
**ステータス:** Open | **ラベル:** なし

**概要:** Debian 13 が GTK 4.18 と libadwaita 1.7 を提供していることを根拠に、最低要件をこれらに引き上げ、それ以前のバージョン向けのコードや回避策を削除する。バージョン条件分岐は新しいバージョン向けのものだけが残る。

**直近のアクティビティ:** jcollie がマージコンフリクトを解消したと報告した。pluiedev は「LGTM sans merge conflicts」として承認し、翻訳文字列そのものではなくファイル名だけが変わる点を @ghostty-org/manager に共有した。

**変更ファイル:** 81 ファイル、+5969/-6227。`src/apprt` 配下 44 ファイルで旧バージョン向けコードを削除し、`po/` 配下の各言語翻訳ファイルと `.pot` を更新している。

**次のアクション:** マージ待ちである。これより古い GTK や libadwaita はサポート対象外となるため、パッケージャは影響を確認する必要がある。

---

### ![PR](icons/git-pull-request.svg) [opengl: fix nvidia needing `EGL_PLATFORM=surfaceless` to launch correctly](https://github.com/ghostty-org/ghostty/pull/14319) `#14319`
**ステータス:** Open | **ラベル:** なし

**概要:** #14279 の `EGL_SURFACE_TYPE` 修正後も、Nvidia ドライバで正しく起動するには `EGL_PLATFORM=surfaceless` が必要で、無いと Mesa にフォールバックしていた。surfaceless ディスプレイを明示的に初期化することで環境変数を不要にする。

**直近のアクティビティ:** youssefadly237 が修正で解決したと報告した。pluiedev は Mesa/Intel で問題ないことを確認して承認し、@ghostty-org/renderer のマージを待っているとコメントした。

**変更ファイル:** 4 ファイル、+25/-11。`pkg/opengl/egl.zig` と `src/renderer/OpenGL.zig` を修正し、vendor の `glad_egl` を更新している。

**次のアクション:** renderer チームによるマージが必要である。

---

### ![Issue](icons/issue-opened.svg) [Meta: macOS Golden Gate (v27)](https://github.com/ghostty-org/ghostty/issues/13001) `#13001`
**ステータス:** Open | **ラベル:** os/macos

**概要:** 2026 年 6 月に発表された macOS Golden Gate (v27) への対応を追跡する Issue である。Ghostty は Golden Gate 上で問題なく動作し、主な作業は新機能への適応であるとしている。ベータ版 macOS の利用者には tip リリースの使用を勧めている。

**直近のアクティビティ:** macOS 27 が正式リリースされたとして修正版の提供時期を尋ねる質問があり、trag1c が修正はすでに tip に入っており v1.4 を約 2 週間後にリリース予定と回答した。

**次のアクション:** v1.4 リリースを待つ。macOS 27 利用者は当面 tip を使う。

---

### ![PR](icons/git-pull-request.svg) [terminal: batch special graphics character writes](https://github.com/ghostty-org/ghostty/pull/14356) `#14356`
**ステータス:** Open | **ラベル:** vt

**概要:** DEC Special Graphics と British 文字セットのバイトを、既存のルックアップテーブルを使って `printSlice()` でまとめて処理する。シングルシフトや Unicode 入力は従来どおり `print()` を使う。80×24 の罫線ボックスを 20,000 回描き直す合成ベンチマークで 296.6 ms から 42.8 ms (6.9 倍) に高速化したと報告している。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**変更ファイル:** 1 ファイル、+127/-24。`src/terminal/Terminal.zig` の `printSlice()` を拡張している。

**次のアクション:** メンテナのレビューが必要である。

---

### ![PR](icons/git-pull-request.svg) [terminal: stop word selection at hard line breaks](https://github.com/ghostty-org/ghostty/pull/14354) `#14354`
**ステータス:** Open | **ラベル:** vt

**概要:** 最終列をダブルクリックすると、ハード改行をまたいで単語選択されてしまう問題を修正する。離れる行の wrap フラグを確認し、ハード改行では選択を止める。ソフトラップされた単語は従来どおり複数行にまたがって選択される。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**変更ファイル:** 1 ファイル、+77/-6。`src/terminal/Screen.zig` の単語選択処理とテストを修正している。

**次のアクション:** メンテナのレビューが必要である。

---

### ![PR](icons/git-pull-request.svg) [gtk: implement kitty clipboard protocol](https://github.com/ghostty-org/ghostty/pull/14061) `#14061`
**ステータス:** Open | **ラベル:** gtk

**概要:** コアと macOS apprt で実装済みの kitty clipboard protocol を GTK apprt にも組み込み、確認ダイアログに画像プレビューを追加する。1.4 の文字列凍結中のため、追加した 2 つの文字列は翻訳対象外とし、1.5 で翻訳対象にする予定である。#10549 を解決する。

**直近のアクティビティ:** vancluever が承認した。OSC5522 で「選択を記憶」スイッチが表示される条件 (`name=` と `pw=` の両方が必要) は問題ないとした一方、コアが許可だけを記録し拒否を記録しない点は今後対処すべきと指摘した。kitty のパスワード機構の強度についても議論があり、仕様自体は転送手段にすぎず安全性は実装と用途に依存するとの見解を示している。

**変更ファイル:** 8 ファイル、+1001/-123。`src/apprt/gtk/class/surface.zig` にプロトコル処理を追加し、`clipboard_confirmation_dialog.zig` と blueprint UI を拡張している。検証用に `test/clipboard-multipart.py` を追加している。

**次のアクション:** マージ待ちである。OSC5522 の拒否記録は将来のコア変更 (macOS 側にも反映が必要) として残る。

---

### ![PR](icons/git-pull-request.svg) [agents: drop CLAUDE.md](https://github.com/ghostty-org/ghostty/pull/14348) `#14348`
**ステータス:** Merged | **ラベル:** なし

**概要:** Claude Code が v2.1.277 以降で AGENTS.md をサポートしたため、`CLAUDE.md` シンボリックリンクを削除する。

**直近のアクティビティ:** jcollie が承認し、マージされた。

**変更ファイル:** 1 ファイル、+0/-1。`CLAUDE.md` を削除している。

**次のアクション:** 対応不要。

---

### ![Issue](icons/issue-opened.svg) [Feature: Per-subprocess energy usage logging for battery diagnostics](https://github.com/ghostty-org/ghostty/issues/11942) `#11942`
**ステータス:** Closed | **ラベル:** なし

**概要:** macOS の Activity Monitor がサブプロセスの電力消費をすべて Ghostty に帰属させるため、バッテリー消費の原因を切り分けられない問題を指摘する。オプトインでサブプロセスごとのエネルギー使用量を定期的にログ出力する機能を提案し、macOS では `proc_pid_rusage()` の `RUSAGE_INFO_V4` 以降を使う実装方針を示している。

**直近のアクティビティ:** AnirudhG07 が、macOS でアイドル時にもレンダラの `DisplayLink` がリフレッシュレートで動き続け、毎秒約 700 回の割り込み wakeup が起きていたと説明した。コミット 6688aa072 (「renderer: park DisplayLink while idle」) で修正されるはずだとコメントしている。

**次のアクション:** 対応不要。クローズ済みである。

---

### ![Issue](icons/issue-opened.svg) [Check whether the `tabs` style works correctly after the first public release of Golden Gate](https://github.com/ghostty-org/ghostty/issues/13070) `#13070`
**ステータス:** Open | **ラベル:** os/macos

**概要:** macOS 27 beta 2 でタブバーまわりの名称変更があったため、正式リリース後に `macos-titlebar-style = tabs` のタブバーが正しく動くか確認する Issue である。Golden Gate 用のサブクラス化の要否や、`hidden` スタイルの回避策についても検討項目に挙げている。tip では現在正常に動作しているとし、tip でタブバーの問題がある場合だけコメントするよう求めている。

**直近のアクティビティ:** mjsteinbaugh が更新への感謝を述べるコメントを投稿した。

**次のアクション:** macOS 27 正式版で UI テストを実行し、最終確認する必要がある。

---

### ![PR](icons/git-pull-request.svg) [Update VOUCHED list](https://github.com/ghostty-org/ghostty/pull/14341) `#14341`
**ステータス:** Merged | **ラベル:** なし

**概要:** pluiedev の Discussion コメントを契機に bot が `VOUCHED` リストを更新する PR であり、@zorzysty を denounce として登録している。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**変更ファイル:** 1 ファイル、+1/-0。`.github/VOUCHED.td` に 1 行追加している。

**次のアクション:** 対応不要。

---

## astral-sh/uv

> 0.12.18 のリリースに加え、lockfile の記録内容の縮小、パッケージ単位の制約（constraint）指定、`--output-format json` の拡充など、リゾルバと出力形式まわりの PR が集中している。自動化 bot によるリファクタリングや安全性修正の PR も多数マージされた。

### ![PR](icons/git-pull-request.svg) [Align arbitrary equality satisfaction with resolution](https://github.com/astral-sh/uv/pull/21931) `#21931`
**ステータス:** Open | **ラベル:** bug

**概要:** `package===1` はリゾルバでは `1.0.0` に解決されるが、充足チェックはレンダリング後のバージョン文字列を比較するため、インストール済みパッケージを非互換と判定してしまう。`===` の比較をパース済みバージョンで行い、インストール再利用と `uv pip check` をリゾルバの結果と一致させる。local version label は引き続き区別され、`===1` は `1.0.0` を受け入れ `1.0.0+local` を拒否する。#21930 の前提となる PR である。

**直近のアクティビティ:** konstin が Approve した。bot がテスト 1 件の追加を報告し、CodSpeed は 6 件のベンチマークで最大 6.18% の性能低下を報告している。

**変更ファイル:** 2 ファイル、+86/-1。`crates/uv-pep440/src/version_specifier.rs` の比較ロジック修正と、`pip_check.rs` へのテスト追加である。

**次のアクション:** CodSpeed の性能低下が本変更と関係するかを確認した上でマージする。

---

### ![PR](icons/git-pull-request.svg) [Improve errors for uv lockfiles used as requirements](https://github.com/astral-sh/uv/pull/21933) `#21933`
**ステータス:** Open | **ラベル:** なし

**概要:** `action.py.lock` や `uv.lock` を requirements として渡すと、現状は `version = 1` の位置で比較演算子のエラーが出る。requirements のパースに失敗した際に既存の uv lock パーサで内容を判定し、requirements.txt 形式が必要であることと、スクリプト用とプロジェクト用の export コマンドを案内する。判定はファイル名ではなく内容で行い、constraints や overrides などにも同じ診断を適用する。#16192 を修正する。

**直近のアクティビティ:** bot がテスト 8 件の追加を報告したのみである。

**変更ファイル:** 5 ファイル、+446/-3。`crates/uv-requirements/src/specification.rs` の診断追加と、`pip_install.rs` および `run.rs` への回帰テスト追加が中心である。

**次のアクション:** メンテナによるレビュー待ちである。作者は Windows GNU 環境でのみ検証しているため、他プラットフォームでの CI 結果を確認する。

---

### ![Issue](icons/issue-opened.svg) [Dependency Dashboard](https://github.com/astral-sh/uv/issues/2658) `#2658`
**ステータス:** Open | **ラベル:** internal

**概要:** Renovate による依存関係更新の管理用ダッシュボードである。長期間更新のない依存として `arcstr` や `byteorder` など 32 件の cargo パッケージを一覧化している。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**次のアクション:** 対応不要である。放棄された依存の置き換えを検討する際の参考とする。

---

### ![PR](icons/git-pull-request.svg) [Update Codex automation dependencies](https://github.com/astral-sh/uv/pull/21876) `#21876`
**ステータス:** Open | **ラベル:** internal, build:skip-release, build:skip-docker

**概要:** Renovate による自動更新 PR で、`@openai/codex` を `0.147.0` から `0.149.1` に、`openai/codex-action` を `v1.11` から `v1.12` に更新する。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**変更ファイル:** 9 ファイル、+19/-19。issue triage や bug 再現などの GitHub Actions ワークフロー定義のバージョン指定のみである。

**次のアクション:** CI 通過後にマージする。

---

### ![Issue](icons/issue-opened.svg) [uv pip install -r pylock.toml ignores `default-groups` entries not also listed in `dependency-groups`](https://github.com/astral-sh/uv/issues/21917) `#21917`
**ステータス:** Open | **ラベル:** なし

**概要:** `pylock.toml` の `default-groups` に含まれるグループ名が `dependency-groups` にも載っている場合のみ uv はそれを既定で有効にし、載っていない場合は警告なしに該当パッケージをスキップする。PEP 751 は `default-groups` の名前を `dependency-groups` に載せるべきでないとしつつ、インストーラは `default-groups` から既定値を作るべきとしており、現状の uv では両方を満たせない。pip は無条件に有効化する。uv 0.9.17、0.12.17、0.12.18 で同じ挙動であり、pipx がこの問題に遭遇している。

**直近のアクティビティ:** 報告者が 2 度にわたって本文を訂正した。当初の「`default-groups` を常に無視する」という主張は誤りで、名前が両方に載る形式では有効化されることを pypa/pipx#2052 の指摘を受けて認めた。また `--group mygroup` も `dependency-groups` に載る名前しか選択しないため回避策にならず、存在しないグループ名も警告なしで受け入れられると報告している。

**次のアクション:** メンテナによるトリアージと、`dependency-groups` を名前集合の正とみなす現行挙動が意図的かどうかの判断が必要である。

---

### ![PR](icons/git-pull-request.svg) [Normalize requirements in tool receipts](https://github.com/astral-sh/uv/pull/21930) `#21930`
**ステータス:** Open | **ラベル:** enhancement

**概要:** `--with` を繰り返すと tool receipt に重複した requirement が記録される問題を修正する。tool のインストールと upgrade の両方で正規化し、`--with 'httpie<1' --with 'httpie<2'` は単一の `httpie<1` として記録される。#14023 をクローズする。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**変更ファイル:** 9 ファイル、+1015/-41。新規の `crates/uv/src/commands/tool/requirements.rs`（+566）に正規化ロジックを実装し、`tool_install.rs` などのテストを拡充している。ベースブランチは #21931 である。

**次のアクション:** 前提の #21931 のマージ後にレビューを進める。

---

### ![Issue](icons/issue-opened.svg) [`uv tool install --with <name> --with <name>` writes duplicate requirements to the receipt](https://github.com/astral-sh/uv/issues/14023) `#14023`
**ステータス:** Open | **ラベル:** bug

**概要:** `uv tool install ruff --with httpie --with httpie` を実行すると `uv-receipt.toml` に `httpie` が 2 回記録される。`--with 'httpie<1.0' --with 'httpie<2.0'` のように specifier が異なる場合もマージされず、別々のエントリになる。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**次のアクション:** 修正 PR #21930 のマージで解決する見込みである。

---

### ![PR](icons/git-pull-request.svg) [Hint when an unreadable lockfile uses a newer format](https://github.com/astral-sh/uv/pull/21890) `#21890`
**ステータス:** Open | **ラベル:** area:error-messages

**概要:** 新しい revision でパースエラーを起こすべきではないが、パースエラーが起きた lockfile がより新しい revision だった場合にその旨をヒントとして表示する。preview の revision は該当機能が有効な場合のみサポート対象とみなし、新しい revision でも読める lockfile は引き続き利用できる。#21864 に関連する。

**直近のアクティビティ:** charliermarsh が、これは明らかに uv 側のバグなので表示自体が不要ではないか、表示するならバグであることを明示すべきではないかと指摘した。charliermarsh と konstin の間でレビューコメントのやり取りがある。

**変更ファイル:** 4 ファイル、+103/-13。`crates/uv-lock/src/lock/mod.rs` の revision 判定と、プロジェクトコマンドおよび `tool/audit.rs` でのヒント表示、`lock.rs` へのテスト追加である。

**次のアクション:** ヒントを出すかどうか、出す場合の文言についてメンテナ間で合意する。

---

### ![PR](icons/git-pull-request.svg) [Sync latest Python releases](https://github.com/astral-sh/uv/pull/21847) `#21847`
**ステータス:** Open (Draft) | **ラベル:** なし

**概要:** 最新の Python リリース情報を取り込む自動更新 PR である。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**変更ファイル:** 1 ファイル、+216/-12。`crates/uv-python/download-metadata.json` のみである。

**次のアクション:** Draft 解除後に内容を確認してマージする。

---

### ![PR](icons/git-pull-request.svg) [Validate `.dist-info` stems at construction](https://github.com/astral-sh/uv/pull/21914) `#21914`
**ステータス:** Merged | **ラベル:** internal

**概要:** wheel メタデータの読み取り処理は `.dist-info` ディレクトリの stem を生の文字列で返し、正規化済みパッケージ名の接頭辞チェックを別途行っていた。`DistInfoStem` 型を導入してチェックを生成時に行い、メタデータ探索の戻り値と展開済みメタデータ読み取りの引数に使う。元の綴りは保持され、既存の緩い名前照合とエラーの優先順位は変わらない。

**直近のアクティビティ:** woodruffw が Approve し、zanieb もレビューコメントを付けた。bot はテスト 4 件追加と 1 件削除を報告し、CodSpeed は性能変化なしと報告した。

**変更ファイル:** 5 ファイル、+219/-180。`crates/uv-metadata` に `dist_info_stem.rs` を新設し、`uv-normalize` の `dist_info_name.rs` を削除している。

**次のアクション:** マージ済みのため対応不要である。

---

### ![PR](icons/git-pull-request.svg) [Hush some PEP 440 version warnings when dry-running Docker pushes](https://github.com/astral-sh/uv/pull/21929) `#21929`
**ステータス:** Merged | **ラベル:** internal, build:push-docker, internal:ci

**概要:** リリース時の Docker push の dry run で PEP 440 バージョンに関する警告が出ていたため、`build-docker-base` ジョブと同じ設定を適用して抑制する。CI のみの変更である。

**直近のアクティビティ:** zanieb が Approve した。

**変更ファイル:** 1 ファイル、+3/-2。`.github/workflows/build-docker.yml` のみである。

**次のアクション:** マージ済みのため対応不要である。

---

### ![PR](icons/git-pull-request.svg) [Add `uv publish --output-format=json`](https://github.com/astral-sh/uv/pull/21831) `#21831`
**ステータス:** Open | **ラベル:** area:cli, preview, area:json-output

**概要:** `uv publish` に `--output-format=json` を追加する。作者は #21827 を簡単にするためと述べている。エラー状態の表現は範囲が大きいため、成功時のみを対象とする。#411 に関連する。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**変更ファイル:** 6 ファイル、+554/-45。`crates/uv/src/commands/publish.rs` の出力処理と CLI 定義の変更、`publish.rs` への統合テスト追加（+381）が中心である。

**次のアクション:** メンテナによるレビュー待ちである。

---

### ![PR](icons/git-pull-request.svg) [Honor UV_QUIET and UV_VERBOSE environment variables](https://github.com/astral-sh/uv/pull/21928) `#21928`
**ステータス:** Closed | **ラベル:** bot:ai-policy-close

**概要:** グローバルな quiet/verbose 設定の解決時に `UV_QUIET` と `UV_VERBOSE` を参照し、`UV_VERBOSE=3` を `-vvv` と同等に扱う。CLI の `-q`/`-v` が優先され、2 つの環境変数の同時指定は競合として扱う。#12435 のクローズを目的としていた。

**直近のアクティビティ:** bot が AI ポリシー違反（AI によるメンテナとのコミュニケーション）を理由にクローズした。

**変更ファイル:** 5 ファイル、+99/-7。`crates/uv-settings` と `crates/uv-static/src/env_vars.rs` への環境変数追加、`settings.rs` の変更、テスト追加である。

**次のアクション:** クローズ済みのため対応不要である。#12435 は未解決のまま残る。

---

### ![PR](icons/git-pull-request.svg) [Honor UV_QUIET / UV_VERBOSE environment variables](https://github.com/astral-sh/uv/pull/21927) `#21927`
**ステータス:** Closed | **ラベル:** bot:ai-policy-close

**概要:** #21928 と同じ作者による同趣旨の PR である。`UV_QUIET` と `UV_VERBOSE` を追加して `GlobalSettings` に組み込み、CLI フラグを優先し、両方の指定をエラーとする。

**直近のアクティビティ:** bot が AI ポリシー違反を理由にクローズした。

**変更ファイル:** 4 ファイル、+134/-3。`crates/uv-settings/src/lib.rs` と `crates/uv/src/settings.rs` が中心である。

**次のアクション:** クローズ済みのため対応不要である。

---

### ![PR](icons/git-pull-request.svg) [Reject invalid Git archive paths in lockfiles](https://github.com/astral-sh/uv/pull/21780) `#21780`
**ステータス:** Merged | **ラベル:** area:error-messages

**概要:** Git ソースに `?path=foo` を含むよう編集された lockfile では、パスにサポート対象のアーカイブ拡張子がないため `uv export --frozen` が panic していた。lockfile の読み込み時に検証し、専用のエラーを表示する。

**直近のアクティビティ:** woodruffw が Approve した。無関係なアカウントから GitHub Copilot Chat の説明文を貼り付けたコメントが付いているが、内容は PR と無関係である。

**変更ファイル:** 2 ファイル、+82/-0。`crates/uv-lock/src/lock/mod.rs` の検証追加と `export.rs` へのテスト追加である。

**次のアクション:** マージ済みのため対応不要である。無関係なコメントはモデレーション対象である。

---

### ![PR](icons/git-pull-request.svg) [Check remote wheel metadata prefetch ranges](https://github.com/astral-sh/uv/pull/21919) `#21919`
**ステータス:** Merged | **ラベル:** bug

**概要:** `wheel_metadata_from_remote_zip` は、不正な ZIP64 サイズからプリフェッチ範囲を計算する際に、ZIP リーダーの検証前に debug ビルドで panic する可能性があった。checked arithmetic を使い、表現できないプリフェッチは省略する。#18605 のフォローアップである。

**直近のアクティビティ:** woodruffw が Approve した。bot がテスト 1 件の追加を報告し、CodSpeed はベンチマーク 1 件で 5.31% の低下を報告した。

**変更ファイル:** 2 ファイル、+84/-13。`crates/uv-client/src/remote_metadata.rs` の修正とテスト追加である。

**次のアクション:** マージ済みのため対応不要である。

---

### ![PR](icons/git-pull-request.svg) [Skip loading Python download metadata when listing installed interpreters](https://github.com/astral-sh/uv/pull/21925) `#21925`
**ステータス:** Merged | **ラベル:** なし

**概要:** インストール済み Python の一覧表示にはダウンロードメタデータが不要なため、読み込みを省略する。ローカル計測では `--only-installed` の実行時間が約 37 ms から約 15 ms に短縮され、約 58〜60% の削減となった。

**直近のアクティビティ:** zanieb が Approve し、charliermarsh が以前に似た変更をしたのではないかと述べた。charliermarsh は多くのケースは対応済みだがこのケースは漏れていたと返答した。

**変更ファイル:** 2 ファイル、+42/-12。`crates/uv/src/commands/python/list.rs` の修正とテスト追加である。

**次のアクション:** マージ済みのため対応不要である。

---

### ![PR](icons/git-pull-request.svg) [filter severity: labels in pr promotion automation](https://github.com/astral-sh/uv/pull/21926) `#21926`
**ステータス:** Merged | **ラベル:** なし

**概要:** 内部 fork から PR を昇格させる際に `severity:` ラベルを除外する。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**変更ファイル:** 1 ファイル、+1/-1。`.github/workflows/promote-pull-request.yml` のみである。

**次のアクション:** マージ済みのため対応不要である。

---

### ![PR](icons/git-pull-request.svg) [Bump version to 0.12.18](https://github.com/astral-sh/uv/pull/21924) `#21924`
**ステータス:** Merged | **ラベル:** なし

**概要:** uv のバージョンを 0.12.18 に上げるリリース PR である。本文はない。

**直近のアクティビティ:** woodruffw が Approve した。

**変更ファイル:** 156 ファイル、+407/-379。`CHANGELOG.md` への追記（+28）と、各クレートの `Cargo.toml` および README のバージョン更新である。

**次のアクション:** 0.12.18 の CHANGELOG を確認する。

---

### ![PR](icons/git-pull-request.svg) [Avoid rebuilding a Windows relative path into an absolute form](https://github.com/astral-sh/uv/pull/21923) `#21923`
**ステータス:** Merged | **ラベル:** なし

**概要:** `SanitizedArchivePath` が正規化時にパスを再構築する際、Windows のドライブ接頭辞を持ち込む可能性があった。`./C:foo` や `parent/../C:foo` に含まれる `Component::Normal("C:foo")` を `PathBuf::push` に渡すと、ドライブ相対パス `C:foo` と解釈される。#19693 で導入された問題である。

**直近のアクティビティ:** woodruffw と charliermarsh が Approve した。

**変更ファイル:** 1 ファイル、+34/-0。`crates/uv-extract/src/archive_path.rs` の修正とユニットテストである。

**次のアクション:** マージ済みのため対応不要である。

---

### ![PR](icons/git-pull-request.svg) [Use uncompressed editable wheels](https://github.com/astral-sh/uv/pull/21918) `#21918`
**ステータス:** Merged | **ラベル:** performance

**概要:** editable wheel は通常の Deflate writer を使っていたが、build backend が持つ非圧縮 writer に切り替え、一時的な wheel エントリを Stored 圧縮にする。通常の wheel の圧縮とインストール後のファイル内容は変わらない。ローカル計測で `build_editable` の時間が 6.8〜7.8% 短縮したが、`uv sync` の再インストール全体では計測可能な改善は見られなかった。

**直近のアクティビティ:** charliermarsh が Approve した。bot がテスト 1 件の追加を報告した。

**変更ファイル:** 2 ファイル、+30/-2。`crates/uv-build-backend/src/lib.rs` と `wheel.rs` である。

**次のアクション:** マージ済みのため対応不要である。

---

### ![PR](icons/git-pull-request.svg) [Support package-scoped dependency constraints](https://github.com/astral-sh/uv/pull/21899) `#21899`
**ステータス:** Open | **ラベル:** enhancement

**概要:** `constraint-dependencies` で、特定パッケージ（任意で特定バージョン）が宣言する依存だけを対象にした制約を指定できるようにする。選ばれる依存は宣言された requirement と、該当するすべてのグローバル制約およびスコープ付き制約を満たす必要がある。制約は既存の依存エッジにのみ適用される。セレクタは lockfile と tool receipt に保持され、変更時には lock を無効化する。

**直近のアクティビティ:** bot がテスト 6 件の追加を報告したのみである。

**変更ファイル:** 33 ファイル、+1025/-249。`crates/uv-configuration/src/constraints.rs`（+257/-36）を中心に、リゾルバ、lockfile、installer、workspace 設定にまたがる。

**次のアクション:** 本 PR をベースに #21900 と #21913 が積まれているため、レビューを優先する。

---

### ![PR](icons/git-pull-request.svg) [Add JSON output for `uv pip install` and `uv pip sync`](https://github.com/astral-sh/uv/pull/21893) `#21893`
**ステータス:** Merged | **ラベル:** enhancement

**概要:** `uv pip install` と `uv pip sync` に `--output-format json` を追加し、進捗メッセージをパースせずにパッケージの変更内容を取得できるようにする。通常実行、`--dry-run`、`--check` に対応し、JSON は stdout、診断は stderr に出力する。`uv sync` と共通の実験的な package-change スキーマを使う。

**直近のアクティビティ:** charliermarsh が Approve した。bot がテスト 1 件の追加を報告した。

**変更ファイル:** 12 ファイル、+431/-91。新規の `crates/uv/src/commands/install_report.rs` に共通レポートを実装し、`project/sync.rs` から処理を移している。`pip_sync.rs` と `json.rs` にテストを追加している。

**次のアクション:** マージ済みのため対応不要である。スキーマは実験的である点に留意する。

---

### ![PR](icons/git-pull-request.svg) [Omit unused runtime settings from lockfiles](https://github.com/astral-sh/uv/pull/21913) `#21913`
**ステータス:** Open | **ラベル:** enhancement, preview

**概要:** `exclude-newer-package` で採用した方針を一般化し、解決時に参照された設定（constraints、overrides、exclusions、dependency metadata、`exclude-newer-package`）だけを lockfile に記録する。lock の検証時には、保持された設定や lockfile のエントリと無関係なパッケージに関する設定の変更を無視する。`--refresh` 時には結果が変わりうるが、これは既存の `exclude-newer-package` と同じリスクとして許容している。#21894 と #21895 を置き換える。

**直近のアクティビティ:** CodSpeed が 31.16% の性能改善を報告したが、実行環境の違いがあると注記している。

**変更ファイル:** 28 ファイル、+2174/-87。新規の `resolution_recorder.rs` と `crates/uv-lock/src/lock/inputs.rs` が中心である。ベースブランチは #21899 である。

**次のアクション:** #21899 のマージ後にレビューを進める。

---

### ![PR](icons/git-pull-request.svg) [Support package-scoped build constraints](https://github.com/astral-sh/uv/pull/21900) `#21900`
**ステータス:** Open (Draft) | **ラベル:** なし

**概要:** `build-constraint-dependencies` にパッケージとバージョンのスコープを追加する。スコープはビルド対象のパッケージを選び、その推移的依存や backend hook が返す requirement を含むビルド環境全体を制約する。グローバル制約と該当するすべてのスコープを組み合わせ、スコープ付き制約とハッシュは lock と tool receipt に記録される。#21899 をベースにしている。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**変更ファイル:** 49 ファイル、+1715/-409。新規の `crates/uv-configuration/src/build_constraints.rs` と、`uv-build-frontend`、`uv-dispatch`、`uv-build-backend` の変更を含む。

**次のアクション:** #21899 のマージと Draft 解除を待つ。

---

### ![PR](icons/git-pull-request.svg) [Fix fragment boundaries in `UrlString::base_str`](https://github.com/astral-sh/uv/pull/21920) `#21920`
**ステータス:** Merged | **ラベル:** bug

**概要:** `UrlString::base_str` は `#` より先に `?` を探すため、fragment 内の `?` によって fragment が配布ファイル名やアーカイブ拡張子チェックに残る場合があった。最初の query または fragment の区切り文字で分割するよう修正する。#10253 のフォローアップである。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**変更ファイル:** 2 ファイル、+10/-2。`crates/uv-distribution-types/src/file.rs` の修正とテストである。

**次のアクション:** マージ済みのため対応不要である。

---

### ![PR](icons/git-pull-request.svg) [Update PubGrub and adapt to the term struct API](https://github.com/astral-sh/uv/pull/21916) `#21916`
**ステータス:** Open (Draft) | **ラベル:** なし

**概要:** 固定している PubGrub の revision を更新し、リゾルバと診断コードを `Term { set, negative }` に対応させる。PubGrub 側の更新により、競合解決時の一時的な否定 term と重複した version-set 演算がなくなる。#21647 の上に積まれ、astral-sh/pubgrub#89 に依存する。公開前に Git 固定の依存をリリース版に置き換える必要がある。

**直近のアクティビティ:** CodSpeed がベンチマーク 1 件で 7.13% の性能改善を報告した。

**変更ファイル:** 6 ファイル、+129/-111。`crates/uv-resolver/src/error.rs` と `pubgrub/report.rs` の API 追従が中心である。

**次のアクション:** astral-sh/pubgrub#89 と #21647 のマージを待つ。

---

### ![PR](icons/git-pull-request.svg) [Store Git checkout markers outside working trees](https://github.com/astral-sh/uv/pull/21891) `#21891`
**ステータス:** Open | **ラベル:** bug

**概要:** Git checkout のルートに `.ok` ファイルを置くと、リポジトリ内の `.ok` ファイルと衝突しうる。マーカーを 1 階層上の `<commit>.ok` に置くよう変更する。#21857 を修正する。

**直近のアクティビティ:** zsol が Approve した。bot がテスト 1 件の追加を報告した。

**変更ファイル:** 6 ファイル、+125/-29。`crates/uv-git/src/git.rs` の修正と、`pip_compile.rs` へのテスト追加、既存スナップショットの更新である。

**次のアクション:** Approve 済みのため、CI 通過後にマージする。

---

### ![PR](icons/git-pull-request.svg) [Use the production allocator in PyPI type benchmarks](https://github.com/astral-sh/uv/pull/21915) `#21915`
**ステータス:** Merged | **ラベル:** internal, area:benchmarks

**概要:** `uv_pypi_types` ベンチマークはシステムアロケータを使っており、glibc のばらつきが小さな digest 計測を左右していた。他のベンチマークと同様に `uv_performance_memory_allocator` をリンクする。

**直近のアクティビティ:** CodSpeed が 32.23% の改善を報告したが、実行環境の違いがあると注記している。

**変更ファイル:** 1 ファイル、+4/-0。`crates/uv-bench/benches/uv_pypi_types.rs` のみである。

**次のアクション:** マージ済みのため対応不要である。

---

### ![PR](icons/git-pull-request.svg) [Share URL userinfo redaction](https://github.com/astral-sh/uv/pull/21912) `#21912`
**ステータス:** Merged | **ラベル:** internal

**概要:** `DisplaySafeUrl` は `Display` と `Debug` で同じ認証情報マスク処理を別々に実装していた。userinfo の判定を共有し、パスワード、単独のユーザー名トークン、Git の汎用ユーザー名を両者で一貫して扱う。URL パースや query パラメータのマスクなどは変わらない。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**変更ファイル:** 1 ファイル、+17/-22。`crates/uv-redacted/src/lib.rs` のみである。

**次のアクション:** マージ済みのため対応不要である。

---

### ![PR](icons/git-pull-request.svg) [Add regression test for uv#21857](https://github.com/astral-sh/uv/pull/21909) `#21909`
**ステータス:** Merged | **ラベル:** internal, internal:testing

**概要:** #21857 を再現する最小の統合テストを追加する。

**直近のアクティビティ:** konstin が #21891 に同じテストがあると指摘し、zanieb はこのテストは分かりやすく、修正 PR でスナップショットの変化を確認できるのが良いと述べた。

**変更ファイル:** 1 ファイル、+76/-0。`crates/uv/tests/pip_install/pip_install.rs` のみである。

**次のアクション:** #21891 のマージ時にテストの重複を整理する。

---

### ![PR](icons/git-pull-request.svg) [Remove unused entry-point extras filtering](https://github.com/astral-sh/uv/pull/21911) `#21911`
**ステータス:** Merged | **ラベル:** internal

**概要:** wheel のインストールは entry point が宣言する extras を常に無視するが、内部パーサには旧インストーラ由来の extras フィルタが残っていた。未使用のフィルタを削除し、`Script` を直接返す。entry point の構文やエラー、launcher 生成は変わらない。

**直近のアクティビティ:** konstin が Approve した。

**変更ファイル:** 3 ファイル、+25/-81。`crates/uv-install-wheel` の `script.rs`、`wheel.rs`、`install.rs` である。

**次のアクション:** マージ済みのため対応不要である。

---

### ![PR](icons/git-pull-request.svg) [Remove redundant test snapshot filters](https://github.com/astral-sh/uv/pull/21908) `#21908`
**ステータス:** Merged | **ラベル:** internal:testing

**概要:** 継承された `RUST_LOG` が `TestContext` から起動したコマンドの出力を変えるため、ローカル実行向けのスナップショットフィルタが増えていた。`RUST_LOG` を環境の引き継ぎから外し、不要になった警告フィルタや、link-mode、interpreter、Pyodide などの古いフィルタを削除する。既存スナップショットは変わらない。

**直近のアクティビティ:** zanieb が複数回のレビューを経て Approve した。

**変更ファイル:** 12 ファイル、+61/-162。`crates/uv-test/src/lib.rs` と、`check.rs` や `workspace.rs` など各テストファイルのフィルタ削除である。

**次のアクション:** マージ済みのため対応不要である。

---

### ![PR](icons/git-pull-request.svg) [Remove unreachable Git fetch state](https://github.com/astral-sh/uv/pull/21910) `#21910`
**ステータス:** Merged | **ラベル:** internal

**概要:** `fetch` は曖昧な Git 参照を GitHub の fast path で解決しなくなったため、refspec を絞り込む分岐に object ID が渡ることはない。この到達不能な分岐と未使用のハッシュ接頭辞ヘルパーを削除する。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**変更ファイル:** 1 ファイル、+8/-35。`crates/uv-git/src/git.rs` のみである。

**次のアクション:** マージ済みのため対応不要である。

---

### ![PR](icons/git-pull-request.svg) [Omit unused dependency-metadata entries from lockfiles](https://github.com/astral-sh/uv/pull/21894) `#21894`
**ステータス:** Open | **ラベル:** preview

**概要:** 現状は設定されたすべての `dependency-metadata` エントリが lockfile に記録され、未使用エントリの変更でも `--locked` が失敗する。リゾルバのメタデータ選択規則を使って既存 lock を検証し、`--preview-features missing-dependency-metadata-lock` 指定時に未使用の宣言を省略する。古い uv は省略された lock を stale とみなすため、opt-in としている。

**直近のアクティビティ:** konstin が Approve し、`None` バージョンの扱いについて質問を残した。省略処理自体は正しいとしている。

**変更ファイル:** 7 ファイル、+636/-25。`crates/uv-lock/src/lock/mod.rs` と `dependency_metadata.rs` の変更、`lock.rs` へのテスト追加（+558）である。

**次のアクション:** #21913 がこの PR を置き換えるとしているため、クローズするか #21913 に統合するかを判断する。

---

### ![Issue](icons/issue-opened.svg) [Install shell completions in `uv tool install`](https://github.com/astral-sh/uv/issues/15766) `#15766`
**ステータス:** Open | **ラベル:** wish, needs-design

**概要:** Homebrew でインストールした `gallery-dl` はシェル補完が効くが、`uv tool install` では効かない。補完ファイルは `~/.local/share/uv/tools/gallery-dl/share/zsh/site-functions` に存在するが、`$fpath` に追加されないことが原因ではないかと報告者は考えている。

**直近のアクティビティ:** sscherfke が pixi や rattler-build の補完スクリプトのインストール方式を参考に実装してはどうかと提案した。

**次のアクション:** 設計方針（`needs-design`）の検討が必要である。

---

### ![PR](icons/git-pull-request.svg) [Isolate test subprocess logging with `UV_INTERNAL__TEST_RUST_LOG`](https://github.com/astral-sh/uv/pull/21906) `#21906`
**ステータス:** Merged | **ラベル:** internal, internal:testing

**概要:** テストハーネスが `RUST_LOG` を継承するため、親プロセスのログ設定がサブプロセスの出力やスナップショットを変えてしまう。他のホスト設定と同様に消去し、スイート全体の上書きには `UV_INTERNAL__TEST_RUST_LOG` を使う。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**変更ファイル:** 3 ファイル、+16/-1。`CONTRIBUTING.md`、`env_vars.rs`、`crates/uv-test/src/lib.rs` である。

**次のアクション:** マージ済みのため対応不要である。

---

### ![PR](icons/git-pull-request.svg) [Remind Codex to feature-gate tests](https://github.com/astral-sh/uv/pull/21886) `#21886`
**ステータス:** Merged | **ラベル:** internal

**概要:** #21883 のような修正 PR を Codex が自動で行えるよう、テストの feature gate について `AGENTS.md` に 1 行追記する。

**直近のアクティビティ:** zanieb と konstin の間でレビューコメントのやり取りがあった。

**変更ファイル:** 1 ファイル、+1/-0。`AGENTS.md` のみである。

**次のアクション:** マージ済みのため対応不要である。

---

### ![Issue](icons/issue-opened.svg) [Constraint flag for `uv add`](https://github.com/astral-sh/uv/issues/6518) `#6518`
**ステータス:** Closed | **ラベル:** enhancement

**概要:** `uv add` に `-c / --constraint` フラグを追加し、制約ファイルを直接渡せるようにする要望である。Apache Airflow のように制約ファイルが事実上必須のパッケージでの利用を想定している。

**直近のアクティビティ:** adamsardar が、`pyproject.toml` の `constraint-dependencies` に `constraints.txt` を指定しても機能しないと報告した。

**次のアクション:** クローズ済みである。追加の報告内容が別 Issue として必要かを確認する。

---

### ![Issue](icons/issue-opened.svg) [Automate updating of minimum dependencies based on time since initial minor release ](https://github.com/astral-sh/uv/issues/7515) `#7515`
**ステータス:** Open | **ラベル:** なし

**概要:** SPEC 0 の方針に沿って、一定期間を過ぎた依存の最小バージョンを `pyproject.toml` 上で自動更新するコマンドを求める要望である。期間の設定、特定依存の除外、除外バージョンや上限の維持、lowest-direct 解決での検証を求めている。

**直近のアクティビティ:** 報告者が `uv add --frozen` を利用する独立ツール `bump-minimum-dependencies` を作成したと報告した。UI が複雑になったため、uv に組み込むより独立ツールが適切と考えるようになったと述べている。

**次のアクション:** 報告者が独立ツールで対応しているため、Issue をクローズするか判断する。

---

### ![PR](icons/git-pull-request.svg) [fix(installer): bound prepare_stream concurrency to prevent file handle exhaustion](https://github.com/astral-sh/uv/pull/21851) `#21851`
**ステータス:** Closed | **ラベル:** bot:ai-policy-close

**概要:** `Preparer::prepare_stream` がすべての配布物を無制限に並行処理し、各ビルドがキャッシュシャードの `.lock` を保持し続けるため、大規模ワークスペースで FD 上限を超えるとの分析に基づく PR である。`buffer_unordered(N)`（`N = available_parallelism()`）で並行数を制限する。#17512 を対象としていた。

**直近のアクティビティ:** Abhirup0 が変更を要求した。wheel のダウンロードも CPU コア数に制限されてスループットが落ちること、`--concurrent-builds` などの既存設定を無視すること、高コア数環境では依然 FD を使い切ることを指摘し、制限はダウンロードではなくビルドとロックに適用すべきとした。PR は AI ポリシー違反を理由にクローズされた。

**変更ファイル:** 1 ファイル、+73/-14。`crates/uv-installer/src/preparer.rs` のみである。

**次のアクション:** クローズ済みである。レビューで示された方針は #17512 の検討材料となる。

---

### ![Issue](icons/issue-opened.svg) ["RECORD file is invalid" recurrent error on Windows](https://github.com/astral-sh/uv/issues/21905) `#21905`
**ステータス:** Open | **ラベル:** bug, area:windows

**概要:** Windows 11 の開発者マシンと Jenkins ノードで、`uv venv` 時に `RECORD file is invalid`（os error 33、ファイルの一部がロックされている）が繰り返し発生する。成功まで 4〜5 回のリトライが必要なこともある。停止できないアンチウイルスソフトが稼働している。

**直近のアクティビティ:** woodruffw が #20792 に沿って AV/EDR ベンダー情報とベンダーへの報告状況を尋ね、報告者は Trellix を使っていると回答した。リトライ実装の要望に対し、woodruffw は成功の見込みがない状態でファイルシステム操作を盲目的にリトライするのはリスクがあるとし、ベンダーへのエスカレーションを依頼した。報告者は了承した。

**次のアクション:** ベンダー側の対応待ちである。uv 側での対応予定はない。

---

### ![PR](icons/git-pull-request.svg) [Use `UV_INTERNAL__TEST_` for Rust test controls](https://github.com/astral-sh/uv/pull/21907) `#21907`
**ステータス:** Merged | **ラベル:** internal, internal:testing

**概要:** リトライ遅延、進捗出力、現在時刻、利用可能バージョンなどのテスト制御用環境変数が `UV_TEST_` 接頭辞を使っていたため、`UV_INTERNAL__TEST_` に統一する。旧名のエイリアスは残さない。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**変更ファイル:** 21 ファイル、+146/-123。`crates/uv-static/src/env_vars.rs` の定義変更と、リゾルバ、クライアント、各テストの参照箇所の更新である。

**次のアクション:** マージ済みのため対応不要である。旧名を使うローカル設定があれば更新する。

---

### ![Issue](icons/issue-opened.svg) [uv seems to fail unpacking a source distribution's tar file](https://github.com/astral-sh/uv/issues/21641) `#21641`
**ステータス:** Closed | **ラベル:** needs-mre

**概要:** `uv add wxpython` で `Invalid tar file` となり、原因として `connection reset` が表示される。失敗するファイルは毎回変わる。pip でのインストールや、ダウンロード済み tar.gz を指定した `uv add` は成功する。

**直近のアクティビティ:** EliteTK がより詳細なログの取得を提案し、報告者は回避策があるため uv 側の問題でなければクローズすると述べてクローズした。

**次のアクション:** クローズ済みのため対応不要である。

---

### ![Issue](icons/issue-opened.svg) [Include license information in cyclonedx export](https://github.com/astral-sh/uv/issues/21617) `#21617`
**ステータス:** Open | **ラベル:** enhancement

**概要:** `uv export --format cyclonedx-1.5` の SBOM に各コンポーネントのライセンス情報を含めてほしいという要望である。コンポーネント構造体にライセンスのフィールドはあるが常に None である。

**直近のアクティビティ:** woodruffw が、ライセンスのメタデータは標準のインデックス API に含まれず、PEP 658 も普及しておらず、解析可能なライセンスフィールドには Metadata 2.4 以上が必要と説明した。ローカルに 2.4 以上のメタデータがある場合に限れば出力できる可能性はあるが、汎用化は難しいとしている。

**次のアクション:** 限定的に対応するかどうか判断が必要である。

---

### ![Issue](icons/issue-opened.svg) [ZIP extraction rejects DEFLATE-compressed empty directory with data descriptor](https://github.com/astral-sh/uv/issues/21644) `#21644`
**ステータス:** Open | **ラベル:** bot:ai-policy-comment

**概要:** uv 0.12.13 で、DEFLATE 圧縮された空ディレクトリ（圧縮後 2 バイト）のサイズが data descriptor に記録された ZIP を展開すると `Bad compressed size` エラーとなる。data descriptor を使わない同内容のアーカイブは成功する。Interactive Brokers の公式 `ibapi` アーカイブでも再現する。

**直近のアクティビティ:** zanieb が、実利用での重要性が不明でシグナルが不足していると述べた。

**次のアクション:** 実利用での影響が示されるまで保留とみられる。

---

### ![Issue](icons/issue-opened.svg) [Investigate why uv holds so many file handles open](https://github.com/astral-sh/uv/issues/17512) `#17512`
**ステータス:** Open | **ラベル:** bug, help wanted

**概要:** sync やビルド、バイトコードコンパイル、Python のアンインストール時に uv が ulimit（Linux の既定値 1024 など）を超えるという報告がある。Cargo は同様の処理で FD を約 70〜80 に抑えており、uv がどの FD を保持しているかと Cargo との違いを調査する。

**直近のアクティビティ:** hesam-oxe が調査計画（大規模ワークスペースでの FD 計測、ロックのライフサイクル追跡、ロックの早期解放またはセマフォの導入）を示し、修正 PR #21851 を提出したと報告した。#21851 は AI ポリシーによりクローズされている。

**次のアクション:** #21851 のレビューで指摘された、ビルドとロックに限定して並行数を制限する方針での再検討が必要である。

---

### ![PR](icons/git-pull-request.svg) [Unify override and exclude code](https://github.com/astral-sh/uv/pull/20689) `#20689`
**ステータス:** Open (Draft) | **ラベル:** internal

**概要:** overrides と excludes は常に一緒に適用されるため、2 つのフィールドを別々に扱うのをやめ、1 つの型に統合する。今後の dependency modifier もこの型に追加できる。

**直近のアクティビティ:** CodSpeed が性能変化なしと報告した。

**変更ファイル:** 36 ファイル、+1030/-1188。新規の `crates/uv-configuration/src/dependency_modifier.rs`（+430）に統合し、`overrides.rs` と `excludes.rs` を縮小している。

**次のアクション:** #21899 や #21913 も同じ設定まわりを変更しているため、競合の整理が必要である。

---

### ![PR](icons/git-pull-request.svg) [Explain requirement mismatches when lock resolution fails](https://github.com/astral-sh/uv/pull/21284) `#21284`
**ステータス:** Open (Draft) | **ラベル:** なし

**概要:** lock チェックがパッケージメタデータの収集中に失敗した場合でも、変更された requirement と lockfile 更新の案内を表示し、無関係なダウンロードエラーが lock 更新の理由を隠さないようにする。元のエラーと既存の lock チェック挙動は維持する。

**直近のアクティビティ:** bot がテスト 2 件の追加を報告したのみである。

**変更ファイル:** 2 ファイル、+193/-33。`crates/uv/src/commands/project/lock.rs` とテストである。

**次のアクション:** Draft 解除を待つ。

---

### ![PR](icons/git-pull-request.svg) [Cover recursive extras with mixed production markers](https://github.com/astral-sh/uv/pull/21226) `#21226`
**ステータス:** Open (Draft) | **ラベル:** なし

**概要:** 再帰的な extra のマーカーテストを拡張し、Python バージョン条件または extra のいずれかで有効になる依存について、extra によって追加される環境のみが保持されることを確認する。テストのみの変更で、#21163 の上に積まれ、#21181 のフォローアップである。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**変更ファイル:** 1 ファイル、+7/-5。`crates/uv-distribution/src/metadata/requires_dist.rs` のみである。

**次のアクション:** #21163 のマージを待つ。

---

### ![PR](icons/git-pull-request.svg) [Allow an empty metadata cache when adding lower bounds](https://github.com/astral-sh/uv/pull/21225) `#21225`
**ステータス:** Open (Draft) | **ラベル:** なし

**概要:** メタデータを持たない lock を再利用すると、メモリ上の配布物インデックスが空になりうる。`uv add` が下限を追加して再度 lock する際に、キャッシュされていないエントリの無効化を許容するよう、キャッシュ存在の assertion を削除する。#21163 の上に積まれている。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**変更ファイル:** 2 ファイル、+38/-4。`crates/uv/src/commands/project/add.rs` の修正と `edit.rs` のテスト拡張である。

**次のアクション:** #21163 のマージを待つ。

---

### ![PR](icons/git-pull-request.svg) [Hashes in build constraints  disable uv build backend fast path](https://github.com/astral-sh/uv/pull/20863) `#20863`
**ステータス:** Closed | **ラベル:** enhancement

**概要:** 通常 uv は互換性のあるバージョンであれば uv build backend の fast path を使うが、明示的な build constraints やハッシュ必須の指定がある場合にも fast path を無効化し、ユーザーの明示的な指定を尊重する。

**直近のアクティビティ:** zanieb と konstin の間で多数のレビューのやり取りがあり、zanieb は一度 Approve したが、PR はマージされずにクローズされた。manfred-kaiser もコメントを残している。

**変更ファイル:** 2 ファイル、+112/-1。`crates/uv/src/commands/build_frontend.rs` と `build.rs` のテストである。

**次のアクション:** クローズ済みである。スコープ付き build constraints の #21900 に方針が引き継がれているかを確認する。

---

### ![Issue](icons/issue-opened.svg) [Cannot `uv pip install` with yanked dependencies](https://github.com/astral-sh/uv/issues/13569) `#13569`
**ステータス:** Closed | **ラベル:** question

**概要:** 社内 Artifactory のパッケージが yank 済みの `futures==3.1.1` に固定依存しており、`uv pip install` が解決に失敗する。pip は警告のみでインストールできるため、uv で yank 済みパッケージを強制インストールする方法を尋ねている。

**直近のアクティビティ:** Winand が、以前に書いた「0.8.20 以降 `uv add` で推移的依存として追加できる」というコメントは誤りだったと訂正した。

**次のアクション:** クローズ済みのため対応不要である。

---

## microsoft/vscode

> Agents Window やセッション管理、Agent Host まわりの PR が大半を占め、クラウドサンドボックスやリモートセッション委譲など大型の変更が並行している。Issue は insiders-released ラベル付きでクローズされた UI 改善が多く、MCP のリダイレクトに関するセキュリティ報告も出ている。

### ![PR](icons/git-pull-request.svg) [Enable tool search for GPT-6 models](https://github.com/microsoft/vscode/pull/337416) `#337416`
**ステータス:** Open | **ラベル:** なし

**概要:** Luna と Sol を含む GPT-6 ファミリーで Copilot SDK の tool search を有効化する PR である。GPT-6 と Claude Opus 5.5 のルーティングに回帰テストを追加し、Agent Host のプロンプトスナップショット対象を Opus 4.8 から Opus 5 と Opus 5.5 に置き換えている。

**直近のアクティビティ:** Copilot レビューは指摘なしで承認推奨、chrmarti が Approve した。github-actions[bot]がスクリーンショット差分 1 件を報告している。

**変更ファイル:** 25 ファイル、+44/-30。`toolSearchDeferral.ts` のモデルファミリー判定変更と、`copilotSessionLauncher.test.ts`、Opus 5.5 のキャプチャ YAML、各モデルのプロンプトスナップショット群の更新が中心である。

**次のアクション:** 承認済みのためマージ待ちである。

---

### ![PR](icons/git-pull-request.svg) [Respect editor font family for inline code in rendered Markdown](https://github.com/microsoft/vscode/pull/337427) `#337427`
**ステータス:** Open | **ラベル:** なし

**概要:** `DEFAULT_MARKDOWN_STYLES` のインラインコードがハードコードされた等幅フォントを使い、`editor.fontFamily` を無視していた問題を修正する。`--vscode-editor-font-family` を使い、既存の等幅フォントスタックをフォールバックとして残すことで、拡張機能の README などでフェンスコードブロックと表示をそろえる。

**直近のアクティビティ:** Copilot レビューが初回に「設定フォントが存在しない場合にフォールバックが効かない」と指摘し、修正後の再レビューでは承認推奨となった。人間のレビューはまだない。

**変更ファイル:** 1 ファイル、+1/-1。`markdownDocumentRenderer.ts` のインラインコード用 font-family 指定のみである。

**次のアクション:** メンテナによるレビューと承認が必要である。

---

### ![PR](icons/git-pull-request.svg) [simplify customization hint](https://github.com/microsoft/vscode/pull/337305) `#337305`
**ステータス:** Open (Draft) | **ラベル:** なし

**概要:** 本文はテンプレートのみで説明がない。Copilot レビューの要約によれば、カスタマイズ移行ヒントを実行可能な workspace/user 移行の要約に簡素化し、未対応の MCP サーバーを除外して、アクション名を「Review Migrations」に変更する内容である。

**直近のアクティビティ:** Copilot レビューは指摘なしで承認推奨である。hediet、Yoyokrazy、vritant24、roblourens のレビューが Dismissed 状態になっている。スクリーンショット差分はなし。

**変更ファイル:** 9 ファイル、+106/-141。`customizationMigrationServiceImpl.ts` と `customizationMigrationService.ts`（MCP 設定の保存元情報を追加）、`chatServiceImpl.ts`、関連テストとドキュメントを変更している。

**次のアクション:** Draft 解除と PR 説明の記載、再レビューが必要である。

---

### ![PR](icons/git-pull-request.svg) [chat: refine persistent progress rendering](https://github.com/microsoft/vscode/pull/337425) `#337425`
**ステータス:** Open (Draft) | **ラベル:** なし

**概要:** チャットの persistent progress を Weave アニメーション付きでデフォルト有効にし、Verbose（デフォルト）と Not Verbose の表示モードを追加する。ツールグループの折りたたみ、推論ヘッダーの扱い、進捗レールの位置合わせ、質問ツールの冗長な行の非表示、破棄処理の修正などを含む。既知の制約として、単一ツール描画と整合しないテストが 1 件失敗すること、メモリチェックで +23.53 MB の増加が出たことが記載されている。

**直近のアクティビティ:** Copilot レビューが 2 件の変更推奨を出した（シングルトン描画では通らないテスト、ツール結果詳細がない場合に `toolResultError` が隠れる問題）。スクリーンショット差分はコメントサイズ上限を超え、ワークフローサマリー参照となっている。

**変更ファイル:** 18 ファイル、+2034/-137。`chatThinkingContentPart.ts`、`chatProgressConfiguration.ts`、`chatContentParts.ts` と関連 CSS、アクセシビリティヘルプ、テストやフィクスチャが中心である。

**次のアクション:** 失敗テストとメモリ増加の検証、Copilot 指摘への対応を済ませて Draft を解除する必要がある。

---

### ![PR](icons/git-pull-request.svg) [[cherry-pick] agentHost: keep the default chat title in sync with a session rename](https://github.com/microsoft/vscode/pull/337376) `#337376`
**ステータス:** Closed | **ラベル:** cherry-pick-artifact

**概要:** #337308 のリリースブランチへの cherry-pick である。セッション一覧からセッション名を変更してもヘッダーに古いタイトルが残り、ヘッダーで Enter を押すと古い名前で上書きされるデータ消失を修正する。#331509 でガードが削除されたことによる回帰であり、`SessionTitleContribution._retitleSoleDefaultChat` でセッション名変更を唯一のデフォルトチャットへ反映する。

**直近のアクティビティ:** dmitrivMS と Yoyokrazy が Approve し、Copilot レビューも承認推奨だった。その後 chrmarti が「1.139.0 を公開リリースするため残りの PR をクローズする」とコメントし、マージされずにクローズされた。

**変更ファイル:** 3 ファイル、+82/-1。`sessionTitleContribution.ts` の修正と、`agentService.test.ts`、`protocolContractsSuite.ts` へのテスト追加である。

**次のアクション:** 1.139.0 には含まれないため、main 側の修正が次期リリースで反映されるかを確認する。

---

### ![PR](icons/git-pull-request.svg) [build: extend Windows Electron CI timeout](https://github.com/microsoft/vscode/pull/337419) `#337419`
**ステータス:** Open | **ラベル:** なし

**概要:** Windows Electron の CI ジョブのタイムアウトを 50 分から 70 分に延長し、Browser と Remote スイートは 50 分のまま維持する。node_modules キャッシュミス時に依存インストールで 14〜17 分かかり、スモークテストが時間切れでキャンセルされる事例（build 475824、475850）を根拠としている。

**直近のアクティビティ:** CODENOTIFY bot が lszomoru に通知した。Copilot レビューは指摘なしで承認推奨である。

**変更ファイル:** 1 ファイル、+4/-1。`build/azure-pipelines/win32/product-build-win32-ci.yml` にスイート別のタイムアウト条件を追加している。

**次のアクション:** lszomoru などビルド担当者のレビューと承認が必要である。

---

### ![PR](icons/git-pull-request.svg) [Agent Host: accept claude.ai keychain logins in the Claude native account check](https://github.com/microsoft/vscode/pull/334233) `#334233`
**ステータス:** Open | **ラベル:** なし

**概要:** Claude agent host のネイティブモードで、`claude login` による macOS キーチェーン上の claude.ai サブスクリプションが「未設定」と判定され、モデル一覧が空になる問題（#333738）を修正する。`isClaudeAccountSetUp` が `tokenSource` と `apiKeySource` だけで判定していたため、first-party アカウントが `subscriptionType` または `email` を返す場合も設定済みとみなすよう変更する。

**直近のアクティビティ:** CODENOTIFY bot が TylerLeonhardt に通知した。Copilot レビューは承認推奨だが、PR 説明では `subscriptionType: "Claude Max"` とあるのにテストフィクスチャは `"max"` になっている不一致を指摘した。作者の nengelmann がレビューコメントに返信している。

**変更ファイル:** 3 ファイル、+20/-6。`claudeTransportMode.ts` の判定拡張、`claudeTransportMode.test.ts` へのキーチェーン形状のケース追加、`claudeAgent.ts` のログへのサブスクリプション種別出力である。

**次のアクション:** フィクスチャの値の修正と、TylerLeonhardt らメンテナのレビューが必要である。

---

### ![PR](icons/git-pull-request.svg) [Fix markdown table header alignment in extension details](https://github.com/microsoft/vscode/pull/337415) `#337415`
**ステータス:** Open | **ラベル:** なし

**概要:** 拡張機能詳細画面で、Markdown 表のヘッダーに `:---:` などの配置指定があってもスタイルシートが全ヘッダーを左寄せに上書きしていた問題（#334699）を修正する。`align` 属性を持たないヘッダーにのみ左寄せを適用し、配置を検証するブラウザテストを追加している。

**直近のアクティビティ:** Copilot レビューはリクエスト者のクォータ上限により実行されなかった。人間のコメントはない。

**変更ファイル:** 2 ファイル、+30/-2。`markdownDocumentRenderer.ts` の CSS セレクタ変更と、`markdownDocumentRenderer.test.ts` のテスト追加である。

**次のアクション:** メンテナによるレビューが必要である。

---

### ![PR](icons/git-pull-request.svg) [Open saved Copilot shell output without retaining short results](https://github.com/microsoft/vscode/pull/335016) `#335016`
**ステータス:** Open (Draft) | **ラベル:** なし

**概要:** Copilot の非 PTY シェル出力の保存ファイルを、ヘッダーアクションや Terminal Output Accessible View から読み取り専用エディターで開けるようにする。短い出力や空の出力のトランスクリプトは保持せず、アーティファクトに裏付けられた記述子だけを残す。ホスト側で既存の 10 MiB 転送上限を適用し、ファイルが存在しない場合は部分出力で代替せず明示的にエラーとする。

**直近のアクティビティ:** 作者の anthonykim1 がレビューしやすさのため Copilot 専用に範囲を絞り、Claude 対応を #337389、Codex 対応を #337390 に分離したと説明した。3 者の出力が同じ AHP 形状に正規化されることもスクリーンショット付きで示している。Copilot レビューは横断的な変更のため人間による最終確認が必要と判定し、スクリーンショット差分では 19 件が追加された。

**変更ファイル:** 37 ファイル、+3337/-108。`agentHostTerminalManager.ts`、`copilotNonPtyShellTerminals.ts`、`copilotAgentSession.ts`、`mapSessionEvents.ts`、プロトコル状態定義とテストが中心である。

**次のアクション:** Draft 解除と人間のレビューが必要である。マージ後に #337389 と #337390 を main へ付け替える予定である。

---

### ![PR](icons/git-pull-request.svg) [Agents - Fix padding and border radius for "Viewed" action in Agents Window](https://github.com/microsoft/vscode/pull/337417) `#337417`
**ステータス:** Merged | **ラベル:** なし

**概要:** Agents Window のマルチファイル diff エディターにある「Viewed」アクションの padding と border radius を修正する（#337369）。共有スタイルの `2px` が優先されていたため、セレクタの詳細度を上げて `4px` を適用する。

**直近のアクティビティ:** Copilot レビューは指摘なしで承認推奨、bhavyaus が Approve してマージされた。

**変更ファイル:** 1 ファイル、+1/-1。`sessionChangesEditor.css` のセレクタ変更のみである。

**次のアクション:** マージ済みのため対応不要である。

---

### ![PR](icons/git-pull-request.svg) [Guard paged extension lists against incomplete Marketplace responses](https://github.com/microsoft/vscode/pull/337423) `#337423`
**ステータス:** Open | **ラベル:** なし

**概要:** Marketplace が件数 3 を返しつつ最初のページを空で返した場合に、Extensions ビューが `undefined` を描画して無関係な `identifier` エラーを出す問題（#337314 関連）に対処する。読み込んだページを総件数とページサイズで検証し、不整合を明示的に報告して後続ページの再試行を可能にする。Marketplace プロキシ側の不整合自体はサービス側の調査が必要としている。

**直近のアクティビティ:** Copilot レビューはリクエスト者のクォータ上限により実行されなかった。人間のコメントはない。

**変更ファイル:** 3 ファイル、+56/-3。`src/vs/base/common/paging.ts` の検証追加、`paging.test.ts` のテスト、`extensionsViews.ts` のエラー処理である。

**次のアクション:** メンテナによるレビューと、Marketplace 側の不整合の調査が必要である。

---

### ![PR](icons/git-pull-request.svg) [sessions: Add depth to Codicon backgrounds](https://github.com/microsoft/vscode/pull/337291) `#337291`
**ステータス:** Merged | **ラベル:** なし

**概要:** Agents Window のオプトイン Codicons 背景に、遠景、中景、近景の 3 レイヤーによる静的な奥行きを加える。スケール変換を使わずコンパクトなグリフを使い、コンポーザー背後の中央を控えめにする。スクロールやポインター操作では背景を動かさず、ハイコントラストテーマでは壁紙と Celebrate 操作を引き続き表示しない。

**直近のアクティビティ:** mrleemurray の依頼で Copilot がマージコンフリクトを解消した。Copilot レビューは任意スケーリング、スクロール時の不要な opacity 書き込み、ハイコントラストのフィクスチャ不足を指摘し、本文ではこれらへの対応が記載されている。複数のレビューが Dismissed となった後、dmitrivMS が Approve してマージされた。

**変更ファイル:** 6 ファイル、+269/-45。`chatBackgroundRenderer.ts` と `chatBackground.css` の実装、チャットビューのフィクスチャとテスト、スクリーンショット基準の一覧である。

**次のアクション:** マージ済みのため対応不要である。

---

### ![PR](icons/git-pull-request.svg) [Agents/remove automation prioritized notifications](https://github.com/microsoft/vscode/pull/337411) `#337411`
**ステータス:** Open | **ラベル:** triage-needed

**概要:** 本文はテンプレートのみで説明がない。タイトルと変更ファイルから、Agents の inboxOne における自動化の優先通知に関する変更と読み取れる。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**変更ファイル:** 10 ファイル、+1824/-79。`inboxNotificationsService.ts`、`inboxNotificationsView.ts`、`inboxNotificationsToast.ts`、`inboxNotificationsLabels.ts` と CSS、アクセシビリティテストなど `src/vs/sessions/contrib/inboxOne` 配下が中心である。

**次のアクション:** PR 説明の追記とトリアージ、レビュアーの割り当てが必要である。

---

### ![PR](icons/git-pull-request.svg) [Add governed OTel identity capture for Local agents](https://github.com/microsoft/vscode/pull/336814) `#336814`
**ステータス:** Open | **ラベル:** なし

**概要:** Local Copilot Chat に、エンタープライズ管理下でオプトインの ID 情報取得を追加する（#302930、対象は 1.140.0）。エージェント呼び出しスパンに `user.name`、リソースに `process.user.name` と `host.name` を付与する。既定はオフでコンテンツ取得とは独立しており、管理ポリシーが優先され、拒否時はキュー済みデータや SQLite からも ID を除去してリロードまで維持する。

**直近のアクティビティ:** Copilot レビューが変更推奨として、リロードなしで ID が再有効化される問題、コンテンツ取得無効時にツール説明が漏れる問題、リソース属性の優先順位のドキュメント更新を指摘した。作者の rwoll がレビューコメントに返信している。

**変更ファイル:** 42 ファイル、+1621/-72。`policyData.jsonc`、Copilot 拡張の `package.json`、`toolCallingLoop.ts`、`mcpToolCallingLoop.tsx`、`inlineChatIntent.ts`、監視ドキュメント `agent_monitoring.md` とテストが含まれる。

**次のアクション:** Copilot 指摘への対応状況の確認と、テレメトリやプライバシー観点での人間のレビューが必要である。

---

### ![PR](icons/git-pull-request.svg) [Chat: Hide empty confirmation dropdowns](https://github.com/microsoft/vscode/pull/337414) `#337414`
**ステータス:** Merged | **ラベル:** なし

**概要:** 確認ボタンの `moreActions` が空または区切りのみでも分割ボタンとして描画され、選択肢のないドロップダウンが表示されていた問題（#337412）を修正する。実際の追加アクションが 1 つ以上ある場合だけ分割ボタンを描画する。

**直近のアクティビティ:** bhavyaus が Approve し、Copilot レビューも承認推奨でマージされた。

**変更ファイル:** 2 ファイル、+17/-2。`chatConfirmationWidget.ts` の条件変更と、そのテストへの回帰ケース追加である。

**次のアクション:** マージ済みのため対応不要である。

---

### ![PR](icons/git-pull-request.svg) [Reduce automatic post-edit review for OpenAI Agent Host models](https://github.com/microsoft/vscode/pull/337409) `#337409`
**ステータス:** Merged | **ラベル:** なし

**概要:** OpenAI 系モデルを使う Copilot Agent Host セッションで、編集後の自動的な再読込や全 diff レビューを減らす。SDK の `code_change_rules` セクションに 3 文を追加するプロンプトコントリビューターを登録し、チェック失敗時や正確性に不安がある場合の検証は残す。ワークスペースコンテキストの変更は #337342 に分離している。

**直近のアクティビティ:** Copilot レビューは範囲外のワークスペースコンテキスト機能が非 OpenAI セッションに影響すると指摘したが、本文ではその変更を除外したと説明している。anthonykim1 と dileepyavan のレビューが Dismissed となった後、dileepyavan が Approve してマージされた。

**変更ファイル:** 13 ファイル、+110/-10。`openaiPrompt.ts` と `allPrompts.ts`、`agentHostPromptRegistry.test.ts`、GPT 系 9 件のプロンプトスナップショット、`AGENTS.md` である。

**次のアクション:** マージ済みである。ベンチマークは単独構成で再実行されていないため、効果の確認が残る。

---

### ![PR](icons/git-pull-request.svg) [agentHost: scope changesets to chats](https://github.com/microsoft/vscode/pull/337263) `#337263`
**ステータス:** Open (Draft) | **ラベル:** なし

**概要:** チャットごとのフォルダ分離作業のステップ 3 である。チャット単位の変更（Chat Changes、Last Turn Changes）とセッション全体の集約（Session Changes）を分け、複数チャットを持つセッションで異なるフォルダ、リポジトリ、worktree を正しく追跡する。Changes ビュー、ステータス、コードレビューなどをチャット所有の状態へ移し、同じ作業ディレクトリとベースブランチを持つチャット間では Branch Changes を共有する。

**直近のアクティビティ:** Copilot レビューが変更推奨として、チャットとセッションの更新の並行化、チャット所有者でのセッション Git メタデータへのフォールバック回避、チャット削除時の Git 状態の破棄、pre-0.9 プロトコルクライアントの互換性の 4 件を指摘した。スクリーンショット差分はサイズ上限超過でワークフローサマリー参照となっている。

**変更ファイル:** 166 ファイル、+3093/-1045。`agentHostChangesetService.ts`、`agentHostChangesetCoordinator.ts`、`agentHostGitStateService.ts`、チャットとセッションのプロトコル状態定義などが中心の大規模変更である。

**次のアクション:** Copilot 指摘 4 件への対応と Draft 解除、人間のレビューが必要である。

---

### ![PR](icons/git-pull-request.svg) [Preserve terminal metadata in confirmation UI](https://github.com/microsoft/vscode/pull/337383) `#337383`
**ステータス:** Merged | **ラベル:** なし

**概要:** Copilot SDK 経由で実行するターミナルコマンドの確認 UI で、正しいコマンドとメタデータが表示されるようにする（#337337）。承認オプションの扱いも修正し、表示内容と全承認アクションの回帰テストを追加している。

**直近のアクティビティ:** Copilot レビューは承認推奨、dmitrivMS のレビューは Dismissed、bhavyaus が Approve してマージされた。スクリーンショット差分はなし。

**変更ファイル:** 5 ファイル、+197/-56。`copilotAgentSession.ts`、`abstractToolConfirmationSubPart.ts`、`chatTerminalToolConfirmationSubPart.ts` と関連テストである。

**次のアクション:** マージ済みのため対応不要である。

---

### ![PR](icons/git-pull-request.svg) [agentHost: defer Agent Merge and feedback server tools behind tool search](https://github.com/microsoft/vscode/pull/337380) `#337380`
**ステータス:** Open (Draft) | **ラベル:** なし

**概要:** Agent Merge の 4 ツール、レビューコメント（agent feedback）の 6 ツール、セッション管理の 7 ツールに `deferLoading: true` を付け、全 Copilot SDK セッションで即時に読み込むのではなく `tool_search_tool` の背後に遅延させる。自動タイトル生成が名前で呼ぶ `rename_chat` は即時読み込みのまま残す。

**直近のアクティビティ:** Copilot レビューは指摘なしで承認推奨である。スクリーンショット差分 2 件（Agent Merge メッセージ表示）が報告された。

**変更ファイル:** 8 ファイル、+68/-3。`agentMergeServerTools.ts`、`agentFeedbackServerTools.ts`、`sessionServerTools.ts`、`agentMergePrompt.ts` と各テストである。

**次のアクション:** Draft 解除と人間のレビューが必要である。

---

### ![PR](icons/git-pull-request.svg) [mcp: Reposition migration action](https://github.com/microsoft/vscode/pull/337377) `#337377`
**ステータス:** Open | **ラベル:** なし

**概要:** MCP の移行アクションを移行可能なサーバーグループ内に移し、移行不可サーバー一覧の前に配置する。移行不可サーバーを二次セクションとして区別するため、余白と区切り線を追加し、DOM 構造と順序の回帰テストを追加している。

**直近のアクティビティ:** Copilot レビューは承認推奨、dmitrivMS のレビューは Dismissed、bhavyaus が Approve した。

**変更ファイル:** 4 ファイル、+93/-6。`aiCustomizationManagementEditor.ts` と CSS、エディターのテスト、スクリーンショット基準の一覧である。

**次のアクション:** 承認済みのためマージ待ちである。サンドボックスで実行できなかった Electron テストの CI 結果を確認する。

---

### ![PR](icons/git-pull-request.svg) [Agent Host: add chat.copilot.tgrep.enabled to force-enable tgrep indexed search for Copilot SDK sessions](https://github.com/microsoft/vscode/pull/331668) `#331668`
**ステータス:** Open (Draft) | **ラベル:** なし

**概要:** Agent Host 経由の Copilot SDK セッションで tgrep インデックス検索を強制有効化する実験的設定 `chat.copilot.tgrep.enabled`（既定 `false`、application スコープ）を追加する。ExP で制御でき、明示的なユーザー設定が優先される。有効時は CLI サブプロセスに `USE_TGREP=true` を設定し、`USE_BUILTIN_RIPGREP=false` の上書きを外す。

**直近のアクティビティ:** Copilot レビューが変更内容を要約した。dianatofficial が「最小限で正しい」とコメントし、作者の bhavyaus がレビューコメントに返信している。

**変更ファイル:** 8 ファイル、+85/-10。`copilotCliConfig.ts`、`copilotAgent.ts`、`copilotAgentStartupConfig.ts`、`chat.shared.contribution.ts` と設定転送のコントリビューション、各テストである。

**次のアクション:** Draft 解除と正式な Approve が必要である。

---

### ![PR](icons/git-pull-request.svg) [webview: Fix find in detached editor windows](https://github.com/microsoft/vscode/pull/337403) `#337403`
**ステータス:** Merged | **ラベル:** editor-find, webview

**概要:** エディターグループを補助ウィンドウに移すと、webview の検索要求が元のメインウィンドウ ID を使い、誤った web contents を検索していた問題（#233044）を修正する。検索開始、次/前、停止の要求を現在の所有ウィンドウ経由でルーティングし、既存の `windowId` / `webContentsId` の union でメインと補助を区別する。

**直近のアクティビティ:** Copilot レビューは承認推奨、anthonykim1 が Approve してマージされた。

**変更ファイル:** 4 ファイル、+164/-22。`webviewManagerService.ts`、`webviewMainService.ts`、`webviewElement.ts` と、ルーティングやキャンセルを検証する `webviewElement.test.ts` である。

**次のアクション:** マージ済みのため対応不要である。

---

### ![PR](icons/git-pull-request.svg) [test: Harden Windows Agent Host descendant cleanup](https://github.com/microsoft/vscode/pull/337396) `#337396`
**ステータス:** Open | **ラベル:** なし

**概要:** Windows の Agent Host テストサーバーの子孫プロセス終了処理を堅牢化する。終了前にプロセス名とコマンドラインを記録し、古い親 PID によって再利用された PID に紐付くアクセス不可の分岐を除外する。`taskkill` 失敗時は記録したプロセスが存在しない場合のみエラーを抑制する。Azure build 475775 と 476056 で再現した終了コード 128 と 255 の競合が動機である。

**直近のアクティビティ:** Copilot レビューが、`killTree` 実行前にプロセス同一性を確認しないと再利用 PID を終了させる危険があると指摘した。作者の roblourens が返信し、dmitrivMS のレビューは Dismissed、anthonykim1 が Approve した。

**変更ファイル:** 2 ファイル、+172/-19。`serverIntegrationTestHelpers.ts` とそのテストである。

**次のアクション:** PID 再利用の指摘が解消済みか確認のうえマージする。

---

### ![PR](icons/git-pull-request.svg) [Use Copilot session marketplaces with VS Code GitHub auth fallback](https://github.com/microsoft/vscode/pull/337217) `#337217`
**ステータス:** Open (Draft) | **ラベル:** なし

**概要:** Plugins がライブの Copilot Agent Host セッションを対象とする場合に、Copilot SDK のセッション単位マーケットプレイス API を使う（#337199）。セッション開始前は Local のパスを維持し、ランタイムが取得に失敗したマーケットプレイスだけを Local で補う。非公開の GitHub マーケットプレイスには、ネイティブ Git の認証に失敗した場合 `repo` スコープを持つ VS Code の GitHub セッションで再試行し、資格情報をコマンド引数、設定、ログに残さない。

**直近のアクティビティ:** 作者の anthonykim1 が、ハイブリッド方式でプロセス境界をまたぐため変更が大きくなった理由と、非公開マーケットプレイスでの検証結果（Available 0 から 14、セッション内で 197）を投稿した。Copilot レビューは URL スコープの不一致と Git 2.31 未満の互換性を指摘した。dmitrivMS は AI レビューで 19 件のインラインコメントを付け、キー入力ごとの未キャッシュなスナップショット取得（Critical）、フォールバックの二重取得、`allowExtensionMethods: false` の接続での `install`/`refresh` 公開の意図確認を優先事項として挙げた。

**変更ファイル:** 45 ファイル、+1803/-187。`agentHostProtocolClient.ts`、`agentHostExtensionProtocol.ts`、`localAgentHostService.ts`、`localGitService.ts`、マーケットプレイスのメタ定義など Agent Host と Git 認証まわりが中心である。

**次のアクション:** dmitrivMS の優先 3 件（リクエスト過多とセキュリティ）への対応が必要である。

---

### ![PR](icons/git-pull-request.svg) [Show HydraFusion below Auto in the model picker](https://github.com/microsoft/vscode/pull/337404) `#337404`
**ステータス:** Merged | **ラベル:** なし

**概要:** 両方のモデルピッカーで HydraFusion を Auto の直下に「Research preview」タグ付きで表示する。通常のピッカーでは Auto と同様のルーティング説明をホバーに出し、タブ式ピッカー（`chat.experimentalModelPicker`）では Auto と同様にモデル一覧を折りたたむスイッチを置く。古いビルドでは従来の「Update VS Code」行を維持する。

**直近のアクティビティ:** dmitrivMS が Approve してマージされた。Copilot レビューは、ピン留めや最近の選択がバージョン制限を回避できる点と、ルーティングコストの開示がスクリーンリーダーから読めない点を指摘している。スクリーンショットは 6 件追加された。

**変更ファイル:** 14 ファイル、+320/-64。`modelPickerAutoRow.ts`、`modelPickerHover.ts`、`modelPickerTabbedWidget.ts`、`modelPickerItemSections.ts`、`agentHostLanguageModelProvider.ts` などモデルピッカー一式である。

**次のアクション:** マージ済みである。Copilot が指摘したバージョン制限とアクセシビリティの問題はフォローアップでの対応が必要である。

---

### ![PR](icons/git-pull-request.svg) [Fix virtual tool grouping dropping tools](https://github.com/microsoft/vscode/pull/327576) `#327576`
**ステータス:** Closed | **ラベル:** なし

**概要:** 長時間のエージェントセッションで仮想ツールの再グルーピングにより登録済みツールに到達できなくなる問題 (#324113) を修正する PR である。埋め込みが使えない場合やクラスタリングで単独要素が過剰になる場合にもツールを保持し、ツールの並び替えに対してグルーピングを安定させる。報告された 106 ツールのシナリオを再現するテストも追加している。

**直近のアクティビティ:** Copilot レビューが変更点の概要（ロスレスなフォールバック、使用中ツールの保持、テレメトリ改善）を投稿した。人間のレビューはなく、Draft のまま Close された。

**変更ファイル:** 11 ファイル、+847/-478。`virtualToolGrouper.ts` や `toolEmbeddingsComputer.ts` などの仮想ツール関連ロジックと、新規テスト `toolDropRepro.spec.ts` が中心である。

**次のアクション:** Close 済みのため対応不要。#324113 の修正が別 PR で進むかを確認する。

---

### ![PR](icons/git-pull-request.svg) [Add minimal harness for experimental model family](https://github.com/microsoft/vscode/pull/317613) `#317613`
**ステータス:** Closed | **ラベル:** なし

**概要:** `chat.modelCapabilityOverrides` で `family` を `experimental` に設定したモデル向けに、簡素なプロンプトと限定されたツール群からなる opt-in の「minimal harness」を導入する PR である。ツールは `run_in_terminal`、`read_file`、`grep_search` など少数の許可リストに絞られ、サブエージェントや MCP ツールは除外される。既存のプロンプトリゾルバを持つモデルの挙動は変わらない。

**直近のアクティビティ:** Copilot レビューが概要を投稿したのみで、人間のレビューはない。

**変更ファイル:** 6 ファイル、+165/-15。新規 `defaultMinimalPrompt.tsx`、`promptRegistry.ts`、`toolsService.ts`、`chatModelCapabilities.ts` の変更である。

**次のアクション:** Close 済みのため対応不要。

---

### ![PR](icons/git-pull-request.svg) [chat: preserve sessions across window moves](https://github.com/microsoft/vscode/pull/337232) `#337232`
**ステータス:** Open (Draft) | **ラベル:** chat

**概要:** 自動保存されたチャットをサイドバー、エディター、補助ウィンドウ間で移動すると、移動先が参照を取得する前に最後のモデル参照が解放され、トランスクリプトの初期スナップショットが削除されて再度開けなくなる問題 (#336748) を修正する。移動中はモデルを保持し、移動元のクローズが拒否された場合は移動を中止する。セッション一覧の更新処理を直列化し、削除済みセッションが古い更新で復活しないようにしている。

**直近のアクティビティ:** Copilot レビューが複数回実施され、キャンセルされたエディタークローズの扱い、メタデータのみの変更の未通知、キュー済みリフレッシュの拒否時に呼び出し元が保留のまま残る問題などを指摘した。dmitrivMS がレビューコメントを重ねている。スクリーンショット差分はなし。

**変更ファイル:** 15 ファイル、+1020/-100。`chatMoveActions.ts`、`localAgentSessionsController.ts`、`chatWidgetService.ts` などと、新規テスト `chatMoveActions.test.ts` (+222) である。

**次のアクション:** Copilot の最新指摘（リフレッシュ呼び出し元の保留）への対応と、ライフサイクル変更に対する人間による最終確認。

---

### ![PR](icons/git-pull-request.svg) [Export Agent Host turn and first-response timing diagnostics via OTel](https://github.com/microsoft/vscode/pull/337394) `#337394`
**ステータス:** Merged | **ラベル:** なし

**概要:** Agent Host の既存 OpenTelemetry パイプラインに、コンテンツを含まないタイミング診断を追加する PR である。ホストのターン時間とレンダラーの初回応答計測を長さ 0 のメタデータスパンとして、ファイル、OTLP/HTTP JSON、SQLite の各経路で出力する。旧バージョンや OTel 無効のホストとの互換性を保ち、スキーマを `OTEL.md` に文書化している。

**直近のアクティビティ:** Copilot レビューがテストスタブ欠落による型チェック失敗やスキーマ不変条件違反を指摘した。vijayupadya のレビューを経て dmitrivMS が承認し、マージされた。

**変更ファイル:** 22 ファイル、+720/-21。新規 `OTEL.md`、`agentHostTiming.ts`、`agentHostOTelService.ts` 群とプロトコルクライアントの変更である。

**次のアクション:** マージ済み。遅延分析での活用状況を見守る。

---

### ![PR](icons/git-pull-request.svg) [Improve UI concistency between Hooks page and other pages](https://github.com/microsoft/vscode/pull/337408) `#337408`
**ステータス:** Open (Draft) | **ラベル:** なし

**概要:** カスタマイズモーダルの Hooks ページを他ページと揃える PR である。フック名横のチェックマークを削除し、インラインのコピー/削除ボタンを三点リーダーのオーバーフローメニューに置き換え、右クリックメニューと項目を統一する。キーボード操作と ARIA ラベルは維持している。

**直近のアクティビティ:** CI がスクリーンショット変更を検出した。Copilot レビューは、空メニュー時の状態残留と、file 以外のプロバイダーパスのコピー誤りを指摘した。

**変更ファイル:** 1 ファイル、+15/-66。`aiCustomizationListWidget.ts` のみである。

**次のアクション:** Copilot 指摘 2 件の修正と、スクリーンショット基準ファイルの更新。

---

### ![PR](icons/git-pull-request.svg) [Another redesign of the customizations modal](https://github.com/microsoft/vscode/pull/337399) `#337399`
**ステータス:** Open (Draft) | **ラベル:** なし

**概要:** カスタマイズモーダルの Plugins と MCP のツリー表示を再設計し、実験的設定で切り替え可能な 2 種類のレイアウトを実装する PR である。V1 は User、Workspace、Available でグループ化したタブ絞り込み型のフラットツリー、V2 は Installed と Available にまとめた折りたたみ型ツリーである。枠線の除去、ホバー背景、Install ボタンのセカンダリ化などスタイルも調整している。

**直近のアクティビティ:** Copilot レビューがスコープ絞り込みの退行、利用不可アクションの問題、アクセシビリティと状態管理の未解決課題を指摘した。スクリーンショット差分はコメントのサイズ上限を超えている。

**変更ファイル:** 15 ファイル、+2051/-1336。`pluginListWidget.ts`、`toolsListWidget.ts`、`mcpListWidget.ts` の大幅な書き換えと、新規 `customizationTree.ts`、`customizationToggle.ts` である。

**次のアクション:** Copilot が承認を妨げると判断した退行の修正と、デザイン方針の合意。

---

### ![PR](icons/git-pull-request.svg) [Update Tools page to use real extensions view for marketplace](https://github.com/microsoft/vscode/pull/337406) `#337406`
**ステータス:** Open (Draft) | **ラベル:** なし

**概要:** カスタマイズモーダルの Tools ページで、独自のマーケットプレイス UI をやめ、**Browse Marketplace** ボタンから `@tag:language-model-tools` で絞り込んだ Extensions ビューを開くようにする PR である。独自のマーケットプレイス一覧、拡張詳細 UI、関連スタイルを削除する。Agents ウィンドウでは Extensions ビューが無いためボタンを表示しない。

**直近のアクティビティ:** Copilot レビューが承認前に解決すべき重大な問題があると指摘した。

**変更ファイル:** 5 ファイル、+26/-777。`embeddedExtensionToolsDetail.ts` と `galleryItemRenderer.ts` の削除、`toolsListWidget.ts` と `aiCustomizationManagementEditor.ts` の簡素化である。

**次のアクション:** Copilot 指摘の修正と、未完了のクライアント型チェックの実施。

---

### ![PR](icons/git-pull-request.svg) [Simplify state indicators in MCP customization page](https://github.com/microsoft/vscode/pull/337402) `#337402`
**ステータス:** Open (Draft) | **ラベル:** なし

**概要:** MCP カスタマイズページのインストール済み項目の状態表示を整理する PR である。色付きの状態バッジを廃止し、起動中はスピナー、エラーはトグル横の赤いアイコンと最大 2 行のメッセージ、停止中は灰色の circle-slash アイコンなどに置き換える。Running は既定状態として表示なしとする。

**直近のアクティビティ:** CI のスクリーンショット差分は上限超過。Copilot レビューはレンダラーテストの失敗見込みと、状態描画やアクセシビリティの課題を指摘した。

**変更ファイル:** 4 ファイル、+606/-160。`mcpListWidget.ts`、CSS、テスト `mcpListWidget.test.ts` (+397)、コンポーネントフィクスチャである。

**次のアクション:** テスト失敗と Copilot 指摘への対応。

---

### ![PR](icons/git-pull-request.svg) [Reduce space between customization page title and search bar](https://github.com/microsoft/vscode/pull/337401) `#337401`
**ステータス:** Open (Draft) | **ラベル:** なし

**概要:** カスタマイズモーダルのページタイトルと検索バーの間隔を 20px から 16px に縮め、Agents、MCP Servers、Plugins、Tools の各ページで揃える PR である。

**直近のアクティビティ:** CI のスクリーンショット差分は上限超過。Copilot レビューは Tools ページで検索バーと結果の間隔まで意図せず縮まっていると指摘した。

**変更ファイル:** 1 ファイル、+2/-2。`aiCustomizationManagement.css` のみである。

**次のアクション:** Tools レイアウトの間隔の修正。

---

### ![PR](icons/git-pull-request.svg) [Fix customization modal title when Cloud harness is selected](https://github.com/microsoft/vscode/pull/337407) `#337407`
**ステータス:** Open (Draft) | **ラベル:** なし

**概要:** Cloud ハーネス選択時にカスタマイズモーダルのタイトルが正しく表示されない問題を修正する PR である。`SessionsAICustomizationWorkspaceService` で Cloud ワークスペースにはリポジトリ名ベースのラベルを使い、Cloud とリポジトリ名を組み合わせたタイトルにする。Local と worktree ハーネスは従来どおりフォルダー名を使う。

**直近のアクティビティ:** Copilot レビューは承認推奨で、テスト強化の非ブロッキング提案が 1 件あった。

**変更ファイル:** 2 ファイル、+79/-1。`aiCustomizationWorkspaceService.ts` と回帰テストである。

**次のアクション:** Draft 解除と人間のレビュー。

---

### ![PR](icons/git-pull-request.svg) [Fix webview find widget not working after moving editor group to new window](https://github.com/microsoft/vscode/pull/269509) `#269509`
**ステータス:** Closed | **ラベル:** なし

**概要:** `enableFindWidget` を有効にした Webview を「View: Move Editor Group into New Window」で新しいウィンドウに移すと検索結果がハイライトされなくなる問題 (#233044) を修正する PR である。原因は `WebviewFindWidget` の `_findWidgetFocused` コンテキストキーが旧ウィンドウのサービスに紐付いたままだったことで、`updateContextKeyService()` を追加して再バインドする。

**直近のアクティビティ:** dmitrivMS が #337403 に置き換えられたとコメントして Close した。

**変更ファイル:** 2 ファイル、+10/-1。`webviewFindWidget.ts` と `webviewElement.ts` である。

**次のアクション:** 後継の #337403 を追跡する。

---

### ![PR](icons/git-pull-request.svg) [sessions: add remote session delegation tools](https://github.com/microsoft/vscode/pull/336690) `#336690`
**ステータス:** Open (Draft) | **ラベル:** agent-host

**概要:** Agents ウィンドウでピッカーを使わずにリモートへ作業を委譲するツール群を追加する PR である (#336682)。`list_agent_hosts` と `create_remote_session` はリソースとモデルで絞り込み、最も負荷の低いホストに配置する。`send_remote_message` と `get_remote_session` でフォローアップ送信と状態を確認する。`chat.remoteSessions.tools.enabled` で既定は無効であり、クローンと予算の再設計は別作業とされる。

**直近のアクティビティ:** dmitrivMS が多数のレビューコメントを付けている。Copilot レビューは、モデル無しでは動けないエージェントが自動配置で選ばれうる点と、内部タイムアウトが一般的なキャンセルとして報告される点を指摘した。

**変更ファイル:** 61 ファイル、+6344/-94。`agentHostResources.ts`、`agentRemoteSessionMeta.ts`、`agentHostWorkspaceTrust.ts` などの新規ファイルと `agentService.ts` などの変更である。

**次のアクション:** dmitrivMS と Copilot の指摘への対応。規模が大きいため段階的なレビューが必要である。

---

### ![PR](icons/git-pull-request.svg) [chore: bump foundry-local-sdk to 2.x](https://github.com/microsoft/vscode/pull/335329) `#335329`
**ステータス:** Open (Draft) | **ラベル:** なし

**概要:** foundry-local-sdk を 2.x に更新し、インストール後処理が環境変数でカスタマイズ可能になったことを受けて、同パッケージ専用のビルド手順を削除する PR である。GitHub CI と ADO CI は通過済みで、ADO product build と Windows、Linux での動作確認が未完了である。

**直近のアクティビティ:** Copilot レビューが複数回あり、SDK 2.x のネイティブレイアウトとの非互換、キャンセルとマネージャー破棄の問題、ワーカーキャンセルとキャッシュ修復の競合を順に指摘した。最新は「要精査」で、クロスプラットフォームのランタイムとパッケージングは製品ビルドでの検証待ちとしている。roblourens、aeschli、meganrogge、hediet、vritant24 のレビューはいずれも Dismissed となっている。

**変更ファイル:** 30 ファイル、+669/-578。各 OS の Azure Pipelines 定義と、`foundryLocalInstall.ts` や `foundry-local.yml` の削除が中心である。

**次のアクション:** ADO product build の通過と Windows、Linux での動作確認。

---

### ![PR](icons/git-pull-request.svg) [sessions: Support GitHub sandbox creation in vscode.dev](https://github.com/microsoft/vscode/pull/337387) `#337387`
**ステータス:** Merged | **ラベル:** なし

**概要:** Web 版 Agents ウィンドウから、既存のサンドボックス接続なしでリポジトリを選んで GitHub サンドボックスセッションを作成できるようにする PR である。Copilot セッションプロバイダーをサンドボックス作成専用モードで再利用し、「Work in Repository」ピッカーを共有実装に切り出す。リポジトリ選択時はドラフトのみを作り、最初の送信で環境を確保する。現時点では github.com の公開アカウントのみ対応である。

**直近のアクティビティ:** Copilot レビューが認証スコープの昇格と、ポリシーによる AI 無効化の考慮漏れを指摘し、修正後は「要精査」に変わった。osortega のレビューを経て dmitrivMS が承認し、マージされた。

**変更ファイル:** 33 ファイル、+2010/-278。`copilotCloudSessionsProvider.ts`、`newChatWidget.ts`、`sessionTypePicker.ts`、`webWorkspacePicker.ts` とテストである。

**次のアクション:** マージ済み。本番でのエンドツーエンド検証を確認する。

---

### ![PR](icons/git-pull-request.svg) [Fix CLAUDE_PLUGIN_ROOT in custom agent hooks](https://github.com/microsoft/vscode/pull/337388) `#337388`
**ステータス:** Open (Draft) | **ラベル:** なし

**概要:** プラグイン提供のカスタムエージェントに宣言されたフックで、フォーマット固有のプラグインルート変数を解決する PR である。Claude プラグインのエージェントフックに `CLAUDE_PLUGIN_ROOT` を設定し、Open Plugin の同等の挙動も維持する。

**直近のアクティビティ:** pwang347 が修正前後の録画を投稿した。修正前はフックに `${CLAUDE_PLUGIN_ROOT}` がそのまま渡り、修正後はプラグインディレクトリに解決される。Copilot レビューは空白を含むパスでのシェルエイリアスのクォート誤りと、テストがクロスプラットフォームでない点を指摘した。

**変更ファイル:** 4 ファイル、+92/-17。`pluginParsers.ts`、`hookSchema.ts`、`promptsServiceImpl.ts` とテストである。

**次のアクション:** Copilot 指摘 2 件の修正。

---

### ![PR](icons/git-pull-request.svg) [Open full output for large Claude Bash results](https://github.com/microsoft/vscode/pull/337389) `#337389`
**ステータス:** Open (Draft) | **ラベル:** なし

**概要:** Claude の `Bash` と `BashOutput` の大きな結果を、プレビュー付きのターミナルカードと **Open Full Output** で表示する PR である (#328596 の一部、#335016 に依存)。Claude が保存したファイル (`persistedOutputPath`) をセッション DB に記録し、再読み込みやフォーク時にも同じターミナルリソースを参照できるようにする。ピアやフォークしたチャットでリソースが衝突しうる点などは既知の課題として後続対応とされている。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**変更ファイル:** 12 ファイル、+508/-18。`claudeAgent.ts`、新規 `claudeTerminalOutput.ts`、共有 `terminalOutputContent.ts` とテストである。

**次のアクション:** #335016 のマージ後に `main` へ付け替えてレビューを受ける。

---

### ![PR](icons/git-pull-request.svg) [Open full output for large Codex command results](https://github.com/microsoft/vscode/pull/337390) `#337390`
**ステータス:** Open (Draft) | **ラベル:** なし

**概要:** #337389 の Codex 版で、20 KB を超える `commandExecution` 出力をセッションデータ配下の `terminal-output/` に書き出し、プレビューと **Open Full Output** を表示する PR である。シェルのツール呼び出し ID に Codex の item id を使い、再読み込み後もリソースを維持する。ファイル書き込みを待つため `item/completed` と `turn/completed` をスレッドごとのキューで処理する。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**変更ファイル:** 8 ファイル、+436/-62。`codexAgent.ts`、新規 `codexTerminalOutput.ts`、`codexReplayMapper.ts` とテストである。

**次のアクション:** #335016 のマージ後に `main` へ付け替える。キュー処理の順序問題など既知の課題を後続で扱う。

---

### ![PR](icons/git-pull-request.svg) [Fix sandbox toggle for remote Copilot agent host sessions](https://github.com/microsoft/vscode/pull/337385) `#337385`
**ステータス:** Merged | **ラベル:** なし

**概要:** リモートの Copilot Agent Host セッションでターミナルサンドボックスのトグルを表示し、既定値をクライアントではなくセッションホストの OS に基づいて決める PR である (#337262)。接続切り替え時の古いホスト OS 応答を無視し、管理ポリシーとセッション単位の上書きを維持する。

**直近のアクティビティ:** Copilot レビューがソフト再接続時に非 Windows のフォールバック値のまま固定されうる点を指摘した。dmitrivMS が承認し、マージされた。

**変更ファイル:** 3 ファイル、+197/-11。`agentHostChatInputPicker.ts` とテスト (+176)、設定説明の修正である。

**次のアクション:** マージ済み。再接続時の指摘が後続で扱われるか確認する。

---

### ![PR](icons/git-pull-request.svg) [chat: Discover cloud sandbox sessions in the Editor Window](https://github.com/microsoft/vscode/pull/337189) `#337189`
**ステータス:** Merged | **ラベル:** なし

**概要:** これまで Agents ウィンドウでのみ登録されていたクラウドサンドボックスセッションの検出と接続処理をワークベンチ層で共有し、Editor ウィンドウの Cloud フィルターでも既存セッションを一覧表示できるようにする PR である。環境が利用できない場合は読み取り専用の履歴を保持する。サンドボックス作成は引き続き Agents ウィンドウのみで、プロトコル変更はない。

**直近のアクティビティ:** Copilot レビューが接続ライフサイクル、認証準備、セッション単位の履歴ルーティングに関する 8 件の指摘を出した。osortega が多数のレビューコメントを付け、dmitrivMS が承認してマージされた。

**変更ファイル:** 46 ファイル、+3107/-1587。`cloudSandboxAgentHostContribution.ts` (-810) などの既存実装を共有層へ移設したことが差分の大半である。

**次のアクション:** マージ済み。Editor ウィンドウでの動作を確認する。

---

### ![PR](icons/git-pull-request.svg) [chat: show MCP sign-in progress and preserve live server state](https://github.com/microsoft/vscode/pull/337381) `#337381`
**ステータス:** Merged | **ラベル:** なし

**概要:** MCP のサインイン中に進捗を表示して重複リクエストを防ぎ、完了、キャンセル、失敗後にサインイン操作を元に戻す PR である。認証チャレンジを保持し、再試行時に SDK の認証を再開する。ターン開始時のキャッシュではなくライブの MCP インベントリを参照し、遅れて届いた応答が新しい状態を上書きしないようにする。

**直近のアクティビティ:** Copilot レビューが、キャンセル時に Starting のまま残る問題や並行チャレンジが到達不能になる問題を指摘した。dmitrivMS のレビューは 2 回 Dismissed の後、承認されマージされた。

**変更ファイル:** 10 ファイル、+1517/-110。`copilotAgentSession.ts`、`mcpCustomizationController.ts`、`mcpListWidget.ts` と大量の回帰テストである。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [Install required plugins and show policy-blocked recovery UI](https://github.com/microsoft/vscode/pull/337221) `#337221`
**ステータス:** Open (Draft) | **ラベル:** なし

**概要:** エンタープライズポリシー `enabledPlugins` で必須指定されたプラグインをインストールし、`installed.json` から削除されたエントリを復元する PR である。利用不可のプラグインについては、#337151 で導入済みの最低バージョン UI と表示を揃え、読み取り専用の Chat ビューとエディターバナーを出す。必須プラグインには **Retry** を提供し、インストールが記録されるまでチャット送信をブロックする。

**直近のアクティビティ:** anthonykim1 がインストールと復元の検証記録を投稿し、当初見つかったインストール失敗時の問題は修正済みと更新した。Copilot レビューはインストール ID が収束しない問題や送信ブロックの回避経路などを指摘している。

**変更ファイル:** 30 ファイル、+2005/-73。新規 `managedPluginInstall.ts`、`managedPluginAvailability.contribution.ts`、policyBlocked 関連の UI とテストである。

**次のアクション:** Copilot 指摘の解消と、現行 UI のスクリーンショット取得。

---

### ![PR](icons/git-pull-request.svg) [agent bakeoff](https://github.com/microsoft/vscode/pull/335498) `#335498`
**ステータス:** Open (Draft) | **ラベル:** agent-bakeoff

**概要:** エージェントピッカーに **Run Multiple Agents...** を追加し、複数のエージェントとモデルの組み合わせを独立した worktree で並列実行し、Judge が全 diff を評価して推奨を示す比較機能を実装する PR である。`sessions.chat.compareAgents.enabled` で既定は無効である。所要時間、使用モデル、Judge の選択結果をプライバシーに配慮したテレメトリとして記録する。

**直近のアクティビティ:** meganrogge が、クライアント切断時にも比較処理を継続させるためオーケストレーションを Agent Host 側へ移した理由とテレメトリの範囲を説明した。anthonykim1 が大量にレビューし、Agent Host のフォールバックが比較ワークフローを完了できない点、実験無効時にも比較ツールが通常セッションに公開される点などを P0 として指摘した。無関係のユーザーが通知メール停止を求めるコメントも投稿している。

**変更ファイル:** 128 ファイル、+14812/-364。セッション UI、actionWidget、Agent Host の OTel などに広く及ぶ。

**次のアクション:** anthonykim1 の P0 および P1 指摘を解消する必要がある。

---

### ![PR](icons/git-pull-request.svg) [build: select NOTICE caches from verified producers](https://github.com/microsoft/vscode/pull/337212) `#337212`
**ステータス:** Open (Draft) | **ラベル:** なし

**概要:** キャッシュ済み CG notice の選択を、`cg-notice-baseline` タグで絞り込んだ `latestFromBranch` ダウンロードに変える PR である。スキャナーのみの新しい CI 成果物が利用可能なベースラインを隠してしまう問題（build 467180 で発生）への対処である。公開済みのベースとプロベナンスをバイト単位で照合した後にのみタグを付与する。

**直近のアクティビティ:** Copilot レビューは 2 回とも指摘なしの「要精査」で、Azure の権限や成果物のタイミング、タグ操作は実際のパイプラインでの検証が必要としている。

**変更ファイル:** 3 ファイル、+121/-10。新規 `tagNoticeBaseline.ts`、`product-quality-checks.yml`、手順書 `oss-third-party-notices.instructions.md` である。

**次のアクション:** 検証済みベースラインの初回タグ付けと、管理された環境でのパイプライン検証。

---

### ![PR](icons/git-pull-request.svg) [Update plugin pages table UI to use WorkbenchTable](https://github.com/microsoft/vscode/pull/337386) `#337386`
**ステータス:** Open (Draft) | **ラベル:** なし

**概要:** カスタマイズモーダルのプラグイン詳細ページで、**Details** と **Contains** セクションを独自の行実装から `WorkbenchTable` に置き換える PR である。列を Property と Value、Type と Name と Description に標準化し、独自 CSS を削除する。Marketplace リンクやキーボード操作は維持している。

**直近のアクティビティ:** CI が 12 件のスクリーンショット変更を検出した。Copilot レビューは削除された行が破棄すべきコントロールを保持し続ける点と、操作可能な行にアクセシブルなアクション属性が無い点を指摘した。

**変更ファイル:** 3 ファイル、+380/-248。`embeddedAgentPluginDetail.ts` と CSS、フィクスチャである。

**次のアクション:** Copilot 指摘 2 件の修正とスクリーンショット変更の確認。

---

### ![PR](icons/git-pull-request.svg) [Fix Copilot OTel file span serialization](https://github.com/microsoft/vscode/pull/337373) `#337373`
**ステータス:** Merged | **ラベル:** なし

**概要:** ローカルの Copilot Chat のスパンがファイル出力時に失われる問題 (#319993) を修正する PR である。SDK のスパンが循環参照を含むため、文字列化で `{}` が書かれつつ成功扱いになっていた。公開スパンデータのみをシリアライズし、失敗時は書き込み前に報告するようにした。従来の実装は回帰テスト 13 件中 9 件で失敗する。

**直近のアクティビティ:** Copilot レビューが SDK 互換性の問題 2 件を指摘し、修正後は承認推奨となった。jruales のレビューを経て dmitrivMS が承認し、マージされた。

**変更ファイル:** 3 ファイル、+354/-74。`fileExporters.ts`、テスト `fileExporters.spec.ts`、`agent_monitoring.md` である。

**次のアクション:** マージ済み。

---

### ![Issue](icons/issue-opened.svg) [The mute button links to the wrong keybinding in the editor](https://github.com/microsoft/vscode/issues/337067) `#337067`
**ステータス:** Closed | **ラベル:** bug, insiders-released, voice-mode

**概要:** #336280 の検証中に見つかった不具合である。voice mode でマイク（mute/unmute）アイコンを右クリックすると keybinding 設定の項目が出るが、ボタンに割り当てられたコマンドではなく voice mode 切り替えコマンドが keybindings エディタで開かれる。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**次のアクション:** insiders-released 済みでクローズ済み。Insiders で正しいコマンドが開くか確認する。

---

### ![Issue](icons/issue-opened.svg) [Testing: Misaligned icon for hook warnings in chat](https://github.com/microsoft/vscode/issues/337076) `#337076`
**ステータス:** Closed | **ラベル:** insiders-released

**概要:** #336962 のテスト中に報告された表示崩れである。chat 内で hook の警告を表示する際、アイコンとテキストの位置がずれている。スクリーンショットが添付されている。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**次のアクション:** 修正は Insiders に反映済み。対応不要。

---

### ![Issue](icons/issue-opened.svg) [Explain enterprise minVersion restrictions in Chat, the Agents window, and a window-level banner](https://github.com/microsoft/vscode/issues/337086) `#337086`
**ステータス:** Closed | **ラベル:** feature-request, insiders-released

**概要:** Enterprise Managed Settings の `minVersion` で古いクライアントの AI 機能がブロックされた際に、理由をユーザーへ説明する UI を追加する要望である。Chat には更新通知、エディタには Workspace Trust 風のバナー、Agents window にはブロック用オーバーレイのみを表示する。ポリシーで組み込み更新が無効な場合は管理者への連絡を案内し、既存の強制動作は変えない。実装は #337151 である。

**直近のアクティビティ:** vs-code-engineering[bot]がマージ済み PR #337151 の説明とスクリーンショットを検証用に共有した。汎用のフォールバックリンクは表示せず、**Open Editor Window** で新規エディタウィンドウを開く既存の回復手段を維持する旨が記載されている。

**次のアクション:** 企業ポリシー環境を持つ場合は、通知とバナーの表示内容を Insiders で検証する。

---

### ![Issue](icons/issue-opened.svg) [Automation session is sometimes empty](https://github.com/microsoft/vscode/issues/333521) `#333521`
**ステータス:** Closed | **ラベル:** bug, automations

**概要:** #333367 のテスト中の報告である。Automations view から実行済みの run を開いたあと、Sessions サイドバーの "Automations" で戻って同じ run を再度開くとセッションが空になる。コンソールには `listView.ts` の "ListView is not display:none before measuring row height" 警告が出ている。

**直近のアクティビティ:** dmitrivMS が PR #333789 で修正されたと思われるとコメントしてクローズした。

**次のアクション:** 修正版で再現しないか確認する。

---

### ![Issue](icons/issue-opened.svg) [Agents Window: Remove unified workspace picker flyout hover delay](https://github.com/microsoft/vscode/issues/337087) `#337087`
**ステータス:** Closed | **ラベル:** insiders-released

**概要:** Agents Window の unified workspace picker で、flyout 付きの行がキーボード操作では即座に開くのに、マウスホバーでは既定の遅延を待つ不整合の報告である。`devcontainer.json` を含むフォルダの Local / Dev Container 切り替えで目立つ。picker 内の flyout はホバー時も即時に開き、他の Action List 利用箇所は従来の遅延を保つことが期待動作である。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**次のアクション:** Insiders に反映済み。対応不要。

---

### ![Issue](icons/issue-opened.svg) [Workspace picker: Reduce max number of recent workspaces shown so Remote list is visible](https://github.com/microsoft/vscode/issues/337129) `#337129`
**ステータス:** Closed | **ラベル:** bug, polish, insiders-released

**概要:** Agents window の新しい workspace picker で、最近使ったワークスペースが上限まで並ぶと Remote メニューが隠れ、存在に気付けない問題である。表示するワークスペース数を減らして Remote が必ず見えるようにすることを提案している。1.139.0-insider で確認されている。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**次のアクション:** Insiders に反映済み。対応不要。

---

### ![Issue](icons/issue-opened.svg) [Agent host: file watches fail with ENOPRO in a remote window, because the host is handed vscode-remote URIs it cannot resolve](https://github.com/microsoft/vscode/issues/333665) `#333665`
**ステータス:** Closed | **ラベル:** なし

**概要:** dev container ウィンドウで chat を bind するたびに、agent host が `vscode-remote://` の URI を watch しようとして `ENOPRO: No file system provider found for resource` で失敗する報告である。対象はワークスペースフォルダ、`.claude` ディレクトリ、セッションの attachments ディレクトリの 3 つで、agent host には `vscode-remote` 用の provider がない。報告者はローカルパッチを外しても再現することを確認しており、ファイル変更通知が届かないことと、ログがノイズで埋まることを影響として挙げている。

**直近のアクティビティ:** dmitrivMS が PR #333296 で修正済みとコメントした。

**次のアクション:** dev container 環境で agent host ログから ENOPRO が消えたか確認する。

---

### ![Issue](icons/issue-opened.svg) [Agents Window pickers should support Configure Keybinding from context menu](https://github.com/microsoft/vscode/issues/337144) `#337144`
**ステータス:** Closed | **ラベル:** feature-request, insiders-released, chat

**概要:** unified workspace picker 有効時に、Agents Window の workspace picker と harness picker の右クリックメニューへ **Configure Keybinding** を追加する要望である。選択すると Keyboard Shortcuts が該当の focus コマンドに絞られて開く。unified picker が有効なときのみ表示する。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**次のアクション:** Insiders に反映済み。対応不要。

---

### ![Issue](icons/issue-opened.svg) [Agents Window picker hovers should show configured keybindings](https://github.com/microsoft/vscode/issues/337145) `#337145`
**ステータス:** Closed | **ラベル:** feature-request, insiders-released, chat

**概要:** unified workspace picker 有効時に、workspace picker と harness picker のホバーへ現在設定されている focus 用 keybinding を表示する要望である。固定のキー表記ではなくユーザー設定を解決して表示し、未設定なら通常のツールチップのままにする。keybinding や設定の変更にも追従する。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**次のアクション:** Insiders に反映済み。対応不要。

---

### ![Issue](icons/issue-opened.svg) [Agents Window should align experimental composer controls](https://github.com/microsoft/vscode/issues/337150) `#337150`
**ステータス:** Closed | **ラベル:** feature-request, insiders-released, chat

**概要:** `sessions.chat.experimental.newSessionComposerLayout` と `sessions.chat.unifiedWorkspacePicker.enabled` の両方が有効な場合に、Agents Window の composer のコントロール配置を統一する要望である。新規セッションでは Folder、Branch、New Worktree、右端に Harness の順とし、表示順とキーボード順を一致させる。実行中セッションでは agent mode、approvals、model を主ツールバーにまとめ、context-usage を入力欄の末尾に揃える。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**次のアクション:** 両設定を有効にして Insiders でレイアウトを確認する。

---

### ![Issue](icons/issue-opened.svg) [Hover in question carousel for links is not the same as other hover elements](https://github.com/microsoft/vscode/issues/337169) `#337169`
**ステータス:** Closed | **ラベル:** insiders-released

**概要:** question carousel 内のリンクのホバーが他の要素と異なるブラウザ標準のツールチップになっている報告である。誤ったホバーコンポーネントが使われており、ハイコントラストでも問題が出ると指摘している。

**直近のアクティビティ:** vs-code-engineering[bot]がマージ済み PR #337254 の内容を共有した。carousel のメッセージやタイトルを `ChatContentMarkdownRenderer` で描画し、テーマ付き workbench hover に置き換え、回帰テストも追加している。

**次のアクション:** Insiders で通常テーマとハイコントラストの両方を確認する。

---

### ![Issue](icons/issue-opened.svg) [MCP HTTP redirects: allow/deny policy is not re-evaluated on the redirect destination](https://github.com/microsoft/vscode/issues/337194) `#337194`
**ステータス:** Closed | **ラベル:** insiders-released

**概要:** MCP の HTTP transport（`McpHTTPHandle._fetch`）がリダイレクトを最大 5 回追従する際、スキーム検証と 4 種の認証ヘッダ除去しか行わず、`IAllowedMcpServersService` による許可判定をリダイレクト先で再評価していないというセキュリティ上の報告である。許可済みサーバーから拒否対象サーバーへ 307 でリダイレクトさせると、拒否対象側が MCP セッションを確立できることを commit 7b24b630 で実証している。また `mcp.json` に設定した `X-Api-Key` などの独自ヘッダが別オリジンへそのまま送られる。各ホップでポリシーを再評価して fail closed にすることを提案している。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**次のアクション:** Insiders に反映済み。企業で MCP 許可ポリシーを運用している場合は修正版への更新を優先する。

---

### ![Issue](icons/issue-opened.svg) [Bound Linux smoke-test disk diagnostics to avoid job timeout](https://github.com/microsoft/vscode/issues/337227) `#337227`
**ステータス:** Closed | **ラベル:** vscode-build, insiders-released

**概要:** Linux Electron Tests ジョブで、smoke test 前の診断ステップの `du -xhd1 /` が 12 分 40 秒かかり、50 分のジョブタイムアウトに達した報告である。テスト自体はすべて成功したが、後続の Component Governance がキャンセルされた。再試行では同じ診断が 43 秒で終わっている。`timeout 60s du ...` などで診断処理に時間上限を設けることを提案している。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**次のアクション:** 修正済み。ビルドパイプラインで同様のタイムアウトが再発しないか監視する。

---

### ![Issue](icons/issue-opened.svg) [Perf: pending-confirmation detection does two full O(N) response scans per notification (restore and live streaming)](https://github.com/microsoft/vscode/issues/337246) `#337246`
**ステータス:** Closed | **ラベル:** important, perf, insiders-released, chat, agents-window

**概要:** 長い Copilot セッションを開いた際のレンダラ CPU スパイク調査から見つかった性能問題である。レスポンス変更通知のたびに `ChatResponseModel.isPendingConfirmation` と `AgentSessionApprovalModel._trackModel` の autorun がそれぞれ response parts 全体を走査しており、ストリーミング中は実質 O(N²) になる。restore 時の O(N²) は別途修正済みで、本 issue は残る走査コストを対象とする。トレースでは 2.19 秒の long task の大半が `readObservable` 内の処理だった。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**次のアクション:** Insiders に反映済み。長いセッションでの応答性を確認する。

---

### ![Issue](icons/issue-opened.svg) [Sandbox toggle not shown when using remote agent host](https://github.com/microsoft/vscode/issues/337262) `#337262`
**ステータス:** Closed | **ラベル:** insiders-released

**概要:** macOS 上のリモート agent host に Windows から接続すると、agent は既定で sandbox 内で実行されるのに、その状態の表示と切り替えトグルが出ない報告である。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**次のアクション:** Insiders に反映済み。対応不要。

---

### ![Issue](icons/issue-opened.svg) [Agents web: remote host selector closes during connection updates](https://github.com/microsoft/vscode/issues/337317) `#337317`
**ステータス:** Closed | **ラベル:** insiders-released

**概要:** insiders.vscode.dev/agents のサイドバー左下にあるリモートホスト選択メニューが、バックグラウンドでのホスト接続状態の更新によって勝手に閉じ、ホストを選べない不具合である。複数のリモートホストを解決中に発生する。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**次のアクション:** Insiders に反映済み。対応不要。

---

### ![Issue](icons/issue-opened.svg) [Running outside the sandbox doesn't actually show the command to be run](https://github.com/microsoft/vscode/issues/337337) `#337337`
**ステータス:** Closed | **ラベル:** unreleased

**概要:** 最新 Insiders で、sandbox 外での実行確認時に実行予定のコマンドが表示されない報告である。本文はスクリーンショットのみである。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**次のアクション:** 修正は未リリース（unreleased）。次の Insiders で確認する。

---

### ![Issue](icons/issue-opened.svg) [Agents Window: Viewed button has inconsistent corner radius and horizontal padding](https://github.com/microsoft/vscode/issues/337369) `#337369`
**ステータス:** Closed | **ラベル:** bug, unreleased, agents-window

**概要:** Agents Window の Changes view にある **Viewed** ボタンの角丸と左右パディングが周囲のコントロールと揃っていない表示上の不具合である。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**次のアクション:** 修正は未リリース。次の Insiders で確認する。

---

### ![Issue](icons/issue-opened.svg) [Confirmation buttons show empty dropdown affordance](https://github.com/microsoft/vscode/issues/337412) `#337412`
**ステータス:** Closed | **ラベル:** unreleased

**概要:** chat の確認ボタンが、`moreActions` が空または区切りのみの場合でも split button として描画され、中身のないドロップダウンが出る不具合である。追加アクションが 1 つ以上ある場合のみ split button にすべきとしている。

**直近のアクティビティ:** vs-code-engineering[bot]がマージ済み PR #337414 の内容を共有した。2 つの確認ウィジェット実装に同じ判定を適用し、空、区切りのみ、有効アクションありの各ケースの回帰テストを追加している。

**次のアクション:** 修正は未リリース。次の Insiders で確認する。

---

### ![Issue](icons/issue-opened.svg) [VS Code Pet overlays tip dismissal](https://github.com/microsoft/vscode/issues/333594) `#333594`
**ステータス:** Closed | **ラベル:** なし

**概要:** VS Code Pet の表示が tip の閉じるボタンに重なっているという報告である。本文はスクリーンショットのみである。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**次のアクション:** クローズ済み。対応不要。

---

### ![Issue](icons/issue-opened.svg) [[Bug]"Add File to Chat" silently does nothing in the Agents Window](https://github.com/microsoft/vscode/issues/328684) `#328684`
**ステータス:** Open | **ラベル:** なし

**概要:** VS Code 1.131.0（macOS）で、Agents Window のセッションを開いた状態で `workbench.action.chat.attachFile`（Chat: Add File to Chat）を実行しても何も起きない報告である。添付もエラー表示もなく、通常ウィンドウの chat view と同様に添付されることが期待動作である。

**直近のアクティビティ:** samvallad33 が、フィードバックなしに操作が無視されるのは問題であり、成功またはエラーの通知を出すべきではないかとコメントした。

**次のアクション:** 未トリアージのまま残っている。メンテナによる再現確認とラベル付けが必要である。

---

### ![Issue](icons/issue-opened.svg) [Automations showing unread badge for unread archived sessions](https://github.com/microsoft/vscode/issues/333730) `#333730`
**ステータス:** Closed | **ラベル:** automations

**概要:** Automations がアーカイブ済みの未読セッションに対しても未読バッジを表示してしまう報告である。詳細は社内 Slack へのリンクのみである。

**直近のアクティビティ:** dmitrivMS が PR #333959 で修正済みとコメントした。

**次のアクション:** 修正済み。対応不要。

---

### ![Issue](icons/issue-opened.svg) [Cannot reorder nested chat tabs](https://github.com/microsoft/vscode/issues/333672) `#333672`
**ステータス:** Closed | **ラベル:** feature-request, agents-window

**概要:** #333402 のテスト中の報告である。ネストした chat タブをドラッグ&ドロップで並べ替えようとすると、レイアウト変更として扱われて横並び表示になってしまい、タブの順序変更ができない。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**次のアクション:** クローズ済み。対応状況はリンク先で確認する。

---

### ![Issue](icons/issue-opened.svg) [Clicking on subsession in sessions list is resulting in UI jump](https://github.com/microsoft/vscode/issues/333990) `#333990`
**ステータス:** Closed | **ラベル:** なし

**概要:** 2 つのサブセッションを持つセッションで、セッション一覧からサブセッションをクリックすると UI が跳ねる不具合である。1.137.0-insider で報告された。

**直近のアクティビティ:** dmitrivMS が PR #337356 で修正済みとコメントした。

**次のアクション:** 修正済み。対応不要。

---

### ![Issue](icons/issue-opened.svg) [Got Error Open "New Chat Window" GitHub Copilot](https://github.com/microsoft/vscode/issues/333721) `#333721`
**ステータス:** Closed | **ラベル:** triage-needed, stale

**概要:** Windows で "New Chat Window" を開くと `Error: Not allowed to create elements in child window JavaScript context.` が発生する報告である。`createDormantHost` からの呼び出しで子ウィンドウ側の document を使って要素を生成していることがスタックから読み取れる。

**直近のアクティビティ:** dmitrivMS が PR #332696（1.136）で修正済みとコメントした。

**次のアクション:** 1.136 以降へ更新すれば解消する。対応不要。

---

### ![Issue](icons/issue-opened.svg) [For multi workspace folder setup it would be good to see which workspace folder contributed the customization](https://github.com/microsoft/vscode/issues/333755) `#333755`
**ステータス:** Closed | **ラベル:** ai-customizations

**概要:** #333535 のテスト中に出た要望である。2 フォルダ構成のマルチルートワークスペースで、AI カスタマイズ一覧にどのワークスペースフォルダ由来の項目かが表示されず、識別できるようにしてほしいという内容である。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**次のアクション:** Closed 済みのため対応不要である。

---

### ![Issue](icons/issue-opened.svg) [RemoteTool远程直连图标缺失](https://github.com/microsoft/vscode/issues/334014) `#334014`
**ステータス:** Closed | **ラベル:** ai-translated

**概要:** 1.136.0 (Windows) で RemoteTool のリモート直接接続アイコンが表示されなくなったというバグ報告である。報告者は 1.136 で入った不具合とし、検証してから更新を配信してほしいと述べている。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**次のアクション:** Closed 済みのため対応不要である。

---

### ![Issue](icons/issue-opened.svg) [Diff editor ignores working-tree-encoding, showing entire file as modified](https://github.com/microsoft/vscode/issues/334092) `#334092`
**ステータス:** Open | **ラベル:** なし

**概要:** `.gitattributes` の `working-tree-encoding` (例: UTF-16) が設定されたファイルで、diff エディタが index/HEAD 側も作業ツリーのエンコーディングでデコードするため左側が文字化けし、1 文字の変更でも全行が変更扱いになるという報告である。`git diff` は正しく 1 行差分を示す。報告者は index 側を UTF-8 で読むか `git cat-file --filters` の利用を提案し、binary 指定などの回避策はいずれも不利益があると指摘している。

**直近のアクティビティ:** dmitrivMS が #127441 の重複と思われるとコメントした。

**次のアクション:** #127441 との重複判定を確定し、重複としてクローズするか判断する。

---

### ![Issue](icons/issue-opened.svg) [Multiple Agents Windows](https://github.com/microsoft/vscode/issues/334121) `#334121`
**ステータス:** Closed | **ラベル:** agents-window

**概要:**「Open in Agents」がどの VS Code ウィンドウから押しても単一の Agents ウィンドウしか開かない点について、複数開けるようにしてほしいという機能要望である。プロジェクトごとに別デスクトップで作業する場合、Agents ウィンドウが 1 つだと 1 プロジェクトにしか使えないという理由である。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**次のアクション:** Closed 済みのため対応不要である。

---

### ![Issue](icons/issue-opened.svg) [Reversed output in Hebrew](https://github.com/microsoft/vscode/issues/334183) `#334183`
**ステータス:** Closed | **ラベル:** なし

**概要:** Ubuntu 24.04 の 1.134.0 で、統合ターミナル (bash) に Go の `fmt.Println` でヘブライ語を出力すると文字順が逆転して表示されるという報告である。アラビア語でも起こる可能性があるとしている。

**直近のアクティビティ:** dmitrivMS が「Upstream」とコメントし、上流側の問題としてクローズした。

**次のアクション:** 上流の問題として扱われたため、VS Code 側の対応は不要である。

---

### ![Issue](icons/issue-opened.svg) [Copilot questions unreadable](https://github.com/microsoft/vscode/issues/334214) `#334214`
**ステータス:** Closed | **ラベル:** なし

**概要:** Copilot が複数選択式の質問を出したとき、ハイライトされた選択肢が濃い青背景に黒文字で表示され読めないというバグ報告である (1.135.0, Windows)。

**直近のアクティビティ:** dmitrivMS が #335436 で修正済みとコメントした。

**次のアクション:** 修正は #335436 で入っているため、次のリリースでの解消を確認する程度でよい。

---

### ![Issue](icons/issue-opened.svg) [AI返回错误信息并吃掉流量](https://github.com/microsoft/vscode/issues/334201) `#334201`
**ステータス:** Closed | **ラベル:** なし

**概要:** タイトルは AI がエラーを返し通信量を消費するという趣旨である。本文は「データが大きすぎて送信できずクリップボードに書き込んだので貼り付けてほしい」という定型文とスクリーンショットのみで、具体的な情報が欠けている。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**次のアクション:** Closed 済みのため対応不要である。

---

### ![Issue](icons/issue-opened.svg) [Claude BYOK broken in 1.136: SDK download deadlock for signed-in users](https://github.com/microsoft/vscode/issues/334405) `#334405`
**ステータス:** Closed | **ラベル:** なし

**概要:** Copilot 契約のない GitHub アカウントでサインインし、`ANTHROPIC_BASE_URL` と `ANTHROPIC_AUTH_TOKEN` による BYOK で Claude agent host を使っていた環境で、1.136 へ更新すると Claude が「Upgrade」表示でグレーアウトし使えなくなるという報告である。1.136 で `agentSdks.claude.version` が `0.3.239` に変わったが、`isSdkResolvableWithoutDownload` がバージョン完全一致でキャッシュを探すため旧版しかない環境では解決できず、自動と手動のどちらのダウンロード経路にも到達できない。`agenthost.log` には「SDK not downloaded yet」が繰り返し出力され、製品内で復旧する手段がない。

**直近のアクティビティ:** dmitrivMS が #334316 の一部として修正済みとコメントした。

**次のアクション:** #334316 を含むリリースで BYOK ユーザーの SDK ダウンロードが復旧するかを確認する。

---

### ![Issue](icons/issue-opened.svg) [Vitest extension buttons are outside the panel viewport](https://github.com/microsoft/vscode/issues/334256) `#334256`
**ステータス:** Closed | **ラベル:** bug, testing

**概要:** テスト名やフォルダ階層が長いと、Testing パネルで Vitest 拡張のアクションボタンが表示領域の外に押し出され操作できなくなるという報告である (1.136.0, Windows 10)。報告者は NPM Scripts パネルで修正済みの #251722 と同様に、ボタンを右端に固定しテキスト側を切り詰める挙動を求めている。vitest-dev/vscode 側では VS Code 側で直すべきと判断されている。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**次のアクション:** Closed 済みのため対応不要である。

---

### ![Issue](icons/issue-opened.svg) [Cannot have more than 128 tools per request](https://github.com/microsoft/vscode/issues/337428) `#337428`
**ステータス:** Open | **ラベル:** なし

**概要:** 1.138.0 (Windows) で agent host と BYOK を使って Copilot を利用すると、「502 Cannot have more than 128 tools per request」でリクエストが 5 回リトライ後に失敗するという報告である。

**直近のアクティビティ:** 直近 24 時間のコメントなし (新規起票)

**次のアクション:** トリアージし、agent host が BYOK 経由で送るツール数を 128 以下に抑える仕組みの要否を確認する。

---

### ![Issue](icons/issue-opened.svg) [GitHub Copilot inline suggestions consistently fail in Jupyter notebooks](https://github.com/microsoft/vscode/issues/334263) `#334263`
**ステータス:** Closed | **ラベル:** triage-needed, stale

**概要:** Copilot のインライン提案が `.py` では動くのに `.ipynb` のコードセルでは出ないという報告である。1.134 から 1.136 まで、新規の `--user-data-dir` や最小拡張構成でも再現する。ログには `DocumentMissingInHistoryContext` や、NES リクエストが空の補完を返し `noSuggestions` となる記録、DB 作成失敗によるインメモリ DB へのフォールバックが見られる。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**次のアクション:** stale としてクローズされたため、再発時は再起票が必要である。

---

### ![Issue](icons/issue-opened.svg) [Supported trusted publishing from GitHub Actions](https://github.com/microsoft/vscode/issues/334380) `#334380`
**ステータス:** Closed | **ラベル:** なし

**概要:** PAT が 2026-12-01 に完全廃止される予定のため、GitHub Actions から VS Code Marketplace へ拡張機能を trusted publishing で公開できるようにしてほしいという要望である。現行ドキュメントは Azure DevOps からの非 PAT 公開しか扱っておらず、npm や crates と同等の仕組みを求めている。

**直近のアクティビティ:** dmitrivMS が VS Code クライアントの問題ではないとし、microsoft/vsmarketplace の #1326 と #1422 を案内した。

**次のアクション:** VS Code 側の対応は不要である。関心がある場合は vsmarketplace 側の issue を追う。

---

### ![Issue](icons/issue-opened.svg) [GitHub Copilot Agent fails with 408 user_request_timeout across all models even with MCP disabled](https://github.com/microsoft/vscode/issues/334420) `#334420`
**ステータス:** Closed | **ラベル:** なし

**概要:** Copilot Agent のほぼ全リクエストが HTTP 408 `user_request_timeout` (Timed out reading request body) で失敗するという報告である。MCP (Figma) を無効化してもモデルを変えても再現し、単純な質問でも失敗する。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**次のアクション:** Closed 済みのため対応不要である。

---

### ![Issue](icons/issue-opened.svg) [Copilot Chat cannot resolve installed custom skills](https://github.com/microsoft/vscode/issues/334437) `#334437`
**ステータス:** Open | **ラベル:** なし

**概要:** Agent モードでインストール済みのカスタムスキル `executing-plans` を呼び出すと `Skill "executing-plans" not found` になるという報告である。スキルが 15 個程度を超えると先頭の一部しか有効登録されず、それ以外は名前が見えても自動呼び出しに失敗するとしている。報告者は、失敗時に探索ディレクトリやメタデータの不備などを示すエラーを求めている。

**直近のアクティビティ:** dmitrivMS が報告者に #315350 のコメントにある回避策で解決するか確認を求めた。

**次のアクション:** 報告者が #315350 の回避策を試した結果を待ち、重複として扱うか判断する。

---

### ![Issue](icons/issue-opened.svg) [Alt keyup is missed while a webview editor has focus, silently turning a tab's Close button into "Close Other Editors"](https://github.com/microsoft/vscode/issues/334489) `#334489`
**ステータス:** Closed | **ラベル:** triage-needed, stale

**概要:** Markdown プレビューなど webview ベースのエディタにフォーカスがある状態で Alt+Tab で VS Code に戻ると、Alt の keyup が取りこぼされ、タブの閉じるボタンが「Close Others (Alt+Click)」のままになるという報告である。クリックすると他のエディタがすべて閉じる。ウィンドウのフォーカス変化では修飾キー状態が再同期されず、キー入力でのみ解除されるとしている。

**直近のアクティビティ:** dmitrivMS が 1.137 で修正されるはずとし、#334515 と #334874 を示した。

**次のアクション:** 1.137 以降で再現しないことを確認する。

---

### ![Issue](icons/issue-opened.svg) [Agent session resume fails when an event is missing agentDisplayName](https://github.com/microsoft/vscode/issues/334456) `#334456`
**ステータス:** Closed | **ラベル:** なし

**概要:** 既存のエージェントセッションに追加メッセージを送って再開すると、イベントログの読み込みで `missing field agentDisplayName` となり `session.resume` が失敗するという報告である。報告者は、このフィールドを持たない古いイベントとの互換性を保つか移行処理を入れることを求めている。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**次のアクション:** Closed 済みのため対応不要である。

---

### ![Issue](icons/issue-opened.svg) ["Change Completions Model" is missing](https://github.com/microsoft/vscode/issues/336431) `#336431`
**ステータス:** Closed | **ラベル:** なし

**概要:** Insiders 1.139.0 のコマンドパレットに「Change Completions Model」コマンドが存在しないという報告である。GitHub のドキュメントと設定画面には存在が記載されているとしている。

**直近のアクティビティ:** 重複としてクローズされたことに報告者 s-h-a-d-o-w がカスタムモデルの要望ではないと反論した。dmitrivMS は選択肢が組み込みモデルに限られるならコマンドの意味は何かと問い返した。報告者は Claude Haiku と Opus では応答速度が大きく異なり補完が 4 秒ほどかかる点を挙げ、不要と判断するならドキュメントと設定から記述を削除すべきだと述べた。

**次のアクション:** コマンドを復活させるか、ドキュメントと設定の記述を整理するかをメンテナが判断する。

---

### ![Issue](icons/issue-opened.svg) [Three-finger swipe down no longer shows recent files after updating to macOS 27](https://github.com/microsoft/vscode/issues/337253) `#337253`
**ステータス:** Open | **ラベル:** なし

**概要:** macOS 27 に更新後、VS Code がアクティブな状態で三本指の下スワイプをしても最近開いたファイルやウィンドウが表示されなくなったという報告である (1.138.0)。

**直近のアクティビティ:** IgorKha が同じ現象が起きているとスクリーンショット付きでコメントした。

**次のアクション:** macOS 27 での最近使った項目の登録処理を調査し、トリアージする。

---

### ![Issue](icons/issue-opened.svg) [1.139.0 Endgame](https://github.com/microsoft/vscode/issues/336502) `#336502`
**ステータス:** Open | **ラベル:** endgame-plan

**概要:** 1.139.0 のエンドゲーム計画である。月曜朝の TPI 割り当て、自動リリース停止、Start Endgame ワークフロー実行 (バージョン更新 PR #336991 と #336992) などは完了済みとなっている。以降はテスト計画項目の消化、修正の検証、リリースノート作成が並ぶ。

**直近のアクティビティ:** 直近 24 時間のコメントなし (チェックリストの更新のみ)

**次のアクション:** 残りのチェックリスト (TPI 完了、検証、リリースノート) を進め、1.139.0 のリリースに備える。

---

### ![Issue](icons/issue-opened.svg) [Cannot tell blocked sessions (too sublte)](https://github.com/microsoft/vscode/issues/337426) `#337426`
**ステータス:** Open | **ラベル:** なし

**概要:** Insiders 1.139.0 のセッション一覧がコンパクト表示のため、ユーザー入力待ちでブロックされているセッションを見分けにくいという UI の指摘である。benibenj、sandy081、mrleemurray に cc している。

**直近のアクティビティ:** 直近 24 時間のコメントなし (新規起票)

**次のアクション:** ブロック中セッションの表示強調をデザイン担当と検討する。

---

### ![Issue](icons/issue-opened.svg) [New Session pane is not rendered - TypeError: Cannot read properties of undefined (reading 'removeToolFromConfirmationCarousel')](https://github.com/microsoft/vscode/issues/337234) `#337234`
**ステータス:** Open | **ラベル:** bug, agents-window

**概要:** しばらく使わなかった後に Agents ウィンドウで新規セッションを作ると New Session ペインが描画されず、`removeToolFromConfirmationCarousel` 読み取りの TypeError が出るという報告である。別セッションをクリックしてから戻ると回避できる。

**直近のアクティビティ:** sandy081 が原因を特定した。`ChatGroupsView.setSession` でのセッション切り替え時に `ChatWidget` の input part が list より先に破棄され、`ChatListItemRenderer.addModelToolConfirmation` の後片付けが破棄済みの `widget.inputPart` を参照するという順序の問題である。修正方針として、カルーセル登録時に `ChatInputPart` を保持して削除にも同じものを使う案と、回帰テストを示した。その後 alexdima がまだ再現するとして justschen にも確認を依頼した。

**次のアクション:** 修正が反映されたビルドで再現しない理由を調べ、justschen と原因を再確認する。

---

### ![Issue](icons/issue-opened.svg) [Agent Host: verify OTLP trace endpoint routing and governed identity](https://github.com/microsoft/vscode/issues/337413) `#337413`
**ステータス:** Open | **ラベル:** triage-needed

**概要:** #336814 (ローカル Copilot Chat の identity 取得) のフォローアップである。Copilot Agent Host のテストで、ベース URL `http://localhost:4318` を `OTEL_EXPORTER_OTLP_TRACES_ENDPOINT` に入れるとトレースが `/` に送られ、`/v1/traces` を待つコレクタに届かなかった。公式 OpenTelemetry Collector での再現、明示 URL やポリシー優先順位を壊さないエンドポイント処理の修正、governed identity の検証を求めている。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**次のアクション:** 公式 Collector で再現し、ユーザー指定 URL を広く書き換えない形でベースとシグナル別エンドポイントの扱いを修正する。

---

### ![Issue](icons/issue-opened.svg) [Renaming a chat session in the Chat sidebar fails silently](https://github.com/microsoft/vscode/issues/337424) `#337424`
**ステータス:** Open | **ラベル:** なし

**概要:** サイドバーの Chat ビューのセッション一覧で右クリックから Rename しても名前が変わらず、エラーも出ないという報告である (1.138.0, Dev Container 接続時に再現)。同じチャットをエディタタブとして開いている場合は名前変更が成功する。

**直近のアクティビティ:** 直近 24 時間のコメントなし (新規起票)

**次のアクション:** エディタとして開いていないセッションに対する rename の処理経路を調査する。

---

### ![Issue](icons/issue-opened.svg) [Agents window hangs with thousands of binary text models and a stale origin/HEAD base branch](https://github.com/microsoft/vscode/issues/337420) `#337420`
**ステータス:** Open | **ラベル:** agents-window

**概要:** Insiders 1.139.0 (macOS) で、`Microsoft/vscode-docs` の変更ゼロのセッションを開いていただけで Agents ウィンドウが応答しなくなったという報告である。ローカルの `origin/HEAD` が 2021 年の古い `origin/master` を指したままで、ベース比較が `vnext → master` の 9,883 ファイルに膨らんでいた。ログには PNG/GIF/MP4 などのテキストモデル生成と破棄が大量に記録され、レンダラが約 200% CPU と 8〜9 GB のメモリを消費した。因果関係は CPU プロファイルでは未確認としている。

**直近のアクティビティ:** 直近 24 時間のコメントなし (新規起票)

**次のアクション:** 古い `origin/HEAD` を検出する処理と、バイナリファイルをテキストモデル化しない制御を検討する。

---

### ![Issue](icons/issue-opened.svg) [Generated commit messages often don't accurately describe the actual changes](https://github.com/microsoft/vscode/issues/337421) `#337421`
**ステータス:** Open | **ラベル:** なし

**概要:** Source Control のスパークルボタンで生成したコミットメッセージが、誤ったコミット種別を使い、長すぎ、差分ではなく意図を推測して書くという指摘である (1.138.0, macOS)。`pubspec.yaml` のバージョン更新が `fix:` と記述され、`project.pbxproj` の変更も推測で書かれた例を挙げている。

**直近のアクティビティ:** 直近 24 時間のコメントなし (新規起票)

**次のアクション:** コミットメッセージ生成プロンプトの改善要望としてトリアージする。

---

## qmk/qmk_firmware

> 新規キーボード追加 PR が多数を占め、stale bot による警告が相次いでいる。コア側では OS 検出の誤判定修正とマウス関連 API 拡張が承認済みのままレビュー待ちとなっており、256 個超の LED 対応 Issue にはメンテナから試作コミットが提示された。

### ![PR](icons/git-pull-request.svg) [[OS Detection] Don't classify MacOS sequences as Windows when cnt_02 >= 2](https://github.com/qmk/qmk_firmware/pull/26303) `#26303`
**ステータス:** Open | **ラベル:** core, awaiting review

**概要:** ChibiOS 系キーボードで macOS 26.x が Windows と誤判定される問題を修正する。USB 列挙の終盤に届く wLength=0x04 パケットにより Windows 判定条件 (`cnt_ff >= 2 && cnt_04 >= 1`) が成立し、正しく得られていた `OS_MACOS` が `OS_WINDOWS` で上書きされていた。Windows 判定条件に `setups_data.cnt_02 < 2` を追加することで解消し、既存の Windows シーケンスはすべて `cnt_02 = 0` のため影響しないと説明している。

**直近のアクティビティ:** 作者の bin101 が drashna と filterpaper に対し、この PR への注目を求めるコメントを投稿した。drashna の承認は 6 月末に済んでいる。

**変更ファイル:** 2 ファイル、+8/-1。`quantum/os_detection.c` の判定条件を 1 行変更し、`quantum/os_detection/tests/os_detection.cpp` に該当シーケンスのテストケースを追加している。

**次のアクション:** 2 人目のメンテナによるレビューとマージ判断が必要である。

---

### ![PR](icons/git-pull-request.svg) [Extend core API to enable mouse keymap community modules](https://github.com/qmk/qmk_firmware/pull/26232) `#26232`
**ステータス:** Open | **ラベル:** core, stale

**概要:** `host_mouse_send()` に最後に渡されたボタンのマスクを記録し、`host_last_mouse_buttons()` として公開するコア API 追加である。既存の `host_last_system_usage()` や `host_last_consumer_usage()` と同じ形をとる。Raw HID アダプタなどが移動やホイールのレポートを独自に生成する際に現在のボタン状態を付与できるようにし、クリック中のドラッグが途切れる問題を防ぐ。利用側のコミュニティモジュールも提示されている。

**直近のアクティビティ:** stale bot が 45 日間の非アクティブを理由に stale 指定し、作者の schuay は 2 人目のレビューを待っているとコメントした。drashna は 8 月に承認済みである。

**変更ファイル:** 3 ファイル、+28/-0。`tmk_core/protocol/host.c` と `host.h` に API を追加し、`tests/mousekeys/test_mousekeys.cpp` にテストを追加している。

**次のアクション:** 2 人目のメンテナのレビューが必要である。30 日以内に動きがなければ自動クローズされる。

---

### ![PR](icons/git-pull-request.svg) [Add MyKeyClub MKC75](https://github.com/qmk/qmk_firmware/pull/26448) `#26448`
**ステータス:** Open | **ラベル:** keyboard, keymap

**概要:** MyKeyClub MKC75 (75% キーボード) のサポートを追加する。ベンダーが活動を停止しているため、配布されていたコンパイル済みファームウェアと VIA 定義を LLM の助けを借りて逆解析し、実機で確認しながら再構成したものである。物理的な RGB が無いため RGB ドライバは除外し、ロータリーエンコーダや複数の最下段レイアウト、分割左 Shift、分割 Backspace、ISO Enter、Caps Lock インジケータに対応する。

**直近のアクティビティ:** 直近 24 時間のコメントなし。レビューでは drashna が 2 度承認し、zvecr が変更を要求している。

**変更ファイル:** 4 ファイル、+454/-0。`keyboards/mykeyclub/mkc75/` 配下に `keyboard.json`、default キーマップ、`rules.mk`、`readme.md` を新規追加している。

**次のアクション:** 作者が zvecr の変更要求に対応する必要がある。

---

### ![PR](icons/git-pull-request.svg) [Fossyl one 36](https://github.com/qmk/qmk_firmware/pull/26391) `#26391`
**ステータス:** Open | **ラベル:** invalid, keyboard, keymap, python, stale, dependencies

**概要:** Fossyl one 36 キーボードの追加を意図した PR とみられるが、本文はテンプレートのままで説明が無い。

**直近のアクティビティ:** stale bot が 45 日間の非アクティブを理由に stale 指定した。

**変更ファイル:** 51 ファイル、+2500/-5。`keyboards/handwired/fossyl` (31 ファイル) と `keyboards/handwired/dactyl_manuform` (14 ファイル) に加え、`keyboards/config_converter.py` や `lib/chibios`、`lib/googletest` などサブモジュールの変更も含んでいる。

**次のアクション:** `invalid` ラベルが付いており、作者が変更範囲を整理しない限りクローズされる見込みである。

---

### ![PR](icons/git-pull-request.svg) [Add XL Keyboards XL 87 RGB](https://github.com/qmk/qmk_firmware/pull/26332) `#26332`
**ステータス:** Open | **ラベル:** keyboard, keymap, stale

**概要:** 有線 87 キー ANSI TKL の XL Keyboards XL 87 RGB を追加する。APM32F103CBT6 MCU を STM32F103 互換プラットフォームで動かし、uf2boot ブートローダ、6 x 17 マトリクス、上面 91 個と底面 32 個の WS2812 LED、Flash 末尾 2 KB を使う wear-leveling EEPROM に対応する。実機で入力、RGB Matrix、インジケータ、UF2 更新、EEPROM 永続化などを確認済みである。

**直近のアクティビティ:** stale bot が 45 日間の非アクティブを理由に stale 指定した。レビューは 7 月に zvecr の変更要求と作者の多数の返信があり、8 月に drashna が承認している。

**変更ファイル:** 8 ファイル、+466/-0。`keyboards/xl_keyboards/xl_87_rgb/` 配下に `keyboard.json`、専用リンカスクリプト `STM32F103xB_uf2boot_wl.ld`、`board.h`、`mcuconf.h`、キーマップなどを追加している。

**次のアクション:** zvecr による再レビューが必要である。30 日以内に動きがなければ自動クローズされる。

---

### ![Issue](icons/issue-opened.svg) [Support more than 256 leds](https://github.com/qmk/qmk_firmware/issues/24675) `#24675`
**ステータス:** Open | **ラベル:** question, discussion, help wanted

**概要:** 各キーに LED を 4 個 (2U キーは 10 個) 配置した分割キーボードで合計 324 個の LED を使うため、LED のイテレーションに `uint_8` が使われている箇所の拡張を提案する Issue である。AVR の RAM 制約を考慮し、`RGB_MATRIX_LED_COUNT > 255` のときだけ `uint16_t` になる `num_leds_t` 型を導入する案について助言を求めている。

**直近のアクティビティ:** drashna が動作する可能性のある実装コミットを提示し、mateuszdrwal と lazizbekravshanov に試験を依頼した。

**次のアクション:** 提示されたコミットを当事者が試験し、結果を報告する段階である。

---

### ![PR](icons/git-pull-request.svg) [Add support for Raiju Nisabi](https://github.com/qmk/qmk_firmware/pull/26408) `#26408`
**ステータス:** Open | **ラベル:** keyboard, keymap

**概要:** Raiju Nisabi キーボードのサポートを追加する。以前の PR #25907 から一部修正して出し直したものである。

**直近のアクティビティ:** 直近 24 時間のコメントなし。8 月に zvecr が変更を要求し、drashna がコメントしている。

**変更ファイル:** 4 ファイル、+202/-0。`keyboards/nisabi/` 配下に `config.h`、`keyboard.json`、default キーマップ、`readme.md` を追加している。

**次のアクション:** 作者が zvecr の変更要求に対応する必要がある。

---

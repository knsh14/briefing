{
  "name": "briefing-site",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20.11"
  },
  "scripts": {
    "build": "eleventy",
    "dev": "eleventy --serve",
    "test": "node --test && npm run build && npm run check",
    "check": "node scripts/check-build.mjs"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.1.6",
    "wrangler": "^4.136.1"
  },
  "dependencies": {
    "markdown-it": "^15.0.2"
  }
}
